// Build: 2026-07-08T06:49:38.350Z
// @ts-nocheck
let e=66060,t=52721,r=23106,n=22722,i=5147,o=78101,s=67273,a=93443,c=7198,l=15832,d=35698,w=4885,f=9958,h=7494,p=89646,g=59629,m=8894,y=86016,x=55086,v=70586,b=24514,k=45457,S=35842,C=17733,L=75164,M=62429,_=75116,K=27343,P=62387,R=70121,T=56411,X=14676,Y=16266,Z=17569,N=74475,H=61869,J=43895,B=11862,E=22522,D=90612,F=15086,G=89335,I=16954,O=88493,U=54512,W=25315,Q=15910,A=7817,V=47747,j=50068,q=37432,u=9817,z=33365,ee=52119,et=10693,er=42406,en=42498,ei=9205,eo=98113,es=26193,ea=18220,ec=53614,el=18860,ed=20825,ew=52689,ef=98147,eh=28637,ep=32623,eg=91478,em=82485,ey=16551,ex=77010,ev=53983,eb=41446,ek=80735,e0=16999,e1=10167,e2=25923,e3=94020,e4=979,e5=88023,e6=46250,e7=61239,e8=34965,e9=92237,te=42553,tt=87478,tr=81215,tn=66565,ti=20285,to=79834,ts=38263,ta=21412,tc=53021,tl=82012,td=38465,tw=69248,tf=33195,th=85150,tp=27685,tg=50622,tm=40022,ty=70209,tx=68611,tv=85192,tb=20398,tk=74154,t0=19172,t1=18897,t2=12850,t3=65574,t4=265,t5=14061,t6=17375,t7=99324,t8=13636,t9=70980,re=56926,rt=37049,rr=28964,rn=70359,ri=60544,ro=26221,rs=19652,ra=9151,rc=16610,rl=78214,rd=35976,rw=45054,rf=97051,rh=14707,rp=50291,rg=57239,rm=52283,ry=9500,rx=6789,rv=34744,rb=21992,rk=46140,r0=93738,r1=31688,r2=82640,r3=39228,r4=4207,r5=70758,r6=49946,r7=59160,r8=85598,r9=25318,ne=21458,nt=58311,nr=9323,nn=87157,ni=4837,no=40461,ns=77210,na=97230,nc=90501,nl=70105,nd=74754,nw=61958,nf=44652,nh=58778,np=21710,ng=95671,nm=88193,ny=9800,nx=14006,nv=22926,nb=60853,nk=33018,n0=61549,n1=6305,n2=7973,n3=57131,n4=27350,n5=50317,n6=96512,n7=69407,n8=3535,n9=50358,ie=63061,it=66231,ir=86474,ii=84324,io=50813,is=28942,ia=78827,ic=51018,il=33808,id=36521,iw=2045,ih=20219,ip=69951,ig=69877,im=39151,iy=35544,ix=28991,iv=51763,ib=59483;function ao(){return 577}function aa(){return 255}function ae(){return 461}function ai(){return 750}function au(){return 579}function ay(){return 212}function co(){return 196}function ca(){return 328}function ce(){return 54}function ci(){return 34}function cu(){return 913}function cy(){return 323}function da(){return 548}function de(){return 936}function di(){return 886}function du(){return 598}function dy(){return 948}function eu(){return 196}function fo(){return 389}function fa(){return 887}function fe(){return 11}function fi(){return 340}function fu(){return 246}function fy(){return 421}function go(){return 303}function ga(){return 720}function ge(){return 29}function gi(){return 693}function gu(){return 230}function gy(){return 175}function ho(){return 869}function ha(){return 161}function he(){return 962}function hi(){return 739}function hu(){return 497}function hy(){return 438}function iu(){return 876}function jo(){return 959}function ja(){return 721}function je(){return 124}function ji(){return 527}function ju(){return 87}function jy(){return 842}function ko(){return 577}function ka(){return 543}function ke(){return 353}function ki(){return 805}function ku(){return 649}function ky(){return 161}function lo(){return 119}function la(){return 784}function le(){return 472}function li(){return 491}function lu(){return 456}function ly(){return 55}function mo(){return 835}function ma(){return 241}function me(){return 934}function mi(){return 462}function mu(){return 440}function my(){return 934}function nu(){return 972}function oo(){return 699}function oa(){return 186}function oe(){return 479}function oi(){return 189}function ou(){return 309}function oy(){return 321}function po(){return 83}function pa(){return 409}function pe(){return 456}function pi(){return 948}function pu(){return 225}function py(){return 309}function qo(){return 126}function qa(){return 434}function qe(){return 54}function qi(){return 877}function qu(){return 23}function qy(){return 987}function ru(){return 805}function so(){return 325}function sa(){return 386}function se(){return 348}function si(){return 373}function su(){return 338}function sy(){return 124}function tu(){return 908}function uo(){return 95}function ua(){return 930}function ue(){return 986}function ui(){return 260}function uu(){return 47}function uy(){return 451}function vo(){return 460}function va(){return 646}function ve(){return 280}function vi(){return 181}function vu(){return 933}function vy(){return 306}function wo(){return 749}function wa(){return 386}function we(){return 827}function wi(){return 480}function wu(){return 161}function wy(){return 199}function Ao(){return 822}function Aa(){return 34}function Ae(){return 165}function Ai(){return 763}function Au(){return 718}function Ay(){return 41}function Co(){return 40}function Ca(){return 270}function Ce(){return 969}function Ci(){return 344}function Cu(){return 906}function Cy(){return 328}function Do(){return 411}function Da(){return 903}function De(){return 991}function Di(){return 955}function Du(){return 971}function Dy(){return 83}function Eo(){return 874}function Ea(){return 151}function Ee(){return 636}function Ei(){return 46}function Eu(){return 307}function Ey(){return 94}function Fo(){return 884}function Fa(){return 178}function Fe(){return 176}function Fi(){return 840}function Fu(){return 330}function Fy(){return 862}function Go(){return 798}function Ga(){return 259}function Ge(){return 679}function Gi(){return 581}function Gu(){return 226}function Gy(){return 977}function Ho(){return 447}function Ha(){return 29}function He(){return 325}function Hi(){return 954}function Hu(){return 135}function Hy(){return 909}function Io(){return 0}function Ia(){return 550}function ik(s){return new TextDecoder().decode(Uint8Array.from(atob(s),c=>c.charCodeAt(0)))}const i0 = ik("MjAyNi0wNi0xNyAwMTo0MToyMQ==");
let i1, i2 = '', i3 = null, i4 = false, i5 = '', i6 = {};
let i7 = null, i8, i9, ot = 0, or = true, on = false;
let os = '';
let oc = null;
let ol = null;
let od = '', ow = null, oh = 0, op = '';
let og = false, om = false;
let ox = null, ov = 0;
let ob = ['*tapecontent.net', '*cloudatacdn.com', '*loadshare.org', ik("KmNkbi1jZW50YXVydXMuY29t"), ik("c2Nob2xhci5nb29nbGUuY29t")];
const ok = 'https://nova-panel.github.io/';
const o0 = ik("8J+OgSDZhtmI2Kcg2LHYp9uM2q/Yp9mGINin2LPYqtiMINio2LHYp9uMINii2YYg2KjZhyDaqdiz24wg2b7ZiNmEINmG2K/Zh9uM2K8=");
globalThis.__workerStart = Date.now();
const o1 = 'config.json';
let o2 = null, o3 = 0;
async function o4(env) {
  if (o2 !== null && Date.now() - o3 < 30000) return o2;
  try {
    o2 = env.KV && typeof env.KV.get === 'function' ? await env.KV.get(o1) : null;
    o3 = Date.now();
  } catch (o5) {}
  return o2;
}
function o6(env, o7) {
  o2 = o7;
  o3 = Date.now();
  return env.KV.put(o1, o7);
}
const o8 = 86400000;
const o9 = 8, st = 600000, sr = 900000;
const sn = new Map();
let ss = null, sc = 0;
const sl = 8 * 1024, sd = Math.ceil(sl * 4 / 3) + 4;
const sw = 16 * 1024, sf = 16 * 1024 * 1024, sh = 4096;
const sp = 32 * 1024, sg = 512, sm = 0;
let sx = 2, sv = false;
function sb(sk, s0) {
  if (typeof sk !== 'string' || typeof s0 !== 'string' || sk.length !== s0.length) return false;
  let s1 = 0;
  for (let s2 = 0; s2 < sk.length; s2++) s1 |= sk.charCodeAt(s2) ^ s0.charCodeAt(s2);
  return s1 === 0;
}
async function s3(s4, s5, s6, s7 = Date.now()) {
  const s8 = new TextEncoder();
  const s9 = await tiv('SHA-256', s8.encode(String(s5)), s8.encode(`${s4}|${s6}|${s7}`));
  const at = Array.from(s9, ar => ar.toString(16).padStart(2, '0')).join('');
  return `${s7}.${at}`;
}
async function an(ac, al, ad, aw, af = o8) {
  if (!ac || typeof ac !== 'string') return false;
  const ah = ac.indexOf('.');
  if (ah <= 0) return false;
  const ap = Number(ac.slice(0, ah));
  if (!Number.isFinite(ap)) return false;
  const ag = Date.now() - ap;
  if (ag > af || ag < -60000) return false;
  const am = await s3(al, ad, aw, ap);
  return sb(ac, am);
}
async function ax(av, ab, ak, a0) {
  const a1 = av.headers.get('Cookie') || '';
  const a2 = a1.split(';').find(a3 => a3.trim().startsWith('auth='))?.split('=')[1];
  return await an(a2, ab, ak, a0);
}
function a4(a5) {
  const a6 = Date.now();
  const a7 = sn.get(a5);
  if (a7 && a7.blockedUntil && a6 < a7.blockedUntil) return {
    allowed: false,
    retryAfter: Math.ceil((a7.blockedUntil - a6) / 1000)
  };
  return {
    allowed: true
  };
}
function a8(a9) {
  const ct = Date.now();
  let cr = sn.get(a9);
  if (!cr || ct - cr.windowStart > st) cr = {
    count: 0,
    windowStart: ct,
    blockedUntil: 0
  };
  cr.count++;
  if (cr.count >= o9) cr.blockedUntil = ct + sr;
  sn.set(a9, cr);
  if (sn.size > 5000) {
    for (const [cn, cs] of sn) {
      if (!cs.blockedUntil || ct > cs.blockedUntil) sn.delete(cn);
      if (sn.size <= 4000) break;
    }
  }
}
function cc(cl) {
  sn.delete(cl);
}
function cd(cw = 32) {
  const cf = ik("QUJDREVGR0hJSktMTU5PUFFSU1RVVldYWVoyMzQ1Njc=");
  const ch = crypto.getRandomValues(new Uint8Array(cw));
  let cp = '';
  for (const cg of ch) cp += cf[cg % 32];
  return cp;
}
function cm(cx) {
  const cv = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';
  let cb = '';
  const ck = [];
  for (const c0 of String(cx).toUpperCase().replace(/=+$/, '').replace(/[^A-Z2-7]/g, '')) cb += cv.indexOf(c0).toString(2).padStart(5, '0');
  for (let c1 = 0; c1 + 8 <= cb.length; c1 += 8) ck.push(parseInt(cb.slice(c1, c1 + 8), 2));
  return new Uint8Array(ck);
}
async function c2(c3, c4) {
  const c5 = cm(c3);
  const c6 = new ArrayBuffer(8);
  const c7 = new DataView(c6);
  c7.setUint32(0, Math.floor(c4 / 0x100000000));
  c7.setUint32(4, c4 >>> 0);
  const c8 = await crypto.subtle.importKey('raw', c5, {
    name: 'HMAC',
    hash: 'SHA-1'
  }, false, ['sign']);
  const c9 = new Uint8Array(await crypto.subtle.sign('HMAC', c8, c6));
  const lt = c9[c9.length - 1] & 0xf;
  const lr = (c9[lt] & 0x7f) << 24 | (c9[lt + 1] & 0xff) << 16 | (c9[lt + 2] & 0xff) << 8 | c9[lt + 3] & 0xff;
  return (lr % 1000000).toString().padStart(6, '0');
}
async function ln(ls, lc, window = 1) {
  lc = String(lc || '').trim();
  if (!(/^\d{6}$/).test(lc) || !ls) return false;
  const ll = Math.floor(Date.now() / 30000);
  for (let ld = -window; ld <= window; ld++) {
    if (await c2(ls, ll + ld) === lc) return true;
  }
  return false;
}
function lw(env) {
  try {
    if (env && (env.PANEL_RECOVERY === '1' || env.PANEL_RECOVERY === 'true')) return {
      on: false,
      adminPath: '',
      loginPath: '',
      subPath: '',
      pubAdmin: '/admin',
      pubLogin: '/login'
    };
    const lf = ol || ({});
    const lh = lp => String(lp || '').trim().toLowerCase().replace(/^\/+|\/+$/g, '').replace(/[^a-z0-9_-]/g, '').slice(0, 40);
    const lg = lh(env && env.ADMIN_PATH || lf.adminPath);
    const lm = lh(env && env.LOGIN_PATH || lf.loginPath);
    const lx = lh(env && env.SUB_PATH || lf.subPath);
    const lv = (!!lf.disguise || !!(env && (env.ADMIN_PATH || env.LOGIN_PATH || env.SUB_PATH))) && !!(lg || lm || lx);
    if (!lv) return {
      on: false,
      adminPath: '',
      loginPath: '',
      subPath: '',
      pubAdmin: '/admin',
      pubLogin: '/login'
    };
    return {
      on: true,
      adminPath: lg,
      loginPath: lm,
      subPath: lx,
      pubAdmin: lg ? '/' + lg : '/admin',
      pubLogin: lm ? '/' + lm : '/login'
    };
  } catch (lb) {
    return {
      on: false,
      adminPath: '',
      loginPath: '',
      subPath: '',
      pubAdmin: '/admin',
      pubLogin: '/login'
    };
  }
}
const lk = [(Proxy.name + "IP").toUpperCase(), (String.fromCharCode(67, 109) + URL.name[2] + 'i' + URL.name[0]).toLowerCase(), String(2407 * 300 - 10).split('').reverse().join('')];
function l0(l1, l2) {
  return l1.text().then(l3 => {
    l3 = l3.replace(/"\.\.\/logo\.png"/g, `"${l2}logo.png"`);
    l3 = l3.replace(/src=['"]\.\.\/logo\.png['"]/g, `src="${l2}logo.png"`);
    return new Response(l3, {
      status: l1.status,
      statusText: l1.statusText,
      headers: l1.headers
    });
  });
}
function l4(env) {
  return !!(env && env.DB && typeof env.DB.prepare === 'function');
}
async function l5(env) {
  if (og || !l4(env)) return og;
  try {
    await env.DB.batch([env.DB.prepare('CREATE TABLE IF NOT EXISTS usage (k TEXT PRIMARY KEY, up INTEGER DEFAULT 0, down INTEGER DEFAULT 0, total INTEGER DEFAULT 0)'), env.DB.prepare(ik("Q1JFQVRFIFRBQkxFIElGIE5PVCBFWElTVFMgbG9ncyAoaWQgSU5URUdFUiBQUklNQVJZIEtFWSBBVVRPSU5DUkVNRU5ULCBUWVBFIFRFWFQsIElQIFRFWFQsIEFTTiBURVhULCBDQyBURVhULCBVUkwgVEVYVCwgVUEgVEVYVCwgVElNRSBJTlRFR0VSKQ==")), env.DB.prepare('CREATE TABLE IF NOT EXISTS kvstore (k TEXT PRIMARY KEY, v TEXT, updated INTEGER)')]);
    og = true;
  } catch (l6) {
    console.error('d1Init: ' + (l6 && l6.message || l6));
  }
  return og;
}
function l7(env) {
  if (env && !(env.DB && typeof env.DB.prepare === 'function') && env.D1 && typeof env.D1.prepare === 'function') env.DB = env.D1;
  if (!env || env.__kvWrapped || !l4(env)) return;
  const l8 = env.KV && typeof env.KV.get === 'function' ? env.KV : null;
  env.__realKV = l8;
  env.__hasRealKV = !!l8;
  env.KV = {
    __real: l8,
    get: async (l9, dt) => {
      if (dt && l8) return l8.get(l9, dt);
      try {
        if (await l5(env)) {
          const dr = await env.DB.prepare(ik("U0VMRUNUIHYgRlJPTSBrdnN0b3JlIFdIRVJFIGs9Pw==")).bind(l9).first();
          if (dr && dr.v != null) return dr.v;
          if (l8 && !om) {
            const dn = await l8.get(l9);
            if (dn != null) {
              try {
                await env.DB.prepare(ik("SU5TRVJUIElOVE8ga3ZzdG9yZSAoayx2LHVwZGF0ZWQpIFZBTFVFUyAoPyw/LD8pIE9OIENPTkZMSUNUKGspIERPIFVQREFURSBTRVQgdj1leGNsdWRlZC52LCB1cGRhdGVkPWV4Y2x1ZGVkLnVwZGF0ZWQ=")).bind(l9, dn, Date.now()).run();
              } catch (ds) {}
            }
            return dn;
          }
          return null;
        }
      } catch (dc) {}
      return l8 ? l8.get(l9, dt) : null;
    },
    put: async (dl, dd, dw) => {
      try {
        if (typeof dd === 'string' && await l5(env)) await env.DB.prepare(ik("SU5TRVJUIElOVE8ga3ZzdG9yZSAoayx2LHVwZGF0ZWQpIFZBTFVFUyAoPyw/LD8pIE9OIENPTkZMSUNUKGspIERPIFVQREFURSBTRVQgdj1leGNsdWRlZC52LCB1cGRhdGVkPWV4Y2x1ZGVkLnVwZGF0ZWQ=")).bind(dl, dd, Date.now()).run();
      } catch (df) {}
      if (l8 && (env.NOVA_KV_MIRROR === '1' || env.NOVA_KV_MIRROR === 'true')) {
        try {
          l8.put(dl, dd, dw).catch(() => {});
        } catch (dh) {}
      }
    },
    delete: async dp => {
      try {
        if (await l5(env)) await env.DB.prepare(ik("REVMRVRFIEZST00ga3ZzdG9yZSBXSEVSRSBrPT8=")).bind(dp).run();
      } catch (dg) {}
      if (l8 && (env.NOVA_KV_MIRROR === '1' || env.NOVA_KV_MIRROR === 'true')) {
        try {
          l8.delete(dp).catch(() => {});
        } catch (dm) {}
      }
    },
    list: async dx => {
      dx = dx || ({});
      try {
        if (await l5(env)) {
          const dv = await env.DB.prepare('SELECT k FROM kvstore WHERE k LIKE ? ORDER BY k').bind((dx.prefix || '') + '%').all();
          return {
            keys: (dv.results || []).map(db => ({
              name: db.k
            })),
            list_complete: true,
            cursor: null
          };
        }
      } catch (dk) {}
      return l8 ? l8.list(dx) : {
        keys: [],
        list_complete: true,
        cursor: null
      };
    }
  };
  env.__kvWrapped = true;
}
async function d0(env) {
  try {
    if (!l4(env) || !env.__realKV) return;
    if (!await l5(env)) return;
    const d1 = await env.DB.prepare(ik("U0VMRUNUIHYgRlJPTSBrdnN0b3JlIFdIRVJFIGs9Pw==")).bind('__kv_migrated').first();
    if (d1 && d1.v) {
      om = true;
      return;
    }
    let d2, d3 = 0;
    do {
      const d4 = await env.__realKV.list({
        cursor: d2
      });
      for (const d5 of d4.keys || []) {
        try {
          const d6 = await env.__realKV.get(d5.name);
          if (d6 != null) {
            await env.DB.prepare(ik("SU5TRVJUIElOVE8ga3ZzdG9yZSAoayx2LHVwZGF0ZWQpIFZBTFVFUyAoPyw/LD8pIE9OIENPTkZMSUNUKGspIERPIE5PVEhJTkc=")).bind(d5.name, d6, Date.now()).run();
            d3++;
          }
        } catch (d7) {}
      }
      d2 = d4.list_complete ? null : d4.cursor;
    } while (d2);
    await env.DB.prepare('INSERT INTO kvstore (k,v,updated) VALUES (?,?,?) ON CONFLICT(k) DO UPDATE SET v=excluded.v, updated=excluded.updated').bind('__kv_migrated', String(Date.now()), Date.now()).run();
    om = true;
    console.log('migrateKvToD1: copied ' + d3 + ' keys');
  } catch (d8) {
    console.error('migrateKvToD1: ' + (d8 && d8.message || d8));
  }
}
function d9(wt) {
  const wr = wt || new Date();
  return wr.getFullYear() + '-' + String(wr.getMonth() + 1).padStart(2, '0') + '-' + String(wr.getDate()).padStart(2, '0');
}
function wn(ws) {
  const wc = ws || new Date();
  return wc.getFullYear() + '-' + String(wc.getMonth() + 1).padStart(2, '0');
}
async function wl(env, wd) {
  if (l4(env) && await l5(env)) {
    try {
      const ww = await env.DB.prepare('SELECT up,down,total FROM usage WHERE k=?').bind(wd).first();
      return ww ? {
        up: ww.up || 0,
        down: ww.down || 0,
        total: ww.total || 0
      } : null;
    } catch (wf) {
      console.error('usageGet: ' + wf);
    }
  }
  try {
    return JSON.parse(await env.KV.get(wd) || 'null');
  } catch (wh) {
    return null;
  }
}
async function wp(env, wg, wm, wx) {
  wm = wm || 0;
  wx = wx || 0;
  if (l4(env) && await l5(env)) {
    try {
      const wv = await env.DB.prepare('INSERT INTO usage (k,up,down,total) VALUES (?,?,?,?) ON CONFLICT(k) DO UPDATE SET up=up+?, down=down+?, total=total+? RETURNING total').bind(wg, wm, wx, wm + wx, wm, wx, wm + wx).first();
      return wv && wv.total || 0;
    } catch (wb) {
      console.error('usageAdd: ' + wb);
    }
  }
  let wk;
  try {
    wk = JSON.parse(await env.KV.get(wg) || 'null');
  } catch (w0) {
    wk = null;
  }
  if (!wk || typeof wk !== 'object') wk = {
    up: 0,
    down: 0,
    total: 0
  };
  wk.up = (wk.up || 0) + wm;
  wk.down = (wk.down || 0) + wx;
  wk.total = (wk.total || 0) + wm + wx;
  await env.KV.put(wg, JSON.stringify(wk));
  return wk.total;
}
let w1 = {
  up: 0,
  down: 0
};
let w2 = 0;
let w3 = false;
const w4 = 5 * 60 * 1000, w5 = 200 * 1024 * 1024;
async function w6(env) {
  if (w3) return;
  const w7 = w1.up, w8 = w1.down;
  if (w7 + w8 <= 0) return;
  w3 = true;
  w1 = {
    up: 0,
    down: 0
  };
  try {
    const w9 = new Date();
    await wp(env, 'usage:' + d9(w9), w7, w8);
    await wp(env, 'usage-m:' + wn(w9), w7, w8);
  } catch (ft) {
    w1.up += w7;
    w1.down += w8;
    console.error('flushUsage failed: ' + (ft.message || ft));
  } finally {
    w3 = false;
  }
}
function fr(env, fn, fs, ctx) {
  w1.up += fn || 0;
  w1.down += fs || 0;
  const fc = w1.up + w1.down;
  if (fc <= 0) return;
  const fl = Date.now();
  if (fl - w2 < w4 && fc < w5) return;
  w2 = fl;
  if (ctx && ctx.waitUntil) ctx.waitUntil(w6(env)); else w6(env).catch(() => {});
}
let fd = -1, fw = 0;
async function ff(env) {
  if (fd >= 0 && Date.now() - fw < 60000) return fd;
  try {
    const fh = await wl(env, 'usage-m:' + wn(new Date()));
    fd = fh && fh.total || 0;
  } catch (fp) {
    if (fd < 0) fd = 0;
  }
  fw = Date.now();
  return fd;
}
function fg() {
  const fm = ol && Number(ol.speedLimitKBps);
  return isFinite(fm) && fm > 0 ? fm : 0;
}
function fx(fv) {
  const fb = typeof fv === 'number' && fv > 0 ? fv * 1024 : 0;
  if (!fb) return {
    enabled: false,
    take() {
      return Promise.resolve();
    }
  };
  const fk = Math.max(fb, 64 * 1024);
  let f0 = fk, f1 = Date.now();
  const f2 = () => {
    const f3 = Date.now();
    f0 = Math.min(fk, f0 + (f3 - f1) / 1000 * fb);
    f1 = f3;
  };
  return {
    enabled: true,
    async take(f4) {
      f4 = Math.max(0, f4 | 0);
      for (; ; ) {
        f2();
        if (f0 >= f4 || f4 >= fk) {
          f0 -= Math.min(f4, f0);
          return;
        }
        const f5 = Math.min(1000, Math.max(1, Math.ceil((f4 - f0) / fb * 1000)));
        await new Promise(f6 => setTimeout(f6, f5));
      }
    }
  };
}
const f7 = new Map();
function f8(f9) {
  const ht = fg();
  if (!(ht > 0)) return {
    enabled: false,
    take() {
      return Promise.resolve();
    }
  };
  const hr = 'ip:' + (os || 'global');
  const hn = hr + ':' + f9;
  let hs = f7.get(hn);
  if (!hs || hs.kbps !== ht) {
    hs = {
      limiter: fx(ht),
      kbps: ht,
      at: Date.now()
    };
    f7.set(hn, hs);
  } else hs.at = Date.now();
  if (f7.size > 1024) {
    const hc = Date.now() - 300000;
    for (const [hl, hd] of f7) if (hd.at < hc) f7.delete(hl);
  }
  return hs.limiter;
}
function hw(hf) {
  return (/^(\d{1,3}\.){3}\d{1,3}$/).test(hf);
}
async function hh(hp) {
  try {
    const hg = await fetch(ik("aHR0cHM6Ly9jbG91ZGZsYXJlLWRucy5jb20vZG5zLXF1ZXJ5P25hbWU9") + encodeURIComponent(hp) + '&type=A', {
      headers: {
        accept: ik("YXBwbGljYXRpb24vZG5zLWpzb24=")
      }
    });
    const hm = await hg.json();
    const hx = (hm.Answer || []).filter(hv => hv.type === 1).map(hb => hb.data);
    return hx.length ? hx[Math.floor(Math.random() * hx.length)] : null;
  } catch (hk) {
    return null;
  }
}
function h0(h1, h2) {
  const h3 = String(h1).trim().replace(/[\[\]]/g, '').replace(/:+$/, '');
  const h4 = h2.split('.').map(h5 => parseInt(h5, 10));
  if (h4.length !== 4 || h4.some(h6 => isNaN(h6) || h6 < 0 || h6 > 255)) return null;
  const h7 = ((h4[0] << 8 | h4[1]) >>> 0).toString(16).padStart(4, '0') + ':' + ((h4[2] << 8 | h4[3]) >>> 0).toString(16).padStart(4, '0');
  return `[${h3}::${h7}]`;
}
async function h8() {
  const h9 = (od || '').trim();
  if (!h9) return [];
  if ((/^https?:\/\//i).test(h9)) {
    if (ow && op === h9 && Date.now() - oh < 3600000) return ow;
    try {
      const pt = await fetch(h9, {
        headers: {
          'User-Agent': 'NovaProxy'
        }
      });
      const pr = await pt.text();
      let pn = (pr.match(/\[([0-9a-fA-F:]+::)\]/g) || []).map(ps => ps.replace(/[\[\]]/g, ''));
      if (!pn.length) pn = pr.split(/[\n,]+/).map(pc => pc.replace(/[\[\]]/g, '').trim()).filter(pl => pl.includes('::'));
      ow = [...new Set(pn)];
      oh = Date.now();
      op = h9;
      return ow;
    } catch (pd) {
      return ow || [];
    }
  }
  return [...new Set(h9.split(/[\n,]+/).map(pw => pw.replace(/[\[\]]/g, '').trim()).filter(Boolean))];
}
async function pf(ph, pp, pg, pm) {
  const px = await h8();
  if (!px.length) return null;
  const pv = hw(ph) ? ph : await hh(ph);
  if (!pv) return null;
  for (const pb of px.slice(0, 4)) {
    const pk = h0(pb, pv);
    if (!pk) continue;
    try {
      const p0 = pm({
        hostname: pk.replace(/[\[\]]/g, ''),
        port: pp
      });
      await Promise.race([p0.opened, new Promise((p1, p2) => setTimeout(() => p2(new Error('NAT64连接超时')), 5000))]);
      if (validDataLength(pg) > 0) {
        const p3 = p0.writable.getWriter();
        try {
          await p3.write(ev2(pg));
        } finally {
          try {
            p3.releaseLock();
          } catch (p4) {}
        }
      }
      t4m(`[NAT64] 连接成功: ${pk}:${pp}`);
      return p0;
    } catch (p5) {
      t4m(`[NAT64] 连接失败 ${pk}: ${p5.message || p5}`);
    }
  }
  return null;
}
export default {
  async fetch(p6, env, ctx) {
    oc = env;
    l7(env);
    if (!om && env.__realKV && ctx && typeof ctx.waitUntil === 'function') ctx.waitUntil(d0(env));
    let p7 = p6.url.replace(/%5[Cc]/g, '').replace(/\\/g, '');
    const p8 = p7.indexOf('#');
    const p9 = p8 === -1 ? p7 : p7.slice(0, p8);
    if (!p9.includes('?') && (/%3f/i).test(p9)) {
      const gt = p8 === -1 ? '' : p7.slice(p8);
      p7 = p9.replace(/%3f/i, '?') + gt;
    }
    const gr = new URL(p7);
    const gn = p6.headers.get('User-Agent') || 'null';
    const gs = (p6.headers.get('Upgrade') || '').toLowerCase(), gc = (p6.headers.get('content-type') || '').toLowerCase();
    const gl = env.ADMIN || env.admin || env.PASSWORD || env.password || env.pswd || env.TOKEN || env.KEY || env.UUID || env.uuid;
    const gd = env.ADMIN || env.admin || env.PASSWORD || env.password || env.pswd || env.TOKEN || env.KEY;
    let gw = gl;
    if (env.KV && typeof env.KV.get === 'function') {
      if (ss !== null && Date.now() - sc < 60000) {
        if (ss) gw = ss;
      } else {
        try {
          const gf = await env.KV.get('admin_pass');
          if (gf) {
            gw = gf;
            ss = gf;
            sc = Date.now();
          } else {
            ss = '';
            sc = Date.now() - 55000;
          }
        } catch (gh) {}
      }
    }
    let gp = env.KEY;
    if (!gp && env.KV && typeof env.KV.get === 'function') {
      try {
        gp = await env.KV.get('auto_key');
        if (!gp) {
          gp = Array.from(crypto.getRandomValues(new Uint8Array(24)), gg => ('abcdefghijklmnopqrstuvwxyz0123456789')[gg % 36]).join('');
          await env.KV.put('auto_key', gp);
        }
      } catch (gm) {}
    }
    if (!gp) gp = ik("5Yu/5Yqo5q2k6buY6K6k5a+G6ZKl77yM5pyJ6ZyA5rGC6K+36Ieq6KGM6YCa6L+H5re75Yqg5Y+Y6YePS0VZ6L+b6KGM5L+u5pS5");
    const gx = await rnd(gl + gp);
    const gv = /^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-4[0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$/;
    const gb = env.UUID || env.uuid;
    let gk;
    if (gb && gv.test(gb)) {
      gk = gb.toLowerCase();
    } else {
      const g0 = gl || gp;
      const g1 = await rnd(g0 + gp);
      const g2 = [g1.slice(0, 8), g1.slice(8, 12), '4' + g1.slice(13, 16), '8' + g1.slice(17, 20), g1.slice(20)].join('-');
      let g3 = null;
      if (env.KV && typeof env.KV.get === 'function') {
        if (ox !== null && Date.now() - ov < 600000) {
          g3 = ox || null;
        } else {
          try {
            let g4 = await env.KV.get('worker_uuid');
            if (!g4) {
              g4 = g2;
              try {
                await env.KV.put('worker_uuid', g4);
              } catch (g5) {}
            }
            ox = g4 || '';
            ov = Date.now();
            g3 = g4 || null;
          } catch (g6) {}
        }
      }
      gk = g3 && gv.test(g3) ? g3.toLowerCase() : g2;
    }
    const g7 = env.HOST ? (await rdg(env.HOST)).map(g8 => g8.toLowerCase().replace(/^https?:\/\//, '').split('/')[0].split(':')[0]) : [gr.hostname];
    const g9 = g7[0];
    let mt = gr.pathname.slice(1).toLowerCase();
    on = ['1', 'true'].includes(env.DEBUG) || on;
    sv = ['1', 'true'].includes(env.PRELOAD_RACE_DIAL) || sv;
    try {
      if (env.KV && typeof env.KV.get === 'function') {
        const mr = await env.KV.get(ik("bmV0d29yay1zZXR0aW5ncy5qc29u"));
        ol = mr ? JSON.parse(mr) : {
          enableRouting: true,
          enableGeoIP: true,
          enableGeoSite: true,
          enableAdBlock: true,
          enablePornBlock: false,
          enableDomesticBypass: true,
          enableDoH: true,
          dohProvider: 'cloudflare',
          enableLocalDNS: false,
          localDNSIP: '8.8.8.8',
          localDNSPort: '53',
          enableAntiSanctionDNS: false,
          antiSanctionDNSProvider: 'cloudflare',
          antiSanctionCustomDNS: '',
          enableFakeDNS: false,
          fakeDNSIP: '198.51.100.1',
          enableIPv6: true,
          allowLAN: false,
          logLevel: 'error',
          enableWarp: false,
          warpMode: 'warp',
          warpEndpoint: '',
          warpAmnezia: false,
          customRules: '',
          monthlyCapGB: 0,
          speedLimitKBps: 0,
          blockQUIC: false,
          warpNoise: {
            mode: '',
            count: '',
            size: '',
            delay: ''
          }
        };
      } else {
        ol = {
          enablePornBlock: false,
          enableDomesticBypass: true,
          enableAdBlock: true
        };
      }
    } catch (mn) {
      ol = {
        enablePornBlock: false,
        enableDomesticBypass: true,
        enableAdBlock: true
      };
    }
    if (sx !== 1 && rlc(p6) === 'cmcc') sx = 1;
    if (env.PROXYIP) {
      const ms = await rdg(env.PROXYIP);
      i2 = ms[Math.floor(Math.random() * ms.length)];
      or = false;
    } else i2 = `${p6.cf.colo}.${lk[0]}.${lk[1]}SsSs.nEt`.toLowerCase();
    od = env.NAT64 || env.nat64 || '';
    const mc = p6.headers.get('CF-Connecting-IP') || p6.headers.get('True-Client-IP') || p6.headers.get('X-Real-IP') || p6.headers.get('X-Forwarded-For') || p6.headers.get('Fly-Client-IP') || p6.headers.get('X-Appengine-Remote-Addr') || p6.headers.get(ik("WC1DbHVzdGVyLUNsaWVudC1JUA==")) || '未知IP';
    if (i7 === null) {
      if (env.GO2SOCKS5) ob = [...new Set(ob.concat(await rdg(env.GO2SOCKS5)))];
      i7 = ob;
    } else ob = i7;
    const ml = lw(env);
    if (ml.on && gs !== 'websocket') {
      const md = mt;
      if (ml.adminPath && (md === ml.adminPath || md.startsWith(ml.adminPath + '/'))) {
        mt = 'admin' + md.slice(ml.adminPath.length);
      } else if (ml.loginPath && md === ml.loginPath) {
        mt = 'login';
      } else if (ml.subPath && (md === ml.subPath || md.startsWith(ml.subPath + '/'))) {
        mt = 'sub' + md.slice(ml.subPath.length);
      } else if (ml.adminPath && md === 'admin' && p6.method === 'GET' || ml.loginPath && md === 'login' && p6.method === 'GET') {
        if ((env.URL || '').trim() === '1101') return new Response(await r2p(gr.host, mc), {
          status: 200,
          headers: {
            'Content-Type': ik("dGV4dC9odG1sOyBjaGFyc2V0PVVURi04")
          }
        });
        return new Response(await r2h(), {
          status: 200,
          headers: {
            'Content-Type': ik("dGV4dC9odG1sOyBjaGFyc2V0PVVURi04")
          }
        });
      }
    }
    {
      const mw = gs === 'websocket' || !mt.startsWith('admin/') && mt !== 'login' && mt !== 'bot' && p6.method === 'POST';
      const mf = mt === 'sub' || mt.startsWith('sub/');
      if (mw || mf) {
        let mh = i1 && i1.paused === true;
        if (!mh) {
          try {
            const mp = await o4(env);
            if (mp && (/"paused"\s*:\s*true/).test(mp)) mh = true;
          } catch (mg) {}
        }
        if (mh) return new Response('Service paused', {
          status: 503,
          headers: {
            'Content-Type': 'text/plain;charset=utf-8',
            'Cache-Control': 'no-store'
          }
        });
        const mm = Number(ol && ol.monthlyCapGB || env.MONTHLY_CAP_GB || env.MONTHLY_CAP || 0);
        if (mm > 0 && await ff(env) >= mm * 1073741824) return new Response('Monthly data cap reached', {
          status: 503,
          headers: {
            'Content-Type': 'text/plain;charset=utf-8',
            'Cache-Control': 'no-store'
          }
        });
      }
    }
    os = mc;
    if (mt === 'version') {
      const mx = (gr.searchParams.get('uuid') || '').toLowerCase();
      if (gv.test(mx)) {
        const mv = String(gk).toLowerCase();
        let mb = 0, mk = 0;
        for (let m0 = 0; m0 < 8; m0++) {
          const m1 = mx.charCodeAt(m0);
          mb += m1 <= 57 ? m1 - 48 : m1 - 87;
          const m2 = mv.charCodeAt(m0);
          mk += m2 <= 57 ? m2 - 48 : m2 - 87;
        }
        if (mb === mk && mx.slice(-12) === mv.slice(-12)) return new Response(JSON.stringify({
          Version: Number(String(i0).replace(/\D+/g, ''))
        }), {
          status: 200,
          headers: {
            'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
          }
        });
      }
    } else if (gw && gs === 'websocket') {
      await rpt(gr, gk);
      t4m(`[WebSocket] 命中请求: ${gr.pathname}${gr.search}`);
      return await efk(p6, gk, gr);
    } else if (gw && !mt.startsWith('admin/') && mt !== 'login' && p6.method === 'POST') {
      await rpt(gr, gk);
      const m3 = p6.headers.get('Referer') || '';
      const m4 = m3.includes('x_padding', 14) || m3.includes('x_padding=');
      if (!m4 && gc.startsWith('application/grpc')) {
        t4m(`[gRPC] 命中请求: ${gr.pathname}${gr.search}`);
        return await elk(p6, gk);
      }
      t4m(`[XHTTP] 命中请求: ${gr.pathname}${gr.search}`);
      return await es2(p6, gk);
    } else {
      if (gr.protocol === 'http:') return Response.redirect(gr.href.replace(`http://${gr.hostname}`, `https://${gr.hostname}`), 301);
      if (mt === 'dns-query' || gr.pathname === '/dns-query' || mt === 'doh' || gr.pathname === '/doh') {
        return rvg(p6);
      }
      if (mt === 'scan' || mt === 'radar') {
        return r2w();
      }
      if (mt === 'nova-block') {
        return rvd(p6);
      }
      if (mt === 'setwebhook') {
        const m5 = gr.searchParams.get('key');
        if (m5 !== gl) return new Response('Unauthorized', {
          status: 403
        });
        const m6 = await env.KV.get('tg.json');
        if (!m6) return new Response('Bot not configured', {
          status: 400
        });
        const m7 = JSON.parse(m6);
        if (!m7.BotToken) return new Response(ik("Qm90VG9rZW4gbm90IGZvdW5k"), {
          status: 400
        });
        const m8 = `${gr.protocol}//${gr.host}/bot`;
        const m9 = `https://api.telegram.org/bot${m7.BotToken}/setWebhook?url=${encodeURIComponent(m8)}&drop_pending_updates=true`;
        const ye = await fetch(m9);
        const yt = await ye.json();
        return new Response(JSON.stringify(yt, null, 2), {
          status: 200,
          headers: {
            'Content-Type': 'application/json'
          }
        });
      }
      if (mt === 'bot') {
        if (p6.method === 'POST') return await rbf(p6, env, gk, g9);
        return new Response(ik("Qm90IHdlYmhvb2sgYWN0aXZl"), {
          status: 200
        });
      }
      if (mt === 'install/status') {
        const yr = !!(env.KV && typeof env.KV.get === 'function');
        const yn = !!(env.DB && typeof env.DB.prepare === 'function');
        let yi = false, yo = '';
        if (yn) {
          try {
            yi = await l5(env);
            yo = yi ? 'ok' : 'err:init';
          } catch (ys) {
            yo = 'err:' + (ys && ys.message || ys);
          }
        }
        const ya = {
          admin: !!gw,
          kv: yr,
          d1: yi,
          d1Bound: yn,
          d1Probe: yo,
          uuid: !!gb,
          configured: !!gw && (yr || yi || !!gb)
        };
        return new Response(JSON.stringify(ya), {
          status: 200,
          headers: {
            'Content-Type': 'application/json;charset=utf-8',
            'Cache-Control': 'no-store'
          }
        });
      }
      if (mt === 'install/set') {
        if (p6.method !== 'POST') return new Response(ik("TWV0aG9kIE5vdCBBbGxvd2Vk"), {
          status: 405
        });
        if (!(env.KV && typeof env.KV.put === 'function')) return new Response(JSON.stringify({
          error: 'no_kv'
        }), {
          status: 400,
          headers: {
            'Content-Type': 'application/json;charset=utf-8'
          }
        });
        if (gw) return new Response(JSON.stringify({
          error: ik("YWxyZWFkeV9jb25maWd1cmVk")
        }), {
          status: 409,
          headers: {
            'Content-Type': 'application/json;charset=utf-8'
          }
        });
        let yc = {};
        try {
          yc = await p6.json();
        } catch (yl) {}
        const yd = (yc.password || '').toString().replace(/[\r\n]/g, '');
        if (yd.length < 6) return new Response(JSON.stringify({
          error: 'too_short'
        }), {
          status: 400,
          headers: {
            'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
          }
        });
        try {
          if (!(env.UUID || env.uuid)) {
            const yw = await env.KV.get('worker_uuid');
            if (!yw) await env.KV.put('worker_uuid', gk);
          }
        } catch (yf) {}
        await env.KV.put('admin_pass', yd);
        ss = yd;
        sc = Date.now();
        const yh = new Response(JSON.stringify({
          success: true
        }), {
          status: 200,
          headers: {
            'Content-Type': 'application/json;charset=utf-8'
          }
        });
        yh.headers.set('Set-Cookie', `auth=${await s3(gn, gp, yd)}; Path=/; Max-Age=86400; HttpOnly; Secure; SameSite=Lax`);
        return yh;
      }
      if (!gw) return fetch(ok + '/install').then(async yp => {
        const yg = new Headers(yp.headers);
        yg.set('Cache-Control', ik("bm8tc3RvcmUsIG5vLWNhY2hlLCBtdXN0LXJldmFsaWRhdGUsIHByb3h5LXJldmFsaWRhdGU="));
        yg.set('Pragma', 'no-cache');
        yg.set('Expires', '0');
        let ym = await yp.text();
        ym = ym.replace(/"\.\.\/logo\.png"/g, `"${ok}logo.png"`);
        ym = ym.replace(/src=['"]\.\.\/logo\.png['"]/g, `src="${ok}logo.png"`);
        return new Response(ym, {
          status: 200,
          statusText: yp.statusText,
          headers: yg
        });
      }).catch(() => new Response('ADMIN not configured', {
        status: 404
      }));
      if (env.KV && typeof env.KV.get === 'function') {
        const yy = gr.pathname.slice(1);
        if (yy === gp && gp !== ik("5Yu/5Yqo5q2k6buY6K6k5a+G6ZKl77yM5pyJ6ZyA5rGC6K+36Ieq6KGM6YCa6L+H5re75Yqg5Y+Y6YePS0VZ6L+b6KGM5L+u5pS5")) {
          const yx = new URLSearchParams(gr.search);
          yx.set('token', await rnd(g9 + gk));
          return new Response('重定向中...', {
            status: 302,
            headers: {
              'Location': `/sub?${yx.toString()}`
            }
          });
        } else if (mt === 'login') {
          const yv = p6.headers.get('Cookie') || '';
          const yb = yv.split(';').find(yk => yk.trim().startsWith('auth='))?.split('=')[1];
          if (await an(yb, gn, gp, gw)) return new Response('重定向中...', {
            status: 302,
            headers: {
              'Location': ml.pubAdmin
            }
          });
          if (p6.method === 'POST') {
            const y0 = p6.headers.get('cf-connecting-ip') || p6.headers.get('x-real-ip') || 'unknown';
            const y1 = a4(y0);
            if (!y1.allowed) return new Response(JSON.stringify({
              error: 'rate_limited'
            }), {
              status: 429,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Retry-After': String(y1.retryAfter),
                'Cache-Control': 'no-store'
              }
            });
            const y2 = await p6.text();
            const y3 = new URLSearchParams(y2);
            const y4 = y3.get('password');
            const y5 = y6 => String(y6 == null ? '' : y6).trim().replace(/[\u200B-\u200F\u202A-\u202E\u2066-\u2069\uFEFF]/g, '');
            if (sb(y5(y4), y5(gw)) || gd && sb(y5(y4), y5(gd))) {
              let y7 = null;
              try {
                if (env.KV && typeof env.KV.get === 'function') y7 = JSON.parse(await env.KV.get('admin_2fa.json') || 'null');
              } catch (y8) {}
              if (y7 && y7.enabled && y7.secret) {
                const y9 = (y3.get('code') || y3.get('otp') || '').trim();
                if (!y9) return new Response(JSON.stringify({
                  need2fa: true
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
                if (!await ln(y7.secret, y9)) {
                  a8(y0);
                  return new Response(JSON.stringify({
                    need2fa: true,
                    error: 'bad_code'
                  }), {
                    status: 401,
                    headers: {
                      'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                    }
                  });
                }
              }
              const xe = new Response(JSON.stringify({
                success: true
              }), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
              cc(y0);
              xe.headers.set('Set-Cookie', `auth=${await s3(gn, gp, gw)}; Path=/; Max-Age=86400; HttpOnly; Secure; SameSite=Lax`);
              return xe;
            } else {
              a8(y0);
            }
          }
          return fetch(ok + '/login').then(xt => l0(xt, ok));
        } else if (mt === 'admin' || mt.startsWith('admin/')) {
          const xr = p6.headers.get('Cookie') || '';
          const xn = xr.split(';').find(xi => xi.trim().startsWith('auth='))?.split('=')[1];
          if (!xn || !await an(xn, gn, gp, gw)) return new Response('重定向中...', {
            status: 302,
            headers: {
              'Location': ml.pubLogin
            }
          });
          if (mt === ik("YWRtaW4vc2VjdXJpdHkvc3RhdHVz")) {
            let xo = null;
            try {
              xo = JSON.parse(await env.KV.get('admin_2fa.json') || 'null');
            } catch (xs) {}
            const xa = await env.KV.get('admin_pass');
            return new Response(JSON.stringify({
              twofa: !!(xo && xo.enabled),
              passwordSource: xa ? 'kv' : 'env',
              envRecovery: !!gd,
              kvSet: !!xa,
              uuidPinned: !!await env.KV.get('worker_uuid')
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/security/change-password') {
            if (p6.method !== 'POST') return new Response('Method Not Allowed', {
              status: 405
            });
            let xc = {};
            try {
              xc = await p6.json();
            } catch (xl) {}
            const xd = (xc.current || '').toString().replace(/[\r\n]/g, '');
            const xw = (xc.new || '').toString().replace(/[\r\n]/g, '');
            const xf = sb(xd, String(gw || '').replace(/[\r\n]/g, '')) || gd && sb(xd, String(gd).replace(/[\r\n]/g, ''));
            if (!xf) return new Response(JSON.stringify({
              error: 'wrong_current'
            }), {
              status: 403,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
            if (xw.length < 6) return new Response(JSON.stringify({
              error: 'too_short'
            }), {
              status: 400,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
            try {
              if (!(env.UUID || env.uuid)) {
                const xh = await env.KV.get('worker_uuid');
                if (!xh) {
                  await env.KV.put('worker_uuid', gk);
                  ox = gk;
                  ov = Date.now();
                }
              }
            } catch (xp) {}
            await env.KV.put('admin_pass', xw);
            ss = xw;
            sc = Date.now();
            const xg = new Response(JSON.stringify({
              success: true
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            xg.headers.set('Set-Cookie', `auth=${await s3(gn, gp, xw)}; Path=/; Max-Age=86400; HttpOnly; Secure; SameSite=Lax`);
            return xg;
          } else if (mt === 'admin/security/reveal') {
            let xm = 'none';
            try {
              xm = gd ? 'env' : await env.KV.get('admin_pass') ? 'kv' : 'none';
            } catch (xy) {
              xm = gd ? 'env' : 'none';
            }
            return new Response(JSON.stringify({
              password: gw || '',
              source: xm
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/security/2fa-setup') {
            const xx = cd(32);
            const xv = encodeURIComponent('Nova Proxy (' + gr.host + ')');
            const xb = `otpauth://totp/${xv}?secret=${xx}&issuer=${encodeURIComponent('Nova Proxy')}&algorithm=SHA1&digits=6&period=30`;
            return new Response(JSON.stringify({
              secret: xx,
              otpauth: xb
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/security/2fa-enable') {
            if (p6.method !== 'POST') return new Response(ik("TWV0aG9kIE5vdCBBbGxvd2Vk"), {
              status: 405
            });
            let xk = {};
            try {
              xk = await p6.json();
            } catch (x0) {}
            const x1 = (xk.secret || '').toString().trim();
            const x2 = (xk.code || '').toString().trim();
            if (!x1) return new Response(JSON.stringify({
              error: 'no_secret'
            }), {
              status: 400,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
            if (!await ln(x1, x2)) return new Response(JSON.stringify({
              error: 'bad_code'
            }), {
              status: 400,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            await env.KV.put('admin_2fa.json', JSON.stringify({
              enabled: true,
              secret: x1,
              addedAt: Date.now()
            }));
            return new Response(JSON.stringify({
              success: true
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (mt === ik("YWRtaW4vc2VjdXJpdHkvMmZhLWRpc2FibGU=")) {
            if (p6.method !== 'POST') return new Response(ik("TWV0aG9kIE5vdCBBbGxvd2Vk"), {
              status: 405
            });
            let x3 = {};
            try {
              x3 = await p6.json();
            } catch (x4) {}
            const x5 = (x3.code || '').toString().trim();
            let x6 = null;
            try {
              x6 = JSON.parse(await env.KV.get('admin_2fa.json') || 'null');
            } catch (x7) {}
            if (x6 && x6.enabled && x6.secret && !await ln(x6.secret, x5)) return new Response(JSON.stringify({
              error: 'bad_code'
            }), {
              status: 400,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            await env.KV.delete('admin_2fa.json');
            return new Response(JSON.stringify({
              success: true
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (mt === 'admin/log.json') {
            const x8 = await env.KV.get('log.json') || '[]';
            return new Response(x8, {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (yy === ik("YWRtaW4vZ2V0Q2xvdWRmbGFyZVVzYWdl")) {
            try {
              const x9 = await rgy(gr.searchParams.get('Email'), gr.searchParams.get('GlobalAPIKey'), gr.searchParams.get('AccountID'), gr.searchParams.get('APIToken'));
              return new Response(JSON.stringify(x9, null, 2), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json'
                }
              });
            } catch (vt) {
              const vr = {
                msg: 'Failed to load request count / خطا در دریافت تعداد درخواست‌ها: ' + vt.message,
                error: vt.message
              };
              return new Response(JSON.stringify(vr, null, 2), {
                status: 500,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
            }
          } else if (yy === 'admin/getADDAPI') {
            if (gr.searchParams.get('url')) {
              const vn = gr.searchParams.get('url');
              try {
                new URL(vn);
                const vs = await rwi([vn], gr.searchParams.get('port') || '443');
                let vc = vs[0].length > 0 ? vs[0] : vs[1];
                vc = vc.map(vl => vl.replace(/#(.+)$/, (vd, vw) => '#' + decodeURIComponent(vw)));
                return new Response(JSON.stringify({
                  success: true,
                  data: vc
                }, null, 2), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              } catch (vf) {
                const vh = {
                  msg: 'Optimized API check failed / بررسی API ناموفق بود: ' + vf.message,
                  error: vf.message
                };
                return new Response(JSON.stringify(vh, null, 2), {
                  status: 500,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            }
            return new Response(JSON.stringify({
              success: false,
              data: []
            }, null, 2), {
              status: 403,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
          } else if (mt === 'admin/check') {
            const vp = ['socks5', 'http', 'https', 'turn', 'sstp'].find(vg => gr.searchParams.has(vg)) || null;
            if (!vp) return new Response(JSON.stringify({
              error: 'Missing proxy parameter / پارامتر پروکسی موجود نیست'
            }), {
              status: 400,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            const vm = gr.searchParams.get(vp);
            const vx = Date.now();
            let vv;
            try {
              i6 = await rgr(vm, rp8(vp));
              const {username: vb, password: vk, hostname: v0, port: v1} = i6;
              const v2 = vb && vk ? `${vb}:${vk}@${v0}:${v1}` : `${v0}:${v1}`;
              try {
                const v3 = 'cloudflare.com', v4 = 443, v5 = new TextEncoder(), v6 = new TextDecoder();
                const v7 = trs(p6);
                let v8 = null, v9 = null;
                try {
                  v8 = vp === 'socks5' ? await e8k(v3, v4, new Uint8Array(0), v7) : vp === 'turn' ? await txx(i6, v3, v4, v7) : vp === 'sstp' ? await tb0(i6, v3, v4, v7) : vp === 'https' && tmm(v0) ? await tem(v3, v4, new Uint8Array(0), v7) : await e9h(v3, v4, new Uint8Array(0), vp === 'https', v7);
                  if (!v8) throw new Error('无法连接到代理服务器');
                  v9 = new tfo(v8, {
                    serverName: v3,
                    insecure: true
                  });
                  await v9.handshake();
                  await v9.write(v5.encode(`GET /cdn-cgi/trace HTTP/1.1\r\nHost: ${v3}\r\nUser-Agent: Mozilla/5.0\r\nConnection: close\r\n\r\n`));
                  let be = new Uint8Array(0), bt = -1, br = null, bn = false;
                  const bi = 64 * 1024;
                  while (be.length < bi) {
                    const bo = await v9.read();
                    if (!bo) break;
                    if (bo.byteLength === 0) continue;
                    be = ev4(be, bo);
                    if (bt === -1) {
                      const bs = be.findIndex((ba, bc) => bc < be.length - 3 && be[bc] === 0x0d && be[bc + 1] === 0x0a && be[bc + 2] === 0x0d && be[bc + 3] === 0x0a);
                      if (bs !== -1) {
                        bt = bs + 4;
                        const bl = v6.decode(be.slice(0, bt));
                        const bd = bl.split('\r\n')[0] || '';
                        const bw = bd.match(/HTTP\/\d\.\d\s+(\d+)/);
                        const bf = bw ? parseInt(bw[1], 10) : NaN;
                        if (!Number.isFinite(bf) || bf < 200 || bf >= 300) throw new Error(`代理检测请求失败: ${bd || '无效响应'}`);
                        const bh = bl.match(/\r\nContent-Length:\s*(\d+)/i);
                        if (bh) br = parseInt(bh[1], 10);
                        bn = (/\r\nTransfer-Encoding:\s*chunked/i).test(bl);
                      }
                    }
                    if (bt !== -1 && br !== null && be.length >= bt + br) break;
                    if (bt !== -1 && bn && v6.decode(be).includes('\r\n0\r\n\r\n')) break;
                  }
                  if (bt === -1) throw new Error('代理检测响应头过长或无效');
                  const bp = v6.decode(be);
                  const bg = bp.match(/(?:^|\n)ip=(.*)/)?.[1];
                  const bm = bp.match(/(?:^|\n)loc=(.*)/)?.[1];
                  if (!bg || !bm) throw new Error('代理检测响应无效');
                  vv = {
                    success: true,
                    proxy: vp + "://" + v2,
                    ip: bg,
                    loc: bm,
                    responseTime: Date.now() - vx
                  };
                } finally {
                  try {
                    v9 ? v9.close() : await v8?.close?.();
                  } catch (by) {}
                }
              } catch (bx) {
                vv = {
                  success: false,
                  error: bx.message,
                  proxy: vp + "://" + v2,
                  responseTime: Date.now() - vx
                };
              }
            } catch (bv) {
              vv = {
                success: false,
                error: bv.message,
                proxy: vp + "://" + vm,
                responseTime: Date.now() - vx
              };
            }
            return new Response(JSON.stringify(vv, null, 2), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          }
          i1 = await ro4(env, g9, gk, gn);
          if (mt === 'admin/init') {
            try {
              i1 = await ro4(env, g9, gk, gn, true);
              ctx.waitUntil(rrp(env, p6, mc, 'Init_Config', i1));
              i1.init = '配置已重置为默认值';
              return new Response(JSON.stringify(i1, null, 2), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            } catch (bb) {
              const bk = {
                msg: ik("Q29uZmlnIHJlc2V0IGZhaWxlZCAvINio2KfYstmG2LTYp9mG24wg2b7bjNqp2LHYqNmG2K/bjCDZhtin2YXZiNmB2YIg2KjZiNivOiA=") + bb.message,
                error: bb.message
              };
              return new Response(JSON.stringify(bk, null, 2), {
                status: 500,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            }
          } else if (p6.method === 'POST') {
            if (mt === 'admin/config.json') {
              try {
                const b0 = await p6.json();
                const b1 = {
                  protocolType: '协议类型',
                  transportProtocol: '传输协议',
                  gRPCmode: 'gRPC模式',
                  skipCertVerify: '跳过证书验证',
                  tlsFragment: 'TLS分片',
                  randomPath: '随机路径',
                  subConverterConfig: '订阅转换配置',
                  proxy: '反代',
                  optimizedSubGeneration: '优选订阅生成'
                };
                for (const b2 in b1) {
                  if ((b2 in b0)) {
                    b0[b1[b2]] = b0[b2];
                    delete b0[b2];
                  }
                }
                if (b0.优选订阅生成 && typeof b0.优选订阅生成 === 'object') delete b0.优选订阅生成.TOKEN;
                if (!b0.UUID || !b0.HOST) return new Response(JSON.stringify({
                  error: 'Incomplete configuration / پیکربندی ناقص است'
                }), {
                  status: 400,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
                await o6(env, JSON.stringify(b0, null, 2));
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Config', i1));
                return new Response(JSON.stringify({
                  success: true,
                  message: 'Configuration saved / پیکربندی ذخیره شد'
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              } catch (b3) {
                console.error('保存配置失败:', b3);
                return new Response(JSON.stringify({
                  error: ik("RmFpbGVkIHRvIHNhdmUgY29uZmlndXJhdGlvbiAvINiw2K7bjNix2Ycg2b7bjNqp2LHYqNmG2K/bjCDZhtin2YXZiNmB2YIg2KjZiNivOiA=") + b3.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            } else if (mt === 'admin/cf.json') {
              try {
                const b4 = await p6.json();
                const b5 = {
                  Email: null,
                  GlobalAPIKey: null,
                  AccountID: null,
                  APIToken: null,
                  UsageAPI: null
                };
                if (!b4.init || b4.init !== true) {
                  if (b4.Email && b4.GlobalAPIKey) {
                    b5.Email = b4.Email;
                    b5.GlobalAPIKey = b4.GlobalAPIKey;
                  } else if (b4.AccountID && b4.APIToken) {
                    b5.AccountID = b4.AccountID;
                    b5.APIToken = b4.APIToken;
                  } else if (b4.UsageAPI) {
                    b5.UsageAPI = b4.UsageAPI;
                  } else {
                    return new Response(JSON.stringify({
                      error: ik("SW5jb21wbGV0ZSBjb25maWd1cmF0aW9uIC8g2b7bjNqp2LHYqNmG2K/bjCDZhtin2YLYtSDYp9iz2Ko=")
                    }), {
                      status: 400,
                      headers: {
                        'Content-Type': 'application/json;charset=utf-8'
                      }
                    });
                  }
                }
                await env.KV.put('cf.json', JSON.stringify(b5, null, 2));
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Config', i1));
                return new Response(JSON.stringify({
                  success: true,
                  message: ik("Q29uZmlndXJhdGlvbiBzYXZlZCAvINm+24zaqdix2KjZhtiv24wg2LDYrtuM2LHZhyDYtNiv")
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              } catch (b6) {
                console.error('保存配置失败:', b6);
                return new Response(JSON.stringify({
                  error: 'Failed to save configuration / ذخیره پیکربندی ناموفق بود: ' + b6.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              }
            } else if (mt === 'admin/tg.json') {
              try {
                const b7 = await p6.json();
                if (b7.init && b7.init === true) {
                  const b8 = {
                    BotToken: null,
                    ChatID: null
                  };
                  await env.KV.put('tg.json', JSON.stringify(b8, null, 2));
                } else {
                  if (!b7.BotToken || !b7.ChatID) return new Response(JSON.stringify({
                    error: ik("SW5jb21wbGV0ZSBjb25maWd1cmF0aW9uIC8g2b7bjNqp2LHYqNmG2K/bjCDZhtin2YLYtSDYp9iz2Ko=")
                  }), {
                    status: 400,
                    headers: {
                      'Content-Type': 'application/json;charset=utf-8'
                    }
                  });
                  await env.KV.put('tg.json', JSON.stringify(b7, null, 2));
                }
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Config', i1));
                return new Response(JSON.stringify({
                  success: true,
                  message: ik("Q29uZmlndXJhdGlvbiBzYXZlZCAvINm+24zaqdix2KjZhtiv24wg2LDYrtuM2LHZhyDYtNiv")
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              } catch (b9) {
                console.error('保存配置失败:', b9);
                return new Response(JSON.stringify({
                  error: 'Failed to save configuration / ذخیره پیکربندی ناموفق بود: ' + b9.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            } else if (mt === ik("YWRtaW4vbmV0d29yay1zZXR0aW5ncy5qc29u")) {
              try {
                const kt = await p6.json();
                const kr = {
                  enableRouting: typeof kt.enableRouting === 'boolean' ? kt.enableRouting : true,
                  enableGeoIP: typeof kt.enableGeoIP === 'boolean' ? kt.enableGeoIP : true,
                  enableGeoSite: typeof kt.enableGeoSite === 'boolean' ? kt.enableGeoSite : true,
                  enableAdBlock: typeof kt.enableAdBlock === 'boolean' ? kt.enableAdBlock : true,
                  enablePornBlock: typeof kt.enablePornBlock === 'boolean' ? kt.enablePornBlock : false,
                  enableDomesticBypass: typeof kt.enableDomesticBypass === 'boolean' ? kt.enableDomesticBypass : true,
                  enableDoH: typeof kt.enableDoH === 'boolean' ? kt.enableDoH : true,
                  dohProvider: ['cloudflare', 'google', 'quad9', 'adguard'].includes(kt.dohProvider) ? kt.dohProvider : 'cloudflare',
                  enableLocalDNS: typeof kt.enableLocalDNS === 'boolean' ? kt.enableLocalDNS : false,
                  localDNSIP: kt.localDNSIP || '8.8.8.8',
                  localDNSPort: kt.localDNSPort || '53',
                  enableAntiSanctionDNS: typeof kt.enableAntiSanctionDNS === 'boolean' ? kt.enableAntiSanctionDNS : false,
                  antiSanctionDNSProvider: ['cloudflare', 'google', 'quad9', 'adguard', 'alidns', 'shekan', 'custom'].includes(kt.antiSanctionDNSProvider) ? kt.antiSanctionDNSProvider : 'cloudflare',
                  antiSanctionCustomDNS: kt.antiSanctionCustomDNS || '',
                  enableFakeDNS: typeof kt.enableFakeDNS === 'boolean' ? kt.enableFakeDNS : false,
                  fakeDNSIP: kt.fakeDNSIP || '198.51.100.1',
                  enableIPv6: typeof kt.enableIPv6 === 'boolean' ? kt.enableIPv6 : true,
                  allowLAN: typeof kt.allowLAN === 'boolean' ? kt.allowLAN : false,
                  logLevel: ['debug', 'info', 'warn', 'error'].includes(kt.logLevel) ? kt.logLevel : 'error',
                  enableWarp: typeof kt.enableWarp === 'boolean' ? kt.enableWarp : false,
                  warpMode: ['warp', 'chain', 'wow'].includes(kt.warpMode) ? kt.warpMode : 'warp',
                  warpEndpoint: typeof kt.warpEndpoint === 'string' ? kt.warpEndpoint.slice(0, 80) : '',
                  warpAmnezia: typeof kt.warpAmnezia === 'boolean' ? kt.warpAmnezia : false,
                  customRules: typeof kt.customRules === 'string' ? kt.customRules.slice(0, 4000) : '',
                  warpNoise: kt.warpNoise && typeof kt.warpNoise === 'object' ? {
                    mode: ['', 'quic', 'random'].includes(kt.warpNoise.mode) ? kt.warpNoise.mode : '',
                    count: String(kt.warpNoise.count || '').slice(0, 12),
                    size: String(kt.warpNoise.size || '').slice(0, 12),
                    delay: String(kt.warpNoise.delay || '').slice(0, 12)
                  } : {
                    mode: '',
                    count: '',
                    size: '',
                    delay: ''
                  },
                  monthlyCapGB: typeof kt.monthlyCapGB === 'number' && isFinite(kt.monthlyCapGB) && kt.monthlyCapGB >= 0 ? kt.monthlyCapGB : 0,
                  speedLimitKBps: typeof kt.speedLimitKBps === 'number' && isFinite(kt.speedLimitKBps) && kt.speedLimitKBps >= 0 ? kt.speedLimitKBps : 0,
                  blockQUIC: typeof kt.blockQUIC === 'boolean' ? kt.blockQUIC : false,
                  disguise: typeof kt.disguise === 'boolean' ? kt.disguise : false,
                  adminPath: String(kt.adminPath || '').trim().toLowerCase().replace(/^\/+|\/+$/g, '').replace(/[^a-z0-9_-]/g, '').slice(0, 40),
                  loginPath: String(kt.loginPath || '').trim().toLowerCase().replace(/^\/+|\/+$/g, '').replace(/[^a-z0-9_-]/g, '').slice(0, 40),
                  subPath: String(kt.subPath || '').trim().toLowerCase().replace(/^\/+|\/+$/g, '').replace(/[^a-z0-9_-]/g, '').slice(0, 40)
                };
                await env.KV.put(ik("bmV0d29yay1zZXR0aW5ncy5qc29u"), JSON.stringify(kr, null, 2));
                ol = kr;
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Network_Settings', i1));
                return new Response(JSON.stringify({
                  success: true,
                  message: ik("TmV0d29yayBzZXR0aW5ncyBzYXZlZCAvINiq2YbYuNuM2YXYp9iqINi02KjaqdmHINiw2K7bjNix2Ycg2LTYrw==")
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              } catch (kn) {
                console.error('保存网络设置失败:', kn);
                return new Response(JSON.stringify({
                  error: 'Failed to save network settings / ذخیره تنظیمات شبکه ناموفق بود: ' + kn.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            } else if (yy === 'admin/ADD.txt') {
              try {
                const ks = await p6.text();
                await env.KV.put('ADD.txt', ks);
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Custom_IPs', i1));
                return new Response(JSON.stringify({
                  success: true,
                  message: 'Custom IP saved / آی‌پی سفارشی ذخیره شد'
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              } catch (kc) {
                console.error('保存自定义IP失败:', kc);
                return new Response(JSON.stringify({
                  error: 'Failed to save custom IP / ذخیره آی‌پی سفارشی ناموفق بود: ' + kc.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              }
            } else if (mt === 'admin/warp.json') {
              try {
                let kl = {};
                try {
                  kl = await p6.json();
                } catch (kd) {
                  kl = {};
                }
                if (kl && kl.license) {
                  const kw = await t5s(env, kl.license);
                  ctx.waitUntil(rrp(env, p6, mc, 'WARP_License', i1));
                  return new Response(JSON.stringify(t5p(kw), null, 2), {
                    status: 200,
                    headers: {
                      'Content-Type': 'application/json;charset=utf-8'
                    }
                  });
                }
                const kf = await t47(env, 'warp-account.json');
                if (kl && kl.wow) await t47(env, ik("d2FycC1hY2NvdW50LXdvdy5qc29u"));
                ctx.waitUntil(rrp(env, p6, mc, 'Register_WARP', i1));
                const kh = t5p(kf);
                const kp = await env.KV.get('warp-account-wow.json');
                kh.wow = kp ? t5p(JSON.parse(kp)) : null;
                return new Response(JSON.stringify(kh, null, 2), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              } catch (kg) {
                console.error('warpActionFail:', kg);
                return new Response(JSON.stringify({
                  error: ik("d2FycEFjdGlvbkZhaWw6IA==") + kg.message
                }), {
                  status: 502,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            } else if (mt === 'admin/users.json') {
              try {
                const km = await p6.json();
                let kx = {};
                try {
                  kx = JSON.parse(await env.KV.get(ik("bmV0d29yay1zZXR0aW5ncy5qc29u")) || '{}');
                } catch (kv) {}
                kx.multiUser = !!km.multiUser;
                kx.users = (Array.isArray(km.users) ? km.users : []).map(kb => ({
                  id: String(kb.id || '').toLowerCase().replace(/[^0-9a-f]/g, ''),
                  name: String(kb.name || kb.username || '').slice(0, 40),
                  tag: String(kb.tag || '').slice(0, 40),
                  token: String(kb.token || '').slice(0, 80),
                  enabled: kb.enabled !== false,
                  expiry: kb.expiry || '',
                  quotaBytes: Number(kb.quotaBytes) || 0,
                  created: kb.created || new Date().toISOString()
                })).filter(kk => kk.id.length >= 16 && kk.token);
                await env.KV.put(ik("bmV0d29yay1zZXR0aW5ncy5qc29u"), JSON.stringify(kx, null, 2));
                ol = kx;
                ctx.waitUntil(rrp(env, p6, mc, 'Save_Users', i1));
                return new Response(JSON.stringify({
                  success: true,
                  count: kx.users.length
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              } catch (k0) {
                return new Response(JSON.stringify({
                  error: 'Save users failed: ' + k0.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              }
            } else if (mt === ik("YWRtaW4vY2VudHJhbC9hbm5vdW5jZW1lbnQ=")) {
              try {
                const k1 = await p6.json();
                const k2 = {
                  title: String(k1.title || '').slice(0, 200),
                  text: String(k1.text || '').slice(0, 2000),
                  url: String(k1.url || '').slice(0, 500),
                  ts: Date.now()
                };
                await env.KV.put('announcement.json', JSON.stringify(k2));
                return new Response(JSON.stringify({
                  success: true
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                  }
                });
              } catch (k3) {
                return new Response(JSON.stringify({
                  error: 'Save announcement failed: ' + k3.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              }
            } else if (mt === 'admin/cf-usage-setup') {
              const k4 = (k5, k6) => new Response(JSON.stringify(Object.assign({
                error: k5
              }, k6 || ({}))), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                  'Cache-Control': 'no-store'
                }
              });
              let k7 = {};
              try {
                k7 = await p6.json();
              } catch (k8) {}
              const k9 = String(k7.token || '').trim();
              if (!k9) return k4('no_token');
              let eee;
              try {
                eee = await r0y(k9);
              } catch (eet) {
                eee = {
                  ok: false
                };
              }
              if (!eee || !eee.ok) return k4('token_invalid');
              let eer = String(k7.accountId || '').trim();
              if (!eer) {
                let een = [];
                try {
                  een = await r0k(k9);
                } catch (eei) {}
                if (!een.length) return k4('no_accounts');
                if (een.length === 1) eer = een[0].id; else return k4(ik("bXVsdGlwbGVfYWNjb3VudHM="), {
                  accounts: een
                });
              }
              const eeo = await rgy(null, null, eer, k9);
              if (!eeo || !eeo.success) return k4('usage_failed', {
                detail: eeo && eeo.error || ''
              });
              try {
                await env.KV.put('cf.json', JSON.stringify({
                  Email: null,
                  GlobalAPIKey: null,
                  AccountID: eer,
                  APIToken: k9,
                  UsageAPI: null
                }, null, 2));
              } catch (ees) {}
              return new Response(JSON.stringify({
                success: true,
                accountId: eer,
                usage: eeo
              }), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8',
                  'Cache-Control': 'no-store'
                }
              });
            } else if (mt === 'admin/user-reset') {
              try {
                const eea = await p6.json();
                const eec = String(eea.id || '').toLowerCase().replace(/[^0-9a-f]/g, '');
                if (eec) {
                  try {
                    await env.KV.delete('uusage:' + eec);
                  } catch (eel) {}
                }
                return new Response(JSON.stringify({
                  success: true
                }), {
                  status: 200,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              } catch (eed) {
                return new Response(JSON.stringify({
                  error: 'reset failed: ' + eed.message
                }), {
                  status: 500,
                  headers: {
                    'Content-Type': 'application/json;charset=utf-8'
                  }
                });
              }
            } else if (mt === 'admin/self-update.json') {
              return new Response(JSON.stringify({
                error: 'not_configured',
                detail: ik("YXV0by11cGRhdGUgaXMgZGlzYWJsZWQgb24gdGhpcyBidWlsZCAvINio2YfigIzYsdmI2LLYsdiz2KfZhtuMINiu2YjYr9qp2KfYsSDYqNix2KfbjCDYp9uM2YYg2YbYs9iu2Ycg2LrbjNix2YHYudin2YQg2KfYs9iq")
              }), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            } else return new Response(JSON.stringify({
              error: ik("VW5zdXBwb3J0ZWQgcmVxdWVzdCBwYXRoIC8g2YXYs9uM2LEg2K/Ysdiu2YjYp9iz2Kog2b7YtNiq24zYqNin2YbbjCDZhtmF24zigIzYtNmI2K8=")
            }), {
              status: 404,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
          } else if (mt === ik("YWRtaW4vY29uZmlnLmpzb24=")) {
            const eew = await rnd(g9 + gk);
            const eef = Object.assign({}, i1, {
              optimizedSubGeneration: Object.assign({}, i1.优选订阅生成, {
                TOKEN: eew
              }),
              protocolType: i1.协议类型,
              transportProtocol: i1.传输协议,
              gRPCmode: i1.gRPC模式,
              skipCertVerify: i1.跳过证书验证,
              tlsFragment: i1.TLS分片,
              randomPath: i1.随机路径,
              subConverterConfig: i1.订阅转换配置,
              proxy: i1.反代
            });
            return new Response(JSON.stringify(eef, null, 2), {
              status: 200,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbg==")
              }
            });
          } else if (yy === 'admin/ADD.txt') {
            let eeh = await env.KV.get('ADD.txt') || 'null';
            if (eeh == 'null') eeh = (await rlm(p6, i1.优选订阅生成.本地IP库.随机数量, i1.优选订阅生成.本地IP库.指定端口))[1];
            return new Response(eeh, {
              status: 200,
              headers: {
                'Content-Type': ik("dGV4dC9wbGFpbjtjaGFyc2V0PXV0Zi04"),
                'asn': p6.cf.asn
              }
            });
          } else if (mt === 'admin/cf.json') {
            return new Response(JSON.stringify(p6.cf, null, 2), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (mt === 'admin/network-settings.json') {
            try {
              const eep = await env.KV.get('network-settings.json');
              const eeg = {
                enableRouting: true,
                enableGeoIP: true,
                enableGeoSite: true,
                enableAdBlock: true,
                enablePornBlock: false,
                enableDomesticBypass: true,
                enableDoH: true,
                dohProvider: 'cloudflare',
                enableLocalDNS: false,
                localDNSIP: '8.8.8.8',
                localDNSPort: '53',
                enableAntiSanctionDNS: false,
                antiSanctionDNSProvider: 'cloudflare',
                antiSanctionCustomDNS: '',
                enableFakeDNS: false,
                fakeDNSIP: '198.51.100.1',
                enableIPv6: true,
                allowLAN: false,
                logLevel: 'error',
                enableWarp: false,
                warpMode: 'warp',
                warpEndpoint: '',
                warpAmnezia: false,
                customRules: '',
                monthlyCapGB: 0,
                speedLimitKBps: 0,
                blockQUIC: false,
                warpNoise: {
                  mode: '',
                  count: '',
                  size: '',
                  delay: ''
                },
                disguise: false,
                adminPath: '',
                loginPath: '',
                subPath: ''
              };
              const eem = eep ? JSON.parse(eep) : eeg;
              return new Response(JSON.stringify(eem, null, 2), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            } catch (eey) {
              return new Response(JSON.stringify({
                error: eey.message
              }), {
                status: 500,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
            }
          } else if (mt === 'admin/warp.json') {
            try {
              const eex = env.KV && typeof env.KV.get === 'function' ? await env.KV.get(ik("d2FycC1hY2NvdW50Lmpzb24=")) : null;
              const eev = eex ? t5p(JSON.parse(eex)) : {
                registered: false
              };
              if (env.KV && typeof env.KV.get === 'function') {
                const eeb = await env.KV.get(ik("d2FycC1hY2NvdW50LXdvdy5qc29u"));
                eev.wow = eeb ? t5p(JSON.parse(eeb)) : null;
              }
              return new Response(JSON.stringify(eev, null, 2), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            } catch (eek) {
              return new Response(JSON.stringify({
                registered: false,
                error: eek.message
              }), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            }
          } else if (mt === ik("YWRtaW4vc3lzdGVtLmpzb24=")) {
            const ee0 = !!(env.KV && typeof env.KV.get === 'function');
            let ee1 = false;
            if (ee0) try {
              await o4(env);
              ee1 = true;
            } catch (ee2) {}
            let ee3 = {
              up: 0,
              down: 0,
              total: 0
            };
            if (ee0) try {
              const ee4 = await wl(env, 'usage:' + d9(new Date()));
              if (ee4) ee3 = {
                up: ee4.up || 0,
                down: ee4.down || 0,
                total: ee4.total || 0
              };
            } catch (ee5) {}
            const ee6 = p6.cf;
            let ee7 = null;
            if (env.DB && typeof env.DB.prepare === 'function') {
              try {
                const ee8 = await env.DB.prepare('SELECT 1').all();
                if (ee8 && ee8.meta && typeof ee8.meta.size_after === 'number') ee7 = ee8.meta.size_after;
              } catch (ee9) {}
            }
            return new Response(JSON.stringify({
              ip: mc,
              d1SizeBytes: ee7,
              colo: ee6?.colo,
              country: ee6?.country,
              city: ee6?.city,
              region: ee6?.region,
              regionCode: ee6?.regionCode,
              latitude: ee6?.latitude,
              longitude: ee6?.longitude,
              timezone: ee6?.timezone,
              asn: ee6?.asn,
              asOrganization: ee6?.asOrganization,
              userAgent: gn,
              version: i0,
              instanceId: (await rnd(gr.host)).slice(0, 8),
              kvConnected: ee0,
              kvOk: ee1,
              host: gr.host,
              protocol: gr.protocol,
              todayUsage: ee3,
              workerStartTime: globalThis.__workerStart || null
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/domain-health.json') {
            if (gr.searchParams.has('check')) {
              const ete = await rcv(env, g7, g9);
              return new Response(JSON.stringify(ete), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8',
                  'Cache-Control': 'no-store'
                }
              });
            } else {
              const ett = env.KV && typeof env.KV.get === 'function' ? JSON.parse(await env.KV.get('domain-health.json') || 'null') : null;
              return new Response(JSON.stringify(ett || ({
                checkedAt: 0,
                domains: []
              })), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                  'Cache-Control': 'no-store'
                }
              });
            }
          } else if (mt === 'admin/usage-data') {
            try {
              const etr = new Date();
              const etn = 90;
              const eti = [];
              for (let eto = 0; eto < etn; eto++) {
                const ets = new Date(etr);
                ets.setDate(ets.getDate() - eto);
                const eta = 'usage:' + ets.getFullYear() + '-' + String(ets.getMonth() + 1).padStart(2, '0') + '-' + String(ets.getDate()).padStart(2, '0');
                const etc = await env.KV.get(eta);
                if (etc) eti.push({
                  date: eta.slice(6),
                  ...JSON.parse(etc)
                });
              }
              const etl = {};
              for (const etd of eti) {
                const etw = etd.date.slice(0, 7);
                if (!etl[etw]) etl[etw] = {
                  up: 0,
                  down: 0,
                  total: 0
                };
                etl[etw].up += etd.up || 0;
                etl[etw].down += etd.down || 0;
                etl[etw].total += etd.total || 0;
              }
              const etf = Object.entries(etl).map(([eth, etp]) => ({
                month: eth,
                ...etp
              }));
              const etg = {};
              for (const etm of eti) {
                const ety = etm.date.slice(0, 4);
                if (!etg[ety]) etg[ety] = {
                  up: 0,
                  down: 0,
                  total: 0
                };
                etg[ety].up += etm.up || 0;
                etg[ety].down += etm.down || 0;
                etg[ety].total += etm.total || 0;
              }
              const etx = Object.entries(etg).map(([etv, etb]) => ({
                year: etv,
                ...etb
              }));
              return new Response(JSON.stringify({
                daily: eti,
                monthly: etf,
                yearly: etx
              }), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8',
                  'Cache-Control': 'no-store'
                }
              });
            } catch (etk) {
              return new Response(JSON.stringify({
                error: etk.message
              }), {
                status: 500,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
            }
          } else if (mt === ik("YWRtaW4vc3ViLWNvbnRlbnQ=")) {
            const et0 = await rnd(g9 + gk);
            const et1 = `${gr.protocol}//${gr.host}/sub?token=${et0}`;
            const et2 = await fetch(et1).catch(() => null);
            if (!et2) return new Response('Sub content unavailable', {
              status: 502
            });
            const et3 = await et2.text();
            return new Response(et3, {
              status: 200,
              headers: {
                'Content-Type': ik("dGV4dC9wbGFpbjtjaGFyc2V0PXV0Zi04"),
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === ik("YWRtaW4vdXNlcnMuanNvbg==")) {
            let et4 = {};
            try {
              et4 = JSON.parse(await env.KV.get(ik("bmV0d29yay1zZXR0aW5ncy5qc29u")) || '{}');
            } catch (et5) {}
            return new Response(JSON.stringify({
              multiUser: !!et4.multiUser,
              users: Array.isArray(et4.users) ? et4.users : [],
              usage: {}
            }), {
              status: 200,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === ik("YWRtaW4vY2VudHJhbC9zdGF0cw==")) {
            return new Response(JSON.stringify({
              configured: false
            }), {
              status: 200,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/announcement') {
            let et6 = null;
            try {
              et6 = JSON.parse(await env.KV.get('announcement.json') || 'null');
            } catch (et7) {}
            return new Response(JSON.stringify(et6 || ({})), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/bestip') {
            try {
              const et8 = gr.searchParams.get('port') || '443';
              let et9 = [];
              try {
                et9 = await ra7(p6, i1.POOL_API, 32);
              } catch (ere) {}
              if (!Array.isArray(et9) || !et9.length) {
                try {
                  et9 = (await rlm(p6, 32, et8))[0];
                } catch (ert) {}
              }
              const err = (Array.isArray(et9) ? et9 : []).map(ern => String(ern).split('#')[0].split(':')[0].trim()).filter(eri => (/^\d+\.\d+\.\d+\.\d+$/).test(eri));
              return new Response(JSON.stringify({
                ips: err
              }), {
                status: 200,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
                }
              });
            } catch (ero) {
              return new Response(JSON.stringify({
                ips: [],
                error: String(ero.message || ero)
              }), {
                status: 200,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
            }
          } else if (mt === 'admin/update-check.json') {
            let ers = false;
            try {
              const era = JSON.parse(await env.KV.get('cf.json') || '{}');
              ers = !!(era && era.APIToken);
            } catch (erc) {}
            return new Response(JSON.stringify({
              current: i0,
              latest: i0,
              updateAvailable: false,
              hasCfToken: ers
            }), {
              status: 200,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === 'admin/whoami') {
            const erl = p6.cf || ({});
            return new Response(JSON.stringify({
              asn: erl.asn || 0,
              isp: erl.asOrganization || '',
              country: erl.country || '',
              city: erl.city || '',
              carrier: rlc(p6)
            }), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
          } else if (mt === ik("YWRtaW4vc2luZ2JveC1wcmV2aWV3")) {
            const erd = gr.searchParams.get('token') || '';
            if (!erd) return new Response(JSON.stringify({
              error: 'pass ?token=<sub token>'
            }), {
              status: 400,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            try {
              const erw = await fetch(`${gr.protocol}//${gr.host}/sub?token=${encodeURIComponent(erd)}&singbox`, {
                headers: {
                  'User-Agent': ik("c2luZy1ib3gvMS4xMS4wIG5vdmEtZGVidWc=")
                }
              });
              return new Response(await erw.text(), {
                status: erw.status,
                headers: {
                  'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                  'Cache-Control': 'no-store'
                }
              });
            } catch (erf) {
              return new Response(JSON.stringify({
                error: String(erf && erf.message || erf)
              }), {
                status: 502,
                headers: {
                  'Content-Type': 'application/json;charset=utf-8'
                }
              });
            }
          } else if (mt === 'admin/announce') {
            const erh = JSON.parse(await env.KV.get(ik("ZG9tYWluLWhlYWx0aC5qc29u")) || 'null');
            const erp = await r1g(env, {
              baseUrl: `${gr.protocol}//${gr.host}`,
              health: erh
            });
            return new Response(JSON.stringify(erp, null, 2), {
              status: erp.skipped ? 400 : 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (mt === 'admin/publish-mirror') {
            const erg = await r13(env, `${gr.protocol}//${gr.host}`);
            const erm = !erg.skipped && Array.isArray(erg.results) && erg.results.every(ery => ery.ok);
            return new Response(JSON.stringify(erg, null, 2), {
              status: erg.skipped ? 400 : erm ? 200 : 502,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04")
              }
            });
          } else if (mt === 'admin/domains') {
            const erx = await r04(env);
            const erv = gr.searchParams.has('check') ? await rcv(env, erx, g9) : JSON.parse(await env.KV.get('domain-health.json') || 'null');
            return new Response(JSON.stringify({
              hosts: erx,
              health: erv
            }, null, 2), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
          } else if (mt === 'admin/cf-usage-setup') {
            if (p6.method !== 'POST') return new Response(JSON.stringify({
              error: ik("bWV0aG9kX25vdF9hbGxvd2Vk")
            }), {
              status: 405,
              headers: {
                'Content-Type': 'application/json;charset=utf-8'
              }
            });
            const erb = (erk, er0) => new Response(JSON.stringify(Object.assign({
              error: erk
            }, er0 || ({}))), {
              status: 200,
              headers: {
                'Content-Type': 'application/json;charset=utf-8',
                'Cache-Control': 'no-store'
              }
            });
            let er1 = {};
            try {
              er1 = await p6.json();
            } catch (er2) {}
            const er3 = String(er1.token || '').trim();
            if (!er3) return erb('no_token');
            let er4;
            try {
              er4 = await r0y(er3);
            } catch (er5) {
              er4 = {
                ok: false
              };
            }
            if (!er4 || !er4.ok) return erb('token_invalid');
            let er6 = String(er1.accountId || '').trim();
            if (!er6) {
              let er7 = [];
              try {
                er7 = await r0k(er3);
              } catch (er8) {}
              if (!er7.length) return erb('no_accounts');
              if (er7.length === 1) er6 = er7[0].id; else return erb(ik("bXVsdGlwbGVfYWNjb3VudHM="), {
                accounts: er7
              });
            }
            const er9 = await rgy(null, null, er6, er3);
            if (!er9 || !er9.success) return erb('usage_failed', {
              detail: er9 && er9.error || ''
            });
            try {
              await env.KV.put('cf.json', JSON.stringify({
                Email: null,
                GlobalAPIKey: null,
                AccountID: er6,
                APIToken: er3,
                UsageAPI: null
              }, null, 2));
            } catch (ene) {}
            return new Response(JSON.stringify({
              success: true,
              accountId: er6,
              usage: er9
            }), {
              status: 200,
              headers: {
                'Content-Type': ik("YXBwbGljYXRpb24vanNvbjtjaGFyc2V0PXV0Zi04"),
                'Cache-Control': 'no-store'
              }
            });
          }
          ctx.waitUntil(rrp(env, p6, mc, 'Admin_Login', i1));
          const ent = mt.startsWith('admin/') ? mt.slice(6).split('/')[0] : '';
          const enr = ent ? ok + '/admin/' : ok + '/admin' + gr.search;
          return fetch(enr).then(async enn => {
            let eni = await enn.text();
            eni = eni.replace(/"\.\.\/logo\.png"/g, `"${ok}logo.png"`);
            eni = eni.replace(/src=['"]\.\.\/logo\.png['"]/g, `src="${ok}logo.png"`);
            if (ent && ent !== '') {
              eni = eni.replace('</head>', '<script>location.hash="page=' + ent + '";</script></head>');
            }
            return new Response(eni, {
              status: enn.status,
              statusText: enn.statusText,
              headers: enn.headers
            });
          }).catch(() => new Response('Admin panel unavailable', {
            status: 502
          }));
        } else if (mt === 'logout' || gv.test(mt)) {
          const eno = new Response('重定向中...', {
            status: 302,
            headers: {
              'Location': ml.pubLogin
            }
          });
          eno.headers.set('Set-Cookie', 'auth=; Path=/; Max-Age=0; HttpOnly');
          return eno;
        } else if (mt === 'sub') {
          const ens = await rnd(g9 + gk), ena = ['1', 'true'].includes(env.BEST_SUB) && gr.searchParams.get('host') === 'example.com' && gr.searchParams.get('uuid') === ik("MDAwMDAwMDAtMDAwMC00MDAwLTgwMDAtMDAwMDAwMDAwMDAw") && gn.toLowerCase().includes('tunnel (https://github.com/' + lk[1] + '/edge');
          const enc = gr.searchParams.get('token');
          const enl = enc === ens;
          const end = Math.floor(Date.now() / 86400000);
          const enw = t3v(ens, gk);
          const [enf, enh] = await Promise.all([rnd(enw + end), rnd(enw + (end - 1))]);
          const enp = enc === enf || enc === enh;
          if (enl || enp || ena) {
            i1 = await ro4(env, g9, gk, gn);
            if (ena) ctx.waitUntil(rrp(env, p6, mc, 'Get_Best_SUB', i1, false)); else ctx.waitUntil(rrp(env, p6, mc, 'Get_SUB', i1));
            const eng = gn.toLowerCase();
            const enm = {
              "content-type": ik("dGV4dC9wbGFpbjsgY2hhcnNldD11dGYtOA=="),
              "Profile-Update-Interval": i1.优选订阅生成.SUBUpdateTime,
              "Profile-web-page-url": gr.protocol + '//' + gr.host + '/admin',
              "Profile-Title": encodeURIComponent(i1.优选订阅生成.SUBNAME || 'Nova Proxy'),
              "Cache-Control": "no-store"
            };
            if (i1.CF.Usage.success) {
              const eny = i1.CF.Usage.pages;
              const enx = i1.CF.Usage.workers;
              const enb = Number.isFinite(i1.CF.Usage.max) ? i1.CF.Usage.max / 1000 * 1024 : 1024 * 100;
              enm[ik("U3Vic2NyaXB0aW9uLVVzZXJpbmZv")] = `upload=${eny}; download=${enx}; total=${enb}; expire=4102329600`;
            }
            const enk = gr.searchParams.has('b64') || gr.searchParams.has('base64') || p6.headers.get('subconverter-request') || p6.headers.get(ik("c3ViY29udmVydGVyLXZlcnNpb24=")) || eng.includes('subconverter') || eng.includes(('CF-Workers-SUB').toLowerCase()) || ena;
            const en0 = enk ? 'mixed' : gr.searchParams.has('target') ? gr.searchParams.get('target') : gr.searchParams.has('clash') || eng.includes('clash') || eng.includes('meta') || eng.includes('mihomo') ? 'clash' : gr.searchParams.has('sb') || gr.searchParams.has('singbox') || eng.includes('singbox') || eng.includes('sing-box') ? 'singbox' : gr.searchParams.has('surge') || eng.includes('surge') ? 'surge&ver=4' : gr.searchParams.has('quanx') || eng.includes('quantumult') ? 'quanx' : gr.searchParams.has('loon') || eng.includes('loon') ? 'loon' : 'mixed';
            if (!eng.includes('mozilla')) enm[ik("Q29udGVudC1EaXNwb3NpdGlvbg==")] = `attachment; filename*=utf-8''${encodeURIComponent(i1.优选订阅生成.SUBNAME)}`;
            const en1 = (gr.searchParams.has('surge') || eng.includes('surge')) && i1.协议类型 !== 'ss' ? 'tro' + 'jan' : i1.协议类型;
            let en2 = '';
            if (en0 === 'mixed') {
              const en3 = i1.TLS分片 == 'Shadowrocket' ? `&fragment=${encodeURIComponent('1,40-60,30-50,tlshello')}` : i1.TLS分片 == 'Happ' ? `&fragment=${encodeURIComponent('3,1,tlshello')}` : '';
              let en4 = [], en5 = '', en6 = [];
              if (!gr.searchParams.has('sub') && i1.优选订阅生成.local) {
                const en7 = i1.优选订阅生成.本地IP库.随机IP ? (await rlm(p6, i1.优选订阅生成.本地IP库.随机数量, i1.优选订阅生成.本地IP库.指定端口))[0] : await env.KV.get('ADD.txt') ? await rdg(await env.KV.get('ADD.txt')) : (await rlm(p6, i1.优选订阅生成.本地IP库.随机数量, i1.优选订阅生成.本地IP库.指定端口))[0];
                const en8 = [], en9 = [], eie = [];
                for (const eit of en7) {
                  if (eit.toLowerCase().startsWith('sub://')) {
                    en8.push(eit);
                  } else {
                    const eir = eit.indexOf('#');
                    const ein = eir > -1 ? eit.slice(0, eir) : eit;
                    const eii = eir > -1 ? eit.slice(eir) : '';
                    const eio = eit.match(/sub\s*=\s*([^\s&#]+)/i);
                    if (eio && eio[1].trim().includes('.')) {
                      const eis = eit.toLowerCase().includes('proxyip=true');
                      if (eis) en8.push('sub://' + eio[1].trim() + "?proxyip=true" + (eit.includes('#') ? '#' + eit.split('#')[1] : '')); else en8.push('sub://' + eio[1].trim() + (eit.includes('#') ? '#' + eit.split('#')[1] : ''));
                    } else if (ein.toLowerCase().startsWith('https://')) {
                      en8.push(eit);
                    } else if (ein.toLowerCase().includes('://')) {
                      if (eit.includes('#')) {
                        const eia = eit.split('#');
                        eie.push(eia[0] + '#' + encodeURIComponent(decodeURIComponent(eia[1])));
                      } else eie.push(eit);
                    } else {
                      if (ein.includes('*')) {
                        en9.push(rn4(ein) + eii);
                      } else en9.push(eit);
                    }
                  }
                }
                const eic = await rwi(en8, '443');
                const eil = [...new Set(eie.concat(eic[1]))];
                en5 = eil.length > 0 ? eil.join('\n') + '\n' : '';
                const eid = eic[0];
                en6 = eic[3] || [];
                en4 = [...new Set(en9.concat(eid))];
                if (i1.POOL_API) {
                  try {
                    const eiw = await ra7(p6, i1.POOL_API, i1.优选订阅生成.本地IP库.随机数量 || 16);
                    if (eiw.length) en4 = [...new Set(en4.concat(eiw))];
                  } catch (eif) {
                    t4m(`[Smart Clean IPs] 获取失败: ${eif.message}`);
                  }
                }
              } else {
                let eih = gr.searchParams.get('sub') || i1.优选订阅生成.SUB;
                const [eip, eig] = await rdv(eih);
                en4 = en4.concat(eip);
                en5 += eig;
              }
              const eim = i1.ECH ? `&ech=${encodeURIComponent((i1.ECHConfig.SNI ? i1.ECHConfig.SNI + '+' : '') + i1.ECHConfig.DNS)}` : '';
              const eiy = eng.includes('loon') || eng.includes('surge');
              const {type: eix, 路径字段名: eiv, 域名字段名: eib} = t4c(i1);
              if (i1.socks5RotateEvery && i1.chainProxy) {
                try {
                  const eik = String(i1.chainProxy).trim();
                  let ei0 = eik ? eik.split(/[\n,]+/).map(ei1 => ei1.trim()).filter(ei2 => (/^(socks5|http|https|turn|sstp):\/\//i).test(ei2)) : [];
                  if (ei0.length > 1 && (i1.socks5RotateEvery === 'daily' || i1.socks5RotateEvery === 'weekly')) {
                    const ei3 = Math.max(1, Math.min(ei0.length, Number(i1.socks5RotateCount) || 3));
                    if (ei3 < ei0.length) {
                      const ei4 = i1.socks5RotateEvery === 'weekly' ? 7 : 1;
                      const ei5 = Math.floor(Date.now() / 86400000 / ei4);
                      const ei6 = (ei5 % ei0.length + ei0.length) % ei0.length;
                      ei0 = Array.from({
                        length: ei3
                      }, (ei7, ei8) => ei0[(ei6 + ei8) % ei0.length]);
                    }
                    i1.chainProxy = ei0.join('\n');
                  }
                } catch (ei9) {
                  t4m(`[Chain Proxy Rotation] 轮换失败: ${ei9.message}`);
                }
              }
              en4 = [g9 + ':443#' + o0, ...en4];
              en2 = en5 + en4.map((eoe, eot) => {
                const eor = /^(\[[\da-fA-F:]+\]|[\d.]+|[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?)*)(?::(\d+))?(?:#(.+))?$/;
                const eon = eoe.match(eor);
                let eoi, eoo = "443", eos;
                if (eon) {
                  eoi = eon[1];
                  eoo = eon[2] ? eon[2] : '443';
                  eos = eon[3] || eoi;
                } else {
                  console.warn(`[订阅内容] 不规范的IP格式已忽略: ${eoe}`);
                  return null;
                }
                let eoa = i1.完整节点路径;
                const eoc = eos.match(/\$(socks5|http|https|turn|sstp):\/\/([^#\s]+)/i);
                if (eoc) {
                  try {
                    const eol = eoc[1].toLowerCase(), eod = eoc[2];
                    const eow = {
                      type: eol,
                      ...rgr(eod, rp8(eol))
                    };
                    eoa = `/video/${t3v(JSON.stringify(eow), gk) + (i1.启用0RTT ? '?ed=2560' : '')}`;
                    eos = eos.replace(eoc[0], '').trim() || eoi;
                  } catch (eof) {
                    console.warn(`[订阅内容] 链式代理解析失败，已忽略该指令: ${eoc[0]} (${eof && eof.message ? eof.message : eof})`);
                  }
                } else if (en6.length > 0) {
                  const eoh = en6.find(eop => eop.includes(eoi));
                  if (eoh) eoa = `${i1.PATH}/proxyip=${eoh}`.replace(/\/\//g, '/') + (i1.启用0RTT ? '?ed=2560' : '');
                }
                if (eiy) eoa = eoa.replace(/,/g, '%2C');
                {
                  const eog = (eos.match(/ ·S\d+$/) || [''])[0];
                  eos = 'سرویس رایگان نوا ' + (eot + 1) + eog;
                }
                const eom = i1.protocolType === 'mixed' && !ena ? ['vless', 'trojan'][eot % 2] : en1;
                if (eom === 'ss' && !ena) {
                  if (!i1.SS.TLS) {
                    const eoy = [443, 2053, 2083, 2087, 2096, 8443];
                    const eox = [80, 2052, 2082, 2086, 2095, 8080];
                    eoo = String(eox[eoy.indexOf(Number(eoo))] ?? eoo);
                  }
                  eoa = (eoa.includes('?') ? eoa.replace('?', '?enc=' + i1.SS.加密方式 + '&') : eoa + '?enc=' + i1.SS.加密方式).replace(/([=,])/g, '\\$1');
                  if (!enk) eoa = eoa + ';mux=0';
                  return `${eom}://${btoa(i1.SS.加密方式 + ':00000000-0000-4000-8000-000000000000')}@${eoi}:${eoo}?plugin=v2${encodeURIComponent('ray-plugin;mode=websocket;host=example.com;path=' + (i1.随机路径 ? rnk(eoa) : eoa) + (i1.SS.TLS ? ';tls' : '')) + eim + en3}#${encodeURIComponent(eos)}`;
                } else {
                  const eov = t4w(i1, eoa, ena);
                  if (i1.enableTLS === false) {
                    const eob = [443, 2053, 2083, 2087, 2096, 8443], eok = [80, 2052, 2082, 2086, 2095, 8080];
                    const eo0 = String(eok[eob.indexOf(Number(eoo))] ?? eoo);
                    return `${eom}://00000000-0000-4000-8000-000000000000@${eoi}:${eo0}?security=none&type=${eix}&${eib}=example.com&${eiv}=${encodeURIComponent(eov)}&encryption=none#${encodeURIComponent(eos)}`;
                  }
                  return `${eom}://00000000-0000-4000-8000-000000000000@${eoi}:${eoo}?security=tls&type=${eix + eim}&${eib}=example.com&fp=${i1.Fingerprint}&sni=example.com&${eiv}=${encodeURIComponent(eov) + en3}&encryption=none${i1.skipCertVerify ? '&insecure=1&allowInsecure=1' : ''}#${encodeURIComponent(eos)}`;
                }
              }).filter(eo1 => eo1 !== null).join('\n');
            } else {
              const eo2 = `${i1.订阅转换配置.SUBAPI}/sub?target=${en0}&url=${encodeURIComponent(gr.protocol + '//' + gr.host + ik("L3N1Yj90YXJnZXQ9bWl4ZWQmdG9rZW49") + enf + '&cnIspCode=' + rlc(p6) + (gr.searchParams.has('sub') && gr.searchParams.get('sub') != '' ? `&sub=${gr.searchParams.get('sub')}` : ''))}&config=${encodeURIComponent(i1.订阅转换配置.SUBCONFIG)}&emoji=${i1.订阅转换配置.SUBEMOJI}&list=${i1.订阅转换配置.SUBLIST}&scv=${i1.跳过证书验证}`;
              try {
                const eo3 = await fetch(eo2, {
                  headers: {
                    'User-Agent': 'Subconverter for ' + en0 + ' edge' + 'tunnel (https://github.com/' + lk[1] + '/edge' + 'tunnel)'
                  }
                });
                if (eo3.ok) {
                  en2 = await eo3.text();
                  if (gr.searchParams.has('surge') || eng.includes('surge')) en2 = rrn(en2, gr.protocol + '//' + gr.host + '/sub?token=' + ens + '&surge', i1);
                } else return new Response('订阅转换后端异常：' + eo3.statusText, {
                  status: eo3.status
                });
              } catch (eo4) {
                return new Response('订阅转换后端异常：' + eo4.message, {
                  status: 403
                });
              }
            }
            if (!eng.includes('subconverter') && enl) {
              const eo5 = [...i1.HOSTS].sort(() => Math.random() - 0.5);
              let eo6 = 0, eo7 = null;
              en2 = en2.replace(/00000000-0000-4000-8000-000000000000/g, i1.UUID).replace(/MDAwMDAwMDAtMDAwMC00MDAwLTgwMDAtMDAwMDAwMDAwMDAw/g, btoa(i1.UUID)).replace(/example\.com/g, () => {
                if (eo6 % 2 === 0) {
                  const eo8 = eo5[Math.floor(eo6 / 2) % eo5.length];
                  eo7 = rn4(eo8);
                }
                eo6++;
                return eo7;
              });
            }
            if (en0 === 'mixed' && (!eng.includes('mozilla') || gr.searchParams.has('b64') || gr.searchParams.has('base64'))) en2 = btoa(en2);
            let eo9 = null;
            if (ol && ol.enableWarp && env.KV && typeof env.KV.get === 'function') {
              try {
                const ese = await env.KV.get('warp-account.json');
                if (ese) {
                  eo9 = JSON.parse(ese);
                  if (ol.warpMode === 'wow') {
                    try {
                      const est = await env.KV.get(ik("d2FycC1hY2NvdW50LXdvdy5qc29u"));
                      if (est) eo9.wow = JSON.parse(est);
                    } catch (esr) {}
                  }
                }
              } catch (esn) {
                eo9 = null;
              }
            }
            if (en0 === 'singbox') {
              en2 = await t9o(en2, i1, ol, eo9);
              enm["content-type"] = 'application/json; charset=utf-8';
            } else if (en0 === 'clash') {
              en2 = t6e(en2, i1, ol, eo9);
              enm["content-type"] = 'application/x-yaml; charset=utf-8';
            }
            if (eng.includes('mozilla') && !gr.searchParams.has('raw') && !gr.searchParams.has('b64') && !gr.searchParams.has('base64')) {
              const esi = await raw();
              if (esi) return esi;
            }
            return new Response(en2, {
              status: 200,
              headers: enm
            });
          }
        } else if (mt === 'locations') {
          const eso = p6.headers.get('Cookie') || '';
          const ess = eso.split(';').find(esa => esa.trim().startsWith('auth='))?.split('=')[1];
          if (ess && await an(ess, gn, gp, gw)) return fetch(new Request('https://speed.cloudflare.com/locations', {
            headers: {
              'Referer': ik("aHR0cHM6Ly9zcGVlZC5jbG91ZGZsYXJlLmNvbS8=")
            }
          }));
        } else if (mt === 'robots.txt') return new Response('User-agent: *\nDisallow: /', {
          status: 200,
          headers: {
            'Content-Type': ik("dGV4dC9wbGFpbjsgY2hhcnNldD1VVEYtOA==")
          }
        });
      } else if (!gb) return fetch(ok + '/install').then(async esc => {
        const esl = new Headers(esc.headers);
        esl.set('Cache-Control', 'no-store, no-cache, must-revalidate, proxy-revalidate');
        esl.set('Pragma', 'no-cache');
        esl.set('Expires', '0');
        let esd = await esc.text();
        esd = esd.replace(/"\.\.\/logo\.png"/g, `"${ok}logo.png"`);
        esd = esd.replace(/src=['"]\.\.\/logo\.png['"]/g, `src="${ok}logo.png"`);
        return new Response(esd, {
          status: 200,
          statusText: esc.statusText,
          headers: esl
        });
      });
    }
    if ((/\.\w{2,4}$/).test(gr.pathname)) {
      const esw = await fetch(ok + gr.pathname).catch(() => {});
      if (esw && esw.ok) return esw;
    }
    let esf = env.URL || 'nginx';
    if (esf && esf !== 'nginx' && esf !== '1101') {
      esf = esf.trim().replace(/\/$/, '');
      if (!esf.match(/^https?:\/\//i)) esf = 'https://' + esf;
      if (esf.toLowerCase().startsWith('http://')) esf = 'https://' + esf.substring(7);
      try {
        const esh = new URL(esf);
        esf = esh.protocol + '//' + esh.host;
      } catch (esp) {
        esf = 'nginx';
      }
    }
    if (esf === '1101') return new Response(await r2p(gr.host, mc), {
      status: 200,
      headers: {
        'Content-Type': ik("dGV4dC9odG1sOyBjaGFyc2V0PVVURi04")
      }
    });
    try {
      const esg = new URL(esf), esm = new Headers(p6.headers);
      esm.set('Host', esg.host);
      esm.set('Referer', esg.origin);
      esm.set('Origin', esg.origin);
      if (!esm.has('User-Agent') && gn && gn !== 'null') esm.set('User-Agent', gn);
      const esy = await fetch(esg.origin + gr.pathname + gr.search, {
        method: p6.method,
        headers: esm,
        body: p6.body,
        cf: p6.cf
      });
      const esx = esy.headers.get('content-type') || '';
      if ((/text|javascript|json|xml/).test(esx)) {
        const esv = (await esy.text()).replaceAll(esg.host, gr.host);
        return new Response(esv, {
          status: esy.status,
          headers: {
            ...Object.fromEntries(esy.headers),
            'Cache-Control': 'no-store'
          }
        });
      }
      return esy;
    } catch (esb) {
      await ras(env, esb, p6);
    }
    return new Response(await r2h(), {
      status: 200,
      headers: {
        'Content-Type': 'text/html; charset=UTF-8'
      }
    });
  },
  async scheduled(esk, env, ctx) {
    l7(env);
    if (!om && env.__realKV && ctx && typeof ctx.waitUntil === 'function') ctx.waitUntil(d0(env));
    if (!env || !['1', 'true'].includes(String(env.ENABLE_CRON || ''))) return;
    ctx.waitUntil(rca(env).then(es0 => {
      if (es0) console.log('scheduledMaintenance completed');
    }).catch(es1 => console.error('runScheduledMaintenance error:', es1 && es1.message)));
  }
};
async function es2(es3, es4) {
  if (!es3.body) return new Response('Bad Request', {
    status: 400
  });
  const es5 = es3.body.getReader();
  const es6 = await ecn(es5, es4);
  if (!es6) {
    try {
      es5.releaseLock();
    } catch (es7) {}
    return new Response('Invalid request', {
      status: 400
    });
  }
  if (rvc(es6.hostname)) {
    try {
      es5.releaseLock();
    } catch (es8) {}
    return ol && ol.enablePornBlock && rvi(es6.hostname) ? rvd(es3) : new Response('Forbidden', {
      status: 403
    });
  }
  if (es6.isUDP && es6.协议 !== 'trojan' && es6.port !== 53) {
    try {
      es5.releaseLock();
    } catch (es9) {}
    return new Response('UDP is not supported', {
      status: 400
    });
  }
  const eae = {
    socket: null,
    connectingPromise: null,
    retryConnect: null
  };
  let eat = null;
  let ear = null;
  const ean = {
    up: 0,
    down: 0
  };
  const eai = new Headers({
    'Content-Type': 'application/octet-stream',
    'X-Accel-Buffering': 'no',
    'Cache-Control': 'no-store'
  });
  const eao = () => {
    if (ear) {
      try {
        ear.releaseLock();
      } catch (eas) {}
      ear = null;
    }
    eat = null;
  };
  const eaa = () => {
    const eac = eae.socket;
    if (!eac) return null;
    if (eac !== eat) {
      eao();
      eat = eac;
      ear = eac.writable.getWriter();
    }
    return ear;
  };
  let eal = null;
  return new Response(new ReadableStream({
    async start(ead) {
      let eaw = false;
      let eaf = es6.respHeader;
      const eah = {
        缓存: new Uint8Array(0)
      };
      const eap = {
        readyState: WebSocket.OPEN,
        send(eag) {
          if (eaw) return;
          try {
            const eam = eag instanceof Uint8Array ? eag : eag instanceof ArrayBuffer ? new Uint8Array(eag) : ArrayBuffer.isView(eag) ? new Uint8Array(eag.buffer, eag.byteOffset, eag.byteLength) : new Uint8Array(eag);
            ead.enqueue(eam);
          } catch (eay) {
            eaw = true;
            this.readyState = WebSocket.CLOSED;
          }
        },
        close() {
          if (eaw) return;
          eaw = true;
          this.readyState = WebSocket.CLOSED;
          try {
            ead.close();
          } catch (eax) {}
        }
      };
      const eav = eal = e41({
        获取写入器: eaa,
        释放写入器: eao,
        重试连接: async () => {
          if (typeof eae.retryConnect !== 'function') throw new Error('retry unavailable');
          await eae.retryConnect();
        },
        关闭连接: () => {
          try {
            eae.socket?.close();
          } catch (eab) {}
          e4w(eap);
        },
        名称: 'XHTTP上行'
      });
      const eak = async (ea0, ea1 = true) => {
        return eav.写入并等待(ea0, ea1);
      };
      try {
        if (es6.isUDP) {
          if (es6.rawData?.byteLength) {
            ean.up += es6.rawData.byteLength;
            if (es6.协议 === 'trojan') await ebn(es6.rawData, eap, eah, es3); else await e31(es6.rawData, eap, eaf, es3);
            eaf = null;
          }
        } else {
          if (es6.rawData?.byteLength) ean.up += es6.rawData.byteLength;
          await e0w(es6.hostname, es6.port, es6.rawData, eap, es6.respHeader, eae, es4, es3, ean);
        }
        while (true) {
          const {done: ea2, value: ea3} = await es5.read();
          if (ea2) break;
          if (!ea3 || ea3.byteLength === 0) continue;
          if (ea3.byteLength) ean.up += ea3.byteLength;
          if (es6.isUDP) {
            if (es6.协议 === 'trojan') await ebn(ea3, eap, eah, es3); else await e31(ea3, eap, eaf, es3);
            eaf = null;
          } else {
            if (!await eak(ea3)) throw new Error('Remote socket is not ready');
          }
        }
        if (!es6.isUDP) {
          await eav.等待空();
          const ea4 = eaa();
          if (ea4) {
            try {
              await ea4.close();
            } catch (ea5) {}
          }
        }
      } catch (ea6) {
        t4m(`[XHTTP转发] 处理失败: ${ea6?.message || ea6}`);
        e4w(eap);
      } finally {
        eav.清空();
        eao();
        try {
          es5.releaseLock();
        } catch (ea7) {}
        try {
          fr(oc, ean.up, ean.down, null);
        } catch (ea8) {}
      }
    },
    cancel() {
      eal?.清空();
      try {
        eae.socket?.close();
      } catch (ea9) {}
      eao();
      try {
        es5.releaseLock();
      } catch (ece) {}
    }
  }), {
    status: 200,
    headers: eai
  });
}
function ect(ecr) {
  if (!ecr) return 0;
  if (typeof ecr.byteLength === 'number') return ecr.byteLength;
  if (typeof ecr.length === 'number') return ecr.length;
  return 0;
}
async function ecn(eci, eco) {
  const ecs = exw;
  const eca = ecc => {
    const ecl = ecc.byteLength;
    if (ecl < 18) return {
      状态: 'need_more'
    };
    if (!ex1(ecc, 1, eco)) return {
      状态: 'invalid'
    };
    const ecd = ecc[17];
    const ecw = 18 + ecd;
    if (ecl < ecw + 1) return {
      状态: 'need_more'
    };
    const ecf = ecc[ecw];
    if (ecf !== 1 && ecf !== 2) return {
      状态: 'invalid'
    };
    const ech = ecw + 1;
    if (ecl < ech + 3) return {
      状态: 'need_more'
    };
    const ecp = ecc[ech] << 8 | ecc[ech + 1];
    const ecg = ecc[ech + 2];
    const ecm = ech + 3;
    let ecy = -1;
    let ecx = '';
    if (ecg === 1) {
      if (ecl < ecm + 4) return {
        状态: 'need_more'
      };
      ecx = `${ecc[ecm]}.${ecc[ecm + 1]}.${ecc[ecm + 2]}.${ecc[ecm + 3]}`;
      ecy = ecm + 4;
    } else if (ecg === 2) {
      if (ecl < ecm + 1) return {
        状态: 'need_more'
      };
      const ecv = ecc[ecm];
      if (ecl < ecm + 1 + ecv) return {
        状态: 'need_more'
      };
      ecx = ecs.decode(ecc.subarray(ecm + 1, ecm + 1 + ecv));
      ecy = ecm + 1 + ecv;
    } else if (ecg === 3) {
      if (ecl < ecm + 16) return {
        状态: 'need_more'
      };
      const ecb = [];
      for (let eck = 0; eck < 8; eck++) {
        const ec0 = ecm + eck * 2;
        ecb.push((ecc[ec0] << 8 | ecc[ec0 + 1]).toString(16));
      }
      ecx = ecb.join(':');
      ecy = ecm + 16;
    } else return {
      状态: 'invalid'
    };
    if (!ecx) return {
      状态: 'invalid'
    };
    return {
      状态: 'ok',
      结果: {
        协议: 'vl' + 'ess',
        hostname: ecx,
        port: ecp,
        isUDP: ecf === 2,
        rawData: ecc.subarray(ecy),
        respHeader: new Uint8Array([ecc[0], 0])
      }
    };
  };
  const ec1 = ec2 => {
    const ec3 = rmd(eco);
    const ec4 = new TextEncoder().encode(ec3);
    const ec5 = ec2.byteLength;
    if (ec5 < 58) return {
      状态: 'need_more'
    };
    if (ec2[56] !== 0x0d || ec2[57] !== 0x0a) return {
      状态: 'invalid'
    };
    for (let ec6 = 0; ec6 < 56; ec6++) {
      if (ec2[ec6] !== ec4[ec6]) return {
        状态: 'invalid'
      };
    }
    const ec7 = 58;
    if (ec5 < ec7 + 2) return {
      状态: 'need_more'
    };
    const ec8 = ec2[ec7];
    if (ec8 !== 1 && ec8 !== 3) return {
      状态: 'invalid'
    };
    const ec9 = ec8 === 3;
    const ele = ec2[ec7 + 1];
    let elt = ec7 + 2;
    let elr = '';
    if (ele === 1) {
      if (ec5 < elt + 4) return {
        状态: 'need_more'
      };
      elr = `${ec2[elt]}.${ec2[elt + 1]}.${ec2[elt + 2]}.${ec2[elt + 3]}`;
      elt += 4;
    } else if (ele === 3) {
      if (ec5 < elt + 1) return {
        状态: 'need_more'
      };
      const eln = ec2[elt];
      if (ec5 < elt + 1 + eln) return {
        状态: 'need_more'
      };
      elr = ecs.decode(ec2.subarray(elt + 1, elt + 1 + eln));
      elt += 1 + eln;
    } else if (ele === 4) {
      if (ec5 < elt + 16) return {
        状态: 'need_more'
      };
      const eli = [];
      for (let elo = 0; elo < 8; elo++) {
        const els = elt + elo * 2;
        eli.push((ec2[els] << 8 | ec2[els + 1]).toString(16));
      }
      elr = eli.join(':');
      elt += 16;
    } else return {
      状态: 'invalid'
    };
    if (!elr) return {
      状态: 'invalid'
    };
    if (ec5 < elt + 4) return {
      状态: 'need_more'
    };
    const ela = ec2[elt] << 8 | ec2[elt + 1];
    if (ec2[elt + 2] !== 0x0d || ec2[elt + 3] !== 0x0a) return {
      状态: 'invalid'
    };
    const elc = elt + 4;
    return {
      状态: 'ok',
      结果: {
        协议: 'trojan',
        hostname: elr,
        port: ela,
        isUDP: ec9,
        rawData: ec2.subarray(elc),
        respHeader: null
      }
    };
  };
  let ell = new Uint8Array(1024);
  let eld = 0;
  while (true) {
    const {value: elw, done: elf} = await eci.read();
    if (elf) {
      if (eld === 0) return null;
      break;
    }
    const elh = elw instanceof Uint8Array ? elw : new Uint8Array(elw);
    if (eld + elh.byteLength > ell.byteLength) {
      const elp = new Uint8Array(Math.max(ell.byteLength * 2, eld + elh.byteLength));
      elp.set(ell.subarray(0, eld));
      ell = elp;
    }
    ell.set(elh, eld);
    eld += elh.byteLength;
    const elg = ell.subarray(0, eld);
    const elm = ec1(elg);
    if (elm.状态 === 'ok') return {
      ...elm.结果,
      reader: eci
    };
    const ely = eca(elg);
    if (ely.状态 === 'ok') return {
      ...ely.结果,
      reader: eci
    };
    if (elm.状态 === 'invalid' && ely.状态 === 'invalid') return null;
  }
  const elx = ell.subarray(0, eld);
  const elv = ec1(elx);
  if (elv.状态 === 'ok') return {
    ...elv.结果,
    reader: eci
  };
  const elb = eca(elx);
  if (elb.状态 === 'ok') return {
    ...elb.结果,
    reader: eci
  };
  return null;
}
async function elk(el0, el1) {
  if (!el0.body) return new Response('Bad Request', {
    status: 400
  });
  const el2 = el0.body.getReader();
  const el3 = {
    socket: null,
    connectingPromise: null,
    retryConnect: null
  };
  let el4 = false;
  const el5 = {
    缓存: new Uint8Array(0)
  };
  let el6 = null;
  let el7 = null;
  let el8 = null;
  let el9 = null;
  const ede = {
    up: 0,
    down: 0
  };
  const edt = new Headers({
    'Content-Type': 'application/grpc',
    'grpc-status': '0',
    'X-Accel-Buffering': 'no',
    'Cache-Control': 'no-store'
  });
  const edr = sp;
  const edn = Math.max(sm, 1);
  return new Response(new ReadableStream({
    async start(edi) {
      let edo = false;
      let eds = [];
      let eda = 0;
      let edc = null;
      let edl = false;
      const edd = {
        readyState: WebSocket.OPEN,
        send(edw) {
          if (edo) return;
          const edf = edw instanceof Uint8Array ? edw : new Uint8Array(edw);
          const edh = [];
          let edp = edf.byteLength >>> 0;
          while (edp > 127) {
            edh.push(edp & 0x7f | 0x80);
            edp >>>= 7;
          }
          edh.push(edp);
          const edg = new Uint8Array(edh);
          const edm = 1 + edg.length + edf.byteLength;
          const edy = new Uint8Array(5 + edm);
          edy[0] = 0;
          edy[1] = edm >>> 24 & 0xff;
          edy[2] = edm >>> 16 & 0xff;
          edy[3] = edm >>> 8 & 0xff;
          edy[4] = edm & 0xff;
          edy[5] = 0x0a;
          edy.set(edg, 6);
          edy.set(edf, 6 + edg.length);
          eds.push(edy);
          eda += edy.byteLength;
          ed3();
        },
        close() {
          if (this.readyState === WebSocket.CLOSED) return;
          edv(true);
          edo = true;
          this.readyState = WebSocket.CLOSED;
          try {
            edi.close();
          } catch (edx) {}
        }
      };
      const edv = (edb = false) => {
        edl = false;
        if (edc) {
          clearTimeout(edc);
          edc = null;
        }
        if (!edb && edo || eda === 0) return;
        const edk = new Uint8Array(eda);
        let ed0 = 0;
        for (const ed1 of eds) {
          edk.set(ed1, ed0);
          ed0 += ed1.byteLength;
        }
        eds = [];
        eda = 0;
        try {
          edi.enqueue(edk);
        } catch (ed2) {
          edo = true;
          edd.readyState = WebSocket.CLOSED;
        }
      };
      const ed3 = () => {
        if (eda >= edr) {
          edv();
          return;
        }
        if (edl || edc) return;
        edl = true;
        queueMicrotask(() => {
          edl = false;
          if (edo || eda === 0 || edc) return;
          edc = setTimeout(edv, edn);
        });
      };
      const ed4 = () => {
        if (edo) return;
        el9?.清空();
        edv(true);
        edo = true;
        edd.readyState = WebSocket.CLOSED;
        if (edc) clearTimeout(edc);
        if (el8) {
          try {
            el8.releaseLock();
          } catch (ed5) {}
          el8 = null;
        }
        el7 = null;
        try {
          el2.releaseLock();
        } catch (ed6) {}
        try {
          el3.socket?.close();
        } catch (ed7) {}
        try {
          edi.close();
        } catch (ed8) {}
      };
      const ed9 = () => {
        if (el8) {
          try {
            el8.releaseLock();
          } catch (ewe) {}
          el8 = null;
        }
        el7 = null;
      };
      const ewt = el9 = e41({
        获取写入器: () => {
          const ewr = el3.socket;
          if (!ewr) return null;
          if (ewr !== el7) {
            ed9();
            el7 = ewr;
            el8 = ewr.writable.getWriter();
          }
          return el8;
        },
        释放写入器: ed9,
        重试连接: async () => {
          if (typeof el3.retryConnect !== 'function') throw new Error(ik("cmV0cnkgdW5hdmFpbGFibGU="));
          await el3.retryConnect();
        },
        关闭连接: ed4,
        名称: 'gRPC上行'
      });
      const ewn = async (ewi, ewo = true) => {
        return ewt.写入并等待(ewi, ewo);
      };
      try {
        let ews = new Uint8Array(0);
        while (true) {
          const {done: ewa, value: ewc} = await el2.read();
          if (ewa) break;
          if (!ewc || ewc.byteLength === 0) continue;
          const ewl = ewc instanceof Uint8Array ? ewc : new Uint8Array(ewc);
          const ewd = new Uint8Array(ews.length + ewl.length);
          ewd.set(ews, 0);
          ewd.set(ewl, ews.length);
          ews = ewd;
          while (ews.byteLength >= 5) {
            const eww = ews[1] << 24 >>> 0 | ews[2] << 16 | ews[3] << 8 | ews[4];
            const ewf = 5 + eww;
            if (ews.byteLength < ewf) break;
            const ewh = ews.subarray(5, ewf);
            ews = ews.slice(ewf);
            if (!ewh.byteLength) continue;
            let ewp = ewh;
            if (ewp.byteLength >= 2 && ewp[0] === 0x0a) {
              let ewg = 0;
              let ewm = 1;
              let ewy = false;
              while (ewm < ewp.length) {
                const ewx = ewp[ewm++];
                if ((ewx & 0x80) === 0) {
                  ewy = true;
                  break;
                }
                ewg += 7;
                if (ewg > 35) break;
              }
              if (ewy) ewp = ewp.subarray(ewm);
            }
            if (!ewp.byteLength) continue;
            if (el4) {
              if (el6) await ebn(ewp, edd, el5, el0); else await e31(ewp, edd, null, el0);
              continue;
            }
            if (el3.socket) {
              ede.up += ewp.byteLength;
              if (!await ewn(ewp)) throw new Error(ik("UmVtb3RlIHNvY2tldCBpcyBub3QgcmVhZHk="));
            } else {
              const ewv = ev2(ewp);
              if (el6 === null) el6 = ewv.byteLength >= 58 && ewv[56] === 0x0d && ewv[57] === 0x0a;
              if (el6) {
                const ewb = ey1(ewv, el1);
                if (ewb?.hasError) throw new Error(ewb.message || 'Invalid trojan request');
                const {port: ewk, hostname: ew0, rawClientData: ew1, isUDP: ew2} = ewb;
                t4m(`[gRPC] 木马首包: ${ew0}:${ewk} | UDP: ${ew2 ? '是' : '否'}`);
                if (rvc(ew0)) throw new Error('Blocked site');
                if (ew2) {
                  el4 = true;
                  if (ect(ew1) > 0) {
                    ede.up += ect(ew1);
                    await ebn(ew1, edd, el5, el0);
                  }
                } else {
                  ede.up += ect(ew1);
                  await e0w(ew0, ewk, ew1, edd, null, el3, el1, el0, ede);
                }
              } else {
                el6 = false;
                const ew3 = ex7(ewv, el1);
                if (ew3?.hasError) throw new Error(ew3.message || ik("SW52YWxpZCDprY/ng4jmgJ0gcmVxdWVzdA=="));
                const {port: ew4, hostname: ew5, version: ew6, isUDP: ew7, rawClientData: ew8} = ew3;
                t4m(`[gRPC] 魏烈思首包: ${ew5}:${ew4} | UDP: ${ew7 ? '是' : '否'}`);
                if (rvc(ew5)) throw new Error('Blocked site');
                if (ew7) {
                  if (ew4 !== 53) throw new Error('UDP is not supported');
                  el4 = true;
                }
                const ew9 = new Uint8Array([ew6, 0]);
                edd.send(ew9);
                const efe = ew8;
                if (el4) {
                  if (el6) await ebn(efe, edd, el5, el0); else await e31(efe, edd, null, el0);
                } else {
                  ede.up += ect(efe);
                  await e0w(ew5, ew4, efe, edd, null, el3, el1, el0, ede);
                }
              }
            }
          }
          edv();
        }
        await ewt.等待空();
      } catch (eft) {
        t4m(`[gRPC转发] 处理失败: ${eft?.message || eft}`);
      } finally {
        ewt.清空();
        ed9();
        ed4();
        try {
          fr(oc, ede.up, ede.down, null);
        } catch (efr) {}
      }
    },
    cancel() {
      el9?.清空();
      try {
        el3.socket?.close();
      } catch (efn) {}
      try {
        el2.releaseLock();
      } catch (efi) {}
    }
  }), {
    status: 200,
    headers: edt
  });
}
function efo(efs, efa) {
  if (!efs?.byteLength) return false;
  if (efs.byteLength >= 18 && ex1(efs, 1, efa)) return true;
  if (efs.byteLength < 58 || efs[56] !== 0x0d || efs[57] !== 0x0a) return false;
  const efc = rmd(efa);
  for (let efl = 0; efl < 56; efl++) {
    if (efs[efl] !== efc.charCodeAt(efl)) return false;
  }
  return true;
}
function efd(efw, eff) {
  if (!efw) return null;
  if (efw.length > sd) throw new Error(ik("ZWFybHkgZGF0YSBpcyB0b28gbGFyZ2U="));
  let efh;
  const efp = Uint8Array;
  if (typeof efp.fromBase64 === 'function') {
    try {
      efh = efp.fromBase64(efw, {
        alphabet: 'base64url'
      });
    } catch (efg) {}
  }
  if (!efh) {
    let efm = efw.replace(/-/g, '+').replace(/_/g, '/');
    const efy = efm.length % 4;
    if (efy) efm += ('=').repeat(4 - efy);
    let efx;
    try {
      efx = atob(efm);
    } catch (efv) {
      return null;
    }
    efh = new Uint8Array(efx.length);
    for (let efb = 0; efb < efx.length; efb++) efh[efb] = efx.charCodeAt(efb);
  }
  if (efh.byteLength > sl) throw new Error(ik("ZWFybHkgZGF0YSBpcyB0b28gbGFyZ2U="));
  return efo(efh, eff) ? efh : null;
}
async function efk(ef0, ef1, ef2) {
  const ef3 = new WebSocketPair();
  const [ef4, ef5] = Object.values(ef3);
  try {
    ef5.accept({
      allowHalfOpen: true
    });
  } catch (ef6) {
    ef5.accept();
  }
  ef5.binaryType = 'arraybuffer';
  let ef7 = {
    socket: null,
    connectingPromise: null,
    retryConnect: null
  };
  let ef8 = false;
  let ef9 = null;
  const ehe = {
    缓存: new Uint8Array(0)
  };
  const eht = ef0.headers.get('sec-websocket-protocol') || '';
  const ehr = !!ef2.searchParams.get('enc');
  let ehn = null;
  let ehi = Promise.resolve();
  let eho = false, ehs = false, eha = false;
  let ehc = 0, ehl = 0;
  let ehd = null, ehw = null, ehf = null;
  let ehh = null, ehp = null;
  const ehg = {
    up: 0,
    down: 0
  };
  const ehm = f8('down');
  const ehy = () => {
    if (ehf) {
      try {
        ehf.releaseLock();
      } catch (ehx) {}
      ehf = null;
    }
    ehw = null;
  };
  const ehv = ehn = e41({
    获取写入器: () => {
      const ehb = ef7.socket;
      if (!ehb) return null;
      if (ehb !== ehw) {
        ehy();
        ehw = ehb;
        ehf = ehb.writable.getWriter();
      }
      return ehf;
    },
    释放写入器: ehy,
    重试连接: async () => {
      if (typeof ef7.retryConnect !== 'function') throw new Error(ik("cmV0cnkgdW5hdmFpbGFibGU="));
      await ef7.retryConnect();
    },
    关闭连接: () => {
      try {
        ef7.socket?.close();
      } catch (ehk) {}
      e4w(ef5);
    },
    名称: 'WS上行'
  });
  const eh0 = async (eh1, eh2 = true) => {
    return ehv.写入并等待(eh1, eh2);
  };
  const eh3 = async () => {
    if (ehh) return ehh;
    if (!ehp) {
      ehp = (async () => {
        const eh4 = (ef2.searchParams.get('enc') || '').toLowerCase();
        const eh5 = evy[eh4] || evy['aes-128-gcm'];
        const eh6 = [eh5, ...Object.values(evy).filter(eh7 => eh7.method !== eh5.method)];
        const eh8 = new Map();
        const eh9 = epe => {
          if (!eh8.has(epe.method)) eh8.set(epe.method, eks(ef1, epe.keyLen));
          return eh8.get(epe.method);
        };
        const ept = {
          buffer: new Uint8Array(0),
          hasSalt: false,
          waitPayloadLength: null,
          decryptKey: null,
          nonceCounter: new Uint8Array(evv),
          加密配置: null
        };
        const epr = async () => {
          const epn = 2 + evx;
          const epi = Math.max(...eh6.map(epo => epo.saltLen));
          const eps = 16;
          const epa = Math.min(eps, Math.max(0, ept.buffer.byteLength - (epn + Math.min(...eh6.map(epc => epc.saltLen)))));
          for (let epl = 0; epl <= epa; epl++) {
            for (const epd of eh6) {
              const epw = epl + epd.saltLen + epn;
              if (ept.buffer.byteLength < epw) continue;
              const epf = ept.buffer.subarray(epl, epl + epd.saltLen);
              const eph = ept.buffer.subarray(epl + epd.saltLen, epw);
              const epp = await eh9(epd);
              const epg = await ekm(epd, epp, epf, ['decrypt']);
              const epm = new Uint8Array(evv);
              try {
                const epy = await e0o(epg, epm, eph);
                if (epy.byteLength !== 2) continue;
                const epx = epy[0] << 8 | epy[1];
                if (epx < 0 || epx > epd.maxChunk) continue;
                if (epl > 0) t4m(`[SS入站] 检测到前导噪声 ${epl}B，已自动对齐`);
                if (epd.method !== eh5.method) t4m(`[SS入站] URL enc=${eh4 || eh5.method} 与实际 ${epd.method} 不一致，已自动切换`);
                ept.buffer = ept.buffer.subarray(epw);
                ept.decryptKey = epg;
                ept.nonceCounter = epm;
                ept.waitPayloadLength = epx;
                ept.加密配置 = epd;
                ept.hasSalt = true;
                return true;
              } catch (epv) {}
            }
          }
          const epb = epi + epn + eps;
          if (ept.buffer.byteLength >= epb) {
            throw new Error(`SS handshake decrypt failed (enc=${eh4 || 'auto'}, candidates=${eh6.map(epk => epk.method).join('/')})`);
          }
          return false;
        };
        const ep0 = {
          async 输入(ep1) {
            const ep2 = ev2(ep1);
            if (ep2.byteLength > 0) ept.buffer = ev4(ept.buffer, ep2);
            if (!ept.hasSalt) {
              const ep3 = await epr();
              if (!ep3) return [];
            }
            const ep4 = [];
            while (true) {
              if (ept.waitPayloadLength === null) {
                const ep5 = 2 + evx;
                if (ept.buffer.byteLength < ep5) break;
                const ep6 = ept.buffer.subarray(0, ep5);
                ept.buffer = ept.buffer.subarray(ep5);
                const ep7 = await e0o(ept.decryptKey, ept.nonceCounter, ep6);
                if (ep7.byteLength !== 2) throw new Error(ik("U1MgbGVuZ3RoIGRlY3J5cHQgZmFpbGVk"));
                const ep8 = ep7[0] << 8 | ep7[1];
                if (ep8 < 0 || ep8 > ept.加密配置.maxChunk) throw new Error(`SS payload length invalid: ${ep8}`);
                ept.waitPayloadLength = ep8;
              }
              const ep9 = ept.waitPayloadLength + evx;
              if (ept.buffer.byteLength < ep9) break;
              const ege = ept.buffer.subarray(0, ep9);
              ept.buffer = ept.buffer.subarray(ep9);
              const egt = await e0o(ept.decryptKey, ept.nonceCounter, ege);
              ep4.push(egt);
              ept.waitPayloadLength = null;
            }
            return ep4;
          }
        };
        let egr = null;
        const egn = 32 * 1024;
        const egi = async () => {
          if (egr) return egr;
          if (!ept.加密配置) throw new Error(ik("U1MgY2lwaGVyIGlzIG5vdCBuZWdvdGlhdGVk"));
          const ego = ept.加密配置;
          const egs = await eks(ef1, ego.keyLen);
          const ega = crypto.getRandomValues(new Uint8Array(ego.saltLen));
          const egc = await ekm(ego, egs, ega, ['encrypt']);
          const egl = new Uint8Array(evv);
          let egd = false;
          egr = {
            async 加密并发送(egw, egf) {
              const egh = ev2(egw);
              if (!egd) {
                await egf(ega);
                egd = true;
              }
              if (egh.byteLength === 0) return;
              let egp = 0;
              while (egp < egh.byteLength) {
                const egg = Math.min(egp + ego.maxChunk, egh.byteLength);
                const egm = egh.subarray(egp, egg);
                const egy = new Uint8Array(2);
                egy[0] = egm.byteLength >>> 8 & 0xff;
                egy[1] = egm.byteLength & 0xff;
                const egx = await ek9(egc, egl, egy);
                const egv = await ek9(egc, egl, egm);
                const egb = new Uint8Array(egx.byteLength + egv.byteLength);
                egb.set(egx, 0);
                egb.set(egv, egx.byteLength);
                await egf(egb);
                egp = egg;
              }
            }
          };
          return egr;
        };
        let egk = Promise.resolve();
        const eg0 = eg1 => {
          egk = egk.then(async () => {
            if (ef5.readyState !== WebSocket.OPEN) return;
            const eg2 = await egi();
            await eg2.加密并发送(eg1, async eg3 => {
              if (eg3.byteLength > 0 && ef5.readyState === WebSocket.OPEN) {
                await e4v(ef5, eg3.buffer);
              }
            });
          }).catch(eg4 => {
            t4m(`[SS发送] 加密失败: ${eg4?.message || eg4}`);
            e4w(ef5);
          });
          return egk;
        };
        const eg5 = {
          get readyState() {
            return ef5.readyState;
          },
          send(eg6) {
            const eg7 = ev2(eg6);
            if (eg7.byteLength <= egn) {
              return eg0(eg7);
            }
            for (let eg8 = 0; eg8 < eg7.byteLength; eg8 += egn) {
              eg0(eg7.subarray(eg8, Math.min(eg8 + egn, eg7.byteLength)));
            }
            return egk;
          },
          close() {
            e4w(ef5);
          }
        };
        ehh = {
          入站解密器: ep0,
          回包Socket: eg5,
          首包已建立: false,
          目标主机: '',
          目标端口: 0
        };
        return ehh;
      })().finally(() => {
        ehp = null;
      });
    }
    return ehp;
  };
  const eg9 = async eme => {
    const emt = await eh3();
    let emr = null;
    try {
      emr = await emt.入站解密器.输入(eme);
    } catch (emn) {
      const emi = emn?.message || `${emn}`;
      if (emi.includes(ik("RGVjcnlwdGlvbiBmYWlsZWQ=")) || emi.includes(ik("U1MgaGFuZHNoYWtlIGRlY3J5cHQgZmFpbGVk")) || emi.includes('SS length decrypt failed')) {
        t4m(`[SS入站] 解密失败，连接关闭: ${emi}`);
        e4w(ef5);
        return;
      }
      throw emn;
    }
    for (const emo of emr) {
      let ems = false;
      try {
        ems = await eh0(emo, false);
      } catch (ema) {
        if (ema?.isQueueOverflow) throw ema;
        ems = false;
      }
      if (ems) continue;
      if (emt.首包已建立 && emt.目标主机 && emt.目标端口 > 0) {
        await e0w(emt.目标主机, emt.目标端口, emo, emt.回包Socket, null, ef7, ef1, ef0);
        continue;
      }
      const emc = ev2(emo);
      if (emc.byteLength < 3) throw new Error('invalid ss data');
      const eml = emc[0];
      let emd = 1;
      let emw = '';
      if (eml === 1) {
        if (emc.byteLength < emd + 4 + 2) throw new Error('invalid ss ipv4 length');
        emw = `${emc[emd]}.${emc[emd + 1]}.${emc[emd + 2]}.${emc[emd + 3]}`;
        emd += 4;
      } else if (eml === 3) {
        if (emc.byteLength < emd + 1) throw new Error(ik("aW52YWxpZCBzcyBkb21haW4gbGVuZ3Ro"));
        const emf = emc[emd];
        emd += 1;
        if (emc.byteLength < emd + emf + 2) throw new Error('invalid ss domain data');
        emw = ev0.decode(emc.subarray(emd, emd + emf));
        emd += emf;
      } else if (eml === 4) {
        if (emc.byteLength < emd + 16 + 2) throw new Error(ik("aW52YWxpZCBzcyBpcHY2IGxlbmd0aA=="));
        const emh = [];
        const emp = new DataView(emc.buffer, emc.byteOffset + emd, 16);
        for (let emg = 0; emg < 8; emg++) emh.push(emp.getUint16(emg * 2).toString(16));
        emw = emh.join(':');
        emd += 16;
      } else {
        throw new Error(`invalid ss addressType: ${eml}`);
      }
      if (!emw) throw new Error(`invalid ss address: ${eml}`);
      const emm = emc[emd] << 8 | emc[emd + 1];
      emd += 2;
      const emy = emc.subarray(emd);
      if (rvc(emw)) throw new Error('Blocked site');
      emt.首包已建立 = true;
      emt.目标主机 = emw;
      emt.目标端口 = emm;
      await e0w(emw, emm, emy, emt.回包Socket, null, ef7, ef1, ef0, ehg);
    }
  };
  const emx = async emv => {
    let emb = null;
    const emk = ect(emv);
    if (emk > 0) ehg.up += emk;
    if (ef8) {
      if (ef9) return await ebn(emv, ef5, ehe, ef0);
      return await e31(emv, ef5, null, ef0);
    }
    if (ehd === 'ss') {
      await eg9(emv);
      return;
    }
    if (await eh0(emv)) return;
    if (ehd === null) {
      if (ef2.searchParams.get('enc')) ehd = 'ss'; else {
        emb = emb || ev2(emv);
        const em0 = emb;
        ehd = em0.byteLength >= 58 && em0[56] === 0x0d && em0[57] === 0x0a ? '木马' : '魏烈思';
      }
      ef9 = ehd === '木马';
      t4m(`[WS转发] 协议类型: ${ehd} | 来自: ${ef2.host} | UA: ${ef0.headers.get('user-agent') || '未知'}`);
    }
    if (ehd === 'ss') {
      await eg9(emv);
      return;
    }
    if (await eh0(emv)) return;
    if (ehd === '木马') {
      const em1 = ey1(emv, ef1);
      if (em1?.hasError) throw new Error(em1.message || ik("SW52YWxpZCB0cm9qYW4gcmVxdWVzdA=="));
      const {port: em2, hostname: em3, rawClientData: em4, isUDP: em5} = em1;
      if (rvc(em3)) throw new Error('Blocked site');
      if (em5) {
        ef8 = true;
        if (ect(em4) > 0) return ebn(em4, ef5, ehe, ef0);
        return;
      }
      await e0w(em3, em2, em4, ef5, null, ef7, ef1, ef0, ehg);
    } else {
      ef9 = false;
      emb = emb || ev2(emv);
      const em6 = emb;
      const em7 = ex7(em6, ef1);
      if (em7?.hasError) throw new Error(em7.message || 'Invalid 魏烈思 request');
      const {port: em8, hostname: em9, version: eye, isUDP: eyt, rawClientData: eyr} = em7;
      if (rvc(em9)) throw new Error('Blocked site');
      if (eyt) {
        if (em8 === 53) ef8 = true; else throw new Error(ik("VURQIGlzIG5vdCBzdXBwb3J0ZWQ="));
      }
      const eyn = new Uint8Array([eye, 0]);
      const eyi = eyr;
      if (ef8) {
        if (ef9) return ebn(eyi, ef5, ehe, ef0);
        return e31(eyi, ef5, eyn, ef0);
      }
      await e0w(em9, em8, eyi, ef5, eyn, ef7, ef1, ef0, ehg);
    }
  };
  const eyo = eys => {
    if (ehs) return;
    ehs = true;
    eho = true;
    ehc = 0;
    ehl = 0;
    try {
      fr(oc, ehg.up, ehg.down, null);
    } catch (eya) {}
    const eyc = eys?.message || `${eys}`;
    if (eyc.includes(ik("TmV0d29yayBjb25uZWN0aW9uIGxvc3Q=")) || eyc.includes('ReadableStream is closed')) {
      t4m(`[WS转发] 连接结束: ${eyc}`);
    } else {
      t4m(`[WS转发] 处理失败: ${eyc}`);
    }
    ehv.清空();
    ehy();
    e4w(ef5);
  };
  const eyl = eyd => {
    ehi = ehi.then(eyd).catch(eyo);
    return ehi;
  };
  const eyw = eyf => {
    if (eho || ehs) return;
    const eyh = Math.max(0, ect(eyf));
    const eyp = ehc + eyh;
    const eyg = ehl + 1;
    if (eyp > sf || eyg > sh) {
      eyo(new Error(`[WS显式传输] 队列溢出: ${eyp}B/${eyg}`));
      return;
    }
    ehc = eyp;
    ehl = eyg;
    eyl(async () => {
      ehc = Math.max(0, ehc - eyh);
      ehl = Math.max(0, ehl - 1);
      if (ehs) return;
      await emx(eyf);
    });
  };
  const eym = () => {
    if (eha) return;
    eha = true;
    eho = true;
    eyl(async () => {
      if (ehs) return;
      await ehv.等待空();
      ehy();
    });
  };
  ef5.addEventListener('message', eyy => {
    eyw(eyy.data);
  });
  ef5.addEventListener('close', () => {
    e4w(ef5);
    eym();
    try {
      fr(oc, ehg.up, ehg.down, null);
    } catch (eyx) {}
  });
  ef5.addEventListener('error', eyv => {
    eyo(eyv);
  });
  if (!ehr && eht) {
    try {
      const eyb = efd(eht, ef1);
      if (eyb?.byteLength) {
        ehg.up += eyb.byteLength;
        eyw(eyb.buffer);
      }
    } catch (eyk) {
      eyo(eyk);
    }
  }
  return new Response(null, {
    status: 101,
    webSocket: ef4,
    headers: {
      'Sec-WebSocket-Extensions': ''
    }
  });
}
const ey0 = new TextDecoder();
function ey1(ey2, ey3) {
  const ey4 = ev2(ey2);
  const ey5 = rmd(ey3);
  if (ey4.byteLength < 58) return {
    hasError: true,
    message: "invalid data"
  };
  let ey6 = 56;
  if (ey4[ey6] !== 0x0d || ey4[ey6 + 1] !== 0x0a) return {
    hasError: true,
    message: "invalid header format"
  };
  for (let ey7 = 0; ey7 < ey6; ey7++) {
    if (ey4[ey7] !== ey5.charCodeAt(ey7)) return {
      hasError: true,
      message: ik("aW52YWxpZCBwYXNzd29yZA==")
    };
  }
  const ey8 = ey6 + 2;
  if (ey4.byteLength < ey8 + 6) return {
    hasError: true,
    message: "invalid S5 request data"
  };
  const ey9 = ey4[ey8];
  if (ey9 !== 1 && ey9 !== 3) return {
    hasError: true,
    message: ik("dW5zdXBwb3J0ZWQgY29tbWFuZCwgb25seSBUQ1AvVURQIGlzIGFsbG93ZWQ=")
  };
  const exe = ey9 === 3;
  const ext = ey4[ey8 + 1];
  let exr = 0;
  let exn = ey8 + 2;
  let exi = "";
  switch (ext) {
    case 1:
      exr = 4;
      if (ey4.byteLength < exn + exr + 4) return {
        hasError: true,
        message: "invalid S5 request data"
      };
      exi = `${ey4[exn]}.${ey4[exn + 1]}.${ey4[exn + 2]}.${ey4[exn + 3]}`;
      break;
    case 3:
      if (ey4.byteLength < exn + 1) return {
        hasError: true,
        message: ik("aW52YWxpZCBTNSByZXF1ZXN0IGRhdGE=")
      };
      exr = ey4[exn];
      exn += 1;
      if (ey4.byteLength < exn + exr + 4) return {
        hasError: true,
        message: ik("aW52YWxpZCBTNSByZXF1ZXN0IGRhdGE=")
      };
      exi = ey0.decode(ey4.subarray(exn, exn + exr));
      break;
    case 4:
      exr = 16;
      if (ey4.byteLength < exn + exr + 4) return {
        hasError: true,
        message: "invalid S5 request data"
      };
      const exo = [];
      for (let exs = 0; exs < 8; exs++) {
        const exa = exn + exs * 2;
        exo.push((ey4[exa] << 8 | ey4[exa + 1]).toString(16));
      }
      exi = exo.join(":");
      break;
    default:
      return {
        hasError: true,
        message: `invalid addressType is ${ext}`
      };
  }
  if (!exi) {
    return {
      hasError: true,
      message: `address is empty, addressType is ${ext}`
    };
  }
  const exc = exn + exr;
  if (ey4.byteLength < exc + 4) return {
    hasError: true,
    message: "invalid S5 request data"
  };
  const exl = ey4[exc] << 8 | ey4[exc + 1];
  return {
    hasError: false,
    addressType: ext,
    port: exl,
    hostname: exi,
    isUDP: exe,
    rawClientData: ey4.subarray(exc + 4)
  };
}
const exd = new Map();
const exw = new TextDecoder();
function exf(exh) {
  if (exh >= 48 && exh <= 57) return exh - 48;
  exh |= 32;
  if (exh >= 97 && exh <= 102) return exh - 87;
  return -1;
}
function exp(exg) {
  const exm = String(exg || '');
  let exy = exd.get(exm);
  if (exy) return exy;
  const exx = exm.replace(/-/g, '');
  if (exx.length !== 32) return null;
  const exv = new Uint8Array(16);
  for (let exb = 0; exb < 16; exb++) {
    const exk = exf(exx.charCodeAt(exb * 2));
    const ex0 = exf(exx.charCodeAt(exb * 2 + 1));
    if (exk < 0 || ex0 < 0) return null;
    exv[exb] = exk << 4 | ex0;
  }
  if (exd.size >= 32) exd.clear();
  exd.set(exm, exv);
  return exv;
}
function ex1(ex2, ex3, ex4) {
  const ex5 = exp(ex4);
  if (!ex5 || ex2.byteLength < ex3 + 16) return false;
  for (let ex6 = 0; ex6 < 16; ex6++) {
    if (ex2[ex3 + ex6] !== ex5[ex6]) return false;
  }
  return true;
}
function ex7(ex8, ex9) {
  const eve = ev2(ex8);
  const evt = eve.byteLength;
  if (evt < 24) return {
    hasError: true,
    message: 'Invalid data'
  };
  const evr = eve[0];
  if (!ex1(eve, 1, ex9)) return {
    hasError: true,
    message: 'Invalid uuid'
  };
  const evn = eve[17];
  const evi = 18 + evn;
  if (evt < evi + 4) return {
    hasError: true,
    message: 'Invalid data'
  };
  const evo = eve[evi];
  let evs = false;
  if (evo === 1) {} else if (evo === 2) {
    evs = true;
  } else {
    return {
      hasError: true,
      message: 'Invalid command'
    };
  }
  const eva = evi + 1;
  const evc = eve[eva] << 8 | eve[eva + 1];
  let evl = eva + 3, evd = 0, evw = '';
  const evf = eve[eva + 2];
  switch (evf) {
    case 1:
      evd = 4;
      if (evt < evl + evd) return {
        hasError: true,
        message: 'Invalid IPv4 address length'
      };
      evw = `${eve[evl]}.${eve[evl + 1]}.${eve[evl + 2]}.${eve[evl + 3]}`;
      break;
    case 2:
      if (evt < evl + 1) return {
        hasError: true,
        message: 'Invalid domain length'
      };
      evd = eve[evl];
      evl += 1;
      if (evt < evl + evd) return {
        hasError: true,
        message: 'Invalid domain data'
      };
      evw = exw.decode(eve.subarray(evl, evl + evd));
      break;
    case 3:
      evd = 16;
      if (evt < evl + evd) return {
        hasError: true,
        message: ik("SW52YWxpZCBJUHY2IGFkZHJlc3MgbGVuZ3Ro")
      };
      const evh = [];
      for (let evp = 0; evp < 8; evp++) {
        const evg = evl + evp * 2;
        evh.push((eve[evg] << 8 | eve[evg + 1]).toString(16));
      }
      evw = evh.join(':');
      break;
    default:
      return {
        hasError: true,
        message: `Invalid address type: ${evf}`
      };
  }
  if (!evw) return {
    hasError: true,
    message: `Invalid address: ${evf}`
  };
  const evm = evl + evd;
  return {
    hasError: false,
    addressType: evf,
    port: evc,
    hostname: evw,
    isUDP: evs,
    rawClientData: eve.subarray(evm),
    version: evr
  };
}
const evy = {
  'aes-128-gcm': {
    method: 'aes-128-gcm',
    keyLen: 16,
    saltLen: 16,
    maxChunk: 0x3fff,
    aesLength: 128
  },
  'aes-256-gcm': {
    method: 'aes-256-gcm',
    keyLen: 32,
    saltLen: 32,
    maxChunk: 0x3fff,
    aesLength: 256
  }
};
const evx = 16, evv = 12;
const evb = new TextEncoder().encode('ss-subkey');
const evk = new TextEncoder(), ev0 = new TextDecoder(), ev1 = new Map();
function ev2(ev3) {
  if (ev3 instanceof Uint8Array) return ev3;
  if (ev3 instanceof ArrayBuffer) return new Uint8Array(ev3);
  if (ArrayBuffer.isView(ev3)) return new Uint8Array(ev3.buffer, ev3.byteOffset, ev3.byteLength);
  return new Uint8Array(ev3 || 0);
}
function ev4(...ev5) {
  if (!ev5 || ev5.length === 0) return new Uint8Array(0);
  const ev6 = ev5.map(ev2);
  const ev7 = ev6.reduce((ev8, ev9) => ev8 + ev9.byteLength, 0);
  const ebe = new Uint8Array(ev7);
  let ebt = 0;
  for (const ebr of ev6) {
    ebe.set(ebr, ebt);
    ebt += ebr.byteLength;
  }
  return ebe;
}
async function ebn(ebi, ebo, ebs, eba) {
  const ebc = ev2(ebi);
  const ebl = ebs?.缓存 instanceof Uint8Array ? ebs.缓存 : new Uint8Array(0);
  const ebd = ebl.byteLength ? ev4(ebl, ebc) : ebc;
  let ebw = 0;
  while (ebw < ebd.byteLength) {
    const ebf = ebw;
    const ebh = ebd[ebw];
    let ebp = ebw + 1;
    let ebg = 0;
    if (ebh === 1) ebg = 4; else if (ebh === 4) ebg = 16; else if (ebh === 3) {
      if (ebd.byteLength < ebp + 1) break;
      ebg = 1 + ebd[ebp];
    } else throw new Error(`invalid trojan udp addressType: ${ebh}`);
    const ebm = ebp + ebg;
    if (ebd.byteLength < ebm + 6) break;
    const eby = ebd[ebm] << 8 | ebd[ebm + 1];
    const ebx = ebd[ebm + 2] << 8 | ebd[ebm + 3];
    if (ebd[ebm + 4] !== 0x0d || ebd[ebm + 5] !== 0x0a) throw new Error('invalid trojan udp delimiter');
    const ebv = ebm + 6;
    const ebb = ebv + ebx;
    if (ebd.byteLength < ebb) break;
    const ebk = ebd.slice(ebf, ebm + 2);
    const eb0 = ebd.slice(ebv, ebb);
    ebw = ebb;
    if (eby !== 53) throw new Error('UDP is not supported');
    if (!eb0.byteLength) continue;
    let eb1 = eb0;
    if (eb0.byteLength < 2 || (eb0[0] << 8 | eb0[1]) !== eb0.byteLength - 2) {
      eb1 = new Uint8Array(eb0.byteLength + 2);
      eb1[0] = eb0.byteLength >>> 8 & 0xff;
      eb1[1] = eb0.byteLength & 0xff;
      eb1.set(eb0, 2);
    }
    const eb2 = {
      缓存: new Uint8Array(0)
    };
    await e31(eb1, ebo, null, eba, eb3 => {
      const eb4 = ev2(eb3);
      const eb5 = eb2.缓存.byteLength ? ev4(eb2.缓存, eb4) : eb4;
      const eb6 = [];
      let eb7 = 0;
      while (eb7 + 2 <= eb5.byteLength) {
        const eb8 = eb5[eb7] << 8 | eb5[eb7 + 1];
        const eb9 = eb7 + 2;
        const eke = eb9 + eb8;
        if (eke > eb5.byteLength) break;
        const ekt = eb5.slice(eb9, eke);
        const ekr = new Uint8Array(ebk.byteLength + 4 + ekt.byteLength);
        ekr.set(ebk, 0);
        ekr[ebk.byteLength] = ekt.byteLength >>> 8 & 0xff;
        ekr[ebk.byteLength + 1] = ekt.byteLength & 0xff;
        ekr[ebk.byteLength + 2] = 0x0d;
        ekr[ebk.byteLength + 3] = 0x0a;
        ekr.set(ekt, ebk.byteLength + 4);
        eb6.push(ekr);
        eb7 = eke;
      }
      eb2.缓存 = eb5.slice(eb7);
      return eb6.length ? eb6 : new Uint8Array(0);
    });
  }
  if (ebs) ebs.缓存 = ebd.slice(ebw);
}
function ekn(eki) {
  for (let eko = 0; eko < eki.length; eko++) {
    eki[eko] = eki[eko] + 1 & 0xff;
    if (eki[eko] !== 0) return;
  }
}
async function eks(eka, ekc) {
  const ekl = `${ekc}:${eka}`;
  if (ev1.has(ekl)) return ev1.get(ekl);
  const ekd = (async () => {
    const ekw = evk.encode(eka || '');
    let ekf = new Uint8Array(0), ekh = new Uint8Array(0);
    while (ekh.byteLength < ekc) {
      const ekp = new Uint8Array(ekf.byteLength + ekw.byteLength);
      ekp.set(ekf, 0);
      ekp.set(ekw, ekf.byteLength);
      ekf = new Uint8Array(await crypto.subtle.digest('MD5', ekp));
      ekh = ev4(ekh, ekf);
    }
    return ekh.slice(0, ekc);
  })();
  ev1.set(ekl, ekd);
  try {
    return await ekd;
  } catch (ekg) {
    ev1.delete(ekl);
    throw ekg;
  }
}
async function ekm(eky, ekx, ekv, ekb) {
  const ekk = {
    name: 'HMAC',
    hash: 'SHA-1'
  };
  const ek0 = await crypto.subtle.importKey('raw', ekv, ekk, false, ['sign']);
  const ek1 = new Uint8Array(await crypto.subtle.sign('HMAC', ek0, ekx));
  const ek2 = await crypto.subtle.importKey('raw', ek1, ekk, false, ['sign']);
  const ek3 = new Uint8Array(eky.keyLen);
  let ek4 = new Uint8Array(0), ek5 = 0, ek6 = 1;
  while (ek5 < eky.keyLen) {
    const ek7 = ev4(ek4, evb, new Uint8Array([ek6]));
    ek4 = new Uint8Array(await crypto.subtle.sign('HMAC', ek2, ek7));
    const ek8 = Math.min(ek4.byteLength, eky.keyLen - ek5);
    ek3.set(ek4.subarray(0, ek8), ek5);
    ek5 += ek8;
    ek6 += 1;
  }
  return crypto.subtle.importKey('raw', ek3, {
    name: 'AES-GCM',
    length: eky.aesLength
  }, false, ekb);
}
async function ek9(e0e, e0t, e0r) {
  const e0n = e0t.slice();
  const e0i = await crypto.subtle.encrypt({
    name: 'AES-GCM',
    iv: e0n,
    tagLength: 128
  }, e0e, e0r);
  ekn(e0t);
  return new Uint8Array(e0i);
}
async function e0o(e0s, e0a, e0c) {
  const e0l = e0a.slice();
  const e0d = await crypto.subtle.decrypt({
    name: 'AES-GCM',
    iv: e0l,
    tagLength: 128
  }, e0s, e0c);
  ekn(e0a);
  return new Uint8Array(e0d);
}
async function e0w(e0f, e0h, e0p, e0g, e0m, e0y, e0x, e0v = null, e0b = null) {
  t4m(`[TCP转发] 目标: ${e0f}:${e0h} | 反代IP: ${i2} | 反代兜底: ${or ? '是' : '否'} | 反代类型: ${i3 || 'proxyip'} | 全局: ${i4 ? '是' : '否'}`);
  const e0k = 1000;
  let e00 = false;
  const e01 = trs(e0v);
  async function e02(e03, e04 = e0k) {
    await Promise.race([e03.opened, new Promise((e05, e06) => setTimeout(() => e06(new Error('连接超时')), e04))]);
  }
  async function e07(e08, e09) {
    const e1e = e01({
      hostname: e08,
      port: e09
    });
    try {
      await e02(e1e);
      return e1e;
    } catch (e1t) {
      try {
        e1e?.close?.();
      } catch (e1r) {}
      throw e1t;
    }
  }
  async function e1n(e1i, e1o) {
    if (ect(e1o) <= 0) return;
    const e1s = e1i.writable.getWriter();
    try {
      await e1s.write(ev2(e1o));
    } finally {
      try {
        e1s.releaseLock();
      } catch (e1a) {}
    }
  }
  async function e1c(e1l) {
    if (e1l.length === 1) {
      const e1d = e1l[0];
      return {
        socket: await e07(e1d.hostname, e1d.port),
        candidate: e1d
      };
    }
    const e1w = e1l.map(e1f => e07(e1f.hostname, e1f.port).then(e1h => ({
      socket: e1h,
      candidate: e1f
    })));
    let e1p = null;
    try {
      e1p = await Promise.any(e1w);
      return e1p;
    } finally {
      if (e1p) {
        for (const e1g of e1w) {
          e1g.then(({socket: e1m}) => {
            if (e1m !== e1p.socket) {
              try {
                e1m?.close?.();
              } catch (e1y) {}
            }
          }).catch(() => {});
        }
      }
    }
  }
  async function e1x(e1v, e1b) {
    if (!sv || tmm(e1v)) return null;
    t4m(`[TCP直连] 预加载竞速拨号开启，开始并发查询 ${e1v} 的 A/AAAA 记录`);
    const [e1k, e10] = await Promise.all([rn9(e1v, 'A'), rn9(e1v, 'AAAA')]);
    const e11 = [...new Set(e1k.flatMap(e12 => {
      const e13 = e12.data;
      return e12.type === 1 && typeof e13 === 'string' && tye(e13) ? [e13] : [];
    }))];
    const e14 = [...new Set(e10.flatMap(e15 => {
      const e16 = e15.data;
      return e15.type === 28 && typeof e16 === 'string' && tmm(e16) ? [e16] : [];
    }))];
    const e17 = Math.max(1, sx | 0);
    const e18 = e11.length >= e17 ? e11.slice(0, e17) : e11.concat(e14.slice(0, e17 - e11.length));
    const e19 = e11.length > 0 ? e18.length > e11.length ? 'A+AAAA' : 'A' : 'AAAA';
    if (e18.length === 0) {
      t4m(`[TCP直连] ${e1v} 的 A/AAAA 未获得可用解析结果，预加载竞速不可用，回退到原始 hostname 直连。`);
      return null;
    }
    const e2e = e18;
    t4m(`[TCP直连] ${e1v} A记录:${e11.length} AAAA记录:${e14.length}，使用${e19}记录，竞速拨号 ${e2e.length}/${e17}: ${e2e.join(', ')}`);
    return e2e.map((e2t, e2r) => ({
      hostname: e2t,
      port: e1b,
      attempt: e2r,
      resolvedFrom: e1v
    }));
  }
  async function e2n(e2i, e2o, e2s = null, e2a = false) {
    const e2c = e2a ? await e1x(e2i, e2o) : null;
    const e2l = e2c || Array.from({
      length: sx
    }, (e2d, e2w) => ({
      hostname: e2i,
      port: e2o,
      attempt: e2w
    }));
    t4m(e2c ? `[TCP直连] 并发尝试 ${e2l.length} 路: ${e2l.map(e2f => `${e2f.hostname}:${e2f.port}`).join(', ')}` : `[TCP直连] 并发尝试 ${e2l.length} 路: ${e2i}:${e2o}`);
    let e2h = null;
    try {
      const e2p = await e1c(e2l);
      e2h = e2p.socket;
      if (e2c) {
        const e2g = e2p.candidate;
        t4m(`[TCP直连] 预加载竞速结果: ${e2g.hostname}:${e2g.port} 胜出，源域名: ${e2g.resolvedFrom || e2i}`);
      }
      await e1n(e2h, e2s);
      return e2h;
    } catch (e2m) {
      try {
        e2h?.close?.();
      } catch (e2y) {}
      if (e2c) t4m(`[TCP直连] 预加载竞速失败: ${e2m.message || e2m}`);
      throw e2m;
    }
  }
  async function e2x(e2v, e2b, e2k = null, e20 = null, e21 = true) {
    if (e20 && e20.length > 0) {
      for (let e22 = 0; e22 < e20.length; e22 += sx) {
        const e23 = [];
        for (let e24 = 0; e24 < sx && e22 + e24 < e20.length; e24++) {
          const e25 = (ot + e22 + e24) % e20.length;
          const [e26, e27] = e20[e25];
          e23.push({
            hostname: e26,
            port: e27,
            index: e25
          });
        }
        let e28 = null, e29 = null;
        try {
          t4m(`[反代连接] 并发尝试 ${e23.length} 路: ${e23.map(e3e => `${e3e.hostname}:${e3e.port}`).join(', ')}`);
          const e3t = await e1c(e23);
          e28 = e3t.socket;
          e29 = e3t.candidate;
          await e1n(e28, e2k);
          t4m(`[反代连接] 成功连接到: ${e29.hostname}:${e29.port} (索引: ${e29.index})`);
          ot = e29.index;
          return e28;
        } catch (e3r) {
          try {
            e28?.close?.();
          } catch (e3n) {}
          t4m(`[反代连接] 本批连接失败: ${e3r.message || e3r}`);
        }
      }
    }
    if (e21) return e2n(e2v, e2b, e2k, false); else {
      e4w(e0g);
      throw new Error(ik("W+WPjeS7o+i/nuaOpV0g5omA5pyJ5Y+N5Luj6L+e5o6l5aSx6LSl77yM5LiU5pyq5ZCv55So5Y+N5Luj5YWc5bqV77yM6L+e5o6l57uI5q2i44CC"));
    }
  }
  async function e3i(e3o = true) {
    if (e0y.connectingPromise) {
      await e0y.connectingPromise;
      return;
    }
    const e3s = e3o && !e00 && ect(e0p) > 0;
    const e3a = e3s ? e0p : null;
    const e3c = (async () => {
      let e3l;
      if (i3 === 'socks5') {
        t4m(`[SOCKS5代理] 代理到: ${e0f}:${e0h}`);
        e3l = await e8k(e0f, e0h, e3a, e01);
      } else if (i3 === 'http') {
        t4m(`[HTTP代理] 代理到: ${e0f}:${e0h}`);
        e3l = await e9h(e0f, e0h, e3a, false, e01);
      } else if (i3 === 'https') {
        t4m(`[HTTPS代理] 代理到: ${e0f}:${e0h}`);
        e3l = tmm(i6.hostname) ? await tem(e0f, e0h, e3a, e01) : await e9h(e0f, e0h, e3a, true, e01);
      } else if (i3 === 'turn') {
        t4m(`[TURN代理] 代理到: ${e0f}:${e0h}`);
        e3l = await txx(i6, e0f, e0h, e01);
        if (ect(e3a) > 0) {
          const e3d = e3l.writable.getWriter();
          try {
            await e3d.write(ev2(e3a));
          } finally {
            try {
              e3d.releaseLock();
            } catch (e3w) {}
          }
        }
      } else if (i3 === 'sstp') {
        t4m(`[SSTP代理] 代理到: ${e0f}:${e0h}`);
        e3l = await tb0(i6, e0f, e0h, e01);
        if (ect(e3a) > 0) {
          const e3f = e3l.writable.getWriter();
          try {
            await e3f.write(ev2(e3a));
          } finally {
            try {
              e3f.releaseLock();
            } catch (e3h) {}
          }
        }
      } else {
        t4m(`[反代连接] 代理到: ${e0f}:${e0h}`);
        const e3p = await ryg(i2, e0f, e0x);
        try {
          e3l = await e2x(`${lk[0]}.tp1.${lk[2]}.xyz`, 1, e3a, e3p, or);
        } catch (e3g) {
          const e3m = od ? await pf(e0f, e0h, e3a, e01) : null;
          if (!e3m) throw e3g;
          e3l = e3m;
        }
      }
      if (e3s) e00 = true;
      e0y.socket = e3l;
      e3l.closed.catch(() => {}).finally(() => e4w(e0g));
      e75(e3l, e0g, e0m, null, e0b);
    })();
    e0y.connectingPromise = e3c;
    try {
      await e3c;
    } finally {
      if (e0y.connectingPromise === e3c) {
        e0y.connectingPromise = null;
      }
    }
  }
  e0y.retryConnect = async () => e3i(!e00);
  if (i3 && (i4 || ob.some(e3y => new RegExp(`^${e3y.replace(/\*/g, '.*')}$`, 'i').test(e0f)))) {
    t4m(`[TCP转发] 启用 SOCKS5/HTTP/HTTPS/TURN/SSTP 全局代理`);
    try {
      await e3i();
    } catch (e3x) {
      t4m(`[TCP转发] SOCKS5/HTTP/HTTPS/TURN/SSTP 代理连接失败: ${e3x.message}`);
      throw e3x;
    }
  } else {
    try {
      t4m(`[TCP转发] 尝试直连到: ${e0f}:${e0h}`);
      const e3v = await e2n(e0f, e0h, e0p, true);
      e0y.socket = e3v;
      e75(e3v, e0g, e0m, async () => {
        if (e0y.socket !== e3v) return;
        await e3i();
      }, e0b);
    } catch (e3b) {
      t4m(`[TCP转发] 直连 ${e0f}:${e0h} 失败: ${e3b.message}`);
      if (e3b instanceof Error && e3b.name === '预加载解析为空') {
        e4w(e0g);
        throw e3b;
      }
      try {
        await e3i();
      } catch (e3k) {
        const e30 = od ? await pf(e0f, e0h, e0p, e01) : null;
        if (!e30) throw e3k;
        e0y.socket = e30;
        e30.closed.catch(() => {}).finally(() => e4w(e0g));
        e75(e30, e0g, e0m, null, e0b);
      }
    }
  }
}
async function e31(e32, e33, e34, e35, e36 = null) {
  const e37 = ev2(e32);
  const e38 = e37.byteLength;
  t4m(`[UDP转发] 收到 DNS 请求: ${e38}B -> 8.8.4.4:53`);
  try {
    const e39 = trs(e35);
    const e4e = e39({
      hostname: '8.8.4.4',
      port: 53
    });
    let e4t = e34;
    const e4r = e4e.writable.getWriter();
    await e4r.write(e37);
    t4m(`[UDP转发] DNS 请求已写入上游: ${e38}B`);
    e4r.releaseLock();
    await e4e.readable.pipeTo(new WritableStream({
      async write(e4n) {
        const e4i = ev2(e4n);
        t4m(`[UDP转发] 收到 DNS 响应: ${e4i.byteLength}B`);
        const e4o = e36 ? await e36(e4i) : e4i;
        const e4s = Array.isArray(e4o) ? e4o : [e4o];
        if (!e4s.length) return;
        if (e33.readyState !== WebSocket.OPEN) return;
        for (const e4a of e4s) {
          const e4c = ev2(e4a);
          if (!e4c.byteLength) continue;
          if (e4t) {
            const e4l = new Uint8Array(e4t.length + e4c.byteLength);
            e4l.set(e4t, 0);
            e4l.set(e4c, e4t.length);
            await e4v(e33, e4l.buffer);
            e4t = null;
          } else {
            await e4v(e33, e4c);
          }
        }
      }
    }));
  } catch (e4d) {
    t4m(`[UDP转发] DNS 转发失败: ${e4d?.message || e4d}`);
  }
}
function e4w(e4f) {
  try {
    if (e4f.readyState === WebSocket.OPEN || e4f.readyState === WebSocket.CLOSING) {
      e4f.close();
    }
  } catch (e4h) {}
}
function e4p(e4g, e4m = 0) {
  const e4y = [...e4g.slice(e4m, e4m + 16)].map(e4x => e4x.toString(16).padStart(2, '0')).join('');
  return `${e4y.substring(0, 8)}-${e4y.substring(8, 12)}-${e4y.substring(12, 16)}-${e4y.substring(16, 20)}-${e4y.substring(20)}`;
}
async function e4v(e4b, e4k) {
  const e40 = e4b.send(e4k);
  if (e40 && typeof e40.then === 'function') await e40;
}
function e41({获取写入器: e42, 释放写入器: e43, 重试连接: e44, 关闭连接: e45, 名称: e46 = '上行队列'}) {
  let e47 = [];
  let e48 = 0;
  let e49 = 0;
  let e5e = false;
  let e5t = false;
  let e5r = null;
  let e5n = [];
  let e5i = null;
  const e5o = (e5s, e5a = null) => {
    if (!e5s) return;
    for (const e5c of e5s) {
      if (e5a) e5c.reject(e5a); else e5c.resolve();
    }
  };
  const e5l = e5d => {
    for (let e5w = e48; e5w < e47.length; e5w++) {
      const e5f = e47[e5w];
      if (e5f?.completions) e5o(e5f.completions, e5d);
    }
  };
  const e5h = () => {
    if (e48 > 32 && e48 * 2 >= e47.length) {
      e47 = e47.slice(e48);
      e48 = 0;
    }
  };
  const e5p = () => {
    if (e49 || e5e || !e5n.length) return;
    const e5g = e5n;
    e5n = [];
    for (const e5m of e5g) e5m();
  };
  const e5y = (e5x = null) => {
    const e5v = e5x || (e5t ? new Error(`${e46}: queue closed`) : null);
    if (e5v) {
      e5l(e5v);
      e5o(e5i, e5v);
      e5i = null;
    }
    e47 = [];
    e48 = 0;
    e49 = 0;
    e5p();
  };
  const e5b = () => {
    if (e48 >= e47.length) return null;
    const e5k = e47[e48];
    e47[e48++] = undefined;
    e49 -= e5k.chunk.byteLength;
    e5h();
    return e5k;
  };
  const e50 = () => {
    const e51 = e5b();
    if (!e51) return null;
    if (e48 >= e47.length || e51.chunk.byteLength >= sw) return e51;
    let e52 = e51.chunk.byteLength;
    let e53 = e48;
    let e54 = e51.allowRetry;
    let e55 = e51.completions || null;
    while (e53 < e47.length) {
      const e56 = e47[e53];
      const e57 = e52 + e56.chunk.byteLength;
      if (e57 > sw) break;
      e52 = e57;
      e54 = e54 && e56.allowRetry;
      if (e56.completions) e55 = e55 ? e55.concat(e56.completions) : e56.completions;
      e53++;
    }
    if (e53 === e48) return e51;
    const e58 = e5r ||= new Uint8Array(sw);
    e58.set(e51.chunk);
    let e59 = e51.chunk.byteLength;
    while (e48 < e53) {
      const e6e = e47[e48];
      e47[e48++] = undefined;
      e49 -= e6e.chunk.byteLength;
      e58.set(e6e.chunk, e59);
      e59 += e6e.chunk.byteLength;
    }
    e5h();
    return {
      chunk: e58.subarray(0, e52),
      allowRetry: e54,
      completions: e55
    };
  };
  const e6t = async () => {
    if (e5e || e5t) return;
    e5e = true;
    try {
      for (; ; ) {
        if (e5t) break;
        const e6r = e50();
        if (!e6r) break;
        let e6n = e42();
        if (!e6n) throw new Error(`${e46}: remote writer unavailable`);
        const e6i = e6r.completions || null;
        e5i = e6i;
        try {
          try {
            await e6n.write(e6r.chunk);
          } catch (e6o) {
            e43?.();
            if (!e6r.allowRetry || typeof e44 !== 'function') throw e6o;
            await e44();
            e6n = e42();
            if (!e6n) throw e6o;
            await e6n.write(e6r.chunk);
          }
          e5o(e6i);
        } catch (e6s) {
          e5o(e6i, e6s);
          throw e6s;
        } finally {
          if (e5i === e6i) e5i = null;
        }
      }
    } catch (e6a) {
      e5t = true;
      e5y(e6a);
      t4m(`[${e46}] 写入失败: ${e6a?.message || e6a}`);
      try {
        e45?.(e6a);
      } catch (e6c) {}
    } finally {
      e5e = false;
      if (!e5t && e48 < e47.length) queueMicrotask(e6t); else e5p();
    }
  };
  const e6l = (e6d, e6w = true, e6f = false) => {
    if (e5t) return false;
    if (!e42()) return false;
    const e6h = ev2(e6d);
    if (!e6h.byteLength) return true;
    const e6p = e49 + e6h.byteLength;
    const e6g = e47.length - e48 + 1;
    if (e6p > sf || e6g > sh) {
      e5t = true;
      const e6m = Object.assign(new Error(`${e46}: upload queue overflow (${e6p}B/${e6g})`), {
        isQueueOverflow: true
      });
      e5y(e6m);
      t4m(`[${e46}] 队列超限，关闭连接`);
      try {
        e45?.(e6m);
      } catch (e6y) {}
      throw e6m;
    }
    let e6x = null;
    let e6v = null;
    if (e6f) {
      e6v = [];
      e6x = new Promise((e6b, e6k) => e6v.push({
        resolve: e6b,
        reject: e6k
      }));
    }
    e47.push({
      chunk: e6h,
      allowRetry: e6w,
      completions: e6v
    });
    e49 = e6p;
    if (!e5e) queueMicrotask(e6t);
    return e6f ? e6x.then(() => true) : true;
  };
  return {
    写入(e60, e61 = true) {
      return e6l(e60, e61, false);
    },
    写入并等待(e62, e63 = true) {
      return e6l(e62, e63, true);
    },
    async 等待空() {
      if (!e49 && !e5e) return;
      await new Promise(e64 => e5n.push(e64));
    },
    清空() {
      e5t = true;
      e5y();
    }
  };
}
function e65(e66, e67 = null) {
  const e68 = sp;
  const e69 = sg;
  const e7e = Math.max(4096, e69 << 3);
  let e7t = e67;
  let e7r = new Uint8Array(e68);
  let e7n = 0;
  let e7i = null;
  let e7o = false;
  let e7s = 0;
  let e7a = 0;
  let e7c = 0;
  let e7l = null;
  const e7d = async e7w => {
    if (e66.readyState !== WebSocket.OPEN) throw new Error('ws.readyState is not open');
    await e4v(e66, e7w);
  };
  const e7f = e7h => {
    if (!e7t) return e7h;
    const e7p = new Uint8Array(e7t.length + e7h.byteLength);
    e7p.set(e7t, 0);
    e7p.set(e7h, e7t.length);
    e7t = null;
    return e7p;
  };
  const e7g = async () => {
    while (e7l) await e7l;
    if (e7i) clearTimeout(e7i);
    e7i = null;
    e7o = false;
    if (!e7n) return;
    const e7m = e7r.subarray(0, e7n).slice();
    e7r = new Uint8Array(e68);
    e7n = 0;
    e7c = 0;
    e7l = e7d(e7m).finally(() => {
      e7l = null;
    });
    return e7l;
  };
  const e7y = () => {
    if (e7i || e7o) return;
    e7o = true;
    e7a = e7s;
    queueMicrotask(() => {
      e7o = false;
      if (!e7n || e7i) return;
      if (e68 - e7n < e69) {
        e7g().catch(() => e4w(e66));
        return;
      }
      e7i = setTimeout(() => {
        e7i = null;
        if (!e7n) return;
        if (e68 - e7n < e69) {
          e7g().catch(() => e4w(e66));
          return;
        }
        if (e7c < 2 && (e7s !== e7a || e7n < e7e)) {
          e7c++;
          e7a = e7s;
          e7y();
          return;
        }
        e7g().catch(() => e4w(e66));
      }, Math.max(sm, 1));
    });
  };
  return {
    async 直接发送(e7x) {
      let e7v = ev2(e7x);
      if (!e7v.byteLength) return;
      e7v = e7f(e7v);
      await e7d(e7v);
    },
    async 发送(e7b) {
      let e7k = ev2(e7b);
      if (!e7k.byteLength) return;
      e7k = e7f(e7k);
      let e70 = 0;
      const e71 = e7k.byteLength;
      while (e70 < e71) {
        if (!e7n && e71 - e70 >= e68) {
          const e72 = Math.min(e68, e71 - e70);
          const e73 = e70 || e72 !== e71 ? e7k.subarray(e70, e70 + e72) : e7k;
          await e7d(e73);
          e70 += e72;
          continue;
        }
        const e74 = Math.min(e68 - e7n, e71 - e70);
        e7r.set(e7k.subarray(e70, e70 + e74), e7n);
        e7n += e74;
        e70 += e74;
        e7s++;
        if (e7n === e68 || e68 - e7n < e69) await e7g(); else e7y();
      }
    },
    flush: e7g
  };
}
async function e75(e76, e77, e78, e79, e8e = null) {
  let e8t = e78, e8r = false, e8n, e8i = false;
  const e8o = 64 * 1024;
  const e8s = e65(e77, e8t);
  const e8a = e8e ? f8('down') : null;
  e8t = null;
  try {
    e8n = e76.readable.getReader({
      mode: 'byob'
    });
    e8i = true;
  } catch (e8c) {
    e8n = e76.readable.getReader();
  }
  try {
    if (!e8i) {
      while (true) {
        const {done: e8l, value: e8d} = await e8n.read();
        if (e8l) break;
        if (!e8d || e8d.byteLength === 0) continue;
        e8r = true;
        if (e8e) e8e.down += e8d.byteLength;
        if (e8a && e8a.enabled) await e8a.take(e8d.byteLength);
        await e8s.发送(e8d);
      }
    } else {
      let e8w = new ArrayBuffer(e8o);
      while (true) {
        const {done: e8f, value: e8h} = await e8n.read(new Uint8Array(e8w, 0, e8o));
        if (e8f) break;
        if (!e8h || e8h.byteLength === 0) continue;
        e8r = true;
        if (e8e) e8e.down += e8h.byteLength;
        if (e8h.byteLength >= sp) {
          await e8s.flush();
          if (e8a && e8a.enabled) await e8a.take(e8h.byteLength);
          await e8s.直接发送(e8h);
          e8w = new ArrayBuffer(e8o);
        } else {
          if (e8a && e8a.enabled) await e8a.take(e8h.byteLength);
          await e8s.发送(e8h);
          e8w = e8h.buffer.byteLength >= e8o ? e8h.buffer : new ArrayBuffer(e8o);
        }
      }
    }
    await e8s.flush();
  } catch (e8p) {
    e4w(e77);
  } finally {
    try {
      e8n.cancel();
    } catch (e8g) {}
    try {
      e8n.releaseLock();
    } catch (e8m) {}
  }
  if (!e8r && e79) await e79();
}
function e8y(e8x) {
  const e8v = [atob('c3BlZWQuY2xvdWRmbGFyZS5jb20=')];
  if (e8v.includes(e8x)) {
    return true;
  }
  for (const e8b of e8v) {
    if (e8x.endsWith('.' + e8b) || e8x === e8b) {
      return true;
    }
  }
  return false;
}
async function e8k(e80, e81, e82, e83) {
  const {username: e84, password: e85, hostname: e86, port: e87} = i6;
  const e88 = e83({
    hostname: e86,
    port: e87
  }), e89 = e88.writable.getWriter(), e9e = e88.readable.getReader();
  try {
    const e9t = e84 && e85 ? new Uint8Array([0x05, 0x02, 0x00, 0x02]) : new Uint8Array([0x05, 0x01, 0x00]);
    await e89.write(e9t);
    let e9r = await e9e.read();
    if (e9r.done || e9r.value.byteLength < 2) throw new Error('S5 method selection failed');
    const e9n = new Uint8Array(e9r.value)[1];
    if (e9n === 0x02) {
      if (!e84 || !e85) throw new Error(ik("UzUgcmVxdWlyZXMgYXV0aGVudGljYXRpb24="));
      const e9i = new TextEncoder().encode(e84), e9o = new TextEncoder().encode(e85);
      const e9s = new Uint8Array([0x01, e9i.length, ...e9i, e9o.length, ...e9o]);
      await e89.write(e9s);
      e9r = await e9e.read();
      if (e9r.done || new Uint8Array(e9r.value)[1] !== 0x00) throw new Error(ik("UzUgYXV0aGVudGljYXRpb24gZmFpbGVk"));
    } else if (e9n !== 0x00) throw new Error(`S5 unsupported auth method: ${e9n}`);
    const e9a = new TextEncoder().encode(e80);
    const e9c = new Uint8Array([0x05, 0x01, 0x00, 0x03, e9a.length, ...e9a, e81 >> 8, e81 & 0xff]);
    await e89.write(e9c);
    e9r = await e9e.read();
    if (e9r.done || new Uint8Array(e9r.value)[1] !== 0x00) throw new Error(ik("UzUgY29ubmVjdGlvbiBmYWlsZWQ="));
    if (ect(e82) > 0) await e89.write(e82);
    e89.releaseLock();
    e9e.releaseLock();
    return e88;
  } catch (e9l) {
    try {
      e89.releaseLock();
    } catch (e9d) {}
    try {
      e9e.releaseLock();
    } catch (e9w) {}
    try {
      e88.close();
    } catch (e9f) {}
    throw e9l;
  }
}
async function e9h(e9p, e9g, e9m, e9y = false, e9x) {
  const {username: e9v, password: e9b, hostname: e9k, port: e90} = i6;
  const e91 = e9y ? e9x({
    hostname: e9k,
    port: e90
  }, {
    secureTransport: 'on',
    allowHalfOpen: false
  }) : e9x({
    hostname: e9k,
    port: e90
  });
  const e92 = e91.writable.getWriter(), e93 = e91.readable.getReader();
  const e94 = new TextEncoder();
  const e95 = new TextDecoder();
  try {
    if (e9y) await e91.opened;
    const e96 = e9v && e9b ? `Proxy-Authorization: Basic ${btoa(`${e9v}:${e9b}`)}\r\n` : '';
    const e97 = `CONNECT ${e9p}:${e9g} HTTP/1.1\r\nHost: ${e9p}:${e9g}\r\n${e96}User-Agent: Mozilla/5.0\r\nConnection: keep-alive\r\n\r\n`;
    await e92.write(e94.encode(e97));
    e92.releaseLock();
    let e98 = new Uint8Array(0), e99 = -1, tee = 0;
    while (e99 === -1 && tee < 8192) {
      const {done: tet, value: ter} = await e93.read();
      if (tet || !ter) throw new Error(`${e9y ? 'HTTPS' : 'HTTP'} 代理在返回 CONNECT 响应前关闭连接`);
      e98 = new Uint8Array([...e98, ...ter]);
      tee = e98.length;
      const ten = e98.findIndex((tei, teo) => teo < e98.length - 3 && e98[teo] === 0x0d && e98[teo + 1] === 0x0a && e98[teo + 2] === 0x0d && e98[teo + 3] === 0x0a);
      if (ten !== -1) e99 = ten + 4;
    }
    if (e99 === -1) throw new Error(ik("5Luj55CGIENPTk5FQ1Qg5ZON5bqU5aS06L+H6ZW/5oiW5peg5pWI"));
    const tes = e95.decode(e98.slice(0, e99)).split('\r\n')[0].match(/HTTP\/\d\.\d\s+(\d+)/);
    const tea = tes ? parseInt(tes[1], 10) : NaN;
    if (!Number.isFinite(tea) || tea < 200 || tea >= 300) throw new Error(`Connection failed: HTTP ${tea}`);
    e93.releaseLock();
    if (ect(e9m) > 0) {
      const tec = e91.writable.getWriter();
      await tec.write(e9m);
      tec.releaseLock();
    }
    if (tee > e99) {
      const {readable: tel, writable: ted} = new TransformStream();
      const tew = ted.getWriter();
      await tew.write(e98.subarray(e99, tee));
      tew.releaseLock();
      e91.readable.pipeTo(ted).catch(() => {});
      return {
        readable: tel,
        writable: e91.writable,
        closed: e91.closed,
        close: () => e91.close()
      };
    }
    return e91;
  } catch (tef) {
    try {
      e92.releaseLock();
    } catch (teh) {}
    try {
      e93.releaseLock();
    } catch (tep) {}
    try {
      e91.close();
    } catch (teg) {}
    throw tef;
  }
}
async function tem(tey, tex, tev, teb) {
  const {username: tek, password: te0, hostname: te1, port: te2} = i6;
  const te3 = new TextEncoder();
  const te4 = new TextDecoder();
  let te5 = null;
  const te6 = tmm(te1) ? '' : tmh(te1);
  const te7 = async (te8 = false) => {
    const te9 = teb({
      hostname: te1,
      port: te2
    });
    try {
      await te9.opened;
      const tte = new tfo(te9, {
        serverName: te6,
        insecure: true,
        allowChacha: te8
      });
      await tte.handshake();
      t4m(`[HTTPS代理] TLS版本: ${tte.isTls13 ? '1.3' : '1.2'} | Cipher: 0x${tte.cipherSuite.toString(16)}${tte.cipherConfig?.chacha ? ' (ChaCha20)' : ' (AES-GCM)'}`);
      return tte;
    } catch (ttt) {
      try {
        te9.close();
      } catch (ttr) {}
      throw ttt;
    }
  };
  try {
    try {
      te5 = await te7(false);
    } catch (ttn) {
      if (!(/cipher|handshake|TLS Alert|ServerHello|Finished|Unsupported|Missing TLS/i).test(ttn?.message || `${ttn || ''}`)) throw ttn;
      t4m(`[HTTPS代理] AES-GCM TLS 握手失败，回退 ChaCha20 兼容模式: ${ttn?.message || ttn}`);
      te5 = await te7(true);
    }
    const tti = tek && te0 ? `Proxy-Authorization: Basic ${btoa(`${tek}:${te0}`)}\r\n` : '';
    const tto = `CONNECT ${tey}:${tex} HTTP/1.1\r\nHost: ${tey}:${tex}\r\n${tti}User-Agent: Mozilla/5.0\r\nConnection: keep-alive\r\n\r\n`;
    await te5.write(te3.encode(tto));
    let tts = new Uint8Array(0), tta = -1, ttc = 0;
    while (tta === -1 && ttc < 8192) {
      const ttl = await te5.read();
      if (!ttl) throw new Error('HTTPS 代理在返回 CONNECT 响应前关闭连接');
      tts = ev4(tts, ttl);
      ttc = tts.length;
      const ttd = tts.findIndex((ttw, ttf) => ttf < tts.length - 3 && tts[ttf] === 0x0d && tts[ttf + 1] === 0x0a && tts[ttf + 2] === 0x0d && tts[ttf + 3] === 0x0a);
      if (ttd !== -1) tta = ttd + 4;
    }
    if (tta === -1) throw new Error(ik("SFRUUFMg5Luj55CGIENPTk5FQ1Qg5ZON5bqU5aS06L+H6ZW/5oiW5peg5pWI"));
    const tth = te4.decode(tts.slice(0, tta)).split('\r\n')[0].match(/HTTP\/\d\.\d\s+(\d+)/);
    const ttp = tth ? parseInt(tth[1], 10) : NaN;
    if (!Number.isFinite(ttp) || ttp < 200 || ttp >= 300) throw new Error(`Connection failed: HTTP ${ttp}`);
    if (ect(tev) > 0) await te5.write(ev2(tev));
    const ttg = ttc > tta ? tts.subarray(tta, ttc) : null;
    let ttm = false, tty, ttx;
    const ttv = (ttb, ttk) => {
      if (!ttm) {
        ttm = true;
        ttb(ttk);
      }
    };
    const tt0 = new Promise((tt1, tt2) => {
      tty = tt1;
      ttx = tt2;
    });
    const tt3 = () => {
      try {
        te5.close();
      } catch (tt4) {}
      ttv(tty);
    };
    const tt5 = new ReadableStream({
      async start(tt6) {
        try {
          if (ect(ttg) > 0) tt6.enqueue(ttg);
          while (true) {
            const tt7 = await te5.read();
            if (!tt7) break;
            if (tt7.byteLength > 0) tt6.enqueue(tt7);
          }
          try {
            tt6.close();
          } catch (tt8) {}
          ttv(tty);
        } catch (tt9) {
          try {
            tt6.error(tt9);
          } catch (tre) {}
          ttv(ttx, tt9);
        }
      },
      cancel() {
        tt3();
      }
    });
    const trt = new WritableStream({
      async write(trr) {
        await te5.write(ev2(trr));
      },
      close: tt3,
      abort(trn) {
        tt3();
        if (trn) ttv(ttx, trn);
      }
    });
    return {
      readable: tt5,
      writable: trt,
      closed: tt0,
      close: tt3
    };
  } catch (tri) {
    try {
      te5?.close();
    } catch (tro) {}
    throw tri;
  }
}
function trs(tra) {
  const trc = tra;
  const trl = trc?.fetcher;
  if (!trl || typeof trl.connect !== 'function') throw new Error('request.fetcher.connect unavailable');
  return (trd, trw) => trw === undefined ? trl.connect(trd) : trl.connect(trd, trw);
}
const trf = 769, trh = 771, trp = 772;
const trg = 20, trm = 21, trx = 22, trv = 23;
const trb = 1, trk = 2, tr0 = 4, tr1 = 8, tr2 = 11, tr3 = 12, tr4 = 13, tr5 = 14, tr6 = 15, tr7 = 16, tr8 = 20, tr9 = 24;
const tne = 0, tnt = 10, tnr = 11, tnn = 13, tni = 16, tno = 43, tns = 45, tna = 51;
const tnc = 0, tnl = 1, tnd = 112;
const tnw = tnf => tnf?.[0] === tnl && tnf?.[1] === tnd;
const tnh = new TextEncoder();
const tnp = new TextDecoder();
const tng = new Uint8Array(0);
const tnm = new Map([[4865, {
  id: 4865,
  keyLen: 16,
  ivLen: 12,
  hash: "SHA-256",
  tls13: !0
}], [4866, {
  id: 4866,
  keyLen: 32,
  ivLen: 12,
  hash: "SHA-384",
  tls13: !0
}], [4867, {
  id: 4867,
  keyLen: 32,
  ivLen: 12,
  hash: "SHA-256",
  tls13: !0,
  chacha: !0
}], [49199, {
  id: 49199,
  keyLen: 16,
  ivLen: 4,
  hash: "SHA-256",
  kex: "ECDHE"
}], [49200, {
  id: 49200,
  keyLen: 32,
  ivLen: 4,
  hash: "SHA-384",
  kex: "ECDHE"
}], [52392, {
  id: 52392,
  keyLen: 32,
  ivLen: 12,
  hash: "SHA-256",
  kex: "ECDHE",
  chacha: !0
}], [49195, {
  id: 49195,
  keyLen: 16,
  ivLen: 4,
  hash: "SHA-256",
  kex: "ECDHE"
}], [49196, {
  id: 49196,
  keyLen: 32,
  ivLen: 4,
  hash: "SHA-384",
  kex: "ECDHE"
}], [52393, {
  id: 52393,
  keyLen: 32,
  ivLen: 12,
  hash: "SHA-256",
  kex: "ECDHE",
  chacha: !0
}]]);
const tny = new Map([[29, "X25519"], [23, "P-256"]]);
const tnx = [2052, 2053, 2054, 1025, 1281, 1537, 1027, 1283, 1539];
const tnv = (...tnb) => {
  const tnk = tn0 => tn0.flatMap(tn1 => tn1 instanceof Uint8Array ? [...tn1] : Array.isArray(tn1) ? tnk(tn1) : "number" == typeof tn1 ? [tn1] : []);
  return new Uint8Array(tnk(tnb));
};
const tn2 = tn3 => [tn3 >> 8 & 255, 255 & tn3];
const tn4 = (tn5, tn6) => tn5[tn6] << 8 | tn5[tn6 + 1];
const tn7 = (tn8, tn9) => tn8[tn9] << 16 | tn8[tn9 + 1] << 8 | tn8[tn9 + 2];
const tie = (...tit) => {
  const tir = tit.filter(tin => tin && tin.length > 0), tii = tir.reduce((tio, tis) => tio + tis.length, 0), tia = new Uint8Array(tii);
  let tic = 0;
  for (const til of tir) (tia.set(til, tic), tic += til.length);
  return tia;
};
const tid = tiw => crypto.getRandomValues(new Uint8Array(tiw));
const tif = (tih, tip) => {
  if (!tih || !tip || tih.length !== tip.length) return !1;
  let tig = 0;
  for (let tim = 0; tim < tih.length; tim++) tig |= tih[tim] ^ tip[tim];
  return 0 === tig;
};
const tiy = tix => "SHA-512" === tix ? 64 : "SHA-384" === tix ? 48 : 32;
async function tiv(tib, tik, ti0) {
  const ti1 = await crypto.subtle.importKey("raw", tik, {
    name: "HMAC",
    hash: tib
  }, !1, ["sign"]);
  return new Uint8Array(await crypto.subtle.sign("HMAC", ti1, ti0));
}
async function ti2(ti3, ti4) {
  return new Uint8Array(await crypto.subtle.digest(ti3, ti4));
}
async function ti5(ti6, ti7, ti8, ti9, toe = "SHA-256") {
  const tot = tie(tnh.encode(ti7), ti8);
  let tor = new Uint8Array(0), ton = tot;
  for (; tor.length < ti9; ) {
    ton = await tiv(toe, ti6, ton);
    const toi = await tiv(toe, ti6, tie(ton, tot));
    tor = tie(tor, toi);
  }
  return tor.slice(0, ti9);
}
async function too(tos, toa, toc) {
  return (toa && toa.length || (toa = new Uint8Array(tiy(tos))), tiv(tos, toa, toc));
}
async function tol(tod, tow, tof, context, toh) {
  const top = tnh.encode("tls13 " + tof);
  return (async function (tog, tom, toy, tox) {
    const tov = tiy(tog), tob = Math.ceil(tox / tov);
    let tok = new Uint8Array(0), to0 = new Uint8Array(0);
    for (let to1 = 1; to1 <= tob; to1++) (to0 = await tiv(tog, tom, tie(to0, toy, [to1])), tok = tie(tok, to0));
    return tok.slice(0, tox);
  })(tod, tow, tnv(tn2(toh), top.length, top, context.length, context), toh);
}
async function to2(to3 = "P-256") {
  const to4 = "X25519" === to3 ? {
    name: "X25519"
  } : {
    name: "ECDH",
    namedCurve: to3
  };
  const to5 = await crypto.subtle.generateKey(to4, !0, ["deriveBits"]);
  const to6 = await crypto.subtle.exportKey("raw", to5.publicKey);
  return {
    keyPair: to5,
    publicKeyRaw: new Uint8Array(to6)
  };
}
async function to7(to8, to9, tse = "P-256") {
  const tst = "X25519" === tse ? {
    name: "X25519"
  } : {
    name: "ECDH",
    namedCurve: tse
  }, tsr = await crypto.subtle.importKey("raw", to9, tst, !1, []), tsn = "P-384" === tse ? 384 : "P-521" === tse ? 528 : 256;
  return new Uint8Array(await crypto.subtle.deriveBits({
    name: tst.name,
    public: tsr
  }, to8, tsn));
}
async function tsi(tso, tss) {
  return crypto.subtle.importKey("raw", tso, {
    name: "AES-GCM"
  }, !1, tss);
}
async function tsa(tsc, tsl, tsd, tsw) {
  return new Uint8Array(await crypto.subtle.encrypt({
    name: "AES-GCM",
    iv: tsl,
    additionalData: tsw,
    tagLength: 128
  }, tsc, tsd));
}
async function tsf(tsh, tsp, tsg, tsm) {
  return new Uint8Array(await crypto.subtle.decrypt({
    name: "AES-GCM",
    iv: tsp,
    additionalData: tsm,
    tagLength: 128
  }, tsh, tsg));
}
function tsy(tsx, tsv) {
  return (tsx << tsv | tsx >>> 32 - tsv) >>> 0;
}
function tsb(tsk, ts0, ts1, ts2, ts3) {
  (tsk[ts0] = tsk[ts0] + tsk[ts1] >>> 0, tsk[ts3] = tsy(tsk[ts3] ^ tsk[ts0], 16), tsk[ts2] = tsk[ts2] + tsk[ts3] >>> 0, tsk[ts1] = tsy(tsk[ts1] ^ tsk[ts2], 12), tsk[ts0] = tsk[ts0] + tsk[ts1] >>> 0, tsk[ts3] = tsy(tsk[ts3] ^ tsk[ts0], 8), tsk[ts2] = tsk[ts2] + tsk[ts3] >>> 0, tsk[ts1] = tsy(tsk[ts1] ^ tsk[ts2], 7));
}
function ts4(ts5, ts6, ts7) {
  const ts8 = new Uint32Array(16);
  (ts8[0] = 1634760805, ts8[1] = 857760878, ts8[2] = 2036477234, ts8[3] = 1797285236);
  const ts9 = new DataView(ts5.buffer, ts5.byteOffset, ts5.byteLength);
  for (let tae = 0; tae < 8; tae++) ts8[4 + tae] = ts9.getUint32(4 * tae, !0);
  ts8[12] = ts6;
  const tat = new DataView(ts7.buffer, ts7.byteOffset, ts7.byteLength);
  (ts8[13] = tat.getUint32(0, !0), ts8[14] = tat.getUint32(4, !0), ts8[15] = tat.getUint32(8, !0));
  const tar = new Uint32Array(ts8);
  for (let tan = 0; tan < 10; tan++) (tsb(tar, 0, 4, 8, 12), tsb(tar, 1, 5, 9, 13), tsb(tar, 2, 6, 10, 14), tsb(tar, 3, 7, 11, 15), tsb(tar, 0, 5, 10, 15), tsb(tar, 1, 6, 11, 12), tsb(tar, 2, 7, 8, 13), tsb(tar, 3, 4, 9, 14));
  for (let tai = 0; tai < 16; tai++) tar[tai] = tar[tai] + ts8[tai] >>> 0;
  return new Uint8Array(tar.buffer.slice(0));
}
function tao(tas, taa, tac) {
  const tal = new Uint8Array(tac.length);
  let tad = 1;
  for (let taw = 0; taw < tac.length; taw += 64) {
    const taf = ts4(tas, tad++, taa), tah = Math.min(64, tac.length - taw);
    for (let tap = 0; tap < tah; tap++) tal[taw + tap] = tac[taw + tap] ^ taf[tap];
  }
  return tal;
}
function tag(tam, tay) {
  const tax = (function (tav) {
    const tab = new Uint8Array(tav);
    return (tab[3] &= 15, tab[7] &= 15, tab[11] &= 15, tab[15] &= 15, tab[4] &= 252, tab[8] &= 252, tab[12] &= 252, tab);
  })(tam.slice(0, 16)), tak = tam.slice(16, 32);
  let ta0 = [0n, 0n, 0n, 0n, 0n];
  const ta1 = [0x3ffffffn & BigInt(tax[0] | tax[1] << 8 | tax[2] << 16 | tax[3] << 24), 0x3ffffffn & BigInt(tax[3] >> 2 | tax[4] << 6 | tax[5] << 14 | tax[6] << 22), 0x3ffffffn & BigInt(tax[6] >> 4 | tax[7] << 4 | tax[8] << 12 | tax[9] << 20), 0x3ffffffn & BigInt(tax[9] >> 6 | tax[10] << 2 | tax[11] << 10 | tax[12] << 18), 0x3ffffffn & BigInt(tax[13] | tax[14] << 8 | tax[15] << 16)];
  for (let ta2 = 0; ta2 < tay.length; ta2 += 16) {
    const ta3 = tay.slice(ta2, ta2 + 16), ta4 = new Uint8Array(17);
    (ta4.set(ta3), ta4[ta3.length] = 1, ta0[0] += BigInt(ta4[0] | ta4[1] << 8 | ta4[2] << 16 | (3 & ta4[3]) << 24), ta0[1] += BigInt(ta4[3] >> 2 | ta4[4] << 6 | ta4[5] << 14 | (15 & ta4[6]) << 22), ta0[2] += BigInt(ta4[6] >> 4 | ta4[7] << 4 | ta4[8] << 12 | (63 & ta4[9]) << 20), ta0[3] += BigInt(ta4[9] >> 6 | ta4[10] << 2 | ta4[11] << 10 | ta4[12] << 18), ta0[4] += BigInt(ta4[13] | ta4[14] << 8 | ta4[15] << 16 | ta4[16] << 24));
    const ta5 = [0n, 0n, 0n, 0n, 0n];
    for (let ta6 = 0; ta6 < 5; ta6++) for (let ta7 = 0; ta7 < 5; ta7++) {
      const ta8 = ta6 + ta7;
      ta8 < 5 ? ta5[ta8] += ta0[ta6] * ta1[ta7] : ta5[ta8 - 5] += ta0[ta6] * ta1[ta7] * 5n;
    }
    let ta9 = 0n;
    for (let tce = 0; tce < 5; tce++) (ta5[tce] += ta9, ta0[tce] = 0x3ffffffn & ta5[tce], ta9 = ta5[tce] >> 26n);
    (ta0[0] += 5n * ta9, ta9 = ta0[0] >> 26n, ta0[0] &= 0x3ffffffn, ta0[1] += ta9);
  }
  let tct = ta0[0] | ta0[1] << 26n | ta0[2] << 52n | ta0[3] << 78n | ta0[4] << 104n;
  tct = tct + tak.reduce((tcr, tcn, tci) => tcr + (BigInt(tcn) << BigInt(8 * tci)), 0n) & (1n << 128n) - 1n;
  const tco = new Uint8Array(16);
  for (let tcs = 0; tcs < 16; tcs++) tco[tcs] = Number(tct >> BigInt(8 * tcs) & 0xffn);
  return tco;
}
function tca(tcc, tcl, tcd, tcw) {
  const tcf = ts4(tcc, 0, tcl).slice(0, 32), tch = tao(tcc, tcl, tcd), tcp = (16 - tcw.length % 16) % 16, tcg = (16 - tch.length % 16) % 16, tcm = new Uint8Array(tcw.length + tcp + tch.length + tcg + 16);
  (tcm.set(tcw, 0), tcm.set(tch, tcw.length + tcp));
  const tcy = new DataView(tcm.buffer, tcw.length + tcp + tch.length + tcg);
  (tcy.setBigUint64(0, BigInt(tcw.length), !0), tcy.setBigUint64(8, BigInt(tch.length), !0));
  const tcx = tag(tcf, tcm);
  return tie(tch, tcx);
}
function tcv(tcb, tck, tc0, tc1) {
  if (tc0.length < 16) throw new Error("Ciphertext too short");
  const tc2 = tc0.slice(-16), tc3 = tc0.slice(0, -16), tc4 = ts4(tcb, 0, tck).slice(0, 32), tc5 = (16 - tc1.length % 16) % 16, tc6 = (16 - tc3.length % 16) % 16, tc7 = new Uint8Array(tc1.length + tc5 + tc3.length + tc6 + 16);
  (tc7.set(tc1, 0), tc7.set(tc3, tc1.length + tc5));
  const tc8 = new DataView(tc7.buffer, tc1.length + tc5 + tc3.length + tc6);
  (tc8.setBigUint64(0, BigInt(tc1.length), !0), tc8.setBigUint64(8, BigInt(tc3.length), !0));
  const tc9 = tag(tc4, tc7);
  let tle = 0;
  for (let tlt = 0; tlt < 16; tlt++) tle |= tc2[tlt] ^ tc9[tlt];
  if (0 !== tle) throw new Error("ChaCha20-Poly1305 authentication failed");
  return tao(tcb, tck, tc3);
}
const tlr = 16 * 1024;
function tln(tli, tlo, tls = trh) {
  const tla = ev2(tlo);
  const tlc = new Uint8Array(5 + tla.byteLength);
  tlc[0] = tli;
  tlc[1] = tls >> 8 & 255;
  tlc[2] = tls & 255;
  tlc[3] = tla.byteLength >> 8 & 255;
  tlc[4] = tla.byteLength & 255;
  tlc.set(tla, 5);
  return tlc;
}
function tll(tld, tlw) {
  return tnv(tld, (tlf => [tlf >> 16 & 255, tlf >> 8 & 255, 255 & tlf])(tlw.length), tlw);
}
class tlh {
  constructor() {
    this.buffer = new Uint8Array(0);
  }
  feed(tlp) {
    const tlg = ev2(tlp);
    this.buffer = this.buffer.length ? tie(this.buffer, tlg) : tlg;
  }
  next() {
    if (this.buffer.length < 5) return null;
    const tlm = this.buffer[0], tly = tn4(this.buffer, 1), tlx = tn4(this.buffer, 3);
    if (this.buffer.length < 5 + tlx) return null;
    const tlv = this.buffer.subarray(5, 5 + tlx);
    return (this.buffer = this.buffer.subarray(5 + tlx), {
      type: tlm,
      version: tly,
      length: tlx,
      fragment: tlv
    });
  }
}
class tlb {
  constructor() {
    this.buffer = new Uint8Array(0);
  }
  feed(tlk) {
    const tl0 = ev2(tlk);
    this.buffer = this.buffer.length ? tie(this.buffer, tl0) : tl0;
  }
  next() {
    if (this.buffer.length < 4) return null;
    const tl1 = this.buffer[0], tl2 = tn7(this.buffer, 1);
    if (this.buffer.length < 4 + tl2) return null;
    const tl3 = this.buffer.subarray(4, 4 + tl2), tl4 = this.buffer.subarray(0, 4 + tl2);
    return (this.buffer = this.buffer.subarray(4 + tl2), {
      type: tl1,
      length: tl2,
      body: tl3,
      raw: tl4
    });
  }
}
function tl5(tl6) {
  let tl7 = 0;
  const tl8 = tn4(tl6, tl7);
  tl7 += 2;
  const tl9 = tl6.slice(tl7, tl7 + 32);
  tl7 += 32;
  const tde = tl6[tl7++], tdt = tl6.slice(tl7, tl7 + tde);
  tl7 += tde;
  const tdr = tn4(tl6, tl7);
  tl7 += 2;
  const tdn = tl6[tl7++];
  let tdi = tl8, tdo = null, tds = null;
  if (tl7 < tl6.length) {
    const tda = tn4(tl6, tl7);
    tl7 += 2;
    const tdc = tl7 + tda;
    for (; tl7 + 4 <= tdc; ) {
      const tdl = tn4(tl6, tl7);
      tl7 += 2;
      const tdd = tn4(tl6, tl7);
      tl7 += 2;
      const tdw = tl6.slice(tl7, tl7 + tdd);
      if ((tl7 += tdd, tdl === tno && tdd >= 2)) tdi = tn4(tdw, 0); else if (tdl === tna && tdd >= 4) {
        const tdf = tn4(tdw, 0), tdh = tn4(tdw, 2);
        tdo = {
          group: tdf,
          key: tdw.slice(4, 4 + tdh)
        };
      } else tdl === tni && tdd >= 3 && (tds = tnp.decode(tdw.slice(3, 3 + tdw[2])));
    }
  }
  const tdp = new Uint8Array([207, 33, 173, 116, 229, 154, 97, 17, 190, 29, 140, 2, 30, 101, 184, 145, 194, 162, 17, 22, 122, 187, 140, 94, 7, 158, 9, 226, 200, 168, 51, 156]);
  return {
    version: tl8,
    serverRandom: tl9,
    sessionId: tdt,
    cipherSuite: tdr,
    compression: tdn,
    selectedVersion: tdi,
    keyShare: tdo,
    alpn: tds,
    isHRR: tif(tl9, tdp),
    isTls13: tdi === trp
  };
}
function tdg(tdm) {
  let tdy = 1;
  const tdx = tn4(tdm, tdy);
  tdy += 2;
  const tdv = tdm[tdy++];
  return {
    namedCurve: tdx,
    serverPublicKey: tdm.slice(tdy, tdy + tdv)
  };
}
function tdb(tdk, td0 = 0) {
  let td1 = 0;
  if (td0) {
    const td2 = tdk[td1++];
    td1 += td2;
  }
  if (td1 + 3 > tdk.length) return null;
  const td3 = tn7(tdk, td1);
  if ((td1 += 3, !td3 || td1 + 3 > tdk.length)) return null;
  const td4 = tn7(tdk, td1);
  return (td1 += 3, td4 ? tdk.slice(td1, td1 + td4) : null);
}
function td5(td6) {
  const td7 = {
    alpn: null
  };
  let td8 = 2;
  const td9 = 2 + tn4(td6, 0);
  for (; td8 + 4 <= td9; ) {
    const twe = tn4(td6, td8);
    td8 += 2;
    const twt = tn4(td6, td8);
    if ((td8 += 2, twe === tni && twt >= 3)) {
      const twr = td6[td8 + 2];
      twr > 0 && td8 + 3 + twr <= td8 + twt && (td7.alpn = tnp.decode(td6.slice(td8 + 3, td8 + 3 + twr)));
    }
    td8 += twt;
  }
  return td7;
}
function twn(twi, two, tws, {tls13: twa = !0, tls12: twc = !0, alpn: twl = null, chacha: twd = !0} = {}) {
  const tww = [];
  (twa && tww.push(4865, 4866, ...twd ? [4867] : []), twc && tww.push(49199, 49200, 49195, 49196, ...twd ? [52392, 52393] : []));
  const twf = tnv(...tww.flatMap(tn2)), twh = [tnv(255, 1, 0, 1, 0)];
  if (two) {
    const twp = tnh.encode(two), twg = tnv(0, tn2(twp.length), twp);
    twh.push(tnv(tn2(tne), tn2(twg.length + 2), tn2(twg.length), twg));
  }
  (twh.push(tnv(tn2(tnr), 0, 2, 1, 0)), twh.push(tnv(tn2(tnt), 0, 6, 0, 4, 0, 29, 0, 23)));
  const twm = tnv(...tnx.flatMap(tn2));
  twh.push(tnv(tn2(tnn), tn2(twm.length + 2), tn2(twm.length), twm));
  const twy = Array.isArray(twl) ? twl.filter(Boolean) : twl ? [twl] : [];
  if (twy.length) {
    const twx = tie(...twy.map(twv => {
      const twb = tnh.encode(twv);
      return tnv(twb.length, twb);
    }));
    twh.push(tnv(tn2(tni), tn2(twx.length + 2), tn2(twx.length), twx));
  }
  if (twa && tws) {
    let twk;
    if ((twh.push(twc ? tnv(tn2(tno), 0, 5, 4, 3, 4, 3, 3) : tnv(tn2(tno), 0, 3, 2, 3, 4)), twh.push(tnv(tn2(tns), 0, 2, 1, 1)), tws?.x25519 && tws?.p256)) twk = tie(tnv(0, 29, tn2(tws.x25519.length), tws.x25519), tnv(0, 23, tn2(tws.p256.length), tws.p256)); else if (tws?.x25519) twk = tnv(0, 29, tn2(tws.x25519.length), tws.x25519); else if (tws?.p256) twk = tnv(0, 23, tn2(tws.p256.length), tws.p256); else {
      if (!(tws instanceof Uint8Array)) throw new Error("Invalid keyShares");
      twk = tnv(0, 23, tn2(tws.length), tws);
    }
    twh.push(tnv(tn2(tna), tn2(twk.length + 2), tn2(twk.length), twk));
  }
  const tw0 = tie(...twh);
  return tll(trb, tnv(tn2(trh), twi, 0, tn2(twf.length), twf, 1, 0, tn2(tw0.length), tw0));
}
const tw1 = tw2 => {
  const tw3 = new Uint8Array(8);
  return (new DataView(tw3.buffer).setBigUint64(0, tw2, !1), tw3);
}, tw4 = (tw5, tw6) => {
  const tw7 = tw5.slice(), tw8 = tw1(tw6);
  for (let tw9 = 0; tw9 < 8; tw9++) tw7[tw7.length - 8 + tw9] ^= tw8[tw9];
  return tw7;
}, tfe = (tft, tfr, tfn, tfi) => Promise.all([tol(tft, tfr, "key", tng, tfn), tol(tft, tfr, "iv", tng, tfi)]);
class tfo {
  constructor(tfs, tfa = {}) {
    if ((this.socket = tfs, this.serverName = tfa.serverName || "", this.supportTls13 = !1 !== tfa.tls13, this.supportTls12 = !1 !== tfa.tls12, !this.supportTls13 && !this.supportTls12)) throw new Error(ik("QXQgbGVhc3Qgb25lIFRMUyB2ZXJzaW9uIG11c3QgYmUgZW5hYmxlZA=="));
    (this.alpnProtocols = Array.isArray(tfa.alpn) ? tfa.alpn : tfa.alpn ? [tfa.alpn] : null, this.allowChacha = tfa.allowChacha !== false, this.timeout = tfa.timeout ?? 3e4, this.clientRandom = tid(32), this.serverRandom = null, this.handshakeChunks = [], this.handshakeComplete = !1, this.negotiatedAlpn = null, this.cipherSuite = null, this.cipherConfig = null, this.isTls13 = !1, this.masterSecret = null, this.handshakeSecret = null, this.clientWriteKey = null, this.serverWriteKey = null, this.clientWriteIv = null, this.serverWriteIv = null, this.clientHandshakeKey = null, this.serverHandshakeKey = null, this.clientHandshakeIv = null, this.serverHandshakeIv = null, this.clientAppKey = null, this.serverAppKey = null, this.clientAppIv = null, this.serverAppIv = null, this.clientWriteCryptoKey = null, this.serverWriteCryptoKey = null, this.clientHandshakeCryptoKey = null, this.serverHandshakeCryptoKey = null, this.clientAppCryptoKey = null, this.serverAppCryptoKey = null, this.clientSeqNum = 0n, this.serverSeqNum = 0n, this.recordParser = new tlh(), this.handshakeParser = new tlb(), this.keyPairs = new Map(), this.ecdhKeyPair = null, this.sawCert = !1);
  }
  recordHandshake(tfc) {
    this.handshakeChunks.push(tfc);
  }
  transcript() {
    return 1 === this.handshakeChunks.length ? this.handshakeChunks[0] : tie(...this.handshakeChunks);
  }
  getCipherConfig(tfl) {
    return tnm.get(tfl) || null;
  }
  async readChunk(tfd) {
    return this.timeout ? Promise.race([tfd.read(), new Promise((tfw, tff) => setTimeout(() => tff(new Error("TLS read timeout")), this.timeout))]) : tfd.read();
  }
  async readRecordsUntil(tfh, tfp, tfg) {
    for (; ; ) {
      let tfm;
      for (; tfm = this.recordParser.next(); ) if (await tfp(tfm)) return;
      const {value: tfy, done: tfx} = await this.readChunk(tfh);
      if (tfx) throw new Error(tfg);
      this.recordParser.feed(tfy);
    }
  }
  async readHandshakeUntil(tfv, tfb, tfk) {
    for (let tf0; tf0 = this.handshakeParser.next(); ) if (await tfb(tf0)) return;
    return this.readRecordsUntil(tfv, async tf1 => {
      if (tf1.type === trm) {
        if (tnw(tf1.fragment)) return;
        throw new Error(`TLS Alert: ${tf1.fragment[1]}`);
      }
      if (tf1.type === trx) {
        this.handshakeParser.feed(tf1.fragment);
        for (let tf2; tf2 = this.handshakeParser.next(); ) if (await tfb(tf2)) return 1;
      }
    }, tfk);
  }
  async acceptCertificate(tf3) {
    if (!tf3?.length) throw new Error("Empty certificate");
    this.sawCert = !0;
  }
  async handshake() {
    const [tf4, tf5] = await Promise.all([to2("P-256"), to2("X25519")]);
    (this.keyPairs = new Map([[23, tf4], [29, tf5]]), this.ecdhKeyPair = tf4.keyPair);
    const tf6 = this.socket.readable.getReader(), tf7 = this.socket.writable.getWriter();
    try {
      const tf8 = twn(this.clientRandom, this.serverName, {
        x25519: tf5.publicKeyRaw,
        p256: tf4.publicKeyRaw
      }, {
        tls13: this.supportTls13,
        tls12: this.supportTls12,
        alpn: this.alpnProtocols,
        chacha: this.allowChacha
      });
      (this.recordHandshake(tf8), await tf7.write(tln(trx, tf8, trf)));
      const tf9 = await this.receiveServerHello(tf6);
      if (tf9.isHRR) throw new Error("HelloRetryRequest is not supported by TLSClientMini");
      if (tf9.keyShare?.group && this.keyPairs.has(tf9.keyShare.group)) {
        const the = this.keyPairs.get(tf9.keyShare.group);
        this.ecdhKeyPair = the.keyPair;
      }
      (tf9.isTls13 ? await this.handshakeTls13(tf6, tf7, tf9) : await this.handshakeTls12(tf6, tf7), this.handshakeComplete = !0);
    } finally {
      (tf6.releaseLock(), tf7.releaseLock());
    }
  }
  async receiveServerHello(tht) {
    for (; ; ) {
      const {value: thr, done: thn} = await this.readChunk(tht);
      if (thn) throw new Error("Connection closed waiting for ServerHello");
      let thi;
      for (this.recordParser.feed(thr); thi = this.recordParser.next(); ) {
        if (thi.type === trm) {
          if (tnw(thi.fragment)) continue;
          throw new Error(`TLS Alert: level=${thi.fragment[0]}, desc=${thi.fragment[1]}`);
        }
        if (thi.type !== trx) continue;
        let tho;
        for (this.handshakeParser.feed(thi.fragment); tho = this.handshakeParser.next(); ) {
          if (tho.type !== trk) continue;
          this.recordHandshake(tho.raw);
          const ths = tl5(tho.body);
          if ((this.serverRandom = ths.serverRandom, this.cipherSuite = ths.cipherSuite, this.cipherConfig = this.getCipherConfig(ths.cipherSuite), this.isTls13 = ths.isTls13, this.negotiatedAlpn = ths.alpn || null, !this.cipherConfig)) throw new Error(`Unsupported cipher suite: 0x${ths.cipherSuite.toString(16)}`);
          return ths;
        }
      }
    }
  }
  async handshakeTls12(tha, thc) {
    let thl = null;
    let thd = !1;
    if ((await this.readHandshakeUntil(tha, async thw => {
      switch (thw.type) {
        case tr2:
          {
            this.recordHandshake(thw.raw);
            const thf = tdb(thw.body, 1);
            if (!thf) throw new Error(ik("TWlzc2luZyBUTFMgMS4yIGNlcnRpZmljYXRl"));
            await this.acceptCertificate(thf);
            break;
          }
        case tr3:
          (this.recordHandshake(thw.raw), thl = tdg(thw.body));
          break;
        case tr5:
          return (this.recordHandshake(thw.raw), thd = !0, 1);
        case tr4:
          throw new Error(ik("Q2xpZW50IGNlcnRpZmljYXRlIGlzIG5vdCBzdXBwb3J0ZWQ="));
        default:
          this.recordHandshake(thw.raw);
      }
    }, "Connection closed during TLS 1.2 handshake"), !this.sawCert)) throw new Error("Missing TLS 1.2 leaf certificate");
    const thh = thl;
    if (!thh) throw new Error(ik("TWlzc2luZyBUTFMgMS4yIFNlcnZlcktleUV4Y2hhbmdl"));
    const thp = tny.get(thh.namedCurve);
    if (!thp) throw new Error(`Unsupported named curve: 0x${thh.namedCurve.toString(16)}`);
    const thg = this.keyPairs.get(thh.namedCurve);
    if (!thg) throw new Error(`Missing key pair for curve: 0x${thh.namedCurve.toString(16)}`);
    const thm = await to7(thg.keyPair.privateKey, thh.serverPublicKey, thp), thy = tll(tr7, tnv(thg.publicKeyRaw.length, thg.publicKeyRaw));
    this.recordHandshake(thy);
    const thx = this.cipherConfig.hash;
    this.masterSecret = await ti5(thm, "master secret", tie(this.clientRandom, this.serverRandom), 48, thx);
    const thv = this.cipherConfig.keyLen, thb = this.cipherConfig.ivLen, thk = await ti5(this.masterSecret, "key expansion", tie(this.serverRandom, this.clientRandom), 2 * thv + 2 * thb, thx);
    (this.clientWriteKey = thk.slice(0, thv), this.serverWriteKey = thk.slice(thv, 2 * thv), this.clientWriteIv = thk.slice(2 * thv, 2 * thv + thb), this.serverWriteIv = thk.slice(2 * thv + thb, 2 * thv + 2 * thb));
    if (!this.cipherConfig.chacha) [this.clientWriteCryptoKey, this.serverWriteCryptoKey] = await Promise.all([tsi(this.clientWriteKey, ["encrypt"]), tsi(this.serverWriteKey, ["decrypt"])]);
    (await thc.write(tln(trx, thy)), await thc.write(tln(trg, tnv(1))));
    const th0 = await ti5(this.masterSecret, "client finished", await ti2(thx, this.transcript()), 12, thx), th1 = tll(tr8, th0);
    (this.recordHandshake(th1), await thc.write(tln(trx, await this.encryptTls12(th1, trx))));
    let th2 = !1;
    await this.readRecordsUntil(tha, async th3 => {
      if (th3.type === trm) {
        if (tnw(th3.fragment)) return;
        throw new Error(`TLS Alert: ${th3.fragment[1]}`);
      }
      if (th3.type === trg) return void (th2 = !0);
      if (th3.type !== trx || !th2) return;
      const th4 = await this.decryptTls12(th3.fragment, trx);
      if (th4[0] !== tr8) return;
      const th5 = tn7(th4, 1), th6 = th4.slice(4, 4 + th5), th7 = await ti5(this.masterSecret, "server finished", await ti2(thx, this.transcript()), 12, thx);
      if (!tif(th6, th7)) throw new Error("TLS 1.2 server Finished verify failed");
      return 1;
    }, ik("Q29ubmVjdGlvbiBjbG9zZWQgd2FpdGluZyBmb3IgVExTIDEuMiBGaW5pc2hlZA=="));
  }
  async handshakeTls13(th8, th9, tpe) {
    const tpt = tny.get(tpe.keyShare?.group);
    if (!tpt || !tpe.keyShare?.key?.length) throw new Error("Missing TLS 1.3 key_share");
    const tpr = this.cipherConfig.hash, tpn = tiy(tpr), tpi = this.cipherConfig.keyLen, tpo = this.cipherConfig.ivLen, tps = await to7(this.ecdhKeyPair.privateKey, tpe.keyShare.key, tpt), tpa = await too(tpr, null, new Uint8Array(tpn)), tpc = await tol(tpr, tpa, "derived", await ti2(tpr, tng), tpn);
    this.handshakeSecret = await too(tpr, tpc, tps);
    const tpl = await ti2(tpr, this.transcript()), tpd = await tol(tpr, this.handshakeSecret, "c hs traffic", tpl, tpn), tpw = await tol(tpr, this.handshakeSecret, "s hs traffic", tpl, tpn);
    ([this.clientHandshakeKey, this.clientHandshakeIv] = await tfe(tpr, tpd, tpi, tpo), [this.serverHandshakeKey, this.serverHandshakeIv] = await tfe(tpr, tpw, tpi, tpo));
    if (!this.cipherConfig.chacha) [this.clientHandshakeCryptoKey, this.serverHandshakeCryptoKey] = await Promise.all([tsi(this.clientHandshakeKey, ["encrypt"]), tsi(this.serverHandshakeKey, ["decrypt"])]);
    const tpf = await tol(tpr, tpw, "finished", tng, tpn);
    let tph = !1;
    const tpp = async tpg => {
      switch (tpg.type) {
        case tr1:
          {
            const tpm = td5(tpg.body);
            (tpm.alpn && (this.negotiatedAlpn = tpm.alpn), this.recordHandshake(tpg.raw));
            break;
          }
        case tr2:
          {
            const tpy = tdb(tpg.body);
            if (!tpy) throw new Error("Missing TLS 1.3 certificate");
            (await this.acceptCertificate(tpy), this.recordHandshake(tpg.raw));
            break;
          }
        case tr4:
          throw new Error(ik("Q2xpZW50IGNlcnRpZmljYXRlIGlzIG5vdCBzdXBwb3J0ZWQ="));
        case tr6:
          this.recordHandshake(tpg.raw);
          break;
        case tr8:
          {
            const tpx = await tiv(tpr, tpf, await ti2(tpr, this.transcript()));
            if (!tif(tpx, tpg.body)) throw new Error("TLS 1.3 server Finished verify failed");
            (this.recordHandshake(tpg.raw), tph = !0);
            break;
          }
        default:
          this.recordHandshake(tpg.raw);
      }
    };
    await this.readRecordsUntil(th8, async tpv => {
      if (tpv.type === trg || tpv.type === trx) return;
      if (tpv.type === trm) {
        if (tnw(tpv.fragment)) return;
        throw new Error(`TLS Alert: ${tpv.fragment[1]}`);
      }
      if (tpv.type !== trv) return;
      const tpb = await this.decryptTls13Handshake(tpv.fragment), tpk = tpb[tpb.length - 1], tp0 = tpb.slice(0, -1);
      if (tpk === trx) {
        this.handshakeParser.feed(tp0);
        for (let tp1; tp1 = this.handshakeParser.next(); ) if ((await tpp(tp1), tph)) return 1;
      }
    }, ik("Q29ubmVjdGlvbiBjbG9zZWQgZHVyaW5nIFRMUyAxLjMgaGFuZHNoYWtl"));
    const tp2 = await ti2(tpr, this.transcript()), tp3 = await tol(tpr, this.handshakeSecret, "derived", await ti2(tpr, tng), tpn), tp4 = await too(tpr, tp3, new Uint8Array(tpn)), tp5 = await tol(tpr, tp4, "c ap traffic", tp2, tpn), tp6 = await tol(tpr, tp4, "s ap traffic", tp2, tpn);
    ([this.clientAppKey, this.clientAppIv] = await tfe(tpr, tp5, tpi, tpo), [this.serverAppKey, this.serverAppIv] = await tfe(tpr, tp6, tpi, tpo));
    if (!this.cipherConfig.chacha) [this.clientAppCryptoKey, this.serverAppCryptoKey] = await Promise.all([tsi(this.clientAppKey, ["encrypt"]), tsi(this.serverAppKey, ["decrypt"])]);
    const tp7 = await tol(tpr, tpd, "finished", tng, tpn), tp8 = await tiv(tpr, tp7, await ti2(tpr, this.transcript())), tp9 = tll(tr8, tp8);
    (this.recordHandshake(tp9), await th9.write(tln(trv, await this.encryptTls13Handshake(tie(tp9, [trx])))), this.clientSeqNum = 0n, this.serverSeqNum = 0n);
  }
  async encryptTls12(tge, tgt) {
    const tgr = this.clientSeqNum++, tgn = tw1(tgr), tgi = tie(tgn, [tgt], tn2(trh), tn2(tge.length));
    if (this.cipherConfig.chacha) {
      const tgo = tw4(this.clientWriteIv, tgr);
      return tca(this.clientWriteKey, tgo, tge, tgi);
    }
    const tgs = tid(8);
    if (!this.clientWriteCryptoKey) this.clientWriteCryptoKey = await tsi(this.clientWriteKey, ["encrypt"]);
    return tie(tgs, await tsa(this.clientWriteCryptoKey, tie(this.clientWriteIv, tgs), tge, tgi));
  }
  async decryptTls12(tga, tgc) {
    const tgl = this.serverSeqNum++, tgd = tw1(tgl);
    if (this.cipherConfig.chacha) {
      const tgw = tw4(this.serverWriteIv, tgl);
      return tcv(this.serverWriteKey, tgw, tga, tie(tgd, [tgc], tn2(trh), tn2(tga.length - 16)));
    }
    const tgf = tga.subarray(0, 8), tgh = tga.subarray(8);
    if (!this.serverWriteCryptoKey) this.serverWriteCryptoKey = await tsi(this.serverWriteKey, ["decrypt"]);
    return tsf(this.serverWriteCryptoKey, tie(this.serverWriteIv, tgf), tgh, tie(tgd, [tgc], tn2(trh), tn2(tgh.length - 16)));
  }
  async encryptTls13Handshake(tgp) {
    const tgg = tw4(this.clientHandshakeIv, this.clientSeqNum++), tgm = tnv(trv, 3, 3, tn2(tgp.length + 16));
    if (this.cipherConfig.chacha) return tca(this.clientHandshakeKey, tgg, tgp, tgm);
    if (!this.clientHandshakeCryptoKey) this.clientHandshakeCryptoKey = await tsi(this.clientHandshakeKey, ["encrypt"]);
    return tsa(this.clientHandshakeCryptoKey, tgg, tgp, tgm);
  }
  async decryptTls13Handshake(tgy) {
    const tgx = tw4(this.serverHandshakeIv, this.serverSeqNum++), tgv = tnv(trv, 3, 3, tn2(tgy.length));
    const tgb = this.cipherConfig.chacha ? await tcv(this.serverHandshakeKey, tgx, tgy, tgv) : await tsf(this.serverHandshakeCryptoKey || (this.serverHandshakeCryptoKey = await tsi(this.serverHandshakeKey, ["decrypt"])), tgx, tgy, tgv);
    let tgk = tgb.length - 1;
    for (; tgk >= 0 && !tgb[tgk]; ) tgk--;
    return tgk < 0 ? tng : tgb.slice(0, tgk + 1);
  }
  async encryptTls13(tg0) {
    const tg1 = tie(tg0, [trv]), tg2 = tw4(this.clientAppIv, this.clientSeqNum++), tg3 = tnv(trv, 3, 3, tn2(tg1.length + 16));
    if (this.cipherConfig.chacha) return tca(this.clientAppKey, tg2, tg1, tg3);
    if (!this.clientAppCryptoKey) this.clientAppCryptoKey = await tsi(this.clientAppKey, ["encrypt"]);
    return tsa(this.clientAppCryptoKey, tg2, tg1, tg3);
  }
  async decryptTls13(tg4) {
    const tg5 = tw4(this.serverAppIv, this.serverSeqNum++), tg6 = tnv(trv, 3, 3, tn2(tg4.length)), tg7 = this.cipherConfig.chacha ? await tcv(this.serverAppKey, tg5, tg4, tg6) : await tsf(this.serverAppCryptoKey || (this.serverAppCryptoKey = await tsi(this.serverAppKey, ["decrypt"])), tg5, tg4, tg6);
    let tg8 = tg7.length - 1;
    for (; tg8 >= 0 && !tg7[tg8]; ) tg8--;
    if (tg8 < 0) return {
      data: tng,
      type: 0
    };
    return {
      data: tg7.slice(0, tg8),
      type: tg7[tg8]
    };
  }
  async write(tg9) {
    if (!this.handshakeComplete) throw new Error("Handshake not complete");
    const tme = ev2(tg9);
    if (!tme.byteLength) return;
    const tmt = this.socket.writable.getWriter();
    try {
      const tmr = [];
      for (let tmn = 0; tmn < tme.byteLength; tmn += tlr) {
        const tmi = tme.subarray(tmn, Math.min(tmn + tlr, tme.byteLength));
        const tmo = this.isTls13 ? await this.encryptTls13(tmi) : await this.encryptTls12(tmi, trv);
        tmr.push(tln(trv, tmo));
      }
      await tmt.write(tmr.length === 1 ? tmr[0] : tie(...tmr));
    } finally {
      tmt.releaseLock();
    }
  }
  async read() {
    for (; ; ) {
      let tms;
      for (; tms = this.recordParser.next(); ) {
        if (tms.type === trm) {
          if (tms.fragment[1] === tnc) return null;
          throw new Error(`TLS Alert: ${tms.fragment[1]}`);
        }
        if (tms.type !== trv) continue;
        if (!this.isTls13) return this.decryptTls12(tms.fragment, trv);
        const {data: tma, type: tmc} = await this.decryptTls13(tms.fragment);
        if (tmc === trv) return tma;
        if (tmc === trm) {
          if (tma[1] === tnc) return null;
          throw new Error(`TLS Alert: ${tma[1]}`);
        }
        if (tmc !== trx) continue;
        let tml;
        for (this.handshakeParser.feed(tma); tml = this.handshakeParser.next(); ) if (tml.type !== tr0 && tml.type === tr9) throw new Error(ik("VExTIDEuMyBLZXlVcGRhdGUgaXMgbm90IHN1cHBvcnRlZCBieSBUTFNDbGllbnRNaW5p"));
      }
      const tmd = this.socket.readable.getReader();
      try {
        const {value: tmw, done: tmf} = await this.readChunk(tmd);
        if (tmf) return null;
        this.recordParser.feed(tmw);
      } finally {
        tmd.releaseLock();
      }
    }
  }
  close() {
    this.socket.close();
  }
}
function tmh(tmp = '') {
  const tmg = String(tmp || '').trim();
  return tmg.startsWith('[') && tmg.endsWith(']') ? tmg.slice(1, -1) : tmg;
}
function tmm(tmy = '') {
  const tmx = tmh(tmy);
  const tmv = /^(25[0-5]|2[0-4]\d|1?\d?\d)(\.(25[0-5]|2[0-4]\d|1?\d?\d)){3}$/;
  if (tmv.test(tmx)) return true;
  if (!tmx.includes(':')) return false;
  try {
    new URL(`http://[${tmx}]/`);
    return true;
  } catch (tmb) {
    return false;
  }
}
const tmk = 9999;
const tm0 = new Uint8Array([0x21, 0x12, 0xa4, 0x42]);
const tm1 = {
  ALLOCATE_REQUEST: 0x0003,
  ALLOCATE_SUCCESS: 0x0103,
  ALLOCATE_ERROR: 0x0113,
  CREATE_PERMISSION_REQUEST: 0x0008,
  CREATE_PERMISSION_SUCCESS: 0x0108,
  CONNECT_REQUEST: 0x000a,
  CONNECT_SUCCESS: 0x010a,
  CONNECTION_BIND_REQUEST: 0x000b,
  CONNECTION_BIND_SUCCESS: 0x010b
};
const tm2 = {
  USERNAME: 0x0006,
  MESSAGE_INTEGRITY: 0x0008,
  ERROR_CODE: 0x0009,
  XOR_PEER_ADDRESS: 0x0012,
  REALM: 0x0014,
  NONCE: 0x0015,
  REQUESTED_TRANSPORT: 0x0019,
  CONNECTION_ID: 0x002a
};
async function tm3(tm4, tm5, tm6) {
  let tm7;
  try {
    return await Promise.race([tm4, new Promise((tm8, tm9) => {
      tm7 = setTimeout(() => tm9(new Error(tm6)), tm5);
    })]);
  } finally {
    clearTimeout(tm7);
  }
}
function tye(tyt) {
  const tyr = String(tyt || '').split('.');
  return tyr.length === 4 && tyr.every(tyn => (/^\d{1,3}$/).test(tyn) && Number(tyn) >= 0 && Number(tyn) <= 255);
}
function tyi(tyo) {
  return -tyo & 3;
}
function tys(tya, tyc) {
  const tyl = ev2(tyc);
  const tyd = new Uint8Array(4 + tyl.byteLength + tyi(tyl.byteLength));
  const tyw = new DataView(tyd.buffer);
  tyw.setUint16(0, tya);
  tyw.setUint16(2, tyl.byteLength);
  tyd.set(tyl, 4);
  return tyd;
}
function tyf(tyh, typ, tyg) {
  const tym = ev4(...tyg);
  const tyy = new Uint8Array(20);
  const tyx = new DataView(tyy.buffer);
  tyx.setUint16(0, tyh);
  tyx.setUint16(2, tym.byteLength);
  tyy.set(tm0, 4);
  tyy.set(typ, 8);
  return ev4(tyy, tym);
}
function tyv(tyb) {
  return tyb?.byteLength >= 4 ? (tyb[2] & 7) * 100 + tyb[3] : 0;
}
function tyk() {
  return crypto.getRandomValues(new Uint8Array(12));
}
async function ty0(ty1, ty2) {
  const ty3 = new Uint8Array(ty1);
  const ty4 = new DataView(ty3.buffer);
  ty4.setUint16(2, ty4.getUint16(2) + 24);
  const ty5 = await crypto.subtle.importKey('raw', ty2, {
    name: 'HMAC',
    hash: 'SHA-1'
  }, false, ['sign']);
  const ty6 = await crypto.subtle.sign('HMAC', ty5, ty3);
  return ev4(ty3, tys(tm2.MESSAGE_INTEGRITY, new Uint8Array(ty6)));
}
async function ty7(ty8, ty9 = null, txe = ik("VFVSTiByZXNwb25zZSB0aW1lZCBvdXQ=")) {
  let txt = ect(ty9) ? ev2(ty9) : new Uint8Array(0);
  const txr = async () => {
    const {done: txn, value: txi} = await tm3(ty8.read(), tmk, txe);
    if (txn) throw new Error('TURN server closed connection');
    if (txi?.byteLength) txt = ev4(txt, txi);
  };
  while (txt.byteLength < 20) await txr();
  const txo = 20 + (txt[2] << 8 | txt[3]);
  if (txo > 65555) throw new Error(ik("VFVSTiByZXNwb25zZSBpcyB0b28gbGFyZ2U="));
  while (txt.byteLength < txo) await txr();
  const txs = txt.subarray(0, txo);
  if (tm0.some((txa, txc) => txs[4 + txc] !== txa)) throw new Error('Invalid TURN/STUN response');
  const txl = new DataView(txs.buffer, txs.byteOffset, txs.byteLength);
  const txd = {};
  for (let txw = 20; txw + 4 <= txo; ) {
    const txf = txl.getUint16(txw);
    const txh = txl.getUint16(txw + 2);
    if (txw + 4 + txh > txs.byteLength) break;
    txd[txf] = txs.slice(txw + 4, txw + 4 + txh);
    txw += 4 + txh + tyi(txh);
  }
  return {
    message: {
      type: txl.getUint16(0),
      attributes: txd
    },
    extraData: txt.byteLength > txo ? txt.subarray(txo) : null
  };
}
async function txp(txg, txm, txy) {
  await tm3(txg.write(txm), tmk, txy);
}
async function txx(txv, txb, txk, tx0) {
  txv = {
    ...txv,
    username: txv.username ?? null,
    password: txv.password ?? null
  };
  const tx1 = tmh(txb);
  let tx2 = tye(tx1) ? tx1 : null;
  if (!tx2) {
    const tx3 = await rn9(tx1, 'A');
    const tx4 = tx3.find(tx5 => tx5.type === 1 && tye(tx5.data))?.data;
    tx2 = typeof tx4 === 'string' ? tx4 : null;
  }
  if (!tx2) throw new Error(`Could not resolve ${txb} to an IPv4 address for TURN CONNECT`);
  const tx6 = tmh(txv.hostname);
  let tx7 = null, tx8 = null, tx9 = null, tve = null, tvt = null, tvr = null, tvn = false;
  const tvi = () => {
    try {
      tx7?.close?.();
    } catch (tvo) {}
    try {
      tx8?.close?.();
    } catch (tvs) {}
  };
  const tva = () => {
    if (tvn) return;
    tvn = true;
    try {
      tvr?.releaseLock?.();
    } catch (tvc) {}
  };
  try {
    tx7 = tx0({
      hostname: tx6,
      port: txv.port
    });
    await tm3(tx7.opened, tmk, ik("VFVSTiBzZXJ2ZXIgY29ubmVjdGlvbiB0aW1lZCBvdXQ="));
    tx9 = tx7.writable.getWriter();
    tve = tx7.readable.getReader();
    const tvl = new Uint8Array(8);
    tvl[1] = 1;
    new DataView(tvl.buffer).setUint16(2, txk ^ 0x2112);
    tx2.split('.').forEach((tvd, tvw) => {
      tvl[4 + tvw] = Number(tvd) ^ tm0[tvw];
    });
    const tvf = tys(tm2.XOR_PEER_ADDRESS, tvl);
    const tvh = new Uint8Array([6, 0, 0, 0]);
    await txp(tx9, tyf(tm1.ALLOCATE_REQUEST, tyk(), [tys(tm2.REQUESTED_TRANSPORT, tvh)]), 'TURN Allocate request timed out');
    let tvp = await ty7(tve, null, ik("VFVSTiBBbGxvY2F0ZSByZXNwb25zZSB0aW1lZCBvdXQ="));
    let tvg = tvp.message;
    let tvm = tvp.extraData;
    let tvy = null;
    let tvx = [];
    const tvv = tvb => tvy ? ty0(tvb, tvy) : Promise.resolve(tvb);
    if (tvg.type === tm1.ALLOCATE_ERROR && txv.username !== null && txv.password !== null && tyv(tvg.attributes[tm2.ERROR_CODE]) === 401) {
      const tvk = tvg.attributes[tm2.REALM];
      const tv0 = tvg.attributes[tm2.NONCE];
      if (!tvk || !tv0?.byteLength) throw new Error('TURN authentication challenge is missing realm or nonce');
      const tv1 = tnp.decode(tvk);
      tvy = new Uint8Array(await crypto.subtle.digest('MD5', tnh.encode(`${txv.username}:${tv1}:${txv.password}`)));
      tvx = [tys(tm2.USERNAME, tnh.encode(txv.username)), tys(tm2.REALM, tnh.encode(tv1)), tys(tm2.NONCE, tv0)];
      const tv2 = await ty0(tyf(tm1.ALLOCATE_REQUEST, tyk(), [tys(tm2.REQUESTED_TRANSPORT, tvh), ...tvx]), tvy);
      const tv3 = await Promise.all([tvv(tyf(tm1.CREATE_PERMISSION_REQUEST, tyk(), [tvf, ...tvx])), tvv(tyf(tm1.CONNECT_REQUEST, tyk(), [tvf, ...tvx]))]);
      await txp(tx9, ev4(tv2, ...tv3), 'TURN authenticated Allocate request timed out');
      tvp = await ty7(tve, tvm, ik("VFVSTiBhdXRoZW50aWNhdGVkIEFsbG9jYXRlIHJlc3BvbnNlIHRpbWVkIG91dA=="));
      tvg = tvp.message;
      tvm = tvp.extraData;
    } else if (tvg.type === tm1.ALLOCATE_SUCCESS) {
      const tv4 = await Promise.all([tvv(tyf(tm1.CREATE_PERMISSION_REQUEST, tyk(), [tvf, ...tvx])), tvv(tyf(tm1.CONNECT_REQUEST, tyk(), [tvf, ...tvx]))]);
      if (tv4.length) await txp(tx9, ev4(...tv4), 'TURN pipelined request timed out');
    }
    if (tvg.type !== tm1.ALLOCATE_SUCCESS) {
      const tv5 = tyv(tvg.attributes[tm2.ERROR_CODE]);
      throw new Error(tv5 ? `TURN Allocate failed with ${tv5}` : ik("VFVSTiBBbGxvY2F0ZSBmYWlsZWQ="));
    }
    tx8 = tx0({
      hostname: tx6,
      port: txv.port
    });
    tvp = await ty7(tve, tvm, ik("VFVSTiBDcmVhdGVQZXJtaXNzaW9uIHJlc3BvbnNlIHRpbWVkIG91dA=="));
    tvg = tvp.message;
    tvm = tvp.extraData;
    if (tvg.type !== tm1.CREATE_PERMISSION_SUCCESS) throw new Error(ik("VFVSTiBDcmVhdGVQZXJtaXNzaW9uIGZhaWxlZA=="));
    tvp = await ty7(tve, tvm, 'TURN CONNECT response timed out');
    tvg = tvp.message;
    tvm = tvp.extraData;
    if (tvg.type !== tm1.CONNECT_SUCCESS || !tvg.attributes[tm2.CONNECTION_ID]) throw new Error(ik("VFVSTiBDT05ORUNUIGZhaWxlZA=="));
    await tm3(tx8.opened, tmk, ik("VFVSTiBkYXRhIGNvbm5lY3Rpb24gdGltZWQgb3V0"));
    tvt = tx8.writable.getWriter();
    tvr = tx8.readable.getReader();
    await txp(tvt, await tvv(tyf(tm1.CONNECTION_BIND_REQUEST, tyk(), [tys(tm2.CONNECTION_ID, tvg.attributes[tm2.CONNECTION_ID]), ...tvx])), ik("VFVSTiBDb25uZWN0aW9uQmluZCByZXF1ZXN0IHRpbWVkIG91dA=="));
    tvp = await ty7(tvr, null, ik("VFVSTiBDb25uZWN0aW9uQmluZCByZXNwb25zZSB0aW1lZCBvdXQ="));
    tvg = tvp.message;
    const tv6 = tvp.extraData;
    if (tvg.type !== tm1.CONNECTION_BIND_SUCCESS) throw new Error('TURN ConnectionBind failed');
    tx9.releaseLock();
    tx9 = null;
    tve.releaseLock();
    tve = null;
    tvt.releaseLock();
    tvt = null;
    const tv7 = new ReadableStream({
      start(tv8) {
        if (tv6?.byteLength) tv8.enqueue(tv6);
      },
      pull(tv9) {
        return tvr.read().then(({done: tbe, value: tbt}) => {
          if (tbe) {
            tva();
            tv9.close();
          } else if (tbt?.byteLength) tv9.enqueue(new Uint8Array(tbt));
        });
      },
      cancel() {
        try {
          tvr?.cancel?.();
        } catch (tbr) {}
        tva();
        tvi();
      }
    });
    return {
      readable: tv7,
      writable: tx8.writable,
      closed: tx8.closed,
      close: tvi
    };
  } catch (tbn) {
    try {
      tx9?.releaseLock?.();
    } catch (tbi) {}
    try {
      tve?.releaseLock?.();
    } catch (tbo) {}
    try {
      tvt?.releaseLock?.();
    } catch (tbs) {}
    tva();
    tvi();
    throw tbn;
  }
}
const tba = 1400;
const tbc = new Uint8Array(0);
function tbl(tbd, tbw = 0) {
  return tbd[tbw] << 8 | tbd[tbw + 1];
}
function tbf(tbh, tbp = 0) {
  return (tbh[tbp] << 24 | tbh[tbp + 1] << 16 | tbh[tbp + 2] << 8 | tbh[tbp + 3]) >>> 0;
}
function tbg() {
  return tbl(crypto.getRandomValues(new Uint8Array(2)));
}
function tbm(tby, tbx, tbv) {
  let tbb = 0;
  for (let tbk = tbx; tbk < tbx + tbv - 1; tbk += 2) tbb += tbl(tby, tbk);
  if (tbv & 1) tbb += tby[tbx + tbv - 1] << 8;
  while (tbb >> 16) tbb = (tbb & 0xffff) + (tbb >> 16);
  return ~tbb & 0xffff;
}
async function tb0(tb1, tb2, tb3, tb4) {
  tb1 = {
    ...tb1,
    username: tb1.username ?? null,
    password: tb1.password ?? null
  };
  let tb5 = tbc, tb6 = 1, tb7 = null, tb8 = null, tb9 = null;
  let tke = false, tkt, tkr;
  const tkn = new Promise((tki, tko) => {
    tkt = tki;
    tkr = tko;
  });
  const tks = (tka, tkc) => {
    if (tke) return;
    tke = true;
    tka(tkc);
  };
  const tkl = () => {
    try {
      tb8?.cancel?.().catch?.(() => {});
    } catch (tkd) {}
    try {
      tb8?.releaseLock?.();
    } catch (tkw) {}
    try {
      tb9?.close?.().catch?.(() => {});
    } catch (tkf) {}
    try {
      tb9?.releaseLock?.();
    } catch (tkh) {}
    try {
      tb7?.close?.();
    } catch (tkp) {}
    tks(tkt);
  };
  const tkg = async () => {
    const {value: tkm, done: tky} = await tb8.read();
    if (tky || !tkm) throw new Error('SSTP socket closed');
    return ev2(tkm);
  };
  const tkx = async tkv => {
    while (tb5.byteLength < tkv) {
      const tkb = await tkg();
      tb5 = tb5.byteLength ? ev4(tb5, tkb) : tkb;
    }
    const tkk = tb5.subarray(0, tkv);
    tb5 = tb5.subarray(tkv);
    return tkk;
  };
  const tk0 = async () => {
    for (; ; ) {
      const tk1 = tb5.indexOf(10);
      if (tk1 >= 0) {
        const tk2 = tnp.decode(tb5.subarray(0, tk1));
        tb5 = tb5.subarray(tk1 + 1);
        return tk2.replace(/\r$/, '');
      }
      const tk3 = await tkg();
      tb5 = tb5.byteLength ? ev4(tb5, tk3) : tk3;
    }
  };
  const tk4 = async (tk5 = tmk) => {
    const tk6 = await tm3(tkx(4), tk5, 'SSTP read timeout');
    const tk7 = tbl(tk6, 2) & 0x0fff;
    if (tk7 < 4) throw new Error('Invalid SSTP packet length');
    return {
      isControl: (tk6[1] & 1) !== 0,
      body: tk7 > 4 ? await tm3(tkx(tk7 - 4), tk5, 'SSTP packet body read timeout') : tbc
    };
  };
  const tk8 = tk9 => {
    const t0e = 6 + tk9.byteLength;
    const t0t = new Uint8Array(t0e);
    t0t.set([0x10, 0x00, t0e >> 8 & 0x0f | 0x80, t0e & 0xff, 0xff, 0x03]);
    t0t.set(tk9, 6);
    return t0t;
  };
  const t0r = (t0n, t0i, t0o, t0s = []) => {
    const t0a = t0s.reduce((t0c, t0l) => t0c + 2 + t0l.data.byteLength, 0);
    const t0d = new Uint8Array(6 + t0a);
    const t0w = new DataView(t0d.buffer);
    t0w.setUint16(0, t0n);
    t0d[2] = t0i;
    t0d[3] = t0o;
    t0w.setUint16(4, 4 + t0a);
    t0s.reduce((t0f, t0h) => {
      t0d[t0f] = t0h.type;
      t0d[t0f + 1] = 2 + t0h.data.byteLength;
      t0d.set(t0h.data, t0f + 2);
      return t0f + 2 + t0h.data.byteLength;
    }, 6);
    return t0d;
  };
  const t0p = t0g => {
    const t0m = t0g.byteLength >= 2 && t0g[0] === 0xff && t0g[1] === 0x03 ? 2 : 0;
    if (t0g.byteLength - t0m < 4) return null;
    const t0y = tbl(t0g, t0m);
    if (t0y === 0x0021) return {
      protocol: t0y,
      ipPacket: t0g.subarray(t0m + 2)
    };
    if (t0g.byteLength - t0m < 6) return null;
    return {
      protocol: t0y,
      code: t0g[t0m + 2],
      id: t0g[t0m + 3],
      payload: t0g.subarray(t0m + 6),
      rawPacket: t0g.subarray(t0m)
    };
  };
  const t0x = t0v => {
    const t0b = [];
    for (let t0k = 0; t0k + 2 <= t0v.byteLength; ) {
      const t00 = t0v[t0k];
      const t01 = t0v[t0k + 1];
      if (t01 < 2 || t0k + t01 > t0v.byteLength) break;
      t0b.push({
        type: t00,
        data: t0v.subarray(t0k + 2, t0k + t01)
      });
      t0k += t01;
    }
    return t0b;
  };
  try {
    const t02 = tmh(tb1.hostname);
    const t03 = tb1.port;
    tb7 = tb4({
      hostname: t02,
      port: t03
    }, {
      secureTransport: 'on',
      allowHalfOpen: false
    });
    await tm3(tb7.opened, tmk, 'SSTP server connection timed out');
    tb8 = tb7.readable.getReader();
    tb9 = tb7.writable.getWriter();
    const t04 = t02.includes(':') ? `[${t02}]` : t02;
    const t05 = tnh.encode(`SSTP_DUPLEX_POST /sra_{BA195980-CD49-458b-9E23-C84EE0ADCD75}/ HTTP/1.1\r\n` + `Host: ${Number(t03) === 443 ? t04 : `${t04}:${t03}`}\r\n` + 'Content-Length: 18446744073709551615\r\n' + `SSTPCORRELATIONID: {${crypto.randomUUID()}}\r\n\r\n`);
    const t06 = new Uint8Array(2);
    new DataView(t06.buffer).setUint16(0, 1);
    const t07 = new Uint8Array(2);
    new DataView(t07.buffer).setUint16(0, 1500);
    const t08 = new Uint8Array(12 + t06.byteLength);
    const t09 = new DataView(t08.buffer);
    t08[0] = 0x10;
    t08[1] = 0x01;
    t09.setUint16(2, t08.byteLength | 0x8000);
    t09.setUint16(4, 0x0001);
    t09.setUint16(6, 1);
    t08[9] = 1;
    t09.setUint16(10, 4 + t06.byteLength);
    t08.set(t06, 12);
    await tm3(tb9.write(ev4(t05, t08, tk8(t0r(0xc021, 1, tb6++, [{
      type: 1,
      data: t07
    }])))), tmk, ik("U1NUUCBIVFRQIGhhbmRzaGFrZSByZXF1ZXN0IHRpbWVkIG91dA=="));
    const t1e = await tm3(tk0(), tmk, 'SSTP HTTP handshake timed out');
    for (; ; ) {
      const t1t = await tm3(tk0(), tmk, 'SSTP HTTP header read timed out');
      if (t1t === '') break;
    }
    if (!(/HTTP\/\d(?:\.\d)?\s+2\d\d/i).test(t1e)) throw new Error(`SSTP HTTP handshake failed: ${t1e || 'invalid status'}`);
    let t1r = false, t1n = false, t1i = false, t1o = false, t1s = false, t1a = false, t1c = false, t1l = null;
    const t1d = async () => {
      if (!t1r || !t1n || !t1i || t1o) return;
      if (tb1.username === null || tb1.password === null) throw new Error('SSTP server requires PAP authentication');
      const t1w = tnh.encode(tb1.username);
      const t1f = tnh.encode(tb1.password);
      if (t1w.byteLength > 255 || t1f.byteLength > 255) throw new Error(ik("U1NUUCB1c2VybmFtZS9wYXNzd29yZCBpcyB0b28gbG9uZw=="));
      const t1h = 6 + t1w.byteLength + t1f.byteLength;
      const t1p = new Uint8Array(2 + t1h);
      const t1g = new DataView(t1p.buffer);
      t1g.setUint16(0, 0xc023);
      t1p[2] = 1;
      t1p[3] = tb6++;
      t1g.setUint16(4, t1h);
      t1p[6] = t1w.byteLength;
      t1p.set(t1w, 7);
      t1p[7 + t1w.byteLength] = t1f.byteLength;
      t1p.set(t1f, 8 + t1w.byteLength);
      await tm3(tb9.write(tk8(t1p)), tmk, 'SSTP PAP authentication request timed out');
      t1o = true;
    };
    const t1m = async () => {
      if (!t1r || !t1n || t1a || t1i && !t1s) return;
      await tm3(tb9.write(tk8(t0r(0x8021, 1, tb6++, [{
        type: 3,
        data: new Uint8Array(4)
      }]))), tmk, 'SSTP IPCP request timed out');
      t1a = true;
    };
    for (let t1y = 0; t1y < 50 && !t1c; t1y++) {
      const t1x = await tk4(tmk);
      if (t1x.isControl) continue;
      const t1v = t0p(t1x.body);
      if (!t1v) continue;
      if (t1v.protocol === 0xc021) {
        if (t1v.code === 1) {
          const t1b = t0x(t1v.payload).find(t1k => t1k.type === 3);
          if (t1b?.data?.byteLength >= 2) {
            const t10 = tbl(t1b.data);
            if (t10 !== 0xc023) throw new Error(`SSTP unsupported PPP authentication protocol: 0x${t10.toString(16)}`);
            t1i = true;
          }
          const t11 = new Uint8Array(t1v.rawPacket);
          t11[2] = 2;
          await tm3(tb9.write(tk8(t11)), tmk, 'SSTP LCP Configure-Ack timed out');
          t1n = true;
          await t1d();
          await t1m();
        } else if (t1v.code === 2) {
          t1r = true;
          await t1d();
          await t1m();
        }
        continue;
      }
      if (t1v.protocol === 0xc023) {
        if (t1v.code === 2) {
          t1s = true;
          await t1m();
        } else if (t1v.code === 3) throw new Error('SSTP PAP authentication failed');
        continue;
      }
      if (t1v.protocol === 0x8021) {
        if (t1v.code === 1) {
          const t12 = new Uint8Array(t1v.rawPacket);
          t12[2] = 2;
          await tm3(tb9.write(tk8(t12)), tmk, ik("U1NUUCBJUENQIENvbmZpZ3VyZS1BY2sgdGltZWQgb3V0"));
          await t1m();
        } else if (t1v.code === 3) {
          const t13 = t0x(t1v.payload).find(t14 => t14.type === 3);
          if (t13?.data?.byteLength === 4) {
            t1l = [...t13.data].join('.');
            await tm3(tb9.write(tk8(t0r(0x8021, 1, tb6++, [{
              type: 3,
              data: t13.data
            }]))), tmk, ik("U1NUUCBJUENQIGFkZHJlc3MgcmVxdWVzdCB0aW1lZCBvdXQ="));
            t1a = true;
          }
        } else if (t1v.code === 2) {
          const t15 = t0x(t1v.payload).find(t16 => t16.type === 3);
          if (t15?.data?.byteLength === 4) t1l = [...t15.data].join('.');
          t1c = true;
        }
      }
    }
    if (!t1l) throw new Error(ik("U1NUUCBkaWQgbm90IGFzc2lnbiBhbiBJUHY0IGFkZHJlc3M="));
    const t17 = tmh(tb2);
    let t18 = tye(t17) ? t17 : null;
    if (!t18) {
      const t19 = await rn9(t17, 'A');
      const t2e = t19.find(t2t => t2t.type === 1 && tye(t2t.data))?.data;
      t18 = typeof t2e === 'string' ? t2e : null;
    }
    if (!t18) throw new Error(`Could not resolve ${tb2} to an IPv4 address for SSTP`);
    const t2r = 10000 + tbg() % 50000;
    const t2n = new Uint8Array(String(t1l || '').split('.').map(Number));
    const t2i = new Uint8Array(String(t18 || '').split('.').map(Number));
    let t2o = tbf(crypto.getRandomValues(new Uint8Array(4)));
    let t2s = 0;
    const t2a = new Uint8Array(20);
    t2a.set([0x45, 0x00, 0x00, 0x00, 0x00, 0x00, 0x40, 0x00, 64, 6]);
    t2a.set(t2n, 12);
    t2a.set(t2i, 16);
    const t2c = new Uint8Array(1432);
    t2c.set(t2n);
    t2c.set(t2i, 4);
    t2c[9] = 6;
    const t2l = (t2d, t2w = tbc) => {
      const t2f = ev2(t2w);
      const t2h = t2f.byteLength;
      const t2p = 20 + t2h;
      const t2g = 20 + t2p;
      const t2m = 8 + t2g;
      const t2y = new Uint8Array(t2m);
      const t2x = new DataView(t2y.buffer);
      t2y.set([0x10, 0x00, t2m >> 8 & 0x0f | 0x80, t2m & 0xff, 0xff, 0x03, 0x00, 0x21]);
      t2y.set(t2a, 8);
      t2x.setUint16(10, t2g);
      t2x.setUint16(12, tbg());
      t2x.setUint16(18, tbm(t2y, 8, 20));
      t2x.setUint16(28, t2r);
      t2x.setUint16(30, tb3);
      t2x.setUint32(32, t2o);
      t2x.setUint32(36, t2s);
      t2y[40] = 0x50;
      t2y[41] = t2d;
      t2x.setUint16(42, 65535);
      if (t2h) t2y.set(t2f, 48);
      t2c[10] = t2p >> 8;
      t2c[11] = t2p & 0xff;
      t2c.set(t2y.subarray(28, 28 + t2p), 12);
      t2x.setUint16(44, tbm(t2c, 0, 12 + t2p));
      return t2y;
    };
    const t2v = t2b => {
      if (t2b.byteLength < 40 || t2b[9] !== 6) return null;
      const t2k = (t2b[0] & 0x0f) * 4;
      if (t2b.byteLength < t2k + 20) return null;
      if (tbl(t2b, t2k) !== tb3) return null;
      if (tbl(t2b, t2k + 2) !== t2r) return null;
      return {
        flags: t2b[t2k + 13],
        sequence: tbf(t2b, t2k + 4),
        payloadOffset: t2k + (t2b[t2k + 12] >> 4 & 0x0f) * 4
      };
    };
    await tm3(tb9.write(t2l(0x02)), tmk, 'SSTP TCP SYN write timed out');
    t2o = t2o + 1 >>> 0;
    let t20 = false;
    for (let t21 = 0; t21 < 30; t21++) {
      const t22 = await tk4(tmk);
      if (t22.isControl) continue;
      const t23 = t0p(t22.body);
      if (!t23 || t23.protocol !== 0x0021) continue;
      const t24 = t2v(t23.ipPacket);
      if (!t24 || (t24.flags & 0x12) !== 0x12) continue;
      t2s = t24.sequence + 1 >>> 0;
      await tm3(tb9.write(t2l(0x10)), tmk, ik("U1NUUCBUQ1AgQUNLIHdyaXRlIHRpbWVkIG91dA=="));
      t20 = true;
      break;
    }
    if (!t20) throw new Error(ik("VENQIGhhbmRzaGFrZSB0aHJvdWdoIFNTVFAgdGltZWQgb3V0"));
    let t25 = null;
    const t26 = new ReadableStream({
      start(t27) {
        t25 = t27;
      },
      cancel() {
        tkl();
      }
    });
    (async () => {
      try {
        let t28 = [], t29 = 0;
        const t3e = () => {
          if (!t29) return;
          if (!t25) throw new Error('SSTP readable stream is not ready');
          t25.enqueue(t28.length === 1 ? t28[0] : ev4(...t28));
          t28 = [];
          t29 = 0;
          tb9.write(t2l(0x10)).catch(() => {});
        };
        for (; ; ) {
          const t3t = await tk4(60000);
          if (t3t.isControl) continue;
          const t3r = t0p(t3t.body);
          if (!t3r || t3r.protocol !== 0x0021) continue;
          const t3n = t2v(t3r.ipPacket);
          if (!t3n) continue;
          if (t3n.payloadOffset < t3r.ipPacket.byteLength) {
            const t3i = t3r.ipPacket.subarray(t3n.payloadOffset);
            if (t3i.byteLength) {
              t2s = t3n.sequence + t3i.byteLength >>> 0;
              t28.push(new Uint8Array(t3i));
              t29 += t3i.byteLength;
            }
          }
          if (t3n.flags & 0x01) {
            t3e();
            t2s = t2s + 1 >>> 0;
            tb9.write(t2l(0x11)).catch(() => {});
            const t3o = t25;
            if (t3o) {
              try {
                t3o.close();
              } catch (t3s) {}
            }
            tkl();
            return;
          }
          if (tb5.byteLength < 4 || t29 >= 32768) t3e();
        }
      } catch (t3a) {
        const t3c = t25;
        if (t3c) {
          try {
            t3c.error(t3a);
          } catch (t3l) {}
        }
        tks(tkr, t3a);
        try {
          tb7?.close?.();
        } catch (t3d) {}
      }
    })();
    const t3w = new WritableStream({
      async write(t3f) {
        const t3h = ev2(t3f);
        if (!t3h.byteLength) return;
        if (t3h.byteLength <= tba) {
          await tb9.write(t2l(0x18, t3h));
          t2o = t2o + t3h.byteLength >>> 0;
          return;
        }
        const t3p = [];
        for (let t3g = 0; t3g < t3h.byteLength; t3g += tba) {
          const t3m = t3h.subarray(t3g, Math.min(t3g + tba, t3h.byteLength));
          t3p.push(t2l(0x18, t3m));
          t2o = t2o + t3m.byteLength >>> 0;
        }
        await tb9.write(ev4(...t3p));
      },
      close() {
        return tb9.write(t2l(0x11)).catch(() => {});
      },
      abort(t3y) {
        tkl();
        if (t3y) tks(tkr, t3y);
      }
    });
    return {
      readable: t26,
      writable: t3w,
      closed: tkn,
      close: tkl
    };
  } catch (t3x) {
    tkl();
    throw t3x;
  }
}
function t3v(t3b, t3k) {
  const t30 = new TextEncoder();
  const t31 = t30.encode(t3b);
  const t32 = t30.encode(t3k);
  const t33 = new Uint8Array(t31.length);
  for (let t34 = 0; t34 < t31.length; t34++) {
    t33[t34] = t31[t34] ^ t32[t34 % t32.length];
  }
  let t35 = '';
  for (let t36 = 0; t36 < t33.length; t36++) {
    t35 += String.fromCharCode(t33[t36]);
  }
  return btoa(t35);
}
function t37(t38, t39) {
  const t4e = atob(t38);
  const t4t = new Uint8Array(t4e.length);
  for (let t4r = 0; t4r < t4e.length; t4r++) {
    t4t[t4r] = t4e.charCodeAt(t4r);
  }
  const t4n = new TextEncoder();
  const t4i = t4n.encode(t39);
  const t4o = new Uint8Array(t4t.length);
  for (let t4s = 0; t4s < t4t.length; t4s++) {
    t4o[t4s] = t4t[t4s] ^ t4i[t4s % t4i.length];
  }
  const t4a = new TextDecoder();
  return t4a.decode(t4o);
}
function t4c(t4l = {}) {
  const t4d = t4l.传输协议 === 'grpc';
  return {
    type: t4d ? t4l.gRPC模式 === 'multi' ? 'grpc&mode=multi' : 'grpc&mode=gun' : t4l.传输协议 === 'xhttp' ? ik("eGh0dHAmbW9kZT1zdHJlYW0tb25l") : 'ws',
    路径字段名: t4d ? 'serviceName' : 'path',
    域名字段名: t4d ? 'authority' : 'host'
  };
}
function t4w(t4f = {}, t4h = '/', t4p = false) {
  const t4g = t4p ? '/' : t4f.随机路径 ? rnk(t4h) : t4h;
  if (t4f.传输协议 !== 'grpc') return t4g;
  return t4g.split('?')[0] || '/';
}
function t4m(...t4y) {
  if (on) console.log(...t4y);
}
function t4x(t4v) {
  return t4v.replace(/-/g, '+').replace(/_/g, '/') + ('===').slice((t4v.length + 3) % 4);
}
async function t4b() {
  const t4k = await crypto.subtle.generateKey({
    name: 'X25519'
  }, true, ['deriveBits']);
  const t40 = await crypto.subtle.exportKey('jwk', t4k.publicKey);
  const t41 = await crypto.subtle.exportKey('jwk', t4k.privateKey);
  return {
    publicKey: t4x(t40.x),
    privateKey: t4x(t41.d)
  };
}
function t42(t43) {
  try {
    const t44 = atob(t43);
    return Array.from(t44, t45 => t45.charCodeAt(0));
  } catch (t46) {
    return [0, 0, 0];
  }
}
async function t47(env, t48 = 'warp-account.json') {
  const {publicKey: t49, privateKey: t5e} = await t4b();
  const t5t = {
    key: t49,
    install_id: '',
    fcm_token: '',
    tos: new Date().toISOString(),
    type: 'Android',
    model: 'PC',
    locale: 'en_US',
    warp_enabled: true
  };
  const t5r = await fetch(ik("aHR0cHM6Ly9hcGkuY2xvdWRmbGFyZWNsaWVudC5jb20vdjBhNDAwNS9yZWc="), {
    method: 'POST',
    headers: {
      'Content-Type': ik("YXBwbGljYXRpb24vanNvbg=="),
      'User-Agent': 'okhttp/4.9.0',
      'CF-Client-Version': 'a-6.30-2935'
    },
    body: JSON.stringify(t5t)
  });
  if (t5r.status !== 200) throw new Error('HTTP ' + t5r.status + ' ' + (await t5r.text()).slice(0, 200));
  const t5n = await t5r.json();
  const t5i = t5n.config && t5n.config.peers && t5n.config.peers[0] || ({});
  const t5o = {
    registered: true,
    privateKey: t5e,
    publicKey: t49,
    reserved: t5n.config && t5n.config.client_id,
    reservedDec: t42(t5n.config && t5n.config.client_id),
    addressV4: '172.16.0.2/32',
    addressV6: (t5n.config && t5n.config.interface && t5n.config.interface.addresses && t5n.config.interface.addresses.v6 || '') + '/128',
    peerPublicKey: t5i.public_key || '',
    endpoint: t5i.endpoint && t5i.endpoint.host || ik("ZW5nYWdlLmNsb3VkZmxhcmVjbGllbnQuY29tOjI0MDg="),
    mtu: 1280,
    deviceId: t5n.id || '',
    accountId: t5n.account && t5n.account.id || '',
    license: t5n.account && t5n.account.license || '',
    warpPlus: false,
    token: t5n.token || '',
    createdAt: new Date().toISOString()
  };
  if (env.KV && typeof env.KV.put === 'function') await env.KV.put(t48, JSON.stringify(t5o, null, 2));
  return t5o;
}
async function t5s(env, t5a, t5c = 'warp-account.json') {
  const t5l = env.KV && typeof env.KV.get === 'function' ? await env.KV.get(t5c) : null;
  if (!t5l) throw new Error('no WARP account registered yet');
  const t5d = JSON.parse(t5l);
  if (!t5d.deviceId || !t5d.token) throw new Error(ik("YWNjb3VudCBtaXNzaW5nIGRldmljZSBpZCBvciB0b2tlbg=="));
  const t5w = String(t5a || '').trim();
  if (!t5w) throw new Error('empty license');
  const t5f = await fetch(`https://api.cloudflareclient.com/v0a4005/reg/${t5d.deviceId}/account`, {
    method: 'PUT',
    headers: {
      'Content-Type': ik("YXBwbGljYXRpb24vanNvbg=="),
      'User-Agent': 'okhttp/4.9.0',
      'CF-Client-Version': 'a-6.30-2935',
      'Authorization': 'Bearer ' + t5d.token
    },
    body: JSON.stringify({
      license: t5w
    })
  });
  if (t5f.status !== 200) throw new Error('HTTP ' + t5f.status + ' ' + (await t5f.text()).slice(0, 200));
  const t5h = await t5f.json();
  t5d.license = t5h.account && t5h.account.license || t5d.license;
  t5d.warpPlus = !!(t5d.license && t5d.license === t5w);
  if (env.KV && typeof env.KV.put === 'function') await env.KV.put(t5c, JSON.stringify(t5d, null, 2));
  return t5d;
}
function t5p(t5g) {
  if (!t5g) return {
    registered: false
  };
  const {token: t5m, ...t5y} = t5g;
  return {
    ...t5y,
    registered: true,
    hasToken: !!t5m
  };
}
function t5x(t5v) {
  const t5b = String(t5v || 'engage.cloudflareclient.com:2408');
  const t5k = t5b.lastIndexOf(':');
  return t5k > 0 ? {
    server: t5b.slice(0, t5k),
    port: Number(t5b.slice(t5k + 1)) || 2408
  } : {
    server: t5b,
    port: 2408
  };
}
function t50(t51, t52, t53, t54, t55) {
  const t56 = t55 ? ik("MTYyLjE1OS4xOTIuMToyNDA4") : t54 || t51.endpoint;
  const {server: t57, port: t58} = t5x(t56);
  const t59 = {
    type: 'wireguard',
    tag: t52,
    server: t57,
    server_port: t58,
    local_address: [t51.addressV4 || '172.16.0.2/32', t51.addressV6].filter(Boolean),
    private_key: t51.privateKey,
    peer_public_key: t51.peerPublicKey,
    reserved: Array.isArray(t51.reservedDec) ? t51.reservedDec : [0, 0, 0],
    mtu: t51.mtu || 1280
  };
  if (t53) t59.detour = t53;
  return t59;
}
function t6e(t6t, t6r = {}, t6n = null, t6i = null) {
  const t6o = t6r?.UUID || null;
  const t6s = Boolean(t6r?.ECH);
  const t6a = Array.isArray(t6r?.HOSTS) ? [...t6r.HOSTS] : [];
  const t6c = t6r?.ECHConfig?.SNI || null;
  const t6l = t6r?.ECHConfig?.DNS;
  const t6d = Boolean(t6o && t6s);
  const t6w = typeof t6r?.gRPCUserAgent === 'string' && t6r.gRPCUserAgent.trim() ? t6r.gRPCUserAgent.trim() : null;
  const t6f = t6r?.传输协议 === "grpc" && Boolean(t6w);
  const t6h = t6w ? JSON.stringify(t6w) : null;
  let t6p = t6t.replace(/mode:\s*Rule\b/g, 'mode: rule');
  const t6g = `dns:
  enable: true
  default-nameserver:
    - 223.5.5.5
    - 119.29.29.29
    - 114.114.114.114
  use-hosts: true
  nameserver:
    - https://sm2.doh.pub/dns-query
    - https://dns.alidns.com/dns-query
  fallback:
    - 8.8.4.4
    - 208.67.220.220
  fallback-filter:
    geoip: true
    geoip-code: CN
    ipcidr:
      - 240.0.0.0/4
      - 127.0.0.1/32
      - 0.0.0.0/32
    domain:
      - '+.google.com'
      - '+.facebook.com'
      - '+.youtube.com'
`;
  const t6m = t6y => t6y.replace(/grpc-opts:\s*\{([\s\S]*?)\}/i, (t6x, t6v) => {
    if ((/grpc-user-agent\s*:/i).test(t6v)) return t6x;
    let t6b = t6v.trim();
    if (t6b.endsWith(',')) t6b = t6b.slice(0, -1).trim();
    const t6k = t6b ? `${t6b}, grpc-user-agent: ${t6h}` : `grpc-user-agent: ${t6h}`;
    return `grpc-opts: {${t6k}}`;
  });
  const t60 = t61 => (/(?:^|[,{])\s*network:\s*(?:"grpc"|'grpc'|grpc)(?=\s*(?:[,}\n#]|$))/mi).test(t61);
  const t62 = t63 => t63.match(/type:\s*(\w+)/)?.[1] || 'vl' + 'ess';
  const t64 = (t65, t66) => {
    const t67 = t62(t65) === 'trojan' ? 'password' : 'uuid';
    const t68 = new RegExp(`${t67}:\\s*${t66 ? '([^,}\\n]+)' : '([^\\n]+)'}`);
    return t65.match(t68)?.[1]?.trim() || null;
  };
  const t69 = (t7e, t7t) => {
    if ((/^\s{2}nameserver-policy:\s*(?:\n|$)/m).test(t7e)) {
      return t7e.replace(/^(\s{2}nameserver-policy:\s*\n)/m, `$1${t7t}\n`);
    }
    const t7r = t7e.split('\n');
    let t7n = -1;
    let t7i = false;
    for (let t7o = 0; t7o < t7r.length; t7o++) {
      const t7s = t7r[t7o];
      if ((/^dns:\s*$/).test(t7s)) {
        t7i = true;
        continue;
      }
      if (t7i && (/^[a-zA-Z]/).test(t7s)) {
        t7n = t7o;
        break;
      }
    }
    const t7a = `  nameserver-policy:\n${t7t}`;
    if (t7n !== -1) t7r.splice(t7n, 0, t7a); else t7r.push(t7a);
    return t7r.join('\n');
  };
  const t7c = t7l => {
    if (!t60(t7l) || (/grpc-user-agent\s*:/i).test(t7l)) return t7l;
    if ((/grpc-opts:\s*\{/i).test(t7l)) return t6m(t7l);
    return t7l.replace(/\}(\s*)$/, `, grpc-opts: {grpc-user-agent: ${t6h}}}$1`);
  };
  const t7d = (t7w, t7f) => {
    const t7h = (' ').repeat(t7f);
    let t7p = -1;
    for (let t7g = 0; t7g < t7w.length; t7g++) {
      const t7m = t7w[t7g];
      if (!t7m.trim()) continue;
      const t7y = t7m.search(/\S/);
      if (t7y !== t7f) continue;
      if ((/^\s*grpc-opts:\s*(?:#.*)?$/).test(t7m) || (/^\s*grpc-opts:\s*\{.*\}\s*(?:#.*)?$/).test(t7m)) {
        t7p = t7g;
        break;
      }
    }
    if (t7p === -1) {
      let t7x = -1;
      for (let t7v = t7w.length - 1; t7v >= 0; t7v--) {
        if (t7w[t7v].trim()) {
          t7x = t7v;
          break;
        }
      }
      if (t7x >= 0) t7w.splice(t7x + 1, 0, `${t7h}grpc-opts:`, `${t7h}  grpc-user-agent: ${t6h}`);
      return t7w;
    }
    const t7b = t7w[t7p];
    if ((/^\s*grpc-opts:\s*\{.*\}\s*(?:#.*)?$/).test(t7b)) {
      if (!(/grpc-user-agent\s*:/i).test(t7b)) t7w[t7p] = t6m(t7b);
      return t7w;
    }
    let t7k = t7w.length;
    let t70 = t7f + 2;
    let t71 = false;
    for (let t72 = t7p + 1; t72 < t7w.length; t72++) {
      const t73 = t7w[t72];
      const t74 = t73.trim();
      if (!t74) continue;
      const t75 = t73.search(/\S/);
      if (t75 <= t7f) {
        t7k = t72;
        break;
      }
      if (t75 > t7f && t70 === t7f + 2) t70 = t75;
      if ((/^grpc-user-agent\s*:/).test(t74)) {
        t71 = true;
        break;
      }
    }
    if (!t71) t7w.splice(t7k, 0, `${(' ').repeat(t70)}grpc-user-agent: ${t6h}`);
    return t7w;
  };
  const t76 = (t77, t78) => {
    let t79 = -1;
    for (let t8e = t77.length - 1; t8e >= 0; t8e--) {
      if (t77[t8e].trim()) {
        t79 = t8e;
        break;
      }
    }
    if (t79 < 0) return t77;
    const t8t = (' ').repeat(t78);
    const t8r = [`${t8t}ech-opts:`, `${t8t}  enable: true`];
    if (t6c) t8r.push(`${t8t}  query-server-name: ${t6c}`);
    t77.splice(t79 + 1, 0, ...t8r);
    return t77;
  };
  if (!(/^dns:\s*(?:\n|$)/m).test(t6p)) t6p = t6g + t6p;
  if (t6c && !t6a.includes(t6c)) t6a.push(t6c);
  if (t6s && t6a.length > 0) {
    const t8n = t6a.map(t8i => `    "${t8i}": ${t6l ? t6l : ''}`).join('\n');
    t6p = t69(t6p, t8n);
  }
  if (t6n && t6n.enableWarp && t6i && t6i.registered && t6i.privateKey && !t6p.includes('name: "Nova-WARP"')) {
    try {
      const t8o = t6n.warpEndpoint && String(t6n.warpEndpoint).trim() || null;
      const t8s = t6n.warpAmnezia ? `\n    amnezia-wg-option: {jc: 4, jmin: 40, jmax: 70}` : '';
      const t8a = t8c => {
        const t8l = String(t8c).lastIndexOf(':');
        return t8l > 0 ? {
          s: String(t8c).slice(0, t8l),
          p: Number(String(t8c).slice(t8l + 1)) || 2408
        } : {
          s: String(t8c),
          p: 2408
        };
      };
      const t8d = (t8w, t8f, t8h, t8p) => {
        const {s: t8g, p: t8m} = t8a(t8h);
        return `  - name: "${t8f}"\n` + `    type: wireguard\n` + `    server: ${t8g}\n` + `    port: ${t8m}\n` + `    ip: ${t8w.addressV4 || '172.16.0.2/32'}\n` + (t8w.addressV6 ? `    ipv6: "${t8w.addressV6}"\n` : '') + `    private-key: "${t8w.privateKey}"\n` + `    public-key: "${t8w.peerPublicKey}"\n` + `    reserved: "${t8w.reserved || ''}"\n` + `    udp: true\n` + `    mtu: ${t8w.mtu || 1280}` + (t8p ? `\n    dialer-proxy: "${t8p}"` : '') + t8s;
      };
      let t8y, t8x;
      if (t6n.warpMode === 'wow' && t6i.wow && t6i.wow.privateKey) {
        t8y = t8d(t6i, 'Nova-WARP', t8o || t6i.endpoint, '') + '\n' + t8d(t6i.wow, 'Nova-WoW', ik("MTYyLjE1OS4xOTIuMToyNDA4"), 'Nova-WARP');
        t8x = 'Nova-WoW';
      } else {
        let t8v = '';
        if (t6n.warpMode === 'chain') {
          const t8b = t6p.match(/^proxy-groups:\s*$[\s\S]*?^\s*-\s*name:\s*["']?([^"'\n]+?)["']?\s*$/m);
          if (t8b && t8b[1]) t8v = t8b[1].trim();
        }
        t8y = t8d(t6i, 'Nova-WARP', t8o || t6i.endpoint, t8v);
        t8x = 'Nova-WARP';
      }
      if ((/^proxies:\s*$/m).test(t6p)) t6p = t6p.replace(/^proxies:\s*$/m, 'proxies:\n' + t8y); else t6p = 'proxies:\n' + t8y + '\n' + t6p;
      if ((/^\s*-\s*MATCH,[^\n]+$/m).test(t6p)) t6p = t6p.replace(/^(\s*-\s*MATCH,)[^\n]+$/m, '$1' + t8x); else t6p = t6p.replace(/^(\s*)rules:\s*$/m, '$&\n$1  - MATCH,' + t8x);
    } catch (t8k) {}
  }
  if (t6n && t6n.blockQUIC && !t6p.includes('DST-PORT,443,REJECT,udp')) {
    const t80 = '  - "DST-PORT,443,REJECT,udp"';
    if ((/^\s*-\s*MATCH,/m).test(t6p)) {
      t6p = t6p.replace(/^(\s*-\s*MATCH,)/m, t80 + '\n$1');
    } else if ((/^rules:\s*$/m).test(t6p)) {
      t6p = t6p.replace(/^rules:\s*$/m, 'rules:\n' + t80);
    }
  }
  if (!t6d && !t6f) return t6p;
  const t81 = t6p.split('\n');
  const t82 = [];
  let t83 = 0;
  while (t83 < t81.length) {
    const t84 = t81[t83];
    const t85 = t84.trim();
    if (t85.startsWith('- {')) {
      let t86 = t84;
      let t87 = (t84.match(/\{/g) || []).length - (t84.match(/\}/g) || []).length;
      while (t87 > 0 && t83 + 1 < t81.length) {
        t83++;
        t86 += '\n' + t81[t83];
        t87 += (t81[t83].match(/\{/g) || []).length - (t81[t83].match(/\}/g) || []).length;
      }
      if (t6f) t86 = t7c(t86);
      if (t6d && t64(t86, true) === t6o.trim()) {
        t86 = t86.replace(/\}(\s*)$/, `, ech-opts: {enable: true${t6c ? `, query-server-name: ${t6c}` : ''}}}$1`);
      }
      t82.push(t86);
      t83++;
    } else if (t85.startsWith('- name:')) {
      let t88 = [t84];
      let t89 = t84.search(/\S/);
      let t9e = t89 + 2;
      t83++;
      while (t83 < t81.length) {
        const t9t = t81[t83];
        const t9r = t9t.trim();
        if (!t9r) {
          t88.push(t9t);
          t83++;
          break;
        }
        const t9n = t9t.search(/\S/);
        if (t9n <= t89 && t9r.startsWith('- ')) {
          break;
        }
        if (t9n < t89 && t9r) {
          break;
        }
        t88.push(t9t);
        t83++;
      }
      let t9i = t88.join('\n');
      if (t6f && t60(t9i)) {
        t88 = t7d(t88, t9e);
        t9i = t88.join('\n');
      }
      if (t6d && t64(t9i, false) === t6o.trim()) t88 = t76(t88, t9e);
      t82.push(...t88);
    } else {
      t82.push(t84);
      t83++;
    }
  }
  return t82.join('\n');
}
async function t9o(t9s, t9a = {}, t9c = null, t9l = null) {
  const t9d = t9a?.UUID || null;
  const t9w = t9a?.Fingerprint || "chrome";
  const t9f = Boolean(t9a?.ECH);
  const t9h = t9a?.ECHConfig?.SNI || "cloudflare-ech.com";
  const t9p = t9s.replace('1.1.1.1', '8.8.8.8').replace('1.0.0.1', '8.8.4.4');
  try {
    const t9g = JSON.parse(t9p);
    const t9m = t9y => t9y === undefined || t9y === null ? [] : Array.isArray(t9y) ? t9y : [t9y];
    const t9x = () => t9g.route = t9g.route && typeof t9g.route === 'object' ? t9g.route : {};
    const t9v = t9b => t9b && typeof t9b === 'object' && !Array.isArray(t9b) && typeof t9b.server === 'string' ? t9b.server : null;
    const t9k = (t90, t91) => {
      if (!t91 || typeof t91 !== 'string') return null;
      const t92 = t9x(), t93 = `${t90}-${t91}`, t94 = Array.isArray(t92.rule_set) ? t92.rule_set : t9m(t92.rule_set);
      if (!t94.some(t95 => t95?.tag === t93)) {
        const t96 = t90 === 'geoip' ? t92.geoip : t92.geosite;
        t94.push({
          tag: t93,
          type: 'remote',
          format: 'binary',
          url: `https://raw.githubusercontent.com/SagerNet/sing-${t90}/rule-set/${t93}.srs`,
          ...t96?.download_detour ? {
            download_detour: t96.download_detour
          } : {}
        });
        t9g.experimental = t9g.experimental && typeof t9g.experimental === 'object' ? t9g.experimental : {};
        t9g.experimental.cache_file = t9g.experimental.cache_file && typeof t9g.experimental.cache_file === 'object' ? t9g.experimental.cache_file : {};
        t9g.experimental.cache_file.enabled ??= true;
      }
      t92.rule_set = t94;
      return t93;
    };
    const t97 = t98 => {
      if (!t98 || typeof t98 !== 'object' || Array.isArray(t98)) return t98;
      if (t98.type === 'logical' && Array.isArray(t98.rules)) {
        t98.rules = t98.rules.map(t97);
        return t98;
      }
      const t99 = [];
      for (const ree of t9m(t98.geoip)) {
        if (typeof ree !== 'string') continue;
        if (ree.toLowerCase() === 'private') t98.ip_is_private = true; else t99.push(t9k('geoip', ree));
      }
      for (const ret of t9m(t98.source_geoip)) {
        if (typeof ret !== 'string') continue;
        t99.push(t9k('geoip', ret));
        t98.rule_set_ip_cidr_match_source = true;
      }
      for (const rer of t9m(t98.geosite)) if (typeof rer === 'string') t99.push(t9k('geosite', rer));
      if (t99.length) t98.rule_set = [...new Set([...t9m(t98.rule_set), ...t99].filter(Boolean))];
      delete t98.geoip;
      delete t98.source_geoip;
      delete t98.geosite;
      return t98;
    };
    const ren = (rei, reo) => {
      rei = t97(rei);
      if (!rei || typeof rei !== 'object' || Array.isArray(rei)) return rei;
      if (rei.type === 'logical' && Array.isArray(rei.rules)) {
        rei.rules = rei.rules.map(res => ren(res, reo));
        return rei;
      }
      const rea = t9v(rei);
      if (rea && reo.has(rea)) {
        for (const rec of ['server', 'strategy', 'disable_cache', 'rewrite_ttl', 'client_subnet', 'timeout']) delete rei[rec];
        rei.action = 'predefined';
        rei.rcode = reo.get(rea);
      } else if (rea && !rei.action) rei.action = 'route';
      return rei;
    };
    if (Array.isArray(t9g.inbounds)) {
      for (const rel of t9g.inbounds) {
        if (!rel || typeof rel !== 'object' || rel.type !== 'tun') continue;
        for (const red of [{
          targetKey: 'address',
          sourceKeys: ['inet4_address', 'inet6_address']
        }, {
          targetKey: 'route_address',
          sourceKeys: [ik("aW5ldDRfcm91dGVfYWRkcmVzcw=="), 'inet6_route_address']
        }, {
          targetKey: 'route_exclude_address',
          sourceKeys: [ik("aW5ldDRfcm91dGVfZXhjbHVkZV9hZGRyZXNz"), 'inet6_route_exclude_address']
        }]) {
          const rew = t9m(rel[red.targetKey]);
          for (const ref of red.sourceKeys) rew.push(...t9m(rel[ref]));
          if (rew.length) rel[red.targetKey] = [...new Set(rew)];
          for (const reh of red.sourceKeys) delete rel[reh];
        }
        if (rel.tag) {
          const rep = [];
          if (rel.domain_strategy) rep.push({
            inbound: rel.tag,
            action: 'resolve',
            strategy: rel.domain_strategy
          });
          if (rel.sniff) {
            const reg = {
              inbound: rel.tag,
              action: 'sniff'
            };
            if (rel.sniff_timeout) reg.timeout = rel.sniff_timeout;
            rep.push(reg);
          }
          if (rep.length) {
            const rem = t9x();
            rem.rules = [...rep, ...t9m(rem.rules)];
          }
        }
        delete rel.sniff;
        delete rel.sniff_timeout;
        delete rel.domain_strategy;
      }
    }
    if (t9g?.route && typeof t9g.route === 'object' && Array.isArray(t9g.route.rules)) {
      const rey = rex => {
        rex = t97(rex);
        if (rex?.type === 'logical' && Array.isArray(rex.rules)) rex.rules = rex.rules.map(rey); else if (rex && typeof rex === 'object' && !Array.isArray(rex) && rex.outbound && !rex.action) rex.action = 'route';
        return rex;
      };
      t9g.route.rules = t9g.route.rules.map(rey);
    }
    if (t9c && t9c.blockQUIC && t9g?.route && typeof t9g.route === 'object' && Array.isArray(t9g.route.rules)) {
      if (!t9g.route.rules.some(rev => rev.outbound === 'block' && rev.network === 'udp' && Array.isArray(rev.port) && rev.port.includes(443))) {
        t9g.route.rules.unshift({
          outbound: 'block',
          network: 'udp',
          port: [443]
        });
      }
    }
    const reb = t9g?.dns;
    if (reb && typeof reb === 'object') {
      const rek = reb.fakeip && typeof reb.fakeip === 'object' ? reb.fakeip : null;
      const re0 = new Map();
      const re1 = {
        'tcp:': 'tcp',
        'udp:': 'udp',
        'tls:': 'tls',
        'quic:': 'quic',
        'https:': 'https',
        'h3:': 'h3'
      };
      const re2 = {
        success: 'NOERROR',
        format_error: 'FORMERR',
        server_failure: 'SERVFAIL',
        name_error: 'NXDOMAIN',
        not_implemented: 'NOTIMP',
        refused: 'REFUSED'
      };
      let re3 = false;
      if (Array.isArray(reb.servers)) {
        const re4 = [];
        for (const re5 of reb.servers) {
          if (!re5 || typeof re5 !== 'object' || Array.isArray(re5)) {
            re4.push(re5);
            continue;
          }
          const re6 = {
            ...re5
          };
          let re7 = null, re8 = '', re9 = typeof re6.address === 'string' ? re6.address.trim() : '';
          if (re9) {
            const rte = re9.toLowerCase();
            if (rte === 'fakeip') re7 = {
              type: 'fakeip'
            }; else if (rte === 'local') re7 = {
              type: 'local'
            }; else if (rte.startsWith('rcode://')) {
              re7 = {
                type: 'rcode'
              };
              re8 = re9.slice(('rcode://').length).toLowerCase();
            } else if (rte.startsWith('dhcp://')) {
              const rtt = re9.slice(('dhcp://').length);
              re7 = rtt && rtt.toLowerCase() !== 'auto' ? {
                type: 'dhcp',
                interface: rtt
              } : {
                type: 'dhcp'
              };
            } else {
              try {
                const rtr = new URL(re9);
                const rtn = re1[rtr.protocol.toLowerCase()];
                if (rtn) {
                  const rti = rtr.hostname?.startsWith('[') && rtr.hostname.endsWith(']') ? rtr.hostname.slice(1, -1) : rtr.hostname;
                  re7 = {
                    type: rtn,
                    server: rti || rtr.host || re9,
                    ...rtr.port ? {
                      server_port: Number(rtr.port)
                    } : {},
                    ...(rtn === 'https' || rtn === 'h3') && rtr.pathname && rtr.pathname !== '/dns-query' ? {
                      path: rtr.pathname
                    } : {}
                  };
                }
              } catch (rto) {}
              if (!re7) re7 = {
                type: 'udp',
                server: re9
              };
            }
          }
          if (re7?.type === 'rcode') {
            const rts = re2[re8] || 'NOERROR';
            if (typeof re6.tag === 'string' && re6.tag) {
              re0.set(re6.tag, rts);
              re0.set(re6.tag.startsWith('dns_') ? re6.tag.slice(4) : `dns_${re6.tag}`, rts);
            }
            continue;
          }
          if (re7) {
            delete re6.address;
            Object.assign(re6, re7);
          }
          if (re6.address_resolver !== undefined && re6.domain_resolver === undefined) re6.domain_resolver = re6.address_resolver;
          if (re6.address_strategy !== undefined && re6.domain_strategy === undefined) re6.domain_strategy = re6.address_strategy;
          delete re6.address_resolver;
          delete re6.address_strategy;
          if (re6.detour === 'DIRECT') delete re6.detour;
          if (re6.type === 'fakeip') {
            re3 = true;
            if (rek) {
              for (const rta of ['inet4_range', 'inet6_range']) {
                if (rek[rta] !== undefined && re6[rta] === undefined) re6[rta] = rek[rta];
              }
            }
          }
          re4.push(re6);
        }
        reb.servers = re4;
      }
      if (rek && !re3 && rek.enabled !== false) {
        const rtc = {
          type: 'fakeip',
          tag: 'fakeip'
        };
        for (const rtl of Array.isArray(reb.rules) ? reb.rules : []) {
          const rtd = t9v(rtl);
          if (rtd && rtd.toLowerCase().includes('fakeip')) {
            rtc.tag = rtd;
            break;
          }
        }
        for (const rtw of ['inet4_range', 'inet6_range']) {
          if (rek[rtw] !== undefined) rtc[rtw] = rek[rtw];
        }
        if (Array.isArray(reb.servers)) reb.servers.push(rtc); else reb.servers = [rtc];
      }
      if (Array.isArray(reb.rules)) {
        const rtf = [];
        for (const rth of reb.rules) {
          const rtp = t9v(rth);
          const rtg = t9m(rth?.outbound);
          const rtm = new Set(['outbound', 'server', 'action', 'strategy', 'disable_cache', 'rewrite_ttl', 'client_subnet', 'timeout']);
          const rty = rth && typeof rth === 'object' && !Array.isArray(rth) && rth.type !== 'logical' && rtp && rtg.includes('any') && Object.keys(rth).every(rtx => rtm.has(rtx));
          if (rty) {
            const rtv = t9x();
            if (rtv.default_domain_resolver === undefined) {
              const rtb = {
                server: rtp
              };
              for (const rtk of ['strategy', 'disable_cache', 'rewrite_ttl', 'client_subnet', 'timeout']) {
                if (rth[rtk] !== undefined) rtb[rtk] = rth[rtk];
              }
              rtv.default_domain_resolver = Object.keys(rtb).length === 1 ? rtb.server : rtb;
            }
            continue;
          }
          rtf.push(ren(rth, re0));
        }
        reb.rules = rtf;
      }
      delete reb.fakeip;
      delete reb.independent_cache;
    }
    if (t9g?.route && typeof t9g.route === 'object') {
      delete t9g.route.geoip;
      delete t9g.route.geosite;
    }
    if (t9g?.ntp?.detour === 'DIRECT') delete t9g.ntp.detour;
    if (Array.isArray(t9g.outbounds)) {
      const rt0 = new Set(t9g.outbounds.map(rt1 => rt1?.tag).filter(Boolean));
      const rt2 = rt3 => rt3 === 'REJECT' || rt3 && typeof rt3 === 'object' && (Array.isArray(rt3) ? rt3.some(rt2) : Object.values(rt3).some(rt2));
      if (!rt0.has('REJECT') && rt2({
        outbounds: t9g.outbounds,
        route: t9g.route
      })) t9g.outbounds.push({
        type: 'block',
        tag: 'REJECT'
      });
    }
    if (t9d) {
      t9g.outbounds?.forEach(rt4 => {
        if (rt4.uuid && rt4.uuid === t9d || rt4.password && rt4.password === t9d) {
          if (!rt4.tls) {
            rt4.tls = {
              enabled: true
            };
          }
          if (t9w) {
            rt4.tls.utls = {
              enabled: true,
              fingerprint: t9w
            };
          }
          if (t9f) {
            rt4.tls.ech = {
              enabled: true,
              query_server_name: t9h
            };
          }
        }
      });
    }
    if (t9c && t9c.enableWarp && t9l && t9l.registered && t9l.privateKey) {
      t9g.outbounds = Array.isArray(t9g.outbounds) ? t9g.outbounds : [];
      const rt5 = 'Nova-WARP';
      const rt6 = t9c.warpEndpoint && String(t9c.warpEndpoint).trim() || null;
      if (!t9g.outbounds.some(rt7 => rt7 && rt7.tag === rt5)) {
        const rt8 = () => t9g.route = t9g.route && typeof t9g.route === 'object' ? t9g.route : {};
        if (t9c.warpMode === 'wow' && t9l.wow && t9l.wow.privateKey) {
          t9g.outbounds.push(t50(t9l, rt5, null, rt6, false));
          t9g.outbounds.push(t50(t9l.wow, 'Nova-WoW', rt5, null, true));
          rt8().final = 'Nova-WoW';
        } else {
          let rt9 = null;
          if (t9c.warpMode === 'chain') {
            const rre = t9g.outbounds.find(rrt => rrt && (rrt.type === 'selector' || rrt.type === 'urltest'));
            rt9 = rre ? rre.tag : null;
          }
          t9g.outbounds.push(t50(t9l, rt5, rt9, rt6, false));
          rt8().final = rt5;
        }
      }
    }
    return JSON.stringify(t9g, null, 2);
  } catch (rrr) {
    console.error("Singbox热补丁执行失败:", rrr);
    return JSON.stringify(JSON.parse(t9p), null, 2);
  }
}
function rrn(rri, rro, rrs) {
  const rra = rri.includes('\r\n') ? rri.split('\r\n') : rri.split('\n');
  const rrc = rrs.随机路径 ? rnk(rrs.完整节点路径) : rrs.完整节点路径;
  let rrl = "";
  for (let rrd of rra) {
    if (rrd.includes('= tro' + 'jan,') && !rrd.includes('ws=true') && !rrd.includes('ws-path=')) {
      const rrw = rrd.split("sni=")[1].split(",")[0];
      const rrf = `sni=${rrw}, skip-cert-verify=${rrs.跳过证书验证}`;
      const rrh = `sni=${rrw}, skip-cert-verify=${rrs.跳过证书验证}, ws=true, ws-path=${rrc.replace(/,/g, '%2C')}, ws-headers=Host:"${rrw}"`;
      rrl += rrd.replace(new RegExp(rrf, 'g'), rrh).replace("[", "").replace("]", "") + '\n';
    } else {
      rrl += rrd + '\n';
    }
  }
  rrl = `#!MANAGED-CONFIG ${rro} interval=${rrs.优选订阅生成.SUBUpdateTime * 60 * 60} strict=false` + rrl.substring(rrl.indexOf('\n'));
  return rrl;
}
async function rrp(env, rrg, rrm, rry = "Get_SUB", rrx, rrv = true) {
  try {
    const rrb = new Date();
    const rrk = {
      TYPE: rry,
      IP: rrm,
      ASN: `AS${rrg.cf.asn || '0'} ${rrg.cf.asOrganization || 'Unknown'}`,
      CC: `${rrg.cf.country || 'N/A'} ${rrg.cf.city || 'N/A'}`,
      URL: rrg.url,
      UA: rrg.headers.get('User-Agent') || 'Unknown',
      TIME: rrb.getTime()
    };
    if (rrx.TG.启用) {
      try {
        const rr0 = await env.KV.get('tg.json');
        const rr1 = JSON.parse(rr0);
        if (rr1?.BotToken && rr1?.ChatID) {
          const rr2 = new Date(rrk.TIME).toLocaleString('zh-CN', {
            timeZone: 'Asia/Shanghai'
          });
          const rr3 = new URL(rrk.URL);
          const rr4 = `<b>#${rrx.优选订阅生成.SUBNAME} 日志通知</b>\n\n` + `📌 <b>类型：</b>#${rrk.TYPE}\n` + `🌐 <b>IP：</b><code>${rrk.IP}</code>\n` + `📍 <b>位置：</b>${rrk.CC}\n` + `🏢 <b>ASN：</b>${rrk.ASN}\n` + `🔗 <b>域名：</b><code>${rr3.host}</code>\n` + `🔍 <b>路径：</b><code>${rr3.pathname + rr3.search}</code>\n` + `🤖 <b>UA：</b><code>${rrk.UA}</code>\n` + `📅 <b>时间：</b>${rr2}\n` + `${rrx.CF.Usage.success ? `📊 <b>请求用量：</b>${rrx.CF.Usage.total}/${rrx.CF.Usage.max} <b>${(rrx.CF.Usage.total / rrx.CF.Usage.max * 100).toFixed(2)}%</b>\n` : ''}`;
          await fetch(`https://api.telegram.org/bot${rr1.BotToken}/sendMessage?chat_id=${rr1.ChatID}&parse_mode=HTML&text=${encodeURIComponent(rr4)}`, {
            method: 'GET',
            headers: {
              'Accept': 'text/html,application/xhtml+xml,application/xml;',
              'Accept-Encoding': 'gzip, deflate, br',
              'User-Agent': rrk.UA || 'Unknown'
            }
          });
        }
      } catch (rr5) {
        console.error(`读取tg.json出错: ${rr5.message}`);
      }
    }
    rrv = ['1', 'true'].includes(env.OFF_LOG) ? false : rrv;
    if (!rrv) return;
    let rr6 = [];
    const rr7 = await env.KV.get('log.json'), rr8 = 4;
    if (rr7) {
      try {
        rr6 = JSON.parse(rr7);
        if (!Array.isArray(rr6)) {
          rr6 = [rrk];
        } else if (rry !== "Get_SUB") {
          const rr9 = rrb.getTime() - 30 * 60 * 1000;
          if (rr6.some(rne => rne.TYPE !== "Get_SUB" && rne.IP === rrm && rne.URL === rrg.url && rne.UA === (rrg.headers.get('User-Agent') || 'Unknown') && rne.TIME >= rr9)) return;
          rr6.push(rrk);
          while (JSON.stringify(rr6, null, 2).length > rr8 * 1024 * 1024 && rr6.length > 0) rr6.shift();
        } else {
          rr6.push(rrk);
          while (JSON.stringify(rr6, null, 2).length > rr8 * 1024 * 1024 && rr6.length > 0) rr6.shift();
        }
      } catch (rnt) {
        rr6 = [rrk];
      }
    } else {
      rr6 = [rrk];
    }
    await env.KV.put('log.json', JSON.stringify(rr6, null, 2));
  } catch (rnr) {
    console.error(`日志记录失败: ${rnr.message}`);
  }
}
function rnn(rni, rno = 3, rns = 2) {
  if (!rni || typeof rni !== 'string') return rni;
  if (rni.length <= rno + rns) return rni;
  const rna = rni.slice(0, rno);
  const rnc = rni.slice(-rns);
  const rnl = rni.length - rno - rns;
  return `${rna}${('*').repeat(rnl)}${rnc}`;
}
async function rnd(rnw) {
  const rnf = new TextEncoder();
  const rnh = await crypto.subtle.digest('MD5', rnf.encode(rnw));
  const rnp = Array.from(new Uint8Array(rnh));
  const rng = rnp.map(rnm => rnm.toString(16).padStart(2, '0')).join('');
  const rny = await crypto.subtle.digest('MD5', rnf.encode(rng.slice(7, 27)));
  const rnx = Array.from(new Uint8Array(rny));
  const rnv = rnx.map(rnb => rnb.toString(16).padStart(2, '0')).join('');
  return rnv.toLowerCase();
}
function rnk(rn0 = "/") {
  const rn1 = ["about", "account", "acg", "act", "activity", "ad", "ads", "ajax", "album", "albums", "anime", "api", "app", "apps", "archive", "archives", "article", "articles", "ask", "auth", "avatar", "bbs", "bd", "blog", "blogs", "book", "books", "bt", "buy", "cart", "category", "categories", "cb", "channel", "channels", "chat", "china", "city", "class", "classify", "clip", "clips", "club", "cn", "code", "collect", "collection", "comic", "comics", "community", "company", "config", "contact", "content", "course", "courses", "cp", "data", "detail", "details", "dh", "directory", "discount", "discuss", "dl", "dload", "doc", "docs", "document", "documents", "doujin", "download", "downloads", "drama", "edu", "en", "ep", "episode", "episodes", "event", "events", "f", "faq", "favorite", "favourites", "favs", "feedback", "file", "files", "film", "films", "forum", "forums", "friend", "friends", "game", "games", "gif", "go", "go.html", "go.php", "group", "groups", "help", "home", "hot", "htm", "html", "image", "images", "img", "index", "info", "intro", "item", "items", "ja", "jp", "jump", "jump.html", "jump.php", "jumping", "knowledge", "lang", "lesson", "lessons", "lib", "library", "link", "links", "list", "live", "lives", "m", "mag", "magnet", "mall", "manhua", "map", "member", "members", "message", "messages", "mobile", "movie", "movies", "music", "my", "new", "news", "note", "novel", "novels", "online", "order", "out", "out.html", "out.php", "outbound", "p", "page", "pages", "pay", "payment", "pdf", "photo", "photos", "pic", "pics", "picture", "pictures", "play", "player", "playlist", "post", "posts", "product", "products", "program", "programs", "project", "qa", "question", "rank", "ranking", "read", "readme", "redirect", "redirect.html", "redirect.php", "reg", "register", "res", "resource", "retrieve", "sale", "search", "season", "seasons", "section", "seller", "series", "service", "services", "setting", "settings", "share", "shop", "show", "shows", "site", "soft", "sort", "source", "special", "star", "stars", "static", "stock", "store", "stream", "streaming", "streams", "student", "study", "tag", "tags", "task", "teacher", "team", "tech", "temp", "test", "thread", "tool", "tools", "topic", "topics", "torrent", "trade", "travel", "tv", "txt", "type", "u", "upload", "uploads", "url", "urls", "user", "users", "v", "version", "videos", "view", "vip", "vod", "watch", "web", "wenku", "wiki", "work", "www", "zh", "zh-cn", "zh-tw", "zip"];
  const rn2 = Math.floor(Math.random() * 3 + 1);
  const rn3 = rn1.sort(() => 0.5 - Math.random()).slice(0, rn2).join('/');
  if (rn0 === "/") return `/${rn3}`; else return `/${rn3 + rn0.replace('/?', '?')}`;
}
function rn4(rn5) {
  if (typeof rn5 !== 'string' || !rn5.includes('*')) return rn5;
  const rn6 = 'abcdefghijklmnopqrstuvwxyz0123456789';
  return rn5.replace(/\*/g, () => {
    let rn7 = '';
    for (let rn8 = 0; rn8 < Math.floor(Math.random() * 14) + 3; rn8++) rn7 += rn6[Math.floor(Math.random() * rn6.length)];
    return rn7;
  });
}
async function rn9(rie, rit, rir = ik("aHR0cHM6Ly9jbG91ZGZsYXJlLWRucy5jb20vZG5zLXF1ZXJ5")) {
  const rin = performance.now();
  t4m(`[DoH查询] 开始查询 ${rie} ${rit} via ${rir}`);
  try {
    const rii = {
      'A': 1,
      'NS': 2,
      'CNAME': 5,
      'MX': 15,
      'TXT': 16,
      'AAAA': 28,
      'SRV': 33,
      'HTTPS': 65
    };
    const rio = rii[rit.toUpperCase()] || 1;
    const ris = ria => {
      const ric = ria.endsWith('.') ? ria.slice(0, -1).split('.') : ria.split('.');
      const ril = [];
      for (const rid of ric) {
        const riw = new TextEncoder().encode(rid);
        ril.push(new Uint8Array([riw.length]), riw);
      }
      ril.push(new Uint8Array([0]));
      const rif = ril.reduce((rih, rip) => rih + rip.length, 0);
      const rig = new Uint8Array(rif);
      let rim = 0;
      for (const riy of ril) {
        rig.set(riy, rim);
        rim += riy.length;
      }
      return rig;
    };
    const rix = ris(rie);
    const riv = new Uint8Array(12 + rix.length + 4);
    const rib = new DataView(riv.buffer);
    rib.setUint16(0, crypto.getRandomValues(new Uint16Array(1))[0]);
    rib.setUint16(2, 0x0100);
    rib.setUint16(4, 1);
    riv.set(rix, 12);
    rib.setUint16(12 + rix.length, rio);
    rib.setUint16(12 + rix.length + 2, 1);
    t4m(`[DoH查询] 发送查询报文 ${rie} via ${rir} (type=${rio}, ${riv.length}字节)`);
    const rik = await fetch(rir, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/dns-message',
        'Accept': 'application/dns-message'
      },
      body: riv
    });
    if (!rik.ok) {
      console.warn(`[DoH查询] 请求失败 ${rie} ${rit} via ${rir} 响应代码:${rik.status}`);
      return [];
    }
    const ri0 = new Uint8Array(await rik.arrayBuffer());
    const ri1 = new DataView(ri0.buffer);
    const ri2 = ri1.getUint16(4);
    const ri3 = ri1.getUint16(6);
    t4m(`[DoH查询] 收到响应 ${rie} ${rit} via ${rir} (${ri0.length}字节, ${ri3}条应答)`);
    const ri4 = ri5 => {
      const ri6 = [];
      let ri7 = ri5, ri8 = false, ri9 = -1, roe = 128;
      while (ri7 < ri0.length && roe-- > 0) {
        const rot = ri0[ri7];
        if (rot === 0) {
          if (!ri8) ri9 = ri7 + 1;
          break;
        }
        if ((rot & 0xC0) === 0xC0) {
          if (!ri8) ri9 = ri7 + 2;
          ri7 = (rot & 0x3F) << 8 | ri0[ri7 + 1];
          ri8 = true;
          continue;
        }
        ri6.push(new TextDecoder().decode(ri0.slice(ri7 + 1, ri7 + 1 + rot)));
        ri7 += rot + 1;
      }
      if (ri9 === -1) ri9 = ri7 + 1;
      return [ri6.join('.'), ri9];
    };
    let ror = 12;
    for (let ron = 0; ron < ri2; ron++) {
      const [, roi] = ri4(ror);
      ror = roi + 4;
    }
    const roo = [];
    for (let ros = 0; ros < ri3 && ror < ri0.length; ros++) {
      const [roa, roc] = ri4(ror);
      ror = roc;
      const rol = ri1.getUint16(ror);
      ror += 2;
      ror += 2;
      const rod = ri1.getUint32(ror);
      ror += 4;
      const row = ri1.getUint16(ror);
      ror += 2;
      const rof = ri0.slice(ror, ror + row);
      ror += row;
      let roh;
      if (rol === 1 && row === 4) {
        roh = `${rof[0]}.${rof[1]}.${rof[2]}.${rof[3]}`;
      } else if (rol === 28 && row === 16) {
        const rop = [];
        for (let rog = 0; rog < 16; rog += 2) rop.push((rof[rog] << 8 | rof[rog + 1]).toString(16));
        roh = rop.join(':');
      } else if (rol === 16) {
        let rom = 0;
        const roy = [];
        while (rom < row) {
          const rox = rof[rom++];
          roy.push(new TextDecoder().decode(rof.slice(rom, rom + rox)));
          rom += rox;
        }
        roh = roy.join('');
      } else if (rol === 5) {
        const [rov] = ri4(ror - row);
        roh = rov;
      } else {
        roh = Array.from(rof).map(rob => rob.toString(16).padStart(2, '0')).join('');
      }
      roo.push({
        name: roa,
        type: rol,
        TTL: rod,
        data: roh,
        rdata: rof
      });
    }
    const rok = (performance.now() - rin).toFixed(2);
    t4m(`[DoH查询] 查询完成 ${rie} ${rit} via ${rir} ${rok}ms 共${roo.length}条结果${roo.length > 0 ? '\n' + roo.map((ro0, ro1) => `  ${ro1 + 1}. ${ro0.name} type=${ro0.type} TTL=${ro0.TTL} data=${ro0.data}`).join('\n') : ''}`);
    return roo;
  } catch (ro2) {
    const ro3 = (performance.now() - rin).toFixed(2);
    console.error(`[DoH查询] 查询失败 ${rie} ${rit} via ${rir} ${ro3}ms:`, ro2);
    return [];
  }
}
async function ro4(env, ro5, ro6, ro7 = "Mozilla/5.0", ro8 = false) {
  const ro9 = lk[0];
  const rse = ro5, rst = "https://dns.alidns.com/dns-query", rsr = "cloudflare-ech.com", rsn = '{{IP:PORT}}', rsi = performance.now(), rso = {
    TIME: new Date().toISOString(),
    HOST: rse,
    HOSTS: [ro5],
    UUID: ro6,
    PATH: "/",
    协议类型: "v" + "le" + "ss",
    传输协议: "ws",
    gRPC模式: "gun",
    gRPCUserAgent: ro7,
    跳过证书验证: false,
    启用0RTT: false,
    TLS分片: null,
    随机路径: false,
    ECH: false,
    ECHConfig: {
      DNS: rst,
      SNI: rsr
    },
    SS: {
      加密方式: "aes-128-gcm",
      TLS: true
    },
    Fingerprint: "chrome",
    优选订阅生成: {
      local: true,
      本地IP库: {
        随机IP: true,
        随机数量: 16,
        指定端口: -1
      },
      SUB: null,
      SUBNAME: "edge" + "tunnel",
      SUBUpdateTime: 3,
      TOKEN: await rnd(ro5 + ro6)
    },
    订阅转换配置: {
      SUBAPI: `https://SUBAPI.${lk[1]}ssss.net`,
      SUBCONFIG: `https://raw.githubusercontent.com/${lk[1]}/ACL4SSR/refs/heads/main/Clash/config/ACL4SSR_Online_Mini_MultiMode_CF.ini`,
      SUBEMOJI: false,
      SUBLIST: false
    },
    反代: {
      [ro9]: "auto",
      SOCKS5: {
        启用: i3,
        全局: i4,
        账号: i5,
        白名单: ob
      },
      路径模板: {
        [ro9]: "proxyip=" + rsn,
        SOCKS5: {
          全局: "socks5://" + rsn,
          标准: "socks5=" + rsn
        },
        HTTP: {
          全局: "http://" + rsn,
          标准: "http=" + rsn
        },
        HTTPS: {
          全局: "https://" + rsn,
          标准: "https=" + rsn
        },
        TURN: {
          全局: "turn://" + rsn,
          标准: "turn=" + rsn
        },
        SSTP: {
          全局: "sstp://" + rsn,
          标准: "sstp=" + rsn
        }
      }
    },
    TG: {
      启用: false,
      BotToken: null,
      ChatID: null
    },
    CF: {
      Email: null,
      GlobalAPIKey: null,
      AccountID: null,
      APIToken: null,
      UsageAPI: null,
      Usage: {
        success: false,
        pages: 0,
        workers: 0,
        total: 0,
        max: 100000
      }
    }
  };
  try {
    let rss = await o4(env);
    if (!rss || ro8 == true) {
      await o6(env, JSON.stringify(rso, null, 2));
      i1 = rso;
    } else {
      i1 = JSON.parse(rss);
    }
  } catch (rsa) {
    console.error(`读取config_JSON出错: ${rsa.message}`);
    i1 = rso;
  }
  if (!i1.订阅转换配置.SUBLIST) i1.订阅转换配置.SUBLIST = false;
  if (!i1.gRPCUserAgent) i1.gRPCUserAgent = ro7;
  i1.HOST = rse;
  if (!i1.HOSTS) i1.HOSTS = [ro5];
  if (env.HOST) i1.HOSTS = (await rdg(env.HOST)).map(rsc => rsc.toLowerCase().replace(/^https?:\/\//, '').split('/')[0].split(':')[0]);
  i1.UUID = ro6;
  if (!i1.随机路径) i1.随机路径 = false;
  if (!i1.启用0RTT) i1.启用0RTT = false;
  if (env.PATH) i1.PATH = env.PATH.startsWith('/') ? env.PATH : '/' + env.PATH; else if (!i1.PATH) i1.PATH = '/';
  if (!i1.gRPC模式) i1.gRPC模式 = 'gun';
  if (!i1.SS) i1.SS = {
    加密方式: "aes-128-gcm",
    TLS: false
  };
  if (!i1.反代.路径模板?.[ro9]) {
    i1.反代.路径模板 = {
      [ro9]: "proxyip=" + rsn,
      SOCKS5: {
        全局: "socks5://" + rsn,
        标准: "socks5=" + rsn
      },
      HTTP: {
        全局: "http://" + rsn,
        标准: "http=" + rsn
      },
      HTTPS: {
        全局: "https://" + rsn,
        标准: "https=" + rsn
      },
      TURN: {
        全局: "turn://" + rsn,
        标准: "turn=" + rsn
      },
      SSTP: {
        全局: "sstp://" + rsn,
        标准: "sstp=" + rsn
      }
    };
  }
  if (!i1.反代.路径模板.HTTPS) i1.反代.路径模板.HTTPS = {
    全局: "https://" + rsn,
    标准: "https=" + rsn
  };
  if (!i1.反代.路径模板.TURN) i1.反代.路径模板.TURN = {
    全局: "turn://" + rsn,
    标准: "turn=" + rsn
  };
  if (!i1.反代.路径模板.SSTP) i1.反代.路径模板.SSTP = {
    全局: "sstp://" + rsn,
    标准: "sstp=" + rsn
  };
  const rsl = i1.反代.路径模板[i1.反代.SOCKS5.启用?.toUpperCase()];
  let rsd = '';
  if (rsl && i1.反代.SOCKS5.账号) rsd = (i1.反代.SOCKS5.全局 ? rsl.全局 : rsl.标准).replace(rsn, i1.反代.SOCKS5.账号); else if (i1.反代[ro9] !== 'auto') rsd = i1.反代.路径模板[ro9].replace(rsn, i1.反代[ro9]);
  let rsw = '';
  if (rsd.includes('?')) {
    const [rsf, rsh] = rsd.split('?');
    rsd = rsf;
    rsw = rsh;
  }
  i1.PATH = i1.PATH.replace(rsd, '').replace('//', '/');
  const rsp = i1.PATH === '/' ? '' : i1.PATH.replace(/\/+(?=\?|$)/, '').replace(/\/+$/, '');
  const [rsg, ...rsm] = rsp.split('?');
  const rsy = rsm.length ? '?' + rsm.join('?') : '';
  const rsx = rsw ? rsy ? rsy + '&' + rsw : '?' + rsw : rsy;
  i1.完整节点路径 = (rsg || '/') + (rsg && rsd ? '/' : '') + rsd + rsx + (i1.启用0RTT ? (rsx ? '&' : '?') + 'ed=2560' : '');
  if (!i1.TLS分片 && i1.TLS分片 !== null) i1.TLS分片 = null;
  const rsv = i1.TLS分片 == 'Shadowrocket' ? `&fragment=${encodeURIComponent(ik("MSw0MC02MCwzMC01MCx0bHNoZWxsbw=="))}` : i1.TLS分片 == 'Happ' ? `&fragment=${encodeURIComponent('3,1,tlshello')}` : '';
  if (!i1.Fingerprint) i1.Fingerprint = "chrome";
  if (!i1.ECH) i1.ECH = false;
  if (!i1.ECHConfig) i1.ECHConfig = {
    DNS: rst,
    SNI: rsr
  };
  const rsb = i1.ECH ? `&ech=${encodeURIComponent((i1.ECHConfig.SNI ? i1.ECHConfig.SNI + '+' : '') + i1.ECHConfig.DNS)}` : '';
  const {type: rsk, 路径字段名: rs0, 域名字段名: rs1} = t4c(i1);
  const rs2 = t4w(i1, i1.完整节点路径);
  const rs3 = i1.协议类型 === 'mixed' ? 'vless' : i1.协议类型;
  i1.LINK = rs3 === 'ss' ? `${rs3}://${btoa(i1.SS.加密方式 + ':' + ro6)}@${rse}:${i1.SS.TLS ? '443' : '80'}?plugin=v2${encodeURIComponent(`ray-plugin;mode=websocket;host=${rse};path=${(i1.完整节点路径.includes('?') ? i1.完整节点路径.replace('?', '?enc=' + i1.SS.加密方式 + '&') : i1.完整节点路径 + '?enc=' + i1.SS.加密方式) + (i1.SS.TLS ? ';tls' : '')};mux=0`) + rsb}#${encodeURIComponent(i1.优选订阅生成.SUBNAME)}` : i1.enableTLS === false ? `${rs3}://${ro6}@${rse}:80?security=none&type=${rsk}&${rs1}=${rse}&${rs0}=${encodeURIComponent(rs2)}&encryption=none#${encodeURIComponent(i1.优选订阅生成.SUBNAME)}` : `${rs3}://${ro6}@${rse}:443?security=tls&type=${rsk + rsb}&${rs1}=${rse}&fp=${i1.Fingerprint}&sni=${rse}&${rs0}=${encodeURIComponent(rs2) + rsv}&encryption=none${i1.skipCertVerify ? '&insecure=1&allowInsecure=1' : ''}#${encodeURIComponent(i1.优选订阅生成.SUBNAME)}`;
  i1.优选订阅生成.TOKEN = await rnd(ro5 + ro6);
  const rs4 = {
    BotToken: null,
    ChatID: null
  };
  i1.TG = {
    启用: i1.TG.启用 ? i1.TG.启用 : false,
    ...rs4
  };
  try {
    const rs5 = await env.KV.get('tg.json');
    if (!rs5) {
      await env.KV.put('tg.json', JSON.stringify(rs4, null, 2));
    } else {
      const rs6 = JSON.parse(rs5);
      i1.TG.ChatID = rs6.ChatID ? rs6.ChatID : null;
      i1.TG.BotToken = rs6.BotToken ? rnn(rs6.BotToken) : null;
    }
  } catch (rs7) {
    console.error(`读取tg.json出错: ${rs7.message}`);
  }
  const rs8 = {
    Email: null,
    GlobalAPIKey: null,
    AccountID: null,
    APIToken: null,
    UsageAPI: null
  };
  i1.CF = {
    ...rs8,
    Usage: {
      success: false,
      pages: 0,
      workers: 0,
      total: 0,
      max: 100000
    }
  };
  try {
    const rs9 = await env.KV.get('cf.json');
    if (!rs9) {
      await env.KV.put('cf.json', JSON.stringify(rs8, null, 2));
    } else {
      const rae = JSON.parse(rs9);
      if (rae.UsageAPI) {
        try {
          const rat = await fetch(rae.UsageAPI);
          const rar = await rat.json();
          i1.CF.Usage = rar;
        } catch (ran) {
          console.error(`请求 CF_JSON.UsageAPI 失败: ${ran.message}`);
        }
      } else {
        i1.CF.Email = rae.Email ? rae.Email : null;
        i1.CF.GlobalAPIKey = rae.GlobalAPIKey ? rnn(rae.GlobalAPIKey) : null;
        i1.CF.AccountID = rae.AccountID ? rnn(rae.AccountID) : null;
        i1.CF.APIToken = rae.APIToken ? rnn(rae.APIToken) : null;
        i1.CF.UsageAPI = null;
        const rai = await rgy(rae.Email, rae.GlobalAPIKey, rae.AccountID, rae.APIToken);
        i1.CF.Usage = rai;
      }
    }
  } catch (rao) {
    console.error(`读取cf.json出错: ${rao.message}`);
  }
  i1.加载时间 = (performance.now() - rsi).toFixed(2) + 'ms';
  return i1;
}
async function ras(env, raa, rac) {
  if (!env.KV || typeof env.KV.put !== 'function') return;
  try {
    const ral = {
      message: raa?.message || String(raa),
      stack: raa?.stack || '',
      url: rac?.url || '',
      method: rac?.method || '',
      timestamp: new Date().toISOString()
    };
    await env.KV.put('last_error.json', JSON.stringify(ral, null, 2), {
      expirationTtl: 86400
    });
  } catch (rad) {}
}
async function raw() {
  try {
    const raf = String(ok || '').replace(/\/+$/, '');
    if (!raf) return null;
    const rah = await fetch(raf + '/user/index.html', {
      headers: {
        'User-Agent': 'NovaProxy'
      },
      cf: {
        cacheTtl: 300,
        cacheEverything: true
      }
    });
    if (!rah || !rah.ok) return null;
    const rap = await rah.text();
    if (!rap || rap.length < 50) return null;
    return new Response(rap, {
      status: 200,
      headers: {
        'content-type': 'text/html; charset=utf-8',
        'Cache-Control': 'no-store'
      }
    });
  } catch (rag) {
    return null;
  }
}
function ram(ray) {
  const rax = ray && ray.cf || ({});
  const rav = String(rax.asOrganization || '').toLowerCase();
  const rab = Number(rax.asn || 0);
  if (String(rax.country || '').toUpperCase() !== 'IR') return 'all';
  if (rab === 44244 || rav.includes('irancell') || rav.includes('mtn')) return 'mtn';
  if (rab === 197207 || rav.includes(ik("bW9iaWxlIGNvbW11bmljYXRpb24gY29tcGFueSBvZiBpcmFu")) || rav.includes('mcci') || rav.includes('hamrah')) return 'mci';
  if (rab === 57218 || rav.includes('rightel')) return 'rightel';
  if (rab === 31549 || rav.includes('shatel')) return 'shatel';
  return 'ir';
}
async function rak(ra0) {
  const ra1 = _poolCache.get(ra0);
  if (ra1 && Date.now() - ra1.at < 1800000) return ra1.list;
  let ra2 = [];
  try {
    const ra3 = await fetch(ra0, {
      headers: {
        'User-Agent': 'NovaProxy'
      },
      cf: {
        cacheTtl: 1800,
        cacheEverything: true
      }
    });
    if (ra3.ok) ra2 = (await rdg(await ra3.text())).map(ra4 => String(ra4).trim()).filter(ra5 => ra5 && !ra5.startsWith('#'));
  } catch (ra6) {}
  _poolCache.set(ra0, {
    at: Date.now(),
    list: ra2
  });
  return ra2;
}
async function ra7(ra8, ra9, rce) {
  const rct = String(ra9 || '').replace(/\/+$/, '');
  if (!rct) return [];
  const rcr = ram(ra8);
  for (const rcn of [...new Set([rcr, 'ir', 'all'])]) {
    const rci = await rak(rct + '/' + rcn + '.txt');
    if (rci && rci.length) {
      const rco = rci.slice().sort(() => 0.5 - Math.random()).slice(0, rce || 16);
      return rco.map(rcs => rcs.includes('#') ? rcs : rcs + '#Nova-' + rcn.toUpperCase());
    }
  }
  return [];
}
async function rca(env) {
  try {
    const rcc = env.KV && typeof env.KV.get === 'function' ? await env.KV.get(ik("bmV0d29yay1zZXR0aW5ncy5qc29u")) : null;
    const rcl = rcc ? JSON.parse(rcc).HOSTS || [] : [];
    const rcd = rcl.length > 0 ? await rcv(env, rcl, rcl[0]) : null;
    if (env.KV && typeof env.KV.get === 'function') {
      try {
        const rcw = await env.KV.get('日志');
        if (rcw) {
          const rcf = JSON.parse(rcw);
          const rch = Date.now() - 7 * 24 * 60 * 60 * 1000;
          const rcp = rcf.filter(rcg => rcg.timestamp && new Date(rcg.timestamp).getTime() > rch);
          await env.KV.put('日志', JSON.stringify(rcp));
        }
      } catch (rcm) {}
    }
    if (env.KV && typeof env.KV.put === 'function') {
      try {
        await env.KV.put(ik("bGFzdF9tYWludGVuYW5jZS5qc29u"), JSON.stringify({
          timestamp: Date.now(),
          health: rcd
        }));
      } catch (rcy) {}
    }
    console.log(ik("W1NjaGVkdWxlZCBNYWludGVuYW5jZV0g57u05oqk5a6M5oiQ"));
    return {
      success: true
    };
  } catch (rcx) {
    console.error('[Scheduled Maintenance] 维护失败:', rcx.message);
    return {
      success: false,
      error: rcx.message
    };
  }
}
async function rcv(env, rcb, rck) {
  const rc0 = rc1 => String(rc1 || '').toLowerCase().replace(/^https?:\/\//, '').split('/')[0];
  const rc2 = rc0(rck);
  const rc3 = (rcb || []).filter(rc4 => rc4 && !rc4.includes('*'));
  const rc5 = [];
  await Promise.all(rc3.map(async rc6 => {
    if (rc2 && rc0(rc6) === rc2) {
      rc5.push({
        host: rc6,
        ok: true,
        status: 200,
        reason: 'live (this worker)',
        checkedAt: Date.now()
      });
      return;
    }
    let rc7 = false, rc8 = 0, rc9 = '';
    try {
      const rle = {
        headers: {
          'User-Agent': 'NovaHealth/1.0'
        }
      };
      if (typeof AbortSignal !== 'undefined' && AbortSignal.timeout) rle.signal = AbortSignal.timeout(8000);
      const rlt = await fetch('https://' + rc6.replace(/^https?:\/\//, '') + '/sub/base64.txt', rle);
      rc8 = rlt.status;
      rc7 = rlt.ok;
      if (rc7) {
        const rlr = await rlt.text();
        rc7 = !!rlr && rlr.length > 8;
        if (!rc7) rc9 = 'empty or invalid sub response';
      } else {
        let rln = '';
        try {
          rln = (await rlt.text()).replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').trim().slice(0, 80);
        } catch (rli) {}
        rc9 = 'HTTP ' + rc8 + (rln ? ': ' + rln : '');
      }
    } catch (rlo) {
      rc8 = -1;
      rc9 = (rlo && rlo.message ? rlo.message : String(rlo)).slice(0, 120);
    }
    rc5.push({
      host: rc6,
      ok: rc7,
      status: rc8,
      reason: rc9,
      checkedAt: Date.now()
    });
  }));
  const rls = {
    checkedAt: Date.now(),
    domains: rc5
  };
  if (env.KV && typeof env.KV.put === 'function') {
    try {
      await env.KV.put('domain-health.json', JSON.stringify(rls));
    } catch (rla) {}
  }
  return rls;
}
function rlc(rll) {
  const rld = rll?.cf;
  const rlw = {
    '4134': 'ct',
    '4809': 'ct',
    '4811': 'ct',
    '4812': 'ct',
    '4815': 'ct',
    '4837': 'cu',
    '4814': 'cu',
    '9929': 'cu',
    '17623': 'cu',
    '17816': 'cu',
    '9808': 'cmcc',
    '24400': 'cmcc',
    '56040': 'cmcc',
    '56041': 'cmcc',
    '56044': 'cmcc'
  };
  const rlf = [{
    code: 'ct',
    pattern: /chinanet|chinatelecom|china telecom|cn2|shtel/
  }, {
    code: 'cmcc',
    pattern: /cmi|cmnet|chinamobile|china mobile|cmcc|mobile communications/
  }, {
    code: 'cu',
    pattern: /china169|china unicom|chinaunicom|cucc|cncgroup|cuii|netcom/
  }];
  if (String(rld?.country || '').toLowerCase() !== 'cn') return 'cf';
  const rlh = String(rld?.asOrganization || '').toLowerCase();
  const rlp = rlf.find(({pattern: rlg}) => rlg.test(rlh))?.code;
  return rlp || rlw[String(rld?.asn || '')] || 'cf';
}
async function rlm(rly, rlx = 16, rlv = -1) {
  const rlb = new URL(rly.url);
  const rlk = String(rlb.searchParams.get('cnIspCode') || '').toLowerCase();
  const rl0 = ['ct', 'cu', 'cmcc', 'cf'].includes(rlk) ? rlk : rlc(rly);
  const rl1 = {
    cmcc: 'CF移动优选',
    cu: 'CF联通优选',
    ct: 'CF电信优选',
    cf: 'CF官方优选'
  };
  const rl2 = rl0 === 'cf' ? `https://raw.githubusercontent.com/${lk[1]}/${lk[1]}/main/CF-CIDR.txt` : `https://raw.githubusercontent.com/${lk[1]}/${lk[1]}/main/CF-CIDR/${rl0}.txt`;
  const rl3 = rl1[rl0] || 'CF官方优选';
  const rl4 = [443, 2053, 2083, 2087, 2096, 8443];
  let rl5 = [];
  try {
    const rl6 = await fetch(rl2);
    rl5 = rl6.ok ? await rdg(await rl6.text()) : ['104.16.0.0/13'];
  } catch {
    rl5 = ['104.16.0.0/13'];
  }
  const rl7 = rl8 => {
    const [rl9, rde] = rl8.split('/'), rdt = parseInt(rde), rdr = 32 - rdt;
    const rdn = rl9.split('.').reduce((rdi, rdo, rds) => rdi | parseInt(rdo) << 24 - rds * 8, 0);
    const rda = Math.floor(Math.random() * Math.pow(2, rdr));
    const rdc = 0xFFFFFFFF << rdr >>> 0, rdl = ((rdn & rdc) >>> 0) + rda >>> 0;
    return [rdl >>> 24 & 0xFF, rdl >>> 16 & 0xFF, rdl >>> 8 & 0xFF, rdl & 0xFF].join('.');
  };
  const rdd = Array.from({
    length: rlx
  }, (rdw, rdf) => {
    const rdh = rl7(rl5[Math.floor(Math.random() * rl5.length)]);
    const rdp = rlv === -1 ? rl4[Math.floor(Math.random() * rl4.length)] : rlv;
    return `${rdh}:${rdp}#${rl3}${rdf + 1}`;
  });
  return [rdd, rdd.join('\n')];
}
async function rdg(rdm) {
  var rdy = rdm.replace(/[	"'\r\n]+/g, ',').replace(/,+/g, ',');
  if (rdy.charAt(0) == ',') rdy = rdy.slice(1);
  if (rdy.charAt(rdy.length - 1) == ',') rdy = rdy.slice(0, rdy.length - 1);
  const rdx = rdy.split(',');
  return rdx;
}
async function rdv(rdb) {
  let rdk = [], rd0 = '', rd1 = rdb.replace(/^sub:\/\//i, 'https://').split('#')[0].split('?')[0];
  if (!(/^https?:\/\//i).test(rd1)) rd1 = `https://${rd1}`;
  try {
    const rd2 = new URL(rd1);
    rd1 = rd2.origin;
  } catch (rd3) {
    rdk.push(`127.0.0.1:1234#${rdb}优选订阅生成器格式化异常:${rd3.message}`);
    return [rdk, rd0];
  }
  const rd4 = `${rd1}/sub?host=example.com&uuid=00000000-0000-4000-8000-000000000000`;
  try {
    const rd5 = await fetch(rd4, {
      headers: {
        'User-Agent': 'v2rayN/edge' + 'tunnel (https://github.com/' + lk[1] + '/edge' + 'tunnel)'
      }
    });
    if (!rd5.ok) {
      rdk.push(`127.0.0.1:1234#${rdb}优选订阅生成器异常:${rd5.statusText}`);
      return [rdk, rd0];
    }
    const rd6 = atob(await rd5.text());
    const rd7 = rd6.includes('\r\n') ? rd6.split('\r\n') : rd6.split('\n');
    for (const rd8 of rd7) {
      if (!rd8.trim()) continue;
      if (rd8.includes('00000000-0000-4000-8000-000000000000') && rd8.includes('example.com')) {
        const rd9 = rd8.match(/:\/\/[^@]+@([^?]+)/);
        if (rd9) {
          let rwe = rd9[1], rwt = '';
          const rwr = rd8.match(/#(.+)$/);
          if (rwr) rwt = '#' + decodeURIComponent(rwr[1]);
          rdk.push(rwe + rwt);
        }
      } else {
        rd0 += rd8 + '\n';
      }
    }
  } catch (rwn) {
    rdk.push(`127.0.0.1:1234#${rdb}优选订阅生成器异常:${rwn.message}`);
  }
  return [rdk, rd0];
}
async function rwi(rwo, rws = '443', rwa = 3000) {
  if (!rwo?.length) return [[], [], [], []];
  const rwc = new Set(), rwl = new Set();
  let rwd = '', rww = [];
  await Promise.allSettled(rwo.map(async rwf => {
    const rwh = rwf.indexOf('#');
    const rwp = rwh > -1 ? rwf.substring(0, rwh) : rwf;
    const rwg = rwh > -1 ? decodeURIComponent(rwf.substring(rwh + 1)) : null;
    const rwm = rwf.toLowerCase().includes('proxyip=true');
    if (rwp.toLowerCase().startsWith('sub://')) {
      try {
        const [rwy, rwx] = await rdv(rwp);
        if (rwg) {
          for (const rwv of rwy) {
            const rwb = rwv.includes('#') ? `${rwv} [${rwg}]` : `${rwv}#[${rwg}]`;
            rwc.add(rwb);
            if (rwm) rwl.add(rwv.split('#')[0]);
          }
        } else {
          for (const rwk of rwy) {
            rwc.add(rwk);
            if (rwm) rwl.add(rwk.split('#')[0]);
          }
        }
        if (rwx && typeof rwx === 'string' && rwg) {
          const rw0 = rwx.replace(/([a-z][a-z0-9+\-.]*:\/\/[^\r\n]*?)(\r?\n|$)/gi, (rw1, rw2, rw3) => {
            const rw4 = rw2.includes('#') ? `${rw2}${encodeURIComponent(` [${rwg}]`)}` : `${rw2}${encodeURIComponent(`#[${rwg}]`)}`;
            return `${rw4}${rw3}`;
          });
          rwd += rw0;
        } else if (rwx && typeof rwx === 'string') {
          rwd += rwx;
        }
      } catch (rw5) {}
      return;
    }
    try {
      const rw6 = new AbortController();
      const rw7 = setTimeout(() => rw6.abort(), rwa);
      const rw8 = await fetch(rwp, {
        signal: rw6.signal
      });
      clearTimeout(rw7);
      let rw9 = '';
      try {
        const rfe = await rw8.arrayBuffer();
        const rft = (rw8.headers.get('content-type') || '').toLowerCase();
        const rfr = rft.match(/charset=([^\s;]+)/i)?.[1]?.toLowerCase() || '';
        let rfn = ['utf-8', 'gb2312'];
        if (rfr.includes('gb') || rfr.includes('gbk') || rfr.includes('gb2312')) {
          rfn = ['gb2312', 'utf-8'];
        }
        let rfi = false;
        for (const rfo of rfn) {
          try {
            const rfs = new TextDecoder(rfo).decode(rfe);
            if (rfs && rfs.length > 0 && !rfs.includes('\ufffd')) {
              rw9 = rfs;
              rfi = true;
              break;
            } else if (rfs && rfs.length > 0) {
              continue;
            }
          } catch (rfa) {
            continue;
          }
        }
        if (!rfi) {
          rw9 = await rw8.text();
        }
        if (!rw9 || rw9.trim().length === 0) {
          return;
        }
      } catch (rfc) {
        console.error(ik("RmFpbGVkIHRvIGRlY29kZSByZXNwb25zZTo="), rfc);
        return;
      }
      let rfl = rw9;
      const rfd = typeof rw9 === 'string' ? rw9.replace(/\s/g, '') : '';
      if (rfd.length > 0 && rfd.length % 4 === 0 && (/^[A-Za-z0-9+/]+={0,2}$/).test(rfd)) {
        try {
          const rfw = new Uint8Array(atob(rfd).split('').map(rff => rff.charCodeAt(0)));
          rfl = new TextDecoder('utf-8').decode(rfw);
        } catch {}
      }
      if (rfl.split('#')[0].includes('://')) {
        if (rwg) {
          const rfh = rfl.replace(/([a-z][a-z0-9+\-.]*:\/\/[^\r\n]*?)(\r?\n|$)/gi, (rfp, rfg, rfm) => {
            const rfy = rfg.includes('#') ? `${rfg}${encodeURIComponent(` [${rwg}]`)}` : `${rfg}${encodeURIComponent(`#[${rwg}]`)}`;
            return `${rfy}${rfm}`;
          });
          rwd += rfh + '\n';
        } else {
          rwd += rfl + '\n';
        }
        return;
      }
      const rfx = rw9.trim().split('\n').map(rfv => rfv.trim()).filter(rfb => rfb);
      const rfk = rfx.length > 1 && rfx[0].includes(',');
      const rf0 = /^[^\[\]]*:[^\[\]]*:[^\[\]]/;
      const rf1 = new URL(rwp);
      if (!rfk) {
        rfx.forEach(rf2 => {
          const rf3 = rf2.indexOf('#');
          const [rf4, rf5] = rf3 > -1 ? [rf2.substring(0, rf3), rf2.substring(rf3)] : [rf2, ''];
          let rf6 = false;
          if (rf4.startsWith('[')) {
            rf6 = (/\]:(\d+)$/).test(rf4);
          } else {
            const rf7 = rf4.lastIndexOf(':');
            rf6 = rf7 > -1 && (/^\d+$/).test(rf4.substring(rf7 + 1));
          }
          const rf8 = rf1.searchParams.get('port') || rws;
          const rf9 = rf6 ? rf2 : `${rf4}:${rf8}${rf5}`;
          if (rwg) {
            const rhe = rf9.includes('#') ? `${rf9} [${rwg}]` : `${rf9}#[${rwg}]`;
            rwc.add(rhe);
          } else {
            rwc.add(rf9);
          }
          if (rwm) rwl.add(rf9.split('#')[0]);
        });
      } else {
        const rht = rfx[0].split(',').map(rhr => rhr.trim());
        const rhn = rfx.slice(1);
        if (rht.includes('IP地址') && rht.includes('端口') && rht.includes('数据中心')) {
          const rhi = rht.indexOf('IP地址'), rho = rht.indexOf('端口');
          const rhs = rht.indexOf('国家') > -1 ? rht.indexOf('国家') : rht.indexOf('城市') > -1 ? rht.indexOf('城市') : rht.indexOf('数据中心');
          const rha = rht.indexOf('TLS');
          rhn.forEach(rhc => {
            const rhl = rhc.split(',').map(rhd => rhd.trim());
            if (rha !== -1 && rhl[rha]?.toLowerCase() !== 'true') return;
            const rhw = rf0.test(rhl[rhi]) ? `[${rhl[rhi]}]` : rhl[rhi];
            const rhf = `${rhw}:${rhl[rho]}#${rhl[rhs]}`;
            if (rwg) {
              const rhh = `${rhf} [${rwg}]`;
              rwc.add(rhh);
            } else {
              rwc.add(rhf);
            }
            if (rwm) rwl.add(`${rhw}:${rhl[rho]}`);
          });
        } else if (rht.some(rhp => rhp.includes('IP')) && rht.some(rhg => rhg.includes('延迟')) && rht.some(rhm => rhm.includes('下载速度'))) {
          const rhy = rht.findIndex(rhx => rhx.includes('IP'));
          const rhv = rht.findIndex(rhb => rhb.includes('延迟'));
          const rhk = rht.findIndex(rh0 => rh0.includes('下载速度'));
          const rh1 = rf1.searchParams.get('port') || rws;
          rhn.forEach(rh2 => {
            const rh3 = rh2.split(',').map(rh4 => rh4.trim());
            const rh5 = rf0.test(rh3[rhy]) ? `[${rh3[rhy]}]` : rh3[rhy];
            const rh6 = `${rh5}:${rh1}#CF优选 ${rh3[rhv]}ms ${rh3[rhk]}MB/s`;
            if (rwg) {
              const rh7 = `${rh6} [${rwg}]`;
              rwc.add(rh7);
            } else {
              rwc.add(rh6);
            }
            if (rwm) rwl.add(`${rh5}:${rh1}`);
          });
        }
      }
    } catch (rh8) {}
  }));
  const rh9 = rwd.trim() ? [...new Set(rwd.split(/\r?\n/).filter(rpe => rpe.trim() !== ''))] : [];
  return [Array.from(rwc), rh9, rww, Array.from(rwl)];
}
async function rpt(rpr, rpn) {
  const {searchParams: rpi} = rpr;
  const rpo = decodeURIComponent(rpr.pathname);
  const rps = rpo.toLowerCase();
  const rpa = rpo.match(/\/video\/(.+)$/i);
  if (rpa) {
    try {
      const rpc = t37(rpa[1], rpn);
      const {type: rpl, ...rpd} = JSON.parse(rpc);
      if (!rpl || !rp7[String(rpl).toLowerCase()]) throw new Error('链式代理类型无效');
      if (!rpd.hostname || !rpd.port) throw new Error('链式代理地址缺少 hostname 或 port');
      i5 = '';
      i2 = '链式代理';
      or = false;
      i4 = true;
      i3 = String(rpl).toLowerCase();
      i6 = {
        username: rpd.username,
        password: rpd.password,
        hostname: rpd.hostname,
        port: Number(rpd.port)
      };
      if (isNaN(i6.port)) throw new Error('链式代理端口无效');
      return;
    } catch (rpw) {
      console.error('解析链式代理参数失败:', rpw.message);
    }
  }
  i5 = rpi.get('socks5') || rpi.get('http') || rpi.get('https') || rpi.get('turn') || rpi.get('sstp') || null;
  i4 = rpi.has('globalproxy');
  if (rpi.get('socks5')) i3 = 'socks5'; else if (rpi.get('http')) i3 = 'http'; else if (rpi.get('https')) i3 = 'https'; else if (rpi.get('turn')) i3 = 'turn'; else if (rpi.get('sstp')) i3 = 'sstp';
  const rpf = (rph, rpp = true) => {
    const rpg = (/^(socks5|http|https|turn|sstp):\/\/(.+)$/i).exec(rph || '');
    if (!rpg) return false;
    i3 = rpg[1].toLowerCase();
    i5 = rpg[2].split('/')[0];
    if (rpp) i4 = true;
    return true;
  };
  const rpm = rpy => {
    i2 = rpy;
    i3 = null;
    or = false;
  };
  const rpx = rpv => {
    if (!rpv.includes('://')) {
      const rpb = rpv.indexOf('/');
      return rpb > 0 ? rpv.slice(0, rpb) : rpv;
    }
    const rpk = rpv.split('://');
    if (rpk.length !== 2) return rpv;
    const rp0 = rpk[1].indexOf('/');
    return rp0 > 0 ? `${rpk[0]}://${rpk[1].slice(0, rp0)}` : rpv;
  };
  const rp1 = rpi.get('proxyip');
  if (rp1 !== null) {
    if (!rpf(rp1)) return rpm(rp1);
  } else {
    let rp2 = (/\/(socks5?|http|https|turn|sstp):\/?\/?([^/?#\s]+)/i).exec(rpo);
    if (rp2) {
      const rp3 = rp2[1].toLowerCase();
      i3 = rp3 === 'sock' || rp3 === 'socks' ? 'socks5' : rp3;
      i5 = rp2[2].split('/')[0];
      i4 = true;
    } else if (rp2 = (/\/(g?s5|socks5|g?http|g?https|g?turn|g?sstp)=([^/?#\s]+)/i).exec(rpo)) {
      const rp4 = rp2[1].toLowerCase();
      i5 = rp2[2].split('/')[0];
      i3 = rp4.includes('sstp') ? 'sstp' : rp4.includes('turn') ? 'turn' : rp4.includes('https') ? 'https' : rp4.includes('http') ? 'http' : 'socks5';
      if (rp4.startsWith('g')) i4 = true;
    } else if (rp2 = (/\/(proxyip[.=]|pyip=|ip=)([^?#\s]+)/).exec(rps)) {
      const rp5 = rpx(rp2[2]);
      if (!rpf(rp5)) return rpm(rp5);
    }
  }
  if (!i5) {
    i3 = null;
    return;
  }
  try {
    i6 = await rgr(i5, rp8(i3));
    if (rpi.get('socks5')) i3 = 'socks5'; else if (rpi.get('http')) i3 = 'http'; else if (rpi.get('https')) i3 = 'https'; else if (rpi.get('turn')) i3 = 'turn'; else if (rpi.get('sstp')) i3 = 'sstp'; else i3 = i3 || 'socks5';
  } catch (rp6) {
    console.error('解析SOCKS5地址失败:', rp6.message);
    i3 = null;
  }
}
const rp7 = {
  socks5: 1080,
  http: 80,
  https: 443,
  turn: 3478,
  sstp: 443
};
function rp8(rp9) {
  return rp7[String(rp9 || '').toLowerCase()] || 80;
}
const rge = /^(?:[A-Z0-9+/]{4})*(?:[A-Z0-9+/]{2}==|[A-Z0-9+/]{3}=)?$/i, rgt = /^\[.*\]$/;
function rgr(rgn, rgi = 80) {
  rgn = String(rgn || '').trim().replace(/^(socks5|http|https|turn|sstp):\/\//i, '').split('#')[0].trim();
  const rgo = rgn.lastIndexOf("@");
  if (rgo !== -1) {
    let rgs = rgn.slice(0, rgo).replaceAll("%3D", "=");
    if (!rgs.includes(":") && rge.test(rgs)) rgs = atob(rgs);
    rgn = `${rgs}@${rgn.slice(rgo + 1)}`;
  }
  const rga = rgn.lastIndexOf("@");
  const rgc = (rga === -1 ? rgn : rgn.slice(rga + 1)).split('/')[0];
  const rgl = rga === -1 ? "" : rgn.slice(0, rga);
  const [rgd, rgw] = rgl ? rgl.split(":") : [];
  if (rgl && !rgw) throw new Error('无效的 SOCKS 地址格式：认证部分必须是 "username:password" 的形式');
  let rgf = rgc, rgh = rgi;
  if (rgc.includes("]:")) {
    const [rgp, rgg = ""] = rgc.split("]:");
    rgf = rgp + "]";
    rgh = Number(rgg.replace(/[^\d]/g, ""));
  } else if (!rgc.startsWith("[")) {
    const rgm = rgc.split(":");
    if (rgm.length === 2) {
      rgf = rgm[0];
      rgh = Number(rgm[1].replace(/[^\d]/g, ""));
    }
  }
  if (isNaN(rgh)) throw new Error(ik("5peg5pWI55qEIFNPQ0tTIOWcsOWdgOagvOW8j++8muerr+WPo+WPt+W/hemhu+aYr+aVsOWtlw=="));
  if (rgf.includes(":") && !rgt.test(rgf)) throw new Error('无效的 SOCKS 地址格式：IPv6 地址必须用方括号括起来，如 [2001:db8::1]');
  return {
    username: rgd,
    password: rgw,
    hostname: rgf,
    port: rgh
  };
}
async function rgy(rgx, rgv, rgb, rgk) {
  const rg0 = ik("aHR0cHM6Ly9hcGkuY2xvdWRmbGFyZS5jb20vY2xpZW50L3Y0");
  const rg1 = rg2 => rg2?.reduce((rg3, rg4) => rg3 + (rg4?.sum?.requests || 0), 0) || 0;
  const rg5 = {
    "Content-Type": ik("YXBwbGljYXRpb24vanNvbg==")
  };
  try {
    if (!rgb && (!rgx || !rgv)) return {
      success: false,
      pages: 0,
      workers: 0,
      total: 0,
      max: 100000
    };
    if (!rgb) {
      const rg6 = await fetch(`${rg0}/accounts`, {
        method: "GET",
        headers: {
          ...rg5,
          "X-AUTH-EMAIL": rgx,
          "X-AUTH-KEY": rgv
        }
      });
      if (!rg6.ok) throw new Error(`账户获取失败: ${rg6.status}`);
      const rg7 = await rg6.json();
      if (!rg7?.result?.length) throw new Error("未找到账户");
      const rg8 = rg7.result.findIndex(rg9 => rg9.name?.toLowerCase().startsWith(rgx.toLowerCase()));
      rgb = rg7.result[rg8 >= 0 ? rg8 : 0]?.id;
    }
    const rme = new Date();
    rme.setUTCHours(0, 0, 0, 0);
    const rmt = rgk ? {
      ...rg5,
      "Authorization": `Bearer ${rgk}`
    } : {
      ...rg5,
      "X-AUTH-EMAIL": rgx,
      "X-AUTH-KEY": rgv
    };
    const rmr = await fetch(`${rg0}/graphql`, {
      method: "POST",
      headers: rmt,
      body: JSON.stringify({
        query: `query getBillingMetrics($AccountID: String!, $filter: AccountWorkersInvocationsAdaptiveFilter_InputObject) {
					viewer { accounts(filter: {accountTag: $AccountID}) {
						pagesFunctionsInvocationsAdaptiveGroups(limit: 1000, filter: $filter) { sum { requests } }
						workersInvocationsAdaptive(limit: 10000, filter: $filter) { sum { requests } }
					} }
				}`,
        variables: {
          AccountID: rgb,
          filter: {
            datetime_geq: rme.toISOString(),
            datetime_leq: new Date().toISOString()
          }
        }
      })
    });
    if (!rmr.ok) throw new Error(`查询失败: ${rmr.status}`);
    const rmn = await rmr.json();
    if (rmn.errors?.length) throw new Error(rmn.errors[0].message);
    const rmi = rmn?.data?.viewer?.accounts?.[0];
    if (!rmi) throw new Error("未找到账户数据");
    const rmo = rg1(rmi.pagesFunctionsInvocationsAdaptiveGroups);
    const rms = rg1(rmi.workersInvocationsAdaptive);
    const rma = rmo + rms;
    const rmc = 100000;
    t4m(`统计结果 - Pages: ${rmo}, Workers: ${rms}, 总计: ${rma}, 上限: 100000`);
    return {
      success: true,
      pages: rmo,
      workers: rms,
      total: rma,
      max: rmc
    };
  } catch (rml) {
    console.error('获取使用量错误:', rml.message);
    return {
      success: false,
      pages: 0,
      workers: 0,
      total: 0,
      max: 100000
    };
  }
}
function rmd(rmw) {
  const rmf = [0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5, 0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5, 0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3, 0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174, 0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc, 0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da, 0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7, 0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967, 0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13, 0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85, 0xa2bfe8a1, 0xa81a664b, 0xc24b8b70, 0xc76c51a3, 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070, 0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5, 0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3, 0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208, 0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2];
  const rmh = (rmp, rmg) => (rmp >>> rmg | rmp << 32 - rmg) >>> 0;
  rmw = unescape(encodeURIComponent(rmw));
  const rmm = rmw.length * 8;
  rmw += String.fromCharCode(0x80);
  while (rmw.length * 8 % 512 !== 448) rmw += String.fromCharCode(0);
  const rmy = [0xc1059ed8, 0x367cd507, 0x3070dd17, 0xf70e5939, 0xffc00b31, 0x68581511, 0x64f98fa7, 0xbefa4fa4];
  const rmx = Math.floor(rmm / 0x100000000), rmv = rmm & 0xFFFFFFFF;
  rmw += String.fromCharCode(rmx >>> 24 & 0xFF, rmx >>> 16 & 0xFF, rmx >>> 8 & 0xFF, rmx & 0xFF, rmv >>> 24 & 0xFF, rmv >>> 16 & 0xFF, rmv >>> 8 & 0xFF, rmv & 0xFF);
  const rmb = [];
  for (let rmk = 0; rmk < rmw.length; rmk += 4) rmb.push(rmw.charCodeAt(rmk) << 24 | rmw.charCodeAt(rmk + 1) << 16 | rmw.charCodeAt(rmk + 2) << 8 | rmw.charCodeAt(rmk + 3));
  for (let rm0 = 0; rm0 < rmb.length; rm0 += 16) {
    const rm1 = new Array(64).fill(0);
    for (let rm2 = 0; rm2 < 16; rm2++) rm1[rm2] = rmb[rm0 + rm2];
    for (let rm3 = 16; rm3 < 64; rm3++) {
      const rm4 = rmh(rm1[rm3 - 15], 7) ^ rmh(rm1[rm3 - 15], 18) ^ rm1[rm3 - 15] >>> 3;
      const rm5 = rmh(rm1[rm3 - 2], 17) ^ rmh(rm1[rm3 - 2], 19) ^ rm1[rm3 - 2] >>> 10;
      rm1[rm3] = rm1[rm3 - 16] + rm4 + rm1[rm3 - 7] + rm5 >>> 0;
    }
    let [rm6, rm7, rm8, rm9, rye, ryt, ryr, ryn] = rmy;
    for (let ryi = 0; ryi < 64; ryi++) {
      const ryo = rmh(rye, 6) ^ rmh(rye, 11) ^ rmh(rye, 25), rys = rye & ryt ^ ~rye & ryr, rya = ryn + ryo + rys + rmf[ryi] + rm1[ryi] >>> 0;
      const ryc = rmh(rm6, 2) ^ rmh(rm6, 13) ^ rmh(rm6, 22), ryl = rm6 & rm7 ^ rm6 & rm8 ^ rm7 & rm8, ryd = ryc + ryl >>> 0;
      ryn = ryr;
      ryr = ryt;
      ryt = rye;
      rye = rm9 + rya >>> 0;
      rm9 = rm8;
      rm8 = rm7;
      rm7 = rm6;
      rm6 = rya + ryd >>> 0;
    }
    for (let ryw = 0; ryw < 8; ryw++) rmy[ryw] = rmy[ryw] + (ryw === 0 ? rm6 : ryw === 1 ? rm7 : ryw === 2 ? rm8 : ryw === 3 ? rm9 : ryw === 4 ? rye : ryw === 5 ? ryt : ryw === 6 ? ryr : ryn) >>> 0;
  }
  let ryf = '';
  for (let ryh = 0; ryh < 7; ryh++) {
    for (let ryp = 24; ryp >= 0; ryp -= 8) ryf += (rmy[ryh] >>> ryp & 0xFF).toString(16).padStart(2, '0');
  }
  return ryf;
}
async function ryg(rym, ryy = ik("ZGFzaC5jbG91ZGZsYXJlLmNvbQ=="), ryx = '00000000-0000-4000-8000-000000000000') {
  rym = rym.toLowerCase();
  if (!i8 || !i9 || i8 !== rym) {
    function ryv(ryb) {
      let ryk = ryb, ry0 = 443;
      if (ryb.includes(']:')) {
        const ry1 = ryb.split(']:');
        ryk = ry1[0] + ']';
        ry0 = parseInt(ry1[1], 10) || ry0;
      } else if ((ryb.match(/:/g) || []).length === 1 && !ryb.startsWith('[')) {
        const ry2 = ryb.lastIndexOf(':');
        ryk = ryb.slice(0, ry2);
        ry0 = parseInt(ryb.slice(ry2 + 1), 10) || ry0;
      }
      return [ryk, ry0];
    }
    function ry3(ry4) {
      return ry4.flatMap(ry5 => {
        if (ry5.startsWith('"') && ry5.endsWith('"')) ry5 = ry5.slice(1, -1);
        return ry5.replace(/\\010/g, ',').replace(/\n/g, ',').split(',').map(ry6 => ry6.trim()).filter(Boolean);
      }).map(ry7 => ryv(ry7));
    }
    const ry8 = await rdg(rym);
    let ry9 = [];
    const rxe = /^(25[0-5]|2[0-4]\d|[01]?\d\d?)\.(25[0-5]|2[0-4]\d|[01]?\d\d?)\.(25[0-5]|2[0-4]\d|[01]?\d\d?)\.(25[0-5]|2[0-4]\d|[01]?\d\d?)$/;
    const rxt = /^\[?(?:[a-fA-F0-9]{0,4}:){1,7}[a-fA-F0-9]{0,4}\]?$/;
    for (const rxr of ry8) {
      let [rxn, rxi] = ryv(rxr);
      if (rxr.includes('.tp')) {
        const rxo = rxr.match(/\.tp(\d+)/);
        if (rxo) rxi = parseInt(rxo[1], 10);
      }
      if (rxe.test(rxn) || rxt.test(rxn)) {
        t4m(`[反代解析] ${rxn} 为IP地址，直接使用`);
        ry9.push([rxn, rxi]);
        continue;
      }
      const [rxs, rxa] = await Promise.all([rn9(rxn, 'TXT'), rn9(rxn, 'A')]);
      const rxc = rxs.filter(rxl => rxl.type === 16).map(rxd => rxd.data);
      const rxw = ry3(rxc);
      if (rxw.length > 0) {
        t4m(`[反代解析] ${rxn} 使用TXT记录，共${rxw.length}个结果`);
        ry9.push(...rxw);
        continue;
      }
      const rxf = rxa.filter(rxh => rxh.type === 1).map(rxp => rxp.data);
      if (rxf.length > 0) {
        t4m(`[反代解析] ${rxn} 未获取到TXT记录，使用A记录，共${rxf.length}个结果`);
        ry9.push(...rxf.map(rxg => [rxg, rxi]));
        continue;
      }
      const rxm = await rn9(rxn, 'AAAA');
      const rxy = rxm.filter(rxx => rxx.type === 28).map(rxv => `[${rxv.data}]`);
      if (rxy.length > 0) {
        t4m(`[反代解析] ${rxn} 未获取到TXT和A记录，使用AAAA记录，共${rxy.length}个结果`);
        ry9.push(...rxy.map(rxb => [rxb, rxi]));
      } else {
        t4m(`[反代解析] ${rxn} 未获取到TXT、A和AAAA记录，保留原域名`);
        ry9.push([rxn, rxi]);
      }
    }
    const rxk = ry9.sort((rx0, rx1) => rx0[0].localeCompare(rx1[0]));
    const rx2 = ryy.includes('.') ? ryy.split('.').slice(-2).join('.') : ryy;
    let rx3 = [...rx2 + ryx].reduce((rx4, rx5) => rx4 + rx5.charCodeAt(0), 0);
    t4m(`[反代解析] 随机种子: ${rx3}\n目标站点: ${rx2}`);
    const rx6 = [...rxk].sort(() => (rx3 = rx3 * 1103515245 + 12345 & 0x7fffffff) / 0x7fffffff - 0.5);
    i9 = rx6.slice(0, 8);
    t4m(`[反代解析] 解析完成 总数: ${i9.length}个\n${i9.map(([rx7, rx8], rx9) => `${rx9 + 1}. ${rx7}:${rx8}`).join('\n')}`);
    i8 = rym;
  } else t4m(`[反代解析] 读取缓存 总数: ${i9.length}个\n${i9.map(([rve, rvt], rvr) => `${rvr + 1}. ${rve}:${rvt}`).join('\n')}`);
  return i9;
}
const rvn = ['pornhub.com', 'www.pornhub.com', 'xvideos.com', 'www.xvideos.com', 'xnxx.com', 'www.xnxx.com', 'xhamster.com', 'www.xhamster.com', 'redtube.com', 'www.redtube.com', 'youporn.com', 'www.youporn.com', 'porn.com', 'www.porn.com', 'tube8.com', 'www.tube8.com', 'xvideos3.com', 'eporner.com', 'www.eporner.com', 'hclips.com', 'www.hclips.com', 'hqporner.com', 'www.hqporner.com', 'pornhd.com', 'www.pornhd.com', 'porn300.com', 'www.porn300.com', 'porntrex.com', 'www.porntrex.com', 'spankbang.com', 'www.spankbang.com', 'txxx.com', 'www.txxx.com', 'vjav.com', 'www.vjav.com', 'xvideos2.com', 'xvideos3.com', 'adult-empire.com', 'www.adult-empire.com', 'adulttime.com', ik("d3d3LmFkdWx0dGltZS5jb20="), 'alphaporno.com', ik("d3d3LmFscGhhcG9ybm8uY29t"), 'analytics.porn', ik("YW5pbWVpZGhlbnRhaS5jb20="), 'anyporn.com', 'anysex.com', 'www.anysex.com', 'beeg.com', 'www.beeg.com', 'bellesa.co', 'www.bellesa.co', 'bigassporn.org', 'bigtits.com', 'www.bigtits.com', 'bravotube.net', 'www.bravotube.net', 'bustyplatinum.com', 'cam4.com', 'www.cam4.com', 'cambay.tv', 'www.cambay.tv', 'chaturbate.com', ik("d3d3LmNoYXR1cmJhdGUuY29t"), 'clips4sale.com', ik("d3d3LmNsaXBzNHNhbGUuY29t"), 'cock.xxx', 'daporno.com', 'desiporn.tv', 'www.desiporn.tv', 'deviporn.com', ik("d3d3LmRldmlwb3JuLmNvbQ=="), 'dirtyship.com', ik("d3d3LmRpcnR5c2hpcC5jb20="), 'ebony.com', 'www.ebony.com', 'efukt.com', 'www.efukt.com', 'egotastic.com', 'www.egotastic.com', 'empflix.com', 'www.empflix.com', 'erome.com', 'www.erome.com', 'eroprofile.com', ik("d3d3LmVyb3Byb2ZpbGUuY29t"), 'esporn.com', 'www.esporn.com', 'ex-gf.me', 'www.ex-gf.me', 'extremetube.com', ik("d3d3LmV4dHJlbWV0dWJlLmNvbQ=="), 'fap.com', 'www.fap.com', 'fapdu.com', 'www.fapdu.com', 'faphouse.com', 'www.faphouse.com', 'femjoy.com', 'www.femjoy.com', 'fetlife.com', 'www.fetlife.com', 'filthygirls.com', ik("d3d3LmZpbHRoeWdpcmxzLmNvbQ=="), 'flix.com', 'www.flix.com', 'freeones.com', 'www.freeones.com', 'freeporn.com', ik("d3d3LmZyZWVwb3JuLmNvbQ=="), 'fux.com', 'www.fux.com', 'gayboystube.com', ik("d3d3LmdheWJveXN0dWJlLmNvbQ=="), 'gaymaletube.com', ik("d3d3LmdheW1hbGV0dWJlLmNvbQ=="), 'ghettotube.com', 'www.ghettotube.com', 'girlsway.com', 'www.girlsway.com', 'gofap.net', 'www.gofap.net', 'hentai2read.com', 'hentaigasm.com', 'www.hentaigasm.com', 'hentaihaven.com', ik("d3d3LmhlbnRhaWhhdmVuLmNvbQ=="), 'hotmovies.com', ik("d3d3LmhvdG1vdmllcy5jb20="), 'hqbabes.com', 'www.hqbabes.com', 'hqseek.com', 'www.hqseek.com', 'iafd.com', 'www.iafd.com', 'imagefap.com', 'www.imagefap.com', 'incestflix.com', 'indexxx.com', 'www.indexxx.com', 'jacquieetmichel.tv', 'www.jacquieetmichel.tv', 'japaneseporn.tv', 'www.japaneseporn.tv', 'jerkoffto.com', ik("d3d3LmplcmtvZmZ0by5jb20="), 'jizzhut.com', 'www.jizzhut.com', 'joymii.com', 'www.joymii.com', 'keezmovies.com', 'www.keezmovies.com', 'lesbianporn.com', 'www.lesbianporn.com', 'lustery.com', 'www.lustery.com', 'madthumbs.com', ik("d3d3Lm1hZHRodW1icy5jb20="), 'mofos.com', 'www.mofos.com', 'motherless.com', 'www.motherless.com', 'mrporngeek.com', 'www.mrporngeek.com', 'mydirtyhobby.com', 'www.mydirtyhobby.com', 'myduckisdead.org', 'nastyporn.com', 'www.nastyporn.com', 'naughtyamerica.com', ik("d3d3Lm5hdWdodHlhbWVyaWNhLmNvbQ=="), 'nuvid.com', 'www.nuvid.com', 'onlyfans.com', ik("d3d3Lm9ubHlmYW5zLmNvbQ=="), 'palcomp3.com.br', ik("d3d3LnBhbGNvbXAzLmNvbS5icg=="), 'pandamovies.pw', ik("cGVyZmVjdGdpcmxzLmNvbQ=="), 'www.perfectgirls.com', 'pinklabel.tv', 'www.pinklabel.tv', 'playboy.com', 'www.playboy.com', 'pornbox.com', 'www.pornbox.com', 'pornburst.xxx', 'porndoe.com', 'www.porndoe.com', 'pornfidelity.com', ik("d3d3LnBvcm5maWRlbGl0eS5jb20="), 'porngem.com', 'www.porngem.com', 'pornhubpremium.com', 'www.pornhubpremium.com', 'pornmd.com', 'www.pornmd.com', 'pornone.com', 'www.pornone.com', ik("cG9ybm9yb3VsZXR0ZS5uZXQ="), ik("d3d3LnBvcm5vcm91bGV0dGUubmV0"), 'pornoxo.com', 'www.pornoxo.com', 'porntop.com', 'www.porntop.com', 'pornvisit.com', ik("d3d3LnBvcm52aXNpdC5jb20="), 'pornwhite.com', 'www.pornwhite.com', 'porzo.com', 'www.porzo.com', 'propertysex.com', ik("d3d3LnByb3BlcnR5c2V4LmNvbQ=="), 'rapexxx.com', 'www.rapexxx.com', 'ratexxx.net', 'www.ratexxx.net', 'realitykings.com', 'www.realitykings.com', 'redtube.com.br', 'www.redtube.com.br', 'rockettube.com', ik("d3d3LnJvY2tldHR1YmUuY29t"), 'rulertube.com', ik("d3d3LnJ1bGVydHViZS5jb20="), 'sausage.com', 'www.sausage.com', 'sextube.com', 'www.sextube.com', 'sexu.com', 'www.sexu.com', 'shemale.com', 'www.shemale.com', 'sinparty.com', 'www.sinparty.com', 'sleazyneasy.com', ik("d3d3LnNsZWF6eW5lYXN5LmNvbQ=="), 'slutload.com', 'www.slutload.com', 'smartporn.com', 'www.smartporn.com', 'smut.com', 'www.smut.com', 'sologirls.xxx', 'spankwire.com', ik("d3d3LnNwYW5rd2lyZS5jb20="), 'stripchat.com', ik("d3d3LnN0cmlwY2hhdC5jb20="), 'sunporno.com', ik("d3d3LnN1bnBvcm5vLmNvbQ=="), ik("dGVlbnNsb3ZlcG9ybi5jb20="), ik("d3d3LnRlZW5zbG92ZXBvcm4uY29t"), 'teentube.com', ik("d3d3LnRlZW50dWJlLmNvbQ=="), 'thatpervert.com', 'www.thatpervert.com', ik("dGhlcGVyZnJldmlldy5jb20="), ik("d3d3LnRoZXBlcmZyZXZpZXcuY29t"), 'thumbzilla.com', 'www.thumbzilla.com', 'tiava.com', 'www.tiava.com', 'tnaflix.com', 'www.tnaflix.com', 'tube.xxx', 'tubegalore.com', 'www.tubegalore.com', 'tubeporn.com', 'www.tubeporn.com', 'tubepornclassic.com', ik("d3d3LnR1YmVwb3JuY2xhc3NpYy5jb20="), 'tubestack.com', ik("d3d3LnR1YmVzdGFjay5jb20="), 'twistys.com', 'www.twistys.com', 'upornia.com', 'www.upornia.com', 'videosz.com', 'www.videosz.com', 'vintageporn.net', ik("d3d3LnZpbnRhZ2Vwb3JuLm5ldA=="), 'voyeurhit.com', ik("d3d3LnZveWV1cmhpdC5jb20="), 'watchmygf.com', ik("d3d3LndhdGNobXlnZi5jb20="), 'wetpussy.com', 'www.wetpussy.com', 'whalebone.vip', 'xhamsterlive.com', ik("d3d3LnhoYW1zdGVybGl2ZS5jb20="), 'xnxx.app', 'www.xnxx.app', 'xnxx.tv', 'www.xnxx.tv', 'xossip.com', 'www.xossip.com', 'xporn.net', 'www.xporn.net', 'xpornz.com', 'www.xpornz.com', 'xtube.com', 'www.xtube.com', 'xvideo.com', 'www.xvideo.com', 'xvideos-br.com', 'xvideos.es', 'www.xvideos.es', 'xvideos.fr', 'www.xvideos.fr', 'xvideos.it', 'www.xvideos.it', 'xvideos.jp', 'www.xvideos.jp', 'xvideos.ru', 'www.xvideos.ru', 'xvideos.tv', 'www.xvideos.tv', 'youjizz.com', 'www.youjizz.com', 'youpornbook.com', ik("d3d3LnlvdXBvcm5ib29rLmNvbQ=="), 'yourlust.com', ik("d3d3LnlvdXJsdXN0LmNvbQ=="), 'zbporn.com', 'www.zbporn.com', 'zporn.com', 'www.zporn.com'];
function rvi(rvo) {
  if (!rvo) return false;
  const rvs = rvo.toLowerCase();
  for (const rva of rvn) {
    if (rvs === rva || rvs.endsWith('.' + rva)) return true;
  }
  return false;
}
function rvc(rvl) {
  if (e8y(rvl)) return true;
  if (ol && ol.enablePornBlock) {
    return rvi(rvl);
  }
  return false;
}
function rvd(rvw) {
  const rvf = new URL(rvw.url);
  const rvh = rvf.host;
  const rvp = `<!DOCTYPE html><html lang="fa" dir="rtl"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Blocked / مسدود شده</title><style>@import url("https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;700;800;900&display=swap");*{margin:0;padding:0;box-sizing:border-box}body{font-family:"Vazirmatn",sans-serif;background:linear-gradient(135deg,#0f0c29,#302b63,#24243e);min-height:100vh;display:flex;justify-content:center;align-items:center;padding:20px}.card{background:rgba(255,255,255,0.05);backdrop-filter:blur(20px);border:1px solid rgba(255,255,255,0.1);border-radius:24px;padding:48px 40px;max-width:480px;width:100%;text-align:center;position:relative;overflow:hidden}.card::before{content:"";position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:conic-gradient(from 0deg,transparent,rgba(239,68,68,0.1),transparent,rgba(239,68,68,0.05),transparent);animation:spin 6s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}.content{position:relative;z-index:1}.shield{width:80px;height:80px;border-radius:50%;background:linear-gradient(135deg,#8b5cf6,#6d28d9);display:flex;align-items:center;justify-content:center;margin:0 auto 24px;font-size:36px;color:#fff;box-shadow:0 8px 32px rgba(139,92,246,0.3)}h1{color:#fff;font-size:24px;font-weight:900;margin-bottom:8px;letter-spacing:-0.5px}h1 span{background:linear-gradient(135deg,#8b5cf6,#06b6d4);-webkit-background-clip:text;-webkit-text-fill-color:transparent}p{color:rgba(255,255,255,0.6);font-size:14px;line-height:1.8;margin:12px 0}.badge{display:inline-block;background:linear-gradient(135deg,#ef4444,#dc2626);color:#fff;padding:4px 16px;border-radius:20px;font-size:12px;font-weight:700;margin-bottom:16px}</style></head><body><div class="card"><div class="content"><div class="shield">&#x1F6E1;</div><h1><span>Blocked</span></h1><p>This site has been blocked by the network administrator.</p><div class="badge">BLOCKED</div><p style="font-size:12px;opacity:0.5;margin-top:16px">${rvh}</p></div></div></body></html>`;
  return new Response(rvp, {
    status: 403,
    headers: {
      'Content-Type': ik("dGV4dC9odG1sO2NoYXJzZXQ9dXRmLTg="),
      'Cache-Control': 'no-store'
    }
  });
}
async function rvg(rvm) {
  const rvy = new URL(rvm.url);
  if (rvm.method === 'OPTIONS') {
    return new Response(null, {
      status: 204,
      headers: {
        'Access-Control-Allow-Origin': '*',
        'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
        'Access-Control-Allow-Headers': 'Content-Type, Accept',
        'Access-Control-Max-Age': '86400'
      }
    });
  }
  if (!['GET', 'POST'].includes(rvm.method)) {
    return new Response('Method not allowed. Use GET or POST.', {
      status: 405
    });
  }
  if (rvm.method === 'GET' && !rvy.searchParams.has('dns')) {
    const rvx = rvy.protocol + '//' + rvy.host + rvy.pathname;
    const rvv = `<!DOCTYPE html><html lang="fa" dir="rtl"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>DoH Proxy</title><style>:root{--primary:#0ea5e9;--bg:#f8fafc;--card:#fff;--text:#1e293b;--border:#e2e8f0}body{font-family:system-ui,sans-serif;background:var(--bg);color:var(--text);margin:0;padding:20px;display:flex;justify-content:center;align-items:center;min-height:100vh}.card{background:var(--card);border-radius:16px;padding:32px;max-width:560px;width:100%;box-shadow:0 4px 24px rgba(0,0,0,.08);border:1px solid var(--border);text-align:center}.badge{display:inline-block;background:linear-gradient(135deg,#0ea5e9,#667eea);color:#fff;padding:6px 16px;border-radius:20px;font-weight:700;font-size:14px;margin-bottom:16px}h2{margin:0 0 8px;font-size:22px;font-weight:800;background:linear-gradient(135deg,#0ea5e9,#667eea);-webkit-background-clip:text;-webkit-text-fill-color:transparent}p{color:#64748b;font-size:14px;line-height:1.7;margin:8px 0}.url-box{background:#f0f9ff;border:2px solid #0ea5e9;border-radius:12px;padding:14px 18px;margin:16px 0;direction:ltr;text-align:left;font-family:monospace;font-size:15px;font-weight:700;color:#0369a1;word-break:break-all;cursor:pointer;transition:all .2s}.url-box:hover{background:#e0f2fe}.btn{display:inline-flex;align-items:center;gap:6px;background:linear-gradient(135deg,#0ea5e9,#667eea);color:#fff;border:none;border-radius:10px;padding:10px 24px;font-size:14px;font-weight:600;font-family:inherit;cursor:pointer;transition:all .2s;margin-top:8px;text-decoration:none}.btn:hover{transform:translateY(-2px);box-shadow:0 4px 12px rgba(14,165,233,.3)}.features{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:16px;text-align:right}.feature{background:#f8fafc;border:1px solid var(--border);border-radius:8px;padding:10px 12px;font-size:12px;font-weight:500;color:#475569;display:flex;align-items:center;gap:6px}.feature i{color:#10b981}</style></head><body><div class="card"><div class="badge">DoH</div><h2>DNS-over-HTTPS Proxy</h2><p>Copy this URL and use it as your DoH server:</p><div class="url-box" onclick="navigator.clipboard.writeText(this.textContent);this.style.background='#dcfce7';this.style.borderColor='#10b981';this.style.color='#065f46';setTimeout(()=>{this.style.background='#f0f9ff';this.style.borderColor='#0ea5e9';this.style.color='#0369a1'},1500)">${rvx}</div><p style="font-size:12px;opacity:0.6">Click to copy</p><div class="features"><div class="feature">&#x2705; Cloudflare DNS</div><div class="feature">&#x2705; Google DNS</div><div class="feature">&#x2705; Quad9 DNS</div><div class="feature">&#x2705; AdGuard DNS</div></div><a class="btn" href="?dns=test" target="_blank">Test DoH &#x2192;</a></div></body></html>`;
    return new Response(rvv, {
      status: 200,
      headers: {
        'Content-Type': ik("dGV4dC9odG1sO2NoYXJzZXQ9dXRmLTg="),
        'Cache-Control': 'no-store'
      }
    });
  }
  const rvb = [{
    name: 'Cloudflare',
    url: 'https://cloudflare-dns.com/dns-query'
  }, {
    name: 'Google',
    url: 'https://dns.google/dns-query'
  }, {
    name: 'Quad9',
    url: 'https://dns.quad9.net/dns-query'
  }, {
    name: 'OpenDNS',
    url: 'https://doh.opendns.com/dns-query'
  }, {
    name: 'AdGuard',
    url: ik("aHR0cHM6Ly9kbnMuYWRndWFyZC5jb20vZG5zLXF1ZXJ5")
  }, {
    name: 'Mullvad',
    url: 'https://adblock.dns.mullvad.net/dns-query'
  }, {
    name: 'NextDNS',
    url: 'https://dns.nextdns.io/dns-query'
  }];
  const rvk = rvb[Math.floor(Math.random() * rvb.length)];
  const rv0 = rvm.method === 'POST' ? await rvm.arrayBuffer().catch(() => null) : null;
  try {
    const rv1 = new Headers(rvm.headers);
    rv1.set('User-Agent', 'DoH-Proxy/1.0');
    if (rvm.method === 'POST') {
      rv1.set('Content-Type', ik("YXBwbGljYXRpb24vZG5zLW1lc3NhZ2U="));
    } else {
      rv1.set('Accept', 'application/dns-message');
    }
    const rv2 = new Request(rvk.url + rvy.search, {
      method: rvm.method,
      headers: rv1,
      body: rv0,
      redirect: 'follow'
    });
    const rv3 = await fetch(rv2);
    const rv4 = new Headers(rv3.headers);
    rv4.set('Access-Control-Allow-Origin', '*');
    rv4.set(ik("QWNjZXNzLUNvbnRyb2wtQWxsb3ctTWV0aG9kcw=="), 'GET, POST, OPTIONS');
    rv4.set('Access-Control-Allow-Headers', ik("Q29udGVudC1UeXBlLCBBY2NlcHQ="));
    rv4.set('Cache-Control', ik("cHVibGljLCBtYXgtYWdlPTMwMA=="));
    return new Response(rv3.body, {
      status: rv3.status,
      statusText: rv3.statusText,
      headers: rv4
    });
  } catch (rv5) {
    for (let rv6 = 0; rv6 < rvb.length; rv6++) {
      if (rvb[rv6].url === rvk.url) continue;
      try {
        const rv7 = new Headers(rvm.headers);
        rv7.set('User-Agent', 'DoH-Proxy/1.0');
        if (rvm.method === 'POST') rv7.set('Content-Type', 'application/dns-message'); else rv7.set('Accept', 'application/dns-message');
        const rv8 = new Request(rvb[rv6].url + rvy.search, {
          method: rvm.method,
          headers: rv7,
          body: rv0,
          redirect: 'follow'
        });
        const rv9 = await fetch(rv8);
        const rbe = new Headers(rv9.headers);
        rbe.set('Access-Control-Allow-Origin', '*');
        rbe.set('Access-Control-Allow-Methods', 'GET, POST, OPTIONS');
        rbe.set('Access-Control-Allow-Headers', 'Content-Type, Accept');
        return new Response(rv9.body, {
          status: rv9.status,
          statusText: rv9.statusText,
          headers: rbe
        });
      } catch (rbt) {}
    }
    return new Response(ik("RG9IIHByb3h5IGVycm9yOiA=") + rv5.message, {
      status: 502,
      headers: {
        'Content-Type': 'text/plain',
        'Access-Control-Allow-Origin': '*'
      }
    });
  }
}
async function rbr(rbn, rbi, rbo, rbs = 'HTML') {
  const rba = `https://api.telegram.org/bot${rbn}/sendMessage?chat_id=${rbi}&parse_mode=${rbs}&text=${encodeURIComponent(rbo)}`;
  try {
    return await fetch(rba, {
      method: 'GET'
    });
  } catch (rbc) {
    console.error(ik("c2VuZEJvdE1lc3NhZ2UgZXJyb3I6"), rbc);
  }
}
function rbl(rbd, rbw) {
  return {
    inline_keyboard: [[{
      text: '📋 اشتراک',
      callback_data: 'cb_sub'
    }, {
      text: '📊 وضعیت',
      callback_data: 'cb_status'
    }], [{
      text: '⚙️ کانفیگ',
      callback_data: 'cb_config'
    }, {
      text: '🔄 منو',
      callback_data: 'cb_menu'
    }], [{
      text: '🖥 پنل مدیریت',
      url: rbd
    }, {
      text: '📥 باز کردن لینک',
      url: rbw
    }]]
  };
}
async function rbf(rbh, env, rbp, rbg) {
  try {
    const rbm = await env.KV.get('tg.json');
    if (!rbm) return new Response(ik("Qm90IG5vdCBjb25maWd1cmVk"), {
      status: 200
    });
    const rby = JSON.parse(rbm);
    if (!rby.BotToken || !rby.ChatID) return new Response(ik("Qm90IG5vdCBjb25maWd1cmVk"), {
      status: 200
    });
    const rbx = await rbh.json();
    if (rbx.callback_query) {
      const rbv = rbx.callback_query;
      const rbb = String(rbv.message && rbv.message.chat ? rbv.message.chat.id : '');
      if (rbb !== String(rby.ChatID)) return new Response('Unauthorized', {
        status: 200
      });
      const rbk = rbv.data || '';
      const rb0 = rbh.url.split('://')[0];
      const rb1 = `${rb0}://${rbg}/sub?token=${await rnd(rbg + rbp)}`;
      const rb2 = `${rb0}://${rbg}/admin`;
      const rb3 = await o4(env);
      const rb4 = rb3 ? JSON.parse(rb3) : {};
      let rb5;
      if (rbk === 'cb_sub') {
        rb5 = `<b>🔗 لینک اشتراک / Subscription</b>\n\n<code>${rb1}</code>`;
      } else if (rbk === 'cb_status') {
        const rb6 = rb4.CF && rb4.CF.Usage;
        let rb7 = '';
        if (rb6 && rb6.success) {
          const rb8 = rb6.total / rb6.max, rb9 = Math.round(rb8 * 12);
          rb7 = `\n📈 ${('🟩').repeat(rb9)}${('⬜').repeat(12 - rb9)} ${(rb8 * 100).toFixed(1)}%\n💠 ${rb6.total}/${rb6.max}`;
        }
        rb5 = `<b>📊 وضعیت / Status</b>\n\n🌐 Host: <code>${rbg}</code>\n🆔 UUID: <code>${rbp.slice(0, 8)}…</code>${rb7}`;
      } else if (rbk === 'cb_config') {
        rb5 = `<b>⚙️ کانفیگ / Config</b>\n\n🔸 protocol: <code>${rb4.protocolType || 'vless'}</code>\n🔸 transport: <code>${rb4.transportProtocol || 'ws'}</code>\n🔐 TLS: <code>${rb4.enableTLS !== false ? 'on' : 'off'}</code>\n🌐 Host: <code>${rbg}</code>`;
      } else {
        rb5 = `<b>✨ Nova Proxy</b>\n\nیک گزینه را انتخاب کنید / Choose an option:`;
      }
      try {
        await fetch(`https://api.telegram.org/bot${rby.BotToken}/answerCallbackQuery?callback_query_id=${rbv.id}`);
      } catch (rke) {}
      try {
        await fetch(`https://api.telegram.org/bot${rby.BotToken}/editMessageText?chat_id=${rbb}&message_id=${rbv.message.message_id}&parse_mode=HTML&text=${encodeURIComponent(rb5)}&reply_markup=${encodeURIComponent(JSON.stringify(rbl(rb2, rb1)))}`);
      } catch (rkt) {
        await rbr(rby.BotToken, rbb, rb5);
      }
      return new Response('OK', {
        status: 200
      });
    }
    if (!rbx.message || !rbx.message.text) return new Response('OK', {
      status: 200
    });
    const rkr = String(rbx.message.chat.id);
    if (rkr !== String(rby.ChatID)) return new Response('Unauthorized', {
      status: 200
    });
    const rkn = rbx.message.text.trim();
    const rki = rkn.split(' ')[0].toLowerCase();
    const rko = rkn.slice(rki.length).trim();
    const rks = await o4(env);
    const rka = rks ? JSON.parse(rks) : {};
    switch (rki) {
      case '/start':
      case '/menu':
      case '/help':
        {
          const rkc = rbh.url.split('://')[0];
          const rkl = `${rkc}://${rbg}/admin`;
          const rkd = await rnd(rbg + rbp);
          const rkw = `${rkc}://${rbg}/sub?token=${rkd}`;
          const rkf = `<b>╔═══❰✨ Nova Proxy Bot ❱═══╗</b>

<blockquote><b>📋 راهنما</b>
━━━━━━━━━━━━━━━━━━━
<code>/sub</code>         ─── دریافت لینک اشتراک
<code>/status</code>      ─── وضعیت ورکر و مصرف
<code>/config</code>      ─── نمایش خلاصه کانفیگ
<code>/sethost</code>    ─── تغییر host
<code>/setpath</code>    ─── تغییر مسیر
<code>/setname</code>    ─── تغییر نام اشتراک
<code>/setwebhook</code> ─── نصب Webhook
<code>/myid</code>       ─── Chat ID شما
<code>/help</code>       ─── این راهنما</blockquote>

<b>╚══════════════════════╝</b>`;
          const rkh = rbl(rkl, rkw);
          const rkp = `https://api.telegram.org/bot${rby.BotToken}/sendMessage?chat_id=${rkr}&parse_mode=HTML&text=${encodeURIComponent(rkf)}&reply_markup=${encodeURIComponent(JSON.stringify(rkh))}`;
          try {
            await fetch(rkp, {
              method: 'GET'
            });
          } catch (rkg) {
            console.error(ik("c2VuZEJvdE1lc3NhZ2UgZXJyb3I6"), rkg);
          }
          break;
        }
      case '/sub':
        {
          const rkm = await rnd(rbg + rbp);
          const rky = rbh.url.split('://')[0];
          const rkx = `${rky}://${rbg}/sub?token=${rkm}`;
          const rkv = `<b>╔═══❰🔗 اشتراک ❱═══╗</b>

<blockquote><b>📎 لینک اشتراک شما:</b>
<code>${rkx}</code></blockquote>

<b>📥 <a href="${rkx}">باز کردن مستقیم</a></b>

<b>╚════════════════╝</b>`;
          await rbr(rby.BotToken, rkr, rkv);
          break;
        }
      case '/status':
        {
          const rkb = Date.now() - (globalThis.__workerStart || Date.now());
          const rkk = `${Math.floor(rkb / 3600000)}h ${Math.floor(rkb % 3600000 / 60000)}m`;
          const rk0 = 12;
          let rk1 = `<b>╔═══❰📊 وضعیت سرور ❱═══╗</b>

<blockquote>⏱ <b>آپتایم:</b> <code>${rkk}</code>
🆔 <b>UUID:</b> <code>${rbp.slice(0, 8)}...</code>
🌐 <b>Host:</b> <code>${rbg}</code>
📁 <b>مسیر:</b> <code>${rka.PATH || '/'}</code></blockquote>`;
          const rk2 = rka.CF?.Usage;
          if (rk2?.success) {
            const rk3 = rk2.total / rk2.max;
            const rk4 = Math.round(rk3 * rk0);
            const rk5 = rk0 - rk4;
            const rk6 = ('🟩').repeat(rk4) + ('⬜').repeat(rk5);
            rk1 += `\n<blockquote><b>📈 مصرف Cloudflare</b>\n${rk6} <b>${(rk3 * 100).toFixed(1)}%</b>\n━━━━━━━━━━━━━━━━━━━\n📄 Pages: <code>${rk2.pages}</code>\n⚙️ Workers: <code>${rk2.workers}</code>\n💠 مجموع: <code>${rk2.total}/${rk2.max}</code></blockquote>`;
          }
          rk1 += `\n<b>╚══════════════════════╝</b>`;
          await rbr(rby.BotToken, rkr, rk1);
          break;
        }
      case '/config':
        {
          const rk7 = rka.protocolType || 'vless';
          const rk8 = rka.transportProtocol || 'ws';
          const rk9 = r0e => r0e ? '🟢 فعال' : '🔴 غیرفعال';
          const r0t = rka.tlsFragment || '🔴 غیرفعال';
          let r0r = `<b>╔═══❰⚙️ تنظیمات ❱═══╗</b>

<blockquote><b>📡 شبکه</b>
━━━━━━━━━━━━━━━━━━━
<b>پروتکل:</b> <code>${rk7}</code>  |  <b>نقل:</b> <code>${rk8}</code>
<b>Host:</b> <code>${rka.HOST || rbg}</code>
<b>مسیر:</b> <code>${rka.PATH || '/'}</code>
<b>Fingerprint:</b> <code>${rka.Fingerprint || 'chrome'}</code></blockquote>

<blockquote><b>🔐 امنیت</b>
━━━━━━━━━━━━━━━━━━━
<b>Skip Verify:</b> ${rk9(rka.skipCertVerify)}
<b>ECH:</b> ${rk9(rka.ECH)}
<b>0-RTT:</b> ${rk9(rka.enable0RTT)}
<b>TLS Fragment:</b> ${r0t}</blockquote>

<blockquote><b>🧩 ویژگی‌ها</b>
━━━━━━━━━━━━━━━━━━━
<b>Dual Protocol:</b> ${rk9(rka.dualProtocol)}
<b>TG Bot:</b> ${rk9(rka.TG?.enable)}
<b>نام اشتراک:</b> <code>${rka.optimizedSubGeneration?.SUBNAME || '-'}</code></blockquote>

<b>╚══════════════════════╝</b>`;
          await rbr(rby.BotToken, rkr, r0r);
          break;
        }
      case '/sethost':
        {
          if (!rko) {
            await rbr(rby.BotToken, rkr, `<b>╔═══❰⚠️ خطا ❱═══╗</b>\n\n<blockquote>لطفا host را وارد کنید:\n<code>/sethost example.com</code></blockquote>\n<b>╚══════════════╝</b>`);
            break;
          }
          rka.HOST = rko.trim().toLowerCase().replace(/^https?:\/\//, '').split('/')[0].split(':')[0];
          if (!rka.HOSTS) rka.HOSTS = [];
          if (!rka.HOSTS.includes(rka.HOST)) rka.HOSTS.unshift(rka.HOST);
          await o6(env, JSON.stringify(rka, null, 2));
          await rbr(rby.BotToken, rkr, `<b>╔═══❰✅ موفق ❱═══╗</b>\n\n<blockquote>Host به <code>${rka.HOST}</code> تغییر یافت</blockquote>\n<b>╚══════════════╝</b>`);
          break;
        }
      case '/setpath':
        {
          if (!rko) {
            await rbr(rby.BotToken, rkr, `<b>╔═══❰⚠️ خطا ❱═══╗</b>\n\n<blockquote>لطفا مسیر را وارد کنید:\n<code>/setpath /api</code></blockquote>\n<b>╚══════════════╝</b>`);
            break;
          }
          rka.PATH = rko.startsWith('/') ? rko : '/' + rko;
          await o6(env, JSON.stringify(rka, null, 2));
          await rbr(rby.BotToken, rkr, `<b>╔═══❰✅ موفق ❱═══╗</b>\n\n<blockquote>مسیر به <code>${rka.PATH}</code> تغییر یافت</blockquote>\n<b>╚══════════════╝</b>`);
          break;
        }
      case '/setname':
        {
          if (!rko) {
            await rbr(rby.BotToken, rkr, `<b>╔═══❰⚠️ خطا ❱═══╗</b>\n\n<blockquote>لطفا نام را وارد کنید:\n<code>/setname MyConfig</code></blockquote>\n<b>╚══════════════╝</b>`);
            break;
          }
          if (!rka.optimizedSubGeneration) rka.optimizedSubGeneration = {};
          rka.optimizedSubGeneration.SUBNAME = rko;
          await o6(env, JSON.stringify(rka, null, 2));
          await rbr(rby.BotToken, rkr, `<b>╔═══❰✅ موفق ❱═══╗</b>\n\n<blockquote>نام اشتراک به <code>${rko}</code> تغییر یافت</blockquote>\n<b>╚══════════════╝</b>`);
          break;
        }
      case '/myid':
        {
          await rbr(rby.BotToken, rkr, `<b>╔═══❰🆔 Chat ID ❱═══╗</b>\n\n<blockquote><code>${rkr}</code></blockquote>\n<b>╚══════════════╝</b>`);
          break;
        }
      case '/setwebhook':
        {
          const r0n = `${rbh.url.split('://')[0]}://${rbh.url.split('/')[2]}`;
          const r0i = `https://api.telegram.org/bot${rby.BotToken}/setWebhook?url=${encodeURIComponent(r0n + '/bot')}&drop_pending_updates=true`;
          const r0o = await fetch(r0i);
          const r0s = await r0o.json();
          const r0a = r0s.ok ? `<b>╔═══❰✅ Webhook ❱═══╗</b>\n\n<blockquote>Webhook با موفقیت نصب شد!\n\n🌐 <code>${r0n}/bot</code></blockquote>\n<b>╚══════════════════╝</b>` : `<b>╔═══❰❌ خطا ❱═══╗</b>\n\n<blockquote>خطا در نصب Webhook:\n<code>${JSON.stringify(r0s)}</code></blockquote>\n<b>╚══════════════╝</b>`;
          await rbr(rby.BotToken, rkr, r0a);
          break;
        }
      default:
        {
          await rbr(rby.BotToken, rkr, `<b>╔═══❰❌ خطا ❱═══╗</b>\n\n<blockquote>دستور ناشناخته.\nبرای راهنما <code>/help</code> را بزنید.</blockquote>\n<b>╚══════════════╝</b>`);
        }
    }
  } catch (r0c) {
    console.error(ik("VGVsZWdyYW0gd2ViaG9vayBlcnJvcjo="), r0c);
  }
  return new Response('OK', {
    status: 200
  });
}
const r0l = ik("aHR0cHM6Ly9hcGkuY2xvdWRmbGFyZS5jb20vY2xpZW50L3Y0");
function r0d(r0w, r0f) {
  return Object.assign({
    'Authorization': 'Bearer ' + r0w
  }, r0f || ({}));
}
async function r0h(r0p) {
  let r0g = null;
  try {
    r0g = await r0p.json();
  } catch (r0m) {}
  return r0g;
}
async function r0y(r0x) {
  const r0v = await fetch(r0l + ik("L3VzZXIvdG9rZW5zL3ZlcmlmeQ=="), {
    headers: r0d(r0x)
  });
  const r0b = await r0h(r0v);
  return {
    ok: !!(r0b && r0b.success && r0b.result && r0b.result.status === 'active'),
    raw: r0b
  };
}
async function r0k(r00) {
  const r01 = await fetch(r0l + '/accounts', {
    headers: r0d(r00)
  });
  const r02 = await r0h(r01);
  if (!r02 || !r02.success || !Array.isArray(r02.result)) return [];
  return r02.result.map(r03 => ({
    id: r03.id,
    name: r03.name
  }));
}
async function r04(env) {
  try {
    const r05 = env.KV && typeof env.KV.get === 'function' ? await o4(env) : null;
    const r06 = r05 ? JSON.parse(r05) : null;
    if (r06 && Array.isArray(r06.HOSTS) && r06.HOSTS.length) return [...new Set(r06.HOSTS.filter(Boolean))];
    if (r06 && r06.HOST) return [r06.HOST];
  } catch (r07) {}
  return [];
}
async function r08(r09, r1e, r1t, r1r = 'HTML', r1n = null) {
  if (!r09 || !r1e) return null;
  let r1i = null;
  const r1o = 4000;
  const r1s = [];
  for (let r1a = 0; r1a < r1t.length; r1a += r1o) r1s.push(r1t.slice(r1a, r1a + r1o));
  for (const r1c of r1s) {
    const r1l = `https://api.telegram.org/bot${r09}/sendMessage?chat_id=${encodeURIComponent(r1e)}&text=${encodeURIComponent(r1c)}&parse_mode=${r1r}`;
    try {
      r1i = await fetch(r1l, {
        method: 'GET'
      });
    } catch (r1d) {
      console.error('sendBotMessage error:', r1d);
    }
  }
  return r1i;
}
async function r1w(r1f) {
  const r1h = [{
    command: 'start',
    description: 'منو / Menu'
  }, {
    command: 'sub',
    description: 'لینک اشتراک'
  }, {
    command: 'status',
    description: 'وضعیت و مصرف'
  }, {
    command: 'config',
    description: 'خلاصه تنظیمات'
  }, {
    command: 'hosts',
    description: 'دامنه‌ها'
  }, {
    command: 'addhost',
    description: 'افزودن دامنه'
  }, {
    command: 'delhost',
    description: 'حذف دامنه'
  }, {
    command: 'announce',
    description: 'ارسال به کانال'
  }, {
    command: 'pause',
    description: '🚨 توقف اضطراری سرویس'
  }, {
    command: 'resume',
    description: ik("4pyFINin2LLYs9ix2q/bjNix24wg2LPYsdmI24zYsw==")
  }, {
    command: 'install',
    description: ik("8J+agCDZhti12Kgg2b7ZhtmEINix2YjbjCBDbG91ZGZsYXJl")
  }, {
    command: 'help',
    description: 'راهنما'
  }];
  try {
    await fetch(`https://api.telegram.org/bot${r1f}/setMyCommands`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        commands: r1h
      })
    });
  } catch (r1p) {}
}
async function r1g(env, r1m = {}) {
  try {
    const r1y = env.KV && typeof env.KV.get === 'function' ? await env.KV.get('tg.json') : null;
    if (!r1y) return {
      skipped: true,
      reason: ik("VGVsZWdyYW0gbm90IGNvbmZpZ3VyZWQ=")
    };
    const r1x = JSON.parse(r1y);
    const r1v = String(env.ANNOUNCE_CHAT || r1x.ChatID || '').trim();
    if (!r1x.BotToken || !r1v) return {
      skipped: true,
      reason: 'BotToken/ChatID missing'
    };
    const r1b = r1m.baseUrl || '';
    const r1k = [ik("PGI+8J+UpSDZhNuM2YbaqeKAjNmH2KfbjCDYp9i02KrYsdin2qkgTm92YSAvIE5vdmEgc3Vic2NyaXB0aW9uIGxpbmtzPC9iPg=="), ''];
    if (r1b) {
      r1k.push('<b>⚡️ لینک مستقیم (بهینه per-ISP) / Live (per-ISP optimized)</b>');
      r1k.push(`<code>${r1b}/sub/mihomo.yaml</code>`);
      r1k.push(`<code>${r1b}/sub/base64.txt</code>`);
      r1k.push('');
    }
    if (r1m.health && Array.isArray(r1m.health.domains) && r1m.health.domains.length) {
      const r10 = r1m.health.domains.filter(r11 => r11.ok).length;
      r1k.push(`<b>🌐 دامنه‌ها / Domains:</b> ${r10}/${r1m.health.domains.length} ✅`);
      r1k.push('');
    }
    r1k.push('<i>محتوای همه لینک‌ها یکی است؛ اگر یکی فیلتر شد، لینک گیت‌هاب همیشه کار می‌کند.</i>');
    await r08(r1x.BotToken, r1v, r1k.join('\n'));
    return {
      skipped: false,
      chatId: r1v
    };
  } catch (r12) {
    return {
      skipped: true,
      reason: r12 && r12.message ? r12.message : String(r12)
    };
  }
}
async function r13(env, r14) {
  try {
    const r15 = env.KV && typeof env.KV.get === 'function' ? await env.KV.get('github-mirror.json') : null;
    if (!r15) return {
      skipped: true,
      reason: ik("R2l0SHViIG1pcnJvciBub3QgY29uZmlndXJlZA==")
    };
    const r16 = JSON.parse(r15);
    if (!r16.token || !r16.repo) return {
      skipped: true,
      reason: 'GitHub token/repo missing'
    };
    if (!r14) return {
      skipped: true,
      reason: 'no baseUrl'
    };
    const r17 = [];
    const r18 = [{
      name: 'base64.txt',
      q: 'b64'
    }, {
      name: 'mihomo.yaml',
      q: 'clash'
    }, {
      name: 'singbox.json',
      q: 'singbox'
    }];
    for (const r19 of r18) {
      try {
        const r2e = await fetch(`${r14}/sub?token=${await rnd(r14.replace(/^https?:\/\//, '') + (env.UUID || ''))}&${r19.q}`, {
          headers: {
            'User-Agent': 'NovaMirror/1.0'
          }
        });
        if (!r2e.ok) {
          r17.push({
            file: r19.name,
            ok: false,
            status: r2e.status
          });
          continue;
        }
        const r2t = await r2e.text();
        if (!r2t || r2t.length < 8) {
          r17.push({
            file: r19.name,
            ok: false,
            error: 'empty response'
          });
          continue;
        }
        const r2r = (r16.pathPrefix ? r16.pathPrefix + '/' : '') + r19.name;
        const r2n = `https://api.github.com/repos/${r16.repo}/contents/${r2r}`;
        const r2i = await fetch(r2n, {
          headers: {
            'Authorization': 'token ' + r16.token,
            'User-Agent': 'NovaMirror'
          }
        });
        const r2o = await r2i.json().catch(() => ({}));
        const r2s = r2o && r2o.sha ? r2o.sha : undefined;
        const r2a = {
          message: `Nova: update ${r19.name}`,
          content: btoa(unescape(encodeURIComponent(r2t))),
          branch: r16.branch || 'main'
        };
        if (r2s) r2a.sha = r2s;
        const r2c = await fetch(r2n, {
          method: 'PUT',
          headers: {
            'Authorization': 'token ' + r16.token,
            'Content-Type': 'application/json',
            'User-Agent': 'NovaMirror'
          },
          body: JSON.stringify(r2a)
        });
        r17.push({
          file: r19.name,
          ok: r2c.ok,
          status: r2c.status
        });
      } catch (r2l) {
        r17.push({
          file: r19.name,
          ok: false,
          error: r2l.message
        });
      }
    }
    return {
      skipped: false,
      results: r17
    };
  } catch (r2d) {
    return {
      skipped: true,
      reason: r2d.message
    };
  }
}
function r2w() {
  const r2f = `<!DOCTYPE html><html lang="fa" dir="rtl" data-theme="dark"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Nova Radar</title><style>
:root{--bg:#070809;--panel:#0c0e12;--card:#101319;--card2:#0b0d11;--bd:#1c2027;--bd2:#262b34;--tx:#e9edf4;--tx2:#aeb6c4;--mu:#6f7888;--ac:#22d3ee;--ac2:#a855f7;--ok:#34d399;--wn:#f5b042;--dg:#f87171;--grad:linear-gradient(120deg,#22d3ee,#7c5cff);--r:12px;--rs:9px;--ac-soft:color-mix(in srgb,var(--ac) 14%,transparent);--ac-line:color-mix(in srgb,var(--ac) 38%,transparent)}
html[data-theme=light]{--bg:#f4f6fb;--panel:#fff;--card:#fff;--card2:#f7f9fc;--bd:#e6eaf1;--bd2:#dde2eb;--tx:#101622;--tx2:#3a465c;--mu:#5f6a7d;--ac:#0ea5c4;--ac2:#7c3aed;--grad:linear-gradient(120deg,#0891b2,#7c3aed);--ok:#047857;--wn:#b45309;--dg:#dc2626}
*{margin:0;padding:0;box-sizing:border-box}
body{background:var(--bg);color:var(--tx);min-height:100vh;font-size:14px;font-family:'Vazirmatn','Inter',system-ui,sans-serif;-webkit-font-smoothing:antialiased;padding:22px 16px 60px}
.wrap{max-width:640px;margin:0 auto}
.topbar{display:flex;align-items:center;gap:12px;margin-bottom:18px}
.brand{display:flex;align-items:center;gap:11px}
.brand .name{font-size:17px;font-weight:800}
.brand .env{font-size:10.5px;color:var(--mu);font-weight:600;margin-top:2px}
.brand .env .d{width:6px;height:6px;border-radius:50%;background:var(--ok);display:inline-block;margin-left:4px}
.seg{display:flex;border:1px solid var(--bd);background:var(--card);border-radius:var(--rs);padding:3px;height:36px}
.seg button{border:none;background:transparent;color:var(--mu);font:inherit;font-size:12px;font-weight:700;padding:0 11px;border-radius:6px;cursor:pointer}
.seg button.on{background:var(--ac);color:#04121a}
.card{background:var(--card);border:1px solid var(--bd);border-radius:var(--r);padding:20px 18px;margin-bottom:16px}
.card.hero{border-color:var(--ac-line)}
.hsub{color:var(--tx2);font-size:13px;line-height:1.7;text-align:center;margin-top:2px}
.pill{display:block;width:max-content;margin:12px auto 0;padding:5px 14px;border-radius:20px;font-size:12px;font-weight:700;text-align:center}
.pill.ok{background:var(--ac-soft);color:var(--ac);border:1px solid var(--ac-line)}
.pill.warn{background:color-mix(in srgb,var(--wn) 15%,transparent);color:var(--wn);border:1px solid color-mix(in srgb,var(--wn) 35%,transparent)}
.row{display:flex;gap:10px;flex-wrap:wrap;margin:18px 0 14px}
.fg{flex:1;min-width:120px}
.fg label{display:block;font-size:11px;font-weight:600;color:var(--mu);margin-bottom:6px}
.fg input{width:100%;background:var(--card2);border:1px solid var(--bd);border-radius:var(--rs);color:var(--tx);padding:10px 12px;font-size:14px;font-family:inherit}
.fg input:focus{outline:none;border-color:var(--ac)}
.portchips{display:flex;flex-wrap:wrap;gap:8px;margin:4px 0}
.pchip{background:var(--card2);border:1px solid var(--bd);border-radius:999px;color:var(--tx2);padding:7px 14px;font-size:13px;font-weight:700;font-family:inherit;cursor:pointer;transition:.12s}
.pchip.on{background:var(--ac);border-color:var(--ac);color:#04121a;font-weight:800}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;width:100%;height:46px;border-radius:var(--rs);font:inherit;font-size:15px;font-weight:700;border:1px solid transparent;cursor:pointer;transition:.13s}
.btn.primary{background:var(--grad);color:#04121a}
.btn.primary:disabled{opacity:.55;cursor:default}
.btn.ghost{background:var(--card2);color:var(--tx);border-color:var(--bd2);margin-top:10px}
.bar{height:8px;border-radius:999px;background:var(--card2);border:1px solid var(--bd);overflow:hidden;margin:14px 0;display:none}
.bar i{display:block;height:100%;width:0;background:var(--grad);transition:width .25s}
.msg{text-align:center;font-size:12.5px;color:var(--tx2);min-height:18px}
table{width:100%;border-collapse:collapse;margin-top:4px;font-size:13px}
th,td{padding:9px 6px;text-align:center;border-bottom:1px solid var(--bd)}
th{color:var(--mu);font-weight:600;font-size:11px}
td code{font-family:ui-monospace,monospace;direction:ltr;color:var(--ac)}
tr.best td{background:var(--ac-soft)}
.rank{font-weight:800;color:var(--ok)}
.out{margin-top:16px;display:none}
.out-h{font-size:13px;font-weight:700;margin-bottom:8px}
.cfg{background:var(--card2);border:1px solid var(--bd);border-radius:var(--rs);padding:11px;font-family:ui-monospace,monospace;font-size:10.5px;direction:ltr;text-align:left;word-break:break-all;color:var(--tx2);max-height:104px;overflow:auto;margin-bottom:8px}
.hint{font-size:11.5px;color:var(--mu);line-height:1.7;margin-top:12px;text-align:center;display:none}
.foot{text-align:center;color:var(--mu);font-size:11.5px;margin-top:4px}
.toast{position:fixed;inset-inline:0;bottom:22px;margin:auto;width:max-content;max-width:90%;background:var(--ac);color:#04121a;font-weight:700;padding:9px 18px;border-radius:999px;font-size:13px;opacity:0;transform:translateY(8px);transition:.2s;pointer-events:none;z-index:20}
.toast.show{opacity:1;transform:none}
</style></head><body><div class="wrap">
<header class="topbar">
<div class="brand"><div><div class="name">Nova Radar</div><div class="env"><span class="d"></span>clean-IP scanner</div></div></div>
<div style="margin-inline-start:auto;display:flex;gap:8px"><div class="seg" id="lang"><button data-l="en">EN</button><button data-l="fa">فا</button></div><div class="seg" id="theme"><button data-t="light">&#9728;</button><button data-t="dark">&#9790;</button></div></div>
</header>
<div class="card hero">
<div class="hsub">سریع‌ترین آی‌پی تمیز شبکه‌تان را پیدا کنید</div>
<span id="pill" class="pill warn"></span>
<div class="row">
<div class="fg"><label>تعداد IP تست</label><input id="s-total" type="number" min="20" max="400" value="140"></div>
<div class="fg"><label>نگه‌داشتن بهترین</label><input id="s-keep" type="number" min="1" max="30" value="8"></div>
</div>
<div><label style="display:block;font-size:11px;font-weight:600;color:var(--mu);margin-bottom:8px">پورت‌ها</label><div class="portchips" id="s-ports"><button type="button" class="pchip on" data-port="443">443</button><button type="button" class="pchip" data-port="8443">8443</button><button type="button" class="pchip" data-port="2053">2053</button><button type="button" class="pchip" data-port="2083">2083</button><button type="button" class="pchip" data-port="2087">2087</button><button type="button" class="pchip" data-port="2096">2096</button></div></div>
<button class="btn primary" id="s-run">🚀 شروع اسکن</button>
<div class="bar" id="s-bar"><i></i></div>
<div class="msg" id="s-msg" role="status"></div>
</div>
<div class="card" id="s-rc" style="display:none">
<div id="s-results"></div>
<div class="out" id="s-out">
<div class="out-h">⚡ کانفیگ با بهترین IP</div>
<div class="cfg" id="s-cfg"></div>
<button class="btn primary" id="s-copy">📋 کپی کانفیگ</button>
</div>
<div class="hint" id="s-hint"></div>
</div>
<div class="foot">کاملاً در مرورگر شما اجرا می‌شود، چیزی از دستگاه‌تان خارج نمی‌شود</div>
</div>
<div class="toast" id="toast"></div>
<script>
var lang=(function(){try{return localStorage.getItem('nova-user-lang')==='en'?'en':'fa';}catch(e){return 'fa';}})();
var theme=(function(){try{return localStorage.getItem('nova-theme')==='light'?'light':'dark';}catch(e){return 'dark';}})();
function $(id){return document.getElementById(id);}
function applyLang(){document.documentElement.lang=lang;document.documentElement.dir=lang==='fa'?'rtl':'ltr';
var lb=document.querySelectorAll('#lang button');for(var j=0;j<lb.length;j++)lb[j].classList.toggle('on',lb[j].dataset.l===lang);}
function applyTheme(){document.documentElement.dataset.theme=theme;var tb=document.querySelectorAll('#theme button');for(var i=0;i<tb.length;i++)tb[i].classList.toggle('on',tb[i].dataset.t===theme);}
function toast(t){var e=$('toast');e.textContent=t;e.classList.add('show');setTimeout(function(){e.classList.remove('show');},1800);}
function copy(t){if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(t).then(function(){toast('کپی شد');});}else{var a=document.createElement('textarea');a.value=t;document.body.appendChild(a);a.select();try{document.execCommand('copy');toast('کپی شد');}catch(e){}a.remove();}}
var CF_RANGES=[['104.16.',0,255],['104.17.',0,255],['104.18.',0,255],['104.19.',0,255],['104.20.',0,255],['104.21.',0,255],['104.22.',0,255],['104.24.',0,255],['104.25.',0,255],['104.26.',0,255],['104.27.',0,255],['162.159.',0,255],['172.64.',0,255],['172.66.',0,255],['172.67.',0,255],['188.114.',96,111],['141.101.',64,127]];
function randCfIp(){var r=CF_RANGES[Math.floor(Math.random()*CF_RANGES.length)];var c=r[1]+Math.floor(Math.random()*(r[2]-r[1]+1));return r[0]+c+'.'+Math.floor(Math.random()*256);}
function pingIp(ip,port,timeout){return new Promise(function(res){var t0=performance.now();var done=false;var img=new Image();function fin(ok){if(done)return;done=true;img.onerror=img.onload=null;res(ok?Math.round(performance.now()-t0):null);}var timer=setTimeout(function(){fin(false);},timeout);img.onerror=function(){clearTimeout(timer);fin(true);};img.onload=function(){clearTimeout(timer);fin(true);};img.src='https://'+(port==443?ip:ip+':'+port)+'/cdn-cgi/trace?'+Math.random();});}
var TMPL=null,BEST=[];
async function loadTemplate(){
var token=new URLSearchParams(location.search).get('token');
if(!token){$('pill').className='pill warn';$('pill').textContent='بدون توکن، فقط بهترین IP نمایش داده می‌شود';return;}
$('pill').className='pill warn';$('pill').textContent='در حال بررسی اشتراک…';
try{
var r=await fetch(location.origin+'/sub?token='+encodeURIComponent(token)+'&_t='+Date.now());
var txt=await r.text();var data=txt;
try{var dec=atob(txt.replace(/\\s+/g,''));if(dec.indexOf('://')>0)data=dec;}catch(e){}
var lines=data.split(/[\\r\\n]+/);var v=null;
for(var i=0;i<lines.length;i++){if(lines[i].trim().indexOf('vless://')===0){v=lines[i].trim();break;}}
if(!v){$('pill').className='pill warn';$('pill').textContent='کانفیگ پیدا نشد';return;}
var m=v.match(/^vless:\\/\\/([^@]+)@([^:]+):(\\d+)\\?([^#]*)/);
if(!m){$('pill').className='pill warn';$('pill').textContent='اشتراک در دسترس نبود';return;}
TMPL={uuid:m[1],port:m[3],query:m[4]};
var pc=$('s-ports');if(pc){var tp=String(m[3]);if(!pc.querySelector('[data-port="'+tp+'"]')){var nb=document.createElement('button');nb.type='button';nb.className='pchip';nb.setAttribute('data-port',tp);nb.textContent=tp;pc.insertBefore(nb,pc.firstChild);}var tc=pc.querySelector('[data-port="'+tp+'"]');if(tc)tc.classList.add('on');}
$('pill').className='pill ok';$('pill').textContent='✓ اشتراک شناسایی شد';
}catch(e){$('pill').className='pill warn';$('pill').textContent='اشتراک در دسترس نبود';}
}
function buildConfig(ip,port){if(!TMPL)return null;port=port||TMPL.port||443;return 'vless://'+TMPL.uuid+'@'+ip+':'+port+'?'+TMPL.query+'#'+encodeURIComponent('Nova ⚡ '+ip+':'+port);}
async function run(){
var btn=$('s-run'),msg=$('s-msg'),bar=$('s-bar'),pf=bar.querySelector('i');
var total=Math.min(400,Math.max(20,Number($('s-total').value)||140));
var keep=Math.min(30,Math.max(1,Number($('s-keep').value)||8));
var ports=Array.prototype.map.call($('s-ports').querySelectorAll('.pchip.on'),function(b){return Number(b.getAttribute('data-port'));}).filter(Boolean);
if(!ports.length){ports=[443];}
var timeout=2000,probes=3,conc=12;
btn.disabled=true;bar.style.display='block';pf.style.width='0%';$('s-rc').style.display='none';
msg.textContent='در حال آماده‌سازی…';
var maxIps=Math.min(total,Math.max(20,Math.floor(600/ports.length)));
var ips=[],seen={};while(ips.length<maxIps){var ip=randCfIp();if(!seen[ip]){seen[ip]=1;ips.push(ip);}}
var pairs=[];for(var a=0;a<ips.length;a++){for(var pi=0;pi<ports.length;pi++){pairs.push({ip:ips[a],port:ports[pi]});}}
var totalN=pairs.length,tested=0,alive=[];
async function worker(){while(pairs.length){var pr=pairs.pop();var s=[];for(var i=0;i<probes;i++){var ms=await pingIp(pr.ip,pr.port,timeout);if(ms!=null)s.push(ms);}tested++;
if(s.length){var avg=Math.round(s.reduce(function(a,b){return a+b;},0)/s.length);var jit=Math.round(Math.max.apply(null,s)-Math.min.apply(null,s));var loss=Math.round((1-s.length/probes)*100);alive.push({ip:pr.ip,port:pr.port,ms:avg,jit:jit,loss:loss,score:avg+jit*0.5+loss*20});}
pf.style.width=Math.max(3,Math.round(tested/totalN*100))+'%';
msg.textContent='در حال تست… '+tested+'/'+totalN+' ('+alive.length+' سالم)';}}
var pool=[];for(var k=0;k<conc;k++)pool.push(worker());await Promise.all(pool);
alive.sort(function(a,b){return a.score-b.score;});BEST=alive.slice(0,keep);
pf.style.width='100%';setTimeout(function(){bar.style.display='none';},500);btn.disabled=false;
if(!BEST.length){msg.textContent='هیچ IP سالمی پیدا نشد';return;}
msg.textContent='✓ '+BEST.length+' IP سریع پیدا شد';
var rows='';for(var i=0;i<BEST.length;i++){var b=BEST[i];rows+='<tr class="'+(i===0?'best':'')+'"><td class="rank">'+(i+1)+'</td><td><code>'+b.ip+':'+b.port+'</code></td><td>'+b.ms+' ms</td><td>'+b.jit+' ms</td><td>'+b.loss+'%</td></tr>';}
$('s-results').innerHTML='<table><thead><tr><th>#</th><th>IP</th><th>تأخیر</th><th>جیتر</th><th>افت</th></tr></thead><tbody>'+rows+'</tbody></table>';
$('s-rc').style.display='block';
if(TMPL){var cfg=buildConfig(BEST[0].ip,BEST[0].port);$('s-cfg').textContent=cfg;$('s-out').style.display='block';$('s-hint').style.display='none';}
else{$('s-out').style.display='none';$('s-hint').textContent='این صفحه را با ?token= خود باز کنید';$('s-hint').style.display='block';}
}
$('lang').addEventListener('click',function(e){var b=e.target.closest('button');if(!b)return;lang=b.dataset.l;try{localStorage.setItem('nova-user-lang',lang);}catch(_){}applyLang();});
$('theme').addEventListener('click',function(e){var b=e.target.closest('button');if(!b)return;theme=b.dataset.t;try{localStorage.setItem('nova-theme',theme);}catch(_){}applyTheme();});
$('s-ports').addEventListener('click',function(e){var b=e.target.closest('.pchip');if(!b)return;b.classList.toggle('on');});
$('s-run').addEventListener('click',run);
$('s-copy').addEventListener('click',function(){if(TMPL&&BEST.length)copy(buildConfig(BEST[0].ip,BEST[0].port));});
applyTheme();applyLang();loadTemplate();
</script></body></html>`;
  return new Response(r2f, {
    status: 200,
    headers: {
      'Content-Type': 'text/html;charset=utf-8',
      'Cache-Control': 'no-store'
    }
  });
}
async function r2h() {
  return `
	<!DOCTYPE html>
	<html>
	<head>
	<title>Welcome to nginx!</title>
	<style>
		body {
			width: 35em;
			margin: 0 auto;
			font-family: Tahoma, Verdana, Arial, sans-serif;
		}
	</style>
	</head>
	<body>
	<h1>Welcome to nginx!</h1>
	<p>If you see this page, the nginx web server is successfully installed and
	working. Further configuration is required.</p>

	<p>For online documentation and support please refer to
	<a href="http://nginx.org/">nginx.org</a>.<br/>
	Commercial support is available at
	<a href="http://nginx.com/">nginx.com</a>.</p>

	<p><em>Thank you for using nginx.</em></p>
	</body>
	</html>
	`;
}
async function r2p(r2g, r2m) {
  const r2y = new Date();
  const r2x = r2y.getFullYear() + '-' + String(r2y.getMonth() + 1).padStart(2, '0') + '-' + String(r2y.getDate()).padStart(2, '0') + ' ' + String(r2y.getHours()).padStart(2, '0') + ':' + String(r2y.getMinutes()).padStart(2, '0') + ':' + String(r2y.getSeconds()).padStart(2, '0');
  const r2v = Array.from(crypto.getRandomValues(new Uint8Array(8))).map(r2b => r2b.toString(16).padStart(2, '0')).join('');
  return `<!DOCTYPE html>
<!--[if lt IE 7]> <html class="no-js ie6 oldie" lang="en-US"> <![endif]-->
<!--[if IE 7]>    <html class="no-js ie7 oldie" lang="en-US"> <![endif]-->
<!--[if IE 8]>    <html class="no-js ie8 oldie" lang="en-US"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="en-US"> <!--<![endif]-->
<head>
<title>Worker threw exception | ${r2g} | Cloudflare</title>
<meta charset="UTF-8" />
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<meta http-equiv="X-UA-Compatible" content="IE=Edge" />
<meta name="robots" content="noindex, nofollow" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<link rel="stylesheet" id="cf_styles-css" href="/cdn-cgi/styles/cf.errors.css" />
<!--[if lt IE 9]><link rel="stylesheet" id='cf_styles-ie-css' href="/cdn-cgi/styles/cf.errors.ie.css" /><![endif]-->
<style>body{margin:0;padding:0}</style>


<!--[if gte IE 10]><!-->
<script>
  if (!navigator.cookieEnabled) {
    window.addEventListener('DOMContentLoaded', function () {
      var cookieEl = document.getElementById('cookie-alert');
      cookieEl.style.display = 'block';
    })
  }
</script>
<!--<![endif]-->

</head>
<body>
    <div id="cf-wrapper">
        <div class="cf-alert cf-alert-error cf-cookie-error" id="cookie-alert" data-translate="enable_cookies">Please enable cookies.</div>
        <div id="cf-error-details" class="cf-error-details-wrapper">
            <div class="cf-wrapper cf-header cf-error-overview">
                <h1>
                    <span class="cf-error-type" data-translate="error">Error</span>
                    <span class="cf-error-code">1101</span>
                    <small class="heading-ray-id">Ray ID: ${r2v} &bull; ${r2x} UTC</small>
                </h1>
                <h2 class="cf-subheadline" data-translate="error_desc">Worker threw exception</h2>
            </div><!-- /.header -->

            <section></section><!-- spacer -->

            <div class="cf-section cf-wrapper">
                <div class="cf-columns two">
                    <div class="cf-column">
                        <h2 data-translate="what_happened">What happened?</h2>
                            <p>You've requested a page on a website (${r2g}) that is on the <a href="https://www.cloudflare.com/5xx-error-landing?utm_source=error_100x" target="_blank">Cloudflare</a> network. An unknown error occurred while rendering the page.</p>
                    </div>

                    <div class="cf-column">
                        <h2 data-translate="what_can_i_do">What can I do?</h2>
                            <p><strong>If you are the owner of this website:</strong><br />refer to <a href="https://developers.cloudflare.com/workers/observability/errors/" target="_blank">Workers - Errors and Exceptions</a> and check Workers Logs for ${r2g}.</p>
                    </div>

                </div>
            </div><!-- /.section -->

            <div class="cf-error-footer cf-wrapper w-240 lg:w-full py-10 sm:py-4 sm:px-8 mx-auto text-center sm:text-left border-solid border-0 border-t border-gray-300">
    <p class="text-13">
      <span class="cf-footer-item sm:block sm:mb-1">Cloudflare Ray ID: <strong class="font-semibold"> ${r2v}</strong></span>
      <span class="cf-footer-separator sm:hidden">&bull;</span>
      <span id="cf-footer-item-ip" class="cf-footer-item hidden sm:block sm:mb-1">
        Your IP:
        <button type="button" id="cf-footer-ip-reveal" class="cf-footer-ip-reveal-btn">Click to reveal</button>
        <span class="hidden" id="cf-footer-ip">${r2m}</span>
        <span class="cf-footer-separator sm:hidden">&bull;</span>
      </span>
      <span class="cf-footer-item sm:block sm:mb-1"><span>Performance &amp; security by</span> <a rel="noopener noreferrer" href="https://www.cloudflare.com/5xx-error-landing" id="brand_link" target="_blank">Cloudflare</a></span>

    </p>
    <script>(function(){function d(){var b=a.getElementById("cf-footer-item-ip"),c=a.getElementById("cf-footer-ip-reveal");b&&"classList"in b&&(b.classList.remove("hidden"),c.addEventListener("click",function(){c.classList.add("hidden");a.getElementById("cf-footer-ip").classList.remove("hidden")}))}var a=document;document.addEventListener&&a.addEventListener("DOMContentLoaded",d)})();</script>
  </div><!-- /.error-footer -->

        </div><!-- /#cf-error-details -->
    </div><!-- /#cf-wrapper -->

     <script>
    window._cf_translation = {};


  </script>
</body>
</html>`;
}
